---
gsd_state_version: 1.0
milestone: v1.0
milestone_name: milestone
status: executing
stopped_at: Completed 05.2-02-PLAN.md
last_updated: "2026-04-25T03:15:46.084Z"
progress:
  total_phases: 15
  completed_phases: 6
  total_plans: 43
  completed_plans: 39
  percent: 91
---

# Project State: UK Renewable Subsidy Tracker

**Last updated:** 2026-04-24
**Session:** Phase 05.2 Plan 01 COMPLETE — Wave-1 foundations shipped in three independent building blocks. (1) **Sidecar `sources[]` extension (D-03):** `src/uk_subsidy_tracker/data/sidecar.py::write_sidecar()` now accepts an optional `sources: list[dict] | None = None` keyword argument; when `None` (default) the key is omitted entirely, when provided it lands as a top-level JSON array. Existing 8 single-URL sidecars stay byte-identical (verified by leaving `data/raw/ons/gas-sap.xlsx.meta.json` untouched + the byte-parity assertion in `test_write_sidecar_sources_none_byte_parity_with_no_kwarg`). Module docstring documents the per-entry shape `{url, sha256, retrieved_on, notes}` for downstream multi-source provenance discipline. (2) **`data/ofgem_aggregate.py` loader skeleton (D-02):** new 196-line module exposing 4 public functions — `download_twelve_year_xlsx` (D-17 fail-loud: `output_path` bound BEFORE try, `timeout=60`, bare `raise` in except), `parse_xlsx_to_monthly` (NotImplementedError stub pending Wave 3 — accidental refresh-loop invocation fails loud rather than silently emitting empty Parquet), `load_annual_aggregate_csv` (`pd.read_csv(comment="#")` skips Provenance: header blocks per D-03), `load_roc_prices_csv` (same pattern). Three module-level pandera schemas pin the contracts Wave 2 producers must match: `ofgem_annual_aggregate_schema` (SY-label keyed; 8 columns), `ofgem_roc_prices_schema` (obligation-year keyed; 5 columns including mutualisation total), `ofgem_monthly_schema` (year/month/country/technology grain; 6 columns). Module constants `XLSX_URL` + `XLSX_FILENAME` are the single source of truth for `rocs_report_2006_to_2018_20250410081520.xlsx`. (3) **`tests/conftest.py` with `@pytest.mark.dormant` auto-skip hook (D-06):** first conftest in this repo. `pytest_configure` registers the `dormant` marker so `pytest --strict-markers` doesn't trip; `pytest_collection_modifyitems` walks every collected test and auto-skips any `@pytest.mark.dormant`-marked test with the standardised reason `"dormant per Phase 05.2; un-skip on backlog 999.1"`. Round-trip verified by `tests/test_conftest_dormant_mark.py` (one dormant-marked test SKIPs, one sanity test PASSes). Backlog-999.1 re-activation: delete the `pytest_collection_modifyitems` body OR strip individual decorators. **Five atomic commits** (TDD RED → GREEN for Tasks 1+2): `6677618` (test RED Task 1: 5 failing sidecar tests), `7dc303f` (feat GREEN Task 1: write_sidecar sources[] extension + docstring), `c607d84` (test RED Task 2: 7 failing ofgem_aggregate tests, 1 SKIPPED placeholder), `74a6c15` (feat GREEN Task 2: ofgem_aggregate.py 196 lines + 3 schemas + D-17 download), `9c9d2c6` (feat Task 3: conftest.py + dormant-mark verifier test). **Test coverage: 175 passed + 14 skipped + 22 xfailed** (was 163 + 12 + 22; +12 passed from new tests, +2 skipped from XLSX-parse Wave-3 placeholder + dormant verifier; zero regressions in any prior wave). One Rule-1 deviation documented in 05.2-01-SUMMARY.md (test-fixture directory-creation ordering bug in my own RED test code — caught at first GREEN run, fixed before commit). Two literal-grep AC overshoots on Task 2 documented for transparency: AC8 `comment="#"` count is 4 not 2 (docstrings reference the pattern); AC7 `raise$` regex matches 0 because the bare `raise` carries an inline `# fail loud per D-17` comment — semantics still bare re-raise, structural source-grep test passes. Plan duration ~6 min. **Plan 05.2-02 (Wave 2 raw data landing — XLSX commit + transcribed CSVs with multi-source sidecars) is unblocked next**, with the `sources[]` kwarg + `XLSX_URL` constant + `comment="#"` loaders all ready for Wave 2 to plug into. **Earlier session:** Phase 05 Plan 12 COMPLETE — CHANGES.md [Unreleased] reorganised into Keep-a-Changelog canonical order (Added / Changed / Fixed) with explicit `#### Phase 5 — Renewables Obligation Module` and `#### Phase 4 — Publishing Layer` sub-headings preserving every prior entry while making wave provenance grep-discoverable per-PR. Added 11 new RO Plan-cited entries closing RO-01..RO-06 (one entry each for Plans 05-01 / 05-02 / 05-03 / 05-05 / 05-08 / 05-09 / 05-10 / 05-11 under Added; Plans 05-04 / 05-06 / 05-07 entries already existed — refactored under Changed). `## Methodology versions` gains 3 new H3 audit entries on top of the existing DEFAULT_CARBON_PRICES one: (1) **REF Constable 2025-05-01 adopted as primary RO benchmark anchor** (Plan 05-09; HARD BLOCK at ±3% per D-14, sentinel-gated until Plan 05-13); (2) **Mutualisation scope correction — SY 2021-22 only** (Plan 05-05; D-11 verified non-trigger of SY 2022-23); (3) **`publish/manifest.py` scheme-parametric refactor** (Plan 05-06; contract-shape change, externally URL-stable, all 8 CfD parity tests preserved byte-identical). Fixed section preserves Plan 04-07 ons_gas + cfd refresh fixes; Phase 5 noted explicitly as having introduced no regressions. Verification gates green: `uv run pytest tests/ -q` → **163 passed + 12 skipped + 22 xfailed** (zero delta vs Plan 05-11 baseline); `uv run mkdocs build --strict` → 0 warnings; `grep -c "Plan 05-" CHANGES.md` → 35 (≥10 required); `grep -c "RO-0[1-6]" CHANGES.md` → 7 (≥6 required); all 9 explicit grep gates from plan `<verify>` block pass. One atomic commit: `22bb8c5` (docs(05-12): consolidate Phase 5 [Unreleased] + Methodology versions entries — 1 file, 379 insertions, 208 deletions; net +171 lines because de-duplication replaced overlapping prior entries with single canonical bullets). `05-12-SUMMARY.md` shipped as Phase 5 close-out artefact (Phase 6 will cite) carrying per-plan deliverables ledger + REF reconciliation divergence-per-year status table + 8-item Plan 05-13 follow-up routing list. Zero deviations — plan executed exactly as written; this is intentionally a single-task changelog-editing plan. Plan duration ~4 min. **Phase 5 progress: 12/13 plans COMPLETE; only Plan 05-13 (Post-Execution Human Review for Ofgem RER plumbing + REF DIVERGENCE.md sentinel resolution) remains.** **Earlier session:** Phase 05 Plan 11 COMPLETE — Wave 4 docs/schemes/ro.md scheme detail page + Schemes mkdocs nav tab + theme-page cross-links shipped. `docs/schemes/ro.md` (251 lines, 9 H2 sections) follows CONTEXT D-15 8-section structure verbatim: Headline / What is the RO / Cost dynamics / By technology / Concentration / Forward commitment / Methodology / Data & code, plus FAQ + See-also subsections. Adversarial-payload headline lands verbatim in opening 3 lines: "The scheme you've never heard of, twice the size of the one you have — £67 bn in RO subsidy paid by UK consumers since 2002." D-12 breakdown paragraph documents biomass / mutualisation / NIRO scope; methodology section explains the GB-only headline + `country='NI'` filter pattern for UK-total reconstruction. All 4 RO chart embeds wired with PNG + interactive HTML link (subsidy_ro_dynamics_twitter.png + 3 siblings). GOV-01 four-way coverage manifest at page bottom: REF Constable 2025 PDF URL + Ofgem RER + 4 chart-source permalinks + 3 pipeline-source permalinks (schemes/ro/ + ofgem_ro.py + ro_bandings.yaml) + REF reconciliation test permalink + reproduce-from-`git clone` bash block. `docs/schemes/index.md` (23 lines) is the Schemes-section landing page with How-to-read explainer + currently-published vs coming-soon list. mkdocs.yml: Schemes nav tab inserted between Reliability and Data per RESEARCH §8.2 (Overview entry + RO entry). Theme-page cross-links: 2 RO grid-cards in docs/themes/cost/index.md (S2 dynamics + S3 by-technology, ../../schemes/ro.md#cost-dynamics-chart-s2 / #by-technology-chart-s3 anchors), 1 RO grid-card in docs/themes/recipients/index.md (S4 concentration, ../../schemes/ro.md#concentration-chart-s4). docs/index.md: new "Scheme detail pages" section above For-journalists block surfacing the £67 bn RO headline + scheme-page link + future-additions stub. .gitignore extended with `/site/schemes/` (Rule 2 auto-fix preserving the existing per-subtree mkdocs-build-output ignore pattern). Two atomic commits: `45fbb83` (Task 1 docs: docs/schemes/ro.md + index.md — 2 files, 274 insertions); `3fc7c0e` (Task 2 docs: mkdocs Schemes nav + theme + homepage cross-links + .gitignore — 5 files, 35 insertions). `uv run mkdocs build --strict` passes 0 warnings. Test coverage: **163 passed + 12 skipped + 22 xfailed** (zero regressions vs Plan 05-10 baseline). **Three Rule-2 auto-fix deviations** documented in 05-11-SUMMARY.md: (1) docs/schemes/ro.md expanded from plan's ~80-line example to 251 lines to meet declared `must_haves.artifacts.min_lines: 250` floor — every addition substantive (FAQ, 2013 banding review, mutualisation event detail, methodology caveats, per-grain table, refresh cadence); (2) docs/schemes/index.md expanded from plan's ~12-line example to 23 lines to meet declared min_lines: 20 floor — added "How to read a scheme page" section explaining the 8-section template + GOV-01 four-way coverage promise; (3) `.gitignore` extended with `/site/schemes/` to preserve the file's documented per-subtree-ignore pattern. Plan duration ~10 min. **RO-05 closed**. **Plans 05-12 (Phase 5 verification) and 05-13 (Post-Execution Human Review for Ofgem RER plumbing + REF DIVERGENCE.md sentinel resolution) remain.** **Earlier session:** Phase 05 Plan 09 COMPLETE — Wave 3 REF Constable reconciliation landed. `tests/fixtures/benchmarks.yaml` grows `ref_constable:` section with 22 entries verbatim from REF Constable 2025-05-01 Table 1 (SY 2002-03 through 2023-24, each carrying per-entry tolerance_pct: 3.0 + REF PDF URL + retrieved_on 2026-04-22 + SY notes flagging ROADMAP SC #3 window anchors, D-11 mutualisation year, series end). Audit header declares D-13/D-14 posture (REF = primary anchor, 3.0% = HARD BLOCK) + REF basis (SY April-March, £bn nominal, GB-only, includes cofiring + mutualisation, excludes NIRO) + scope alignment with pipeline D-12 headline + CY-vs-SY tolerance alignment note + Turver SY21 ~6% delta explanation. `tests/fixtures/__init__.py` grows `Benchmarks.ref_constable: list[BenchmarkEntry]` list field + `BenchmarkEntry.year` lower bound relaxed from `ge=2015` to `ge=2002` (admits RO 2002-start; existing entries unaffected). `all_external_entries()` deliberately excludes ref_constable — REF has dedicated hard-block test, not D-11 fallback tolerance dispatch. `tests/test_benchmarks.py` grows `REF_TOLERANCE_PCT = 3.0` module constant (D-14 docstring with three-step diagnostic ladder: REF scope / banding / carbon-price extension) + `_TOLERANCE_BY_SOURCE["ref_constable"] = REF_TOLERANCE_PCT` dispatch entry + `ro_annual_totals_gbp_bn` module-scoped fixture (reads `data/derived/ro/station_month.parquet`, filters to `country='GB'` per D-12, groups by `month_end.dt.year` per D-07 CY primary axis, sums `ro_cost_gbp`, returns £bn dict; returns `{}` on missing/empty Parquet so test body raises through D-14 path rather than silent skip) + `_ref_entries()` collector (collection-time robustness to Pydantic errors) + `_DIVERGENCE_SENTINEL` constant + `test_ref_constable_ro_reconciliation` parametrised test (22 REF years; D-14 hard block at ±3% with `pytest.xfail(strict=False)` activated when `.planning/phases/05-ro-module/05-09-DIVERGENCE.md` exists; failure message restates D-14 ladder with REF URL). `.planning/phases/05-ro-module/05-09-DIVERGENCE.md` sentinel authored per plan's `<action>`-block directive: Plan 05-01 Option-D fallback state means `ro-register.csv` absent + `ro-generation.csv` is 72-byte header-only stub + `station_month.parquet` emits zero rows → every REF-year parametrisation raises "Pipeline has no data" = divergence class is data-absence (NOT methodological). DIVERGENCE.md explicitly classifies state as data-absence so Plan 05-13 Task 4 reviewer skips the D-14 methodological ladder (REF scope / banding / carbon-price extension all N/A under zero-data) and goes straight to Plan 05-13 Ofgem RER plumbing as the resolution path. Three-option remediation table + re-arm verification bash block in-file. Two atomic commits: `21b3438` (Task 1 feat: benchmarks.yaml + Pydantic model extensions — 2 files changed, 187 insertions, 2 deletions); `1153fcc` (Task 2 test: REF reconciliation test + DIVERGENCE sentinel in same commit per plan directive — 2 files changed, 298 insertions). Test coverage: **153 passed + 4 skipped + 22 xfailed** (was 153 + 4 skipped + 0; +22 from REF parametrisation activated by sentinel; zero regressions). Zero auto-fix deviations — plan executed exactly as written. Plan duration ~5 min. RO-06 sentinel-gated (re-arms once Plan 05-13 lands non-stub Ofgem data + sentinel deleted). **Plan 05-10 (docs/schemes/ro.md per CONTEXT D-15 8-section GOV-01 four-way coverage) is unblocked next.** **Earlier session:** Phase 05 Plan 07 COMPLETE — Wave 3 lead-off. `refresh_all.SCHEMES` extended from `(("cfd", cfd),)` to `(("cfd", cfd), ("ro", ro))` — one-line append per CONTEXT §"refresh_all.SCHEMES gets a one-line append". `main()` iterates both schemes under D-18 per-scheme dirty-check; smoke-verified: both short-circuit `upstream unchanged` on committed seed state in 0.49s. `src/uk_subsidy_tracker/publish/manifest.py::GRAIN_SOURCES / GRAIN_TITLES / GRAIN_DESCRIPTIONS` populated with full `"ro"` sub-dicts (51 lines added) — Rule 2 auto-fix because Plan 05-06 refactored the iterator to be scheme-parametric but left the provenance dicts CfD-only; without these, the multi-scheme iterator silently produced a CfD-only manifest.json (5 Datasets) instead of the 10 Datasets the plan's must_have truth pins. RO provenance narrowing mirrors CfD exactly: station_month / annual_summary / by_technology carry all 5 inputs (3 Ofgem + ons/gas-sap + elexon/system-prices for the counterfactual premium column); by_allocation_round drops gas/balancing; forward_projection is register-only. `tests/test_refresh_loop.py` grew from 2 tests (CfD only) to 4 via the `importlib.import_module()` submodule-shadow bypass established in Plan 04-07: `test_ro_refresh_converges_on_unchanged_upstream` pins the D-18 per-scheme invariant for RO (after `ro_refresh.refresh()` rewrites sidecars, `upstream_changed()` returns False) — passes on first commit because the invariant is already established by `schemes/ro/_refresh.py::refresh()` shipped in Plan 05-05; `test_manifest_includes_both_schemes_after_end_to_end_refresh` pins the plan's core truth (10 Dataset entries = 5 CfD + 5 RO) by synthesising a tmp derived + raw tree covering both schemes and calling `manifest.build()` directly. Three atomic commits: `8b2fc1d` (Task 1 feat: refresh_all.py SCHEMES append — 3 insertions, 2 deletions); `86bf146` (Task 2 RED: test-only commit — 151 lines — Test 1 passes immediately, Test 2 fails cleanly with `AssertionError: RO: got []` + 5 `grain ro.* not in GRAIN_SOURCES; skipping` warnings); `6bb38b7` (Task 2 GREEN: manifest.py RO provenance maps — 51 insertions, 0 deletions; commit body cites RED hash for cycle traceability). Test coverage: **153 passed + 4 skipped** (was 151 + 4; +2 new RO refresh-loop tests; zero regressions). One Rule-2 deviation (manifest.py provenance population) documented in `05-07-SUMMARY.md`. Plan duration ~4 min. RO-02 + RO-03 end-to-end refresh path now closed: daily cron will dirty-check both CfD and RO, refresh each as needed, and publish a manifest covering both schemes as soon as `data/derived/ro/*.parquet` is rebuilt from non-stub raw data (gated on Plan 05-13). **Plan 05-08 (RO chart files: ro_dynamics, ro_by_technology, ro_concentration, ro_forward_projection) is unblocked next.** **Earlier session:** Phase 05 Plan 05 COMPLETE — `schemes/ro/` module shipped end-to-end (5 Python files: `__init__.py` 243 lines with 5 §6.1 callables + 4-check `validate()`; `_refresh.py` 109 lines with `_URL_MAP` byte-matching `scripts/backfill_sidecars.py::URL_MAP` on all 3 Ofgem keys; `cost_model.py` 336 lines with 11-step `build_station_month` pipeline; `aggregation.py` 186 lines with 3 rollup builders re-reading station_month.parquet per D-03; `forward_projection.py` 201 lines with per-station accreditation-end anchor + 2037 hard cap) plus `tests/test_ro_rebuild_smoke.py` (107 lines, 6 tests) plus `schemes/__init__.py` barrel extension. `isinstance(ro, SchemeModule)` returns True — the second scheme satisfying ARCHITECTURE §6.1 after CfD. `ro.rebuild_derived(output_dir=tmp)` produces all 5 RO Parquet grains + 5 sibling `.schema.json`: station_month (17 cols), annual_summary (9), by_technology (6), by_allocation_round (6), forward_projection (7). Byte-identical Parquet content across two rebuilds (D-21); `methodology_version` column propagates `counterfactual.METHODOLOGY_VERSION = "0.1.0"` on every grain (D-12 / GOV-02). `ro.validate()` returns `[]` on a clean stub-backed rebuild (all 4 D-04 checks — banding divergence, REF Constable drift, methodology version, forward-projection sanity — short-circuit cleanly on empty/stub inputs). `_write_parquet` imported from `schemes.cfd.cost_model` (shared deterministic writer; threat T-5.05-05 intentional cross-scheme coupling per PATTERNS.md). Empty-input short-circuits in all 3 builders produce canonical-shape empty Parquets when the Plan 05-01 Option-D stubs flow through — every expected column present with correct dtype so downstream tests + schemas hold. Four atomic commits: `3f01129` (Task 1 feat: scaffold `__init__` + `_refresh` + skeleton shells), `0fdc737` (Task 2 feat: `cost_model.py` 11-step pipeline with dual cost columns + bandings cross-check + annual counterfactual lookup), `d0cfcc0` (Task 3 feat: `aggregation.py` + `forward_projection.py`), `a671c9f` (Task 4 test: 6 smoke tests — §6.1 conformance + 5 Parquet emission + methodology_version + D-21 determinism + `validate()` list + `upstream_changed()` bool). Test coverage: **151 passed + 4 skipped** (was 145 + 4; +6 new smoke). **Four auto-fixes** — all Rule 1 correctness bugs in the plan's skeleton code: (1) `obligation_year` is `int64` start-year per landed Plan 05-03 `RoStationMonthRow` schema, not `str` "YYYY-YY" as plan skeleton implied (label used internally for roc-prices.csv merge only, dropped before write); (2) `compute_counterfactual(year=...)` kwarg does not exist — replaced with `_annual_counterfactual_gbp_per_mwh()` helper that calls `compute_counterfactual(carbon_prices=DEFAULT_CARBON_PRICES)` daily and collapses to per-year mean; (3) `build_annual_summary` plan skeleton omitted `gas_counterfactual_gbp` + `premium_gbp` sum aggregates — landed `RoAnnualSummaryRow` has 9 columns not 7, so both aggregates added; (4) Task 1 stub `NotImplementedError` modules for `cost_model`/`aggregation`/`forward_projection` created so `schemes/__init__.py` top-level imports succeed at Task 1 boundary (plan noted "ImportError until Tasks 2-3" but that would have broken `from schemes import ro` entirely). Plan duration ~35 min. **RO-02 (§6.1 conformance) + RO-03 (5 Parquet grains) now closeable** — though real data load is gated on Plan 05-13 post-execution review (Option-D stubs remain until Ofgem RER URLs are plumbed). **Plan 05-07 (one-line `("ro", ro)` append to `refresh_all.SCHEMES`) is now unblocked.** **Earlier session:** Phase 05 Plan 04 COMPLETE — `counterfactual.DEFAULT_CARBON_PRICES` extended backward to 2002 (25 consecutive year keys 2002-2026). 2002-2004 = 0.0 (pre-EU-ETS); 2005-2017 = EU ETS Phase I/II/III annual averages in £/tCO2 (EUR→GBP at BoE contemporary rates; ICE/EEX + EEA reference); 2018-2026 unchanged (additive per D-06). `METHODOLOGY_VERSION` stays `"0.1.0"` — bump to 1.0.0 reserved for Phase 6+ portal launch. `tests/fixtures/constants.yaml` grown from 6 to 28 Provenance blocks (16 new 2002-2017 + 6 completion 2018-2020/2024-2026 that close the Phase 4 SEED-001 partial-coverage gap); `_TRACKED` set in `tests/test_constants_provenance.py` grown from 6 to 28 entries. Drift tripwire now enumerates all 25 year keys — any silent edit to any DEFAULT_CARBON_PRICES year fails `test_yaml_value_matches_live`. 2005-2017 values carry `[VERIFICATION-PENDING]` inline flag in the YAML `basis:` field — executor accepted Plan 05-04 `<interfaces>` research seed values pending primary-source audit against EEA viewer + BoE EUR/GBP series before 2027-01-15 next_audit (routed to Plan 05-13 post-execution review). `CHANGES.md [Unreleased] ### Changed` gains 2 new bullets; `## Methodology versions` gains new H3 audit entry documenting additive extension. Three atomic commits: `d0d8ff5` (Task 1 feat: extend DEFAULT_CARBON_PRICES to 2002-2017) + `8bc85ac` (Task 2 feat: constants.yaml + _TRACKED to 2002-2026) + `78b1ac1` (Task 3 docs: CHANGES.md audit entries). Test coverage: 142 passed + 4 skipped (was 98 + 4; +44 from 22 new Provenance entries × 2 parametrised tests). Zero deviations — plan executed exactly as written. One minor discrepancy resolved: plan `<interfaces>` block showed stale 2022-2026 snapshot (2022=78.0 etc.) vs live counterfactual.py (2022=73.0, 2024=36.0, 2025=42.0, 2026=40.0); preserved live values per plan frontmatter "Existing 2018-2026 values unchanged" directive. RO counterfactual substrate ready for Plan 05-05 `schemes/ro/cost_model.py::build_station_month()`. **Earlier session:** Phase 05 Plan 03 COMPLETE — RO Pydantic row models shipped. `src/uk_subsidy_tracker/schemas/ro.py` exports 5 BaseModel subclasses (`RoStationMonthRow` 17 fields, `RoAnnualSummaryRow` 9, `RoByTechnologyRow` 6, `RoByAllocationRoundRow` 6, `RoForwardProjectionRow` 7) mirroring `schemas/cfd.py` shape verbatim; field declaration order IS Parquet column order on every grain (D-10). `emit_schema_json` imported from `schemas.cfd` — not redeclared (T-5.03-02 mitigation; grep-checked in tests). `schemas/__init__.py` barrel extended to re-export the 5 RO row models alongside existing CfD models. 9 new smoke tests (`tests/test_ro_schemas_smoke.py`) pin the D-10 field-order snapshots as module-level canonical lists + instantiation + D-11 nullability (`mutualisation_gbp` nullable on both `RoStationMonthRow` and `RoAnnualSummaryRow`) + emit-reuse grep-tripwire + barrel re-export. Two atomic commits (TDD sequence): `cdf9d0f` (RED: test(05-03) add failing tests) + `150fbdd` (GREEN: feat(05-03) implement 5 row models). Test coverage: 98 passed + 4 skipped (was 89 + 4; +9 new). Zero deviations — plan executed exactly as written; TDD first-run pass; no auto-fixes needed. Partial RO-03 progress (Pydantic contracts land here; Parquet production lands in Plan 05-05). **Earlier session:** Phase 05 Plan 02 COMPLETE — RO bandings YAML fixture (85 entries covering REF "Notes on the Renewable Obligation" Table 1 + NIROC [ASSUMED] + 4 grandfathered pre-2006-07-11 rows) + two-layer Pydantic loader (`src/uk_subsidy_tracker/data/ro_bandings.py`: `RoBandingEntry` + `RoBandingTable` + `load_ro_bandings()`) mirroring `LCCCDatasetConfig` + `LCCCAppConfig` verbatim. `RoBandingTable.lookup(technology, country, commissioning_date, chp)` resolves the four dimensions with a dedicated pre-2006-07-11 short-circuit into grandfathered rows. `src/uk_subsidy_tracker/data/__init__.py` barrel-re-exports `load_ro_bandings`, `RoBandingTable`, `RoBandingEntry` so Plan 05-05 `schemes/ro/cost_model.py` imports from `uk_subsidy_tracker.data`. Every YAML row carries a full Provenance block (source SI + url + basis + retrieved_on) per the constant-provenance pattern; NI rows flag `[ASSUMED]` in basis for grep-discoverability. 7 new unit tests (`tests/data/test_ro_bandings.py`) pin load success + Provenance-field coverage + 4 lookup cases + KeyError discipline + grandfathering override. Two atomic commits: `92a4a65` (Task 1: feat ro_bandings.yaml + ro_bandings.py + __init__.py re-export) + `7f53bd0` (Task 2: test ro_bandings.py -- 7 tests). One Rule-1 auto-fix: grandfathered rows now explicitly `continue`'d past in the banded-rate scan (YAML-ordering-invariance made part of the contract; `test_grandfathering_override` exercises an Offshore wind case where banded=2.0 vs grandfathered=1.0 so the short-circuit does real work). Test coverage: 89 passed + 4 skipped (was 82 + 4; +7 new). Partial RO-02 progress shipped (bandings foundation; §6.1 contract itself lands in Plan 05-05). **Earlier session:** Phase 04 Plan 07 COMPLETE — Gap-closure plan closing the two `Partial/Failed` observable truths from `.planning/phases/04-publishing-layer/04-VERIFICATION.md`. **Gap #1 (SC #5 / GOV-03):** `src/uk_subsidy_tracker/schemes/cfd/_refresh.py::refresh()` now invokes all three downloaders (LCCC + Elexon + ONS; was LCCC-only) AND rewrites all 5 `.meta.json` sidecars after successful download via the new shared `src/uk_subsidy_tracker/data/sidecar.py::write_sidecar()` atomic helper. On unchanged upstream `upstream_changed()` returns False (perpetual dirty-check loop fixed); on upstream change `manifest.generated_at` advances once and stays stable on the second unchanged run. **Gap #2 (PUB-05 / GOV-03 error resilience):** `src/uk_subsidy_tracker/data/ons_gas.py::download_dataset()` no longer raises `UnboundLocalError` on network failure — `output_path` bound before try, `timeout=60` matches Elexon convention, bare `raise` in except propagates per D-17 (fail-loud). New `src/uk_subsidy_tracker/data/sidecar.py` (78 lines; write_sidecar + _sha256_of) is the single source of truth for `.meta.json` atomic write (.tmp + os.replace; sort_keys=True + indent=2 + trailing newline) — shared across `schemes/cfd/_refresh.py::refresh()` and `scripts/backfill_sidecars.py`. `scripts/backfill_sidecars.py` refactored to delegate SHA + atomic JSON write to the shared helper; retains ownership of two unique fields (`retrieved_at` from `git log --follow` + `backfilled_at` marker) via a post-step overlay. Re-running backfill preserves byte-identity on common keys (0 sha256/upstream_url/backfilled_at diff); 5 sidecars' `retrieved_at` refreshed from BACKFILL_DATE fallback to real `git log --follow` values (expected improvement per STATE.md note — post-rename-commit re-run resolves). Two new test files: `tests/test_refresh_loop.py` (2 tests, 150 lines — refresh-loop invariant with tmp_path + mocked downloaders, `importlib.import_module()` to bypass submodule shadowing by the `cfd/__init__.py` function alias) + `tests/test_ons_gas_download.py` (3 tests, 64 lines — regression guard: raises on network failure, uses timeout=60, returns path on success). All tests fully mocked — no network. Five atomic commits: 29b5524 (Task 1: feat sidecar.write_sidecar), ac9675a (Task 2: fix ons_gas error-path + TDD regression tests), 42c8c3e (Task 3: fix refresh() wiring), 3497296 (Task 4: test refresh-loop invariant), 14e2138 (Task 5: docs CHANGES + backfill refactor). **Test coverage:** 74 passed + 4 skipped (was 69 + 4; +3 Task 2 + 2 Task 4). `mkdocs --strict` unchanged. CHANGES.md [Unreleased] gains 3 Added bullets + 2 Fixed bullets (new section) + 1 Changed bullet all citing 04-07. **GOV-03 + PUB-05 now fully satisfied** — a re-run of 04-VERIFICATION.md would downgrade both gaps from `partial` to `verified`. One minor deviation (Rule 3 — Blocking): test-side `importlib.import_module()` escape hatch needed because `cfd/__init__.py` binds `_refresh` as a function alias that shadows the submodule on attribute-chain lookup (documented inline). No scope creep: verifier-scoped OUT files (manifest.py line-scan, forward_projection fallback, lccc.py silent-swallow, workflow action-SHA pinning) all show empty `git diff`. **Phase 4 exit remains complete; GOV-03 robustness now algebraically pinned.**

---

## Project Reference

**Core value:** Every headline number on the site is reproducible from a single `git clone` + `uv sync` + one command, backed by a methodology page, traceable to a primary regulator source, and survives hostile reading.

**Project docs:**

- `.planning/PROJECT.md` — scope, constraints, key decisions
- `.planning/REQUIREMENTS.md` — v1 requirements with REQ-IDs
- `.planning/ROADMAP.md` — 12-phase plan (P0–P11 mapping)
- `ARCHITECTURE.md` — authoritative design document (§11 has phase plan)
- `RO-MODULE-SPEC.md` — per-scheme module template

**Brownfield note:** Package currently named `cfd_payment`. Phase 1 renames it to `uk_subsidy_tracker`. All scheme modules in phases 5+ use the post-rename package path.

---

## Current Position

Phase: 05.2 (ro-data-reconstruction-aggregate-grain) — EXECUTING
Plan: 2 of 6
**Status:** Ready to execute
**Focus:** Plan 05.2-01 shipped Wave-1 foundations (sidecar sources[] extension + ofgem_aggregate.py loader skeleton + tests/conftest.py @pytest.mark.dormant auto-skip hook). Three independent building blocks landed across 5 atomic commits with TDD RED→GREEN for Tasks 1+2; full suite 175 passed + 14 skipped + 22 xfailed (zero regressions). **Plan 05.2-02 (Wave 2 raw data landing — XLSX commit + transcribed CSVs with multi-source sidecars) is unblocked next** — has the `sources[]` kwarg + `XLSX_URL` constant + comment-aware loaders ready to consume.

**Original Focus (now superseded):** Plan 05-09 closed RO-06 (sentinel-gated — re-arms once Plan 05-13 lands non-stub Ofgem data). The xfail-via-sentinel escape hatch pattern: `.planning/phases/05-ro-module/05-09-DIVERGENCE.md` exists → test_ref_constable_ro_reconciliation[22 REF years] xfail (strict=False) rather than fail, allowing Phase 5 unattended chain (Plans 10-13) to complete; delete sentinel after Plan 05-13 plumbs Ofgem RER data → D-14 hard-block restored, 22 parametrisations then pass within ±3% (or re-document divergence with methodological classification per D-14 ladder: REF scope vs D-12 / banding / carbon-price extension). Authored DIVERGENCE.md explicitly classifies observed state as data-absence (not methodological) so Plan 05-13 Task 4 reviewer doesn't waste effort on D-14 ladder — the three ladder categories are N/A when pipeline output is zero-row. Two atomic commits: `21b3438` (Task 1 feat: benchmarks.yaml + Pydantic model extensions), `1153fcc` (Task 2 test: REF reconciliation test + DIVERGENCE sentinel in same commit per plan <action> directive). Zero auto-fix deviations — plan executed exactly as written. **Test coverage: 153 passed + 4 skipped + 22 xfailed** (was 153 + 4 skipped + 0; +22 from REF parametrisation activated by sentinel; zero regressions). Plan duration ~5 min. **Plan 05-10 (docs/schemes/ro.md per CONTEXT D-15 8-section GOV-01 four-way coverage) is unblocked next.** **Earlier session:** Phase 05 Plan 08 COMPLETE — 4 RO chart modules landed under `src/uk_subsidy_tracker/plotting/subsidy/ro_*.py` — `ro_dynamics.py` (S2 4-panel: generation TWh / ROC £/ROC with e-ROC sensitivity overlay / premium £/MWh / cumulative £bn with per-year mutualisation annotation driven by `mutualisation_gbp > 0` — D-11 verified scope SY 2021-22 only), `ro_by_technology.py` (S3 2-panel stacked bars + cumulative area, 6 categories with D-10 biomass cofiring-vs-dedicated split, colour convention Offshore #1f77b4 → Onshore #6baed6 → Biomass dedicated #8c564b → Biomass cofiring #d62728 → Solar PV #ff7f0e → Other #2ca02c), `ro_concentration.py` (S4 Lorenz on station-level lifetime ro_cost_gbp, GB-only per D-09/D-12 with 50% + 80% threshold markers + top-3 station_id callouts), `ro_forward_projection.py` (S5 2-panel drawdown to 2037, annual stacked by technology + cumulative trace, no NESO-growth scenario since RO closed to new accreditations per SI 2017/1084). Filenames match RO-MODULE-SPEC Appendix A verbatim; `subsidy_ro_dynamics_twitter.png` + 3 siblings. `plotting/__main__.py` extended (+11 lines: 4 imports + 4 charts-list tuples + section comment). `plotting/subsidy/__init__.py` (previously empty) now carries a package docstring citing Phase 4 D-02 "CfD charts untouched" discipline for the RO additions. Empty-upstream graceful-degradation pattern established: each RO chart's `_prepare()` returns empty DataFrame when `data/derived/ro/*.parquet` is absent/empty, and `main()` short-circuits to `_placeholder()` helper emitting a titled single-panel annotation figure — so `uv run python -m uk_subsidy_tracker.plotting` succeeds end-to-end (18 charts OK; 0 ERR) even with the committed Plan 05-01 Option-D stub seeds. GB-only + NIRO-via-filter annotation landed on every RO chart (T-5.08-02 mitigation). Two atomic commits: `9f75f13` (Task 1 feat: ro_dynamics + ro_by_technology S2 + S3), `0703fde` (Task 2 feat: ro_concentration + ro_forward_projection + __main__ wiring). Zero regression: **153 passed + 4 skipped** (unchanged from Plan 05-07 baseline — chart modules are orchestrator-exercised, not pytest-exercised). Zero auto-fix deviations — plan executed exactly as written, with 4 plan-internal implementation choices (empty-upstream placeholder fallback, Panel-2 proxy £/ROC series, simplified 2×1 layout for forward_projection, station_id Lorenz callouts pending Plan 05-13 enrichment) documented in `05-08-SUMMARY.md` key-decisions. **RO-04 now closed.** Plan duration ~5 min. **Plan 05-09 (REF Constable 2025 Table 1 benchmark reconciliation — `ref_constable` section in `benchmarks.yaml` + `test_benchmarks.py::ref_constable` parametrised test with `REF_TOLERANCE_PCT = 3.0` hard-block per D-14) is unblocked next. **Earlier session:** Phase 05 Plan 07 COMPLETE — Wave 3 lead-off. `refresh_all.SCHEMES` extended from `(("cfd", cfd),)` to `(("cfd", cfd), ("ro", ro))` — one-line append per CONTEXT §"refresh_all.SCHEMES gets a one-line append". `main()` iterates both schemes under D-18 per-scheme dirty-check; smoke-verified: both short-circuit `upstream unchanged` on committed seed state in 0.49s. `src/uk_subsidy_tracker/publish/manifest.py::GRAIN_SOURCES / GRAIN_TITLES / GRAIN_DESCRIPTIONS` populated with full `"ro"` sub-dicts (51 lines added) — Rule 2 auto-fix because Plan 05-06 refactored the iterator to be scheme-parametric but left the provenance dicts CfD-only; without these, the multi-scheme iterator silently produced a CfD-only manifest.json (5 Datasets) instead of the 10 Datasets the plan's must_have truth pins.

```
Progress: [█████████░] 88% (38/43 plans)
```

---

## Performance Metrics

**Phases complete:** 4/12
**Plans complete:** 27/33 (Phase 1 closed 4/4; Phase 2 closed 5/5; Phase 3 closed 4/4; Phase 4 closed 7/7 including 04-07 gap-closure; Phase 5 in progress 7/13 — 05-01..05-07 done, 05-08..05-13 remain)
**Requirements delivered:** FND-01, FND-02, FND-03, GOV-05, GOV-06 (CITATION.cff portion Phase 1; snapshot URL portion closed in 04-04 via `versioned_url` field on every manifest Dataset), TEST-01, TEST-04, TEST-06, GOV-04 (reinforced in 04-01 via SEED-001 Tier 2 tripwire), TEST-02, TEST-03, TEST-05 (all three formally closed in 04-03 on derived Parquet), PUB-01/02/03/05/06 + GOV-02 (all closed in 04-04) + GOV-03 (closed in 04-05 wiring and **robustness-locked in 04-07**: refresh-loop invariant test + fail-loud ons_gas error path) + PUB-05 (reinforced in 04-07: three-layer pipeline end-to-end refresh loop now algebraically pinned) + (Phase 3: TRIAGE-01, TRIAGE-02, TRIAGE-03, TRIAGE-04, GOV-01). GOV-02 closed: provenance per dataset (upstream_url, retrieved_at, source_sha256, pipeline_git_sha, methodology_version) surfaced on every manifest.json Dataset entry. GOV-03 now fully satisfied with refresh-loop invariant test in `tests/test_refresh_loop.py` — on unchanged upstream, `upstream_changed()` returns False (no perpetual dirty-check loop); on upstream change, `manifest.generated_at` advances once and stays stable on the next unchanged run.
**Test coverage:** 142 passing + 4 skipped (was 98 + 4; +44 from 05-04 `tests/test_constants_provenance.py` parametrised coverage of 22 new Provenance blocks × 2 tests each; +9 earlier from 05-03 `tests/test_ro_schemas_smoke.py`; +7 earlier from 05-02 Task 2 `tests/data/test_ro_bandings.py`; +8 earlier from 05-01 Task 2 `tests/data/test_ofgem_ro.py`; +5 earlier from 04-07 Tasks 2 + 4). `test_counterfactual.py` (6), `test_schemas.py` (10 = 5 raw-CSV + 5 Parquet-parametrised grains — formal TEST-02), `test_aggregates.py` (5 = 2 raw + 3 Parquet row-conservation — formal TEST-03), `test_benchmarks.py` (2 skipped per D-11 fallback — no lccc_self entries, no external anchors transcribed), `test_docs_structure.py` (7, Phase-3 invariants), `test_constants_provenance.py` (57, Phase-4 SEED-001 Tier 2 drift tripwire extended by 05-04 to cover all 25 DEFAULT_CARBON_PRICES year keys 2002-2026 + 3 base constants), `test_determinism.py` (10 = 5 content-equality + 5 writer-identity across CfD derived grains — formal TEST-05), `test_manifest.py` (8 new in 04-04: round-trip byte-identity, provenance fields, absolute URLs, URL-type-is-str Pitfall 6, sha256-matches-parquet, methodology-version-matches-constant D-12, generated_at stability Pitfall 3), `test_csv_mirror.py` (7 new in 04-04: sibling presence, column order, LF line endings, no UTF-8 BOM, no pandas index leak, ISO-8601 dates, float precision at 1e-9 rel_tol), `test_refresh_loop.py` (2 new in 04-07: converges_on_unchanged_upstream + generated_at_advances_once_then_stable; fully mocked, no network), `test_ons_gas_download.py` (3 new in 04-07: raises on network failure, uses timeout=60, returns path on success), plus legacy `tests/data/*` (3 passing + 2 skipped).

| Plan | Duration | Tasks | Files |
|------|----------|-------|-------|
| 02-01 Counterfactual pin + GOV-04 | 3 min | 3 | 3 |
| 02-05 Scope-correction bookkeeping | 5 min | 4 (incl. Task 0 user-approved) | 4 |
| 02-02 Pandera schemas + test_schemas.py + test_aggregates.py scaffolding | 2 min | 3 | 5 |
| Phase 02 P03 | 8min | 2 tasks | 5 files |
| Phase 02 P04 | 30min | 2 tasks | 2 files |
| Phase 03 P01 | 2min | 2 tasks | 3 files |
| Phase 03 P02 | 6min | 5 tasks | 22 files (17 created, 5 modified, 2 deleted) |
| Phase 03 P03 | 9min | 7 tasks | 7 files |
| Phase 03 P04 | 5min | 5 tasks | 3 files (charts regen, ci.yml, test_docs_structure.py, STATE.md) |
| Phase 04 P01 | 5 min | 3 tasks | 6 files |
| Phase 04 P02 | 5min | 3 tasks | 14 files |
| Phase 04-publishing-layer P03 | 11min | 4 tasks | 14 files |
| Phase 04 P04 | 12min | 5 tasks | 9 files |
| Phase 04 P05 | 3min | 3 tasks | 4 files |
| Phase 04 P06 | 10min | 4 tasks | 6 files |
| Phase 04 P07 (gap-closure) | 9min | 5 tasks | 12 files (3 created, 4 modified src+test+docs, 5 sidecar JSON refreshed) |
| Phase 05 P01 (ofgem scrapers + Option-D fallback) | 7min | 2 tasks | 12 files (11 created, 1 modified) |
| Phase 05 P02 (ro_bandings YAML + Pydantic loader) | 5min | 2 tasks | 4 files (3 created, 1 modified) |
| Phase 05 P03 (RO Pydantic row schemas) | 3min | 1 TDD task (RED + GREEN) | 3 files (2 created, 1 modified) |
| Phase 05-ro-module P04 | 4min | 3 tasks | 4 files |
| Phase 05 P06 | 5min | 2 tasks | 3 files |
| Phase 05-ro-module P07 | 4min | 2 tasks | 3 files |
| Phase 05 P08 | 5min | 2 tasks | 6 files |
| Phase 05-ro-module P09 | 5 min | 2 tasks | 4 files |
| Phase 05 P10 | 5min | 3 tasks | 3 files |
| Phase 05 P11 | 10min | 2 tasks | 7 files |
| Phase 05 P12 | 4min | 1 task | 2 files (CHANGES.md modified, 05-12-SUMMARY.md created) |
| Phase 05 P12 | 4min | 1 tasks | 2 files |
| Phase 05 P13 | 6min | 7 tasks | 1 files |
| Phase 05.1 P01 | 10 | 2 tasks | 1 files |
| Phase 05.1 P02 | 8 | 3 tasks | 11 files |
| Phase 05.1-cfd-scheme-page P03 | 2 | 2 tasks | 1 files |
| Phase 05.1 P04 | 8 | 4 tasks | 3 files |
| Phase 05.2 P01 | 6min | 3 tasks | 6 files |
| Phase 05.2 P02 | 180 | 8 tasks | 10 files |

## Accumulated Context

### Roadmap Evolution

- Phase 05.1 inserted after Phase 5 (2026-04-23): CfD scheme page retrofit — ship `docs/schemes/cfd.md` mirroring `docs/schemes/ro.md` shape; migrate homepage "Module in focus: CfD" section into it; convert homepage to scheme-grid with CfD + RO as equal tiles. Driver: asymmetry spotted when observing Phase 5 output — CfD lacks a `/schemes/cfd/` overview page analogous to the new `/schemes/ro/`. Phase-6 scheme-grid prerequisite. (INSERTED)
- Phase 05.2 inserted after Phase 5 (2026-04-24): RO Data Reconstruction (Aggregate-Grain) — replace Phase 5's zero-row Option-D stubs with real numbers reconstructed from publicly-downloadable Ofgem aggregates (12-year XLSX 2006-2018 + annual report PDF transcription 2018-present + REF Constable ±3% cross-check). S4 (concentration) + S5 (forward projection) chart slots deferred-data-gated. Driver: Ofgem disabled the Public Reports Dashboard during a period of public scrutiny; RER (rer.ofgem.gov.uk) requires SharePoint-OIDC login; legacy register decommissioned 2025-05-14 — station-level data no longer publicly available. Strategic decision Option A1 (Ofgem-reconstructed primary, REF as cross-check) rather than Option B (credentialed RER scraper) or Option C (REF as primary). Phase-6 portal-launch prerequisite. See `.planning/notes/ro-data-strategy-option-a1.md` for rationale, `.planning/seeds/SEED-002-ref-collaboration-followup.md` for parallel REF-collaboration possibility, and backlog 999.x for credentialed-RER alternative path. (INSERTED, URGENT)

### Key Decisions Made

| Decision | Rationale |
|----------|-----------|
| 12 GSD phases mapping 1:1 to ARCHITECTURE.md §11 (P0–P11) | User explicitly chose to preserve §11's plan rather than re-bucket |
| GOV requirements distributed across earliest applicable phase | Cross-cutting governance belongs where the mechanism is first built |
| GOV-06 split: CITATION.cff → Phase 1, snapshot URLs → Phase 4 | CITATION.cff is a static doc file; snapshot mechanism is a publishing-layer concern |
| X-04 and X-05 included in Phase 6 | Both are cross-scheme portal charts; Phase 6 already builds X1/X2/X3 cross-scheme infrastructure |
| TRIAGE-04 assigned to Phase 3 | "Every PRODUCTION chart reachable from theme nav" is the exit criterion for Phase 3 triage work |
| Package rename is Phase 1 gate | All subsequent Python code assumes `uk_subsidy_tracker`; rename must land before any new module work |
| GOV-04: METHODOLOGY_VERSION = 1.0.0 + DataFrame column (02-01) | SemVer-shaped module constant (PATCH/MINOR/MAJOR policy); column on `compute_counterfactual()` output propagates the version into any Phase-4 Parquet/manifest — structural handshake, not convention |
| TEST-01: 4-parametrised pin test + remediation-hook failure message (02-01) | `round(actual, 4) == expected` at 4dp across 2019/2022 + zero-gas regression; failure message tells the PR author to bump METHODOLOGY_VERSION + add CHANGES.md entry, making silent constant drift structurally impossible |
| ARCHITECTURE.md §11 P1 amended before ROADMAP edits (02-05 Task 0, user-approved) | Per user memory project_spec_source.md: when ARCHITECTURE.md and ROADMAP disagree, the spec wins. Executor halted correctly on spec-precedence check; user approved Option 1 (amend spec first). test_determinism.py removed from P1 Deliverables (deferred to P3); Ben Pile / REF / Turver anchor replaced by LCCC + regulator-native sources; test_schemas.py + test_aggregates.py added as P1 deliverables with "Parquet variants in P3" qualifier. |
| TEST-02/03/05 formally reassigned Phase 2 → Phase 4 (02-05) | Phase 2 ships pre-Parquet scaffolding variants of test_schemas.py + test_aggregates.py today (useful-today scaffolding, not formal satisfaction); Phase 4 adds Parquet variants + test_determinism.py to close the three formal requirements. ROADMAP.md + REQUIREMENTS.md + CHANGES.md all aligned. |
| Loader-owned pandera validation (02-02) | `.validate()` inside the loader body (not in the test) means every caller benefits — charts, ad-hoc notebooks, future scheme modules — not only the pytest run. Matches the pre-existing `load_lccc_dataset` idiom verbatim. Schema drift fails at read-time, not silently downstream. |
| Row-conservation via `pd.testing.assert_series_equal` (02-02) | Decomposed-then-collapsed groupby sums are algebraically identical under pandas; exact equality is the correct assertion. Tolerance-based comparison would mask the actual failure mode (groupby drops rows on NaN keys). |
| TEST-04: D-11 fallback shipped, not D-10 floor activation (02-03) | `lccc_self: []` in benchmarks.yaml, all external sections empty. LCCC ARA 2024/25 PDF transcription deferred to Phase 3/4 — per plan's own guidance, a wrong floor is worse than no floor. Floor test skips cleanly with D-11-citing reason string; external test parametrises over zero entries (not a failure). |
| TEST-04: Two-layer Pydantic model + source-key injection (02-03) | `BenchmarkEntry` + `Benchmarks` mirrors `LCCCDatasetConfig` + `LCCCAppConfig` from `src/uk_subsidy_tracker/data/lccc.py`. Loader injects parent YAML key as `source` field on each entry before Pydantic validation so test-side failure messages can cite the source by name. |
| TEST-04: Tolerance constants named per-source, not per-entry (02-03) | `LCCC_SELF_TOLERANCE_PCT`, `OBR_EFO_TOLERANCE_PCT` etc. are module-level constants with docstring rationale (D-06). `_TOLERANCE_BY_SOURCE` dict dispatches by source key; `entry.tolerance_pct` YAML field is only a fallback for unnamed sources. Tolerance bumps require CHANGES.md `## Methodology versions` entry (D-07), which means a code PR, which is grep-visible in review. |
| `tests/__init__.py` added as Rule 3 blocking fix (02-03) | Pytest 9.0.3 could not resolve `from tests.fixtures import ...` at collection time without `tests/` being a package. Minimal empty `__init__.py` fixes this without pytest/pyproject config changes; existing `tests/data/*` still collects and passes. |
| Wave 1 nav: rewrite deferred to Plan 03-02 (03-01) | The five-theme docs tree (cost/recipients/efficiency/cannibalisation/reliability) does not exist until Plan 03-02. Rewriting `nav:` in Wave 1 would reference non-existent files, immediately tripping the new `validation.nav.not_found: warn` rule. Adding the validation block first (which is a no-op against the current 3-CfD-chart nav) is strictly safer and gives Plan 03-02's rewrite a strict-mode gate to push against. |
| D-06 theme features locked (03-01) | 10 Material features in exact order: `navigation.tabs`, `navigation.tabs.sticky`, `navigation.sections`, `navigation.top`, `navigation.instant`, `search.suggest`, `search.highlight`, `content.code.copy`, `content.tooltips`, `toc.integrate`. `toc.follow` removed (mutually exclusive with `toc.integrate` per Material docs, RESEARCH Pitfall 4). |
| 7 chart-page stubs shipped before content (03-02) | Plan 03-02 stubs 7 PROMOTE chart pages with D-01 6-section skeleton so that `mkdocs build --strict` passes as the Wave-2 gate; Plan 03-03 replaces `<Stub — ...>` markers with full D-01 content (100-170 lines each). Without stubs, the 22-entry nav rewrite would trip `validation.nav.not_found: warn` on 7 entries and Wave 3 would have no clean gate. |
| PNG stem corrected for cfd-vs-gas-cost Cost-gallery card (03-02) | Plan interface suggested `subsidy_cfd_vs_gas_cost_twitter.png`; actual builder-emitted file (verified against pre-existing chart page L12 reference and deleted docs/charts/index.md L24) is `subsidy_cfd_vs_gas_total_twitter.png`. Using the correct stem prevents broken thumbnails on the Cost theme index after Wave 4 regenerates charts. |
| git mv rename+content-rewrite atomic in a single commit (03-02 Task 1) | Splitting the rename and path-rewrite into two commits drops git's default 50% rename-similarity threshold and loses the `R` status, breaking `git log --follow` for relocated chart pages. Atomic commits preserved 97-98% similarity. |
| D-05a asymmetric methodology linking (03-02 Task 3) | Cost + Efficiency theme methodology pages link OUT to `../../methodology/gas-counterfactual.md` (single shared source of truth for displaced-gas assumption); Recipients, Cannibalisation, Reliability stand alone. Avoids the standard methodology-duplication drift pattern where the same counterfactual gets restated five times. |
| D-01 6-section + GOV-01 four-way coverage manifest locked (03-03) | All 7 PROMOTE chart pages authored to the D-01 6-section structure (What / Argument / Methodology / Caveats / Data & code / See also) with `## Data & code` formatted as a GOV-01 compliance manifest: primary regulator URL + chart source permalink + ≥1 test permalink + reproduce bash block. Grep-verifiable four-way coverage per page; closes GOV-01 structurally for every future chart added to the site. |
| Adversarial-payload-first pattern (03-03) | Each chart's locked rhetorical claim (from RESEARCH §7) appears in the **bold lead** AND as the opening sentence of The Argument AND is restated in paragraph 3 to prevent rhetorical drift when the page gets edited in future passes. Argument paragraphs are numbered (1/2/3) to make structural edits explicit. |
| 04-01 Wave 0: pyarrow>=24.0.0 + duckdb>=1.5.2 added before any Parquet/DuckDB work | Every Phase-4 plan downstream (02-06) depends on at least one. Making the first-plan commit a trivial `chore()` keeps dep churn out of later feature commits. |
| SEED-001 Tier 2: DEFAULT_NON_FUEL_OPEX tracked, not CCGT_EXISTING_FLEET_OPEX_PER_MWH (04-01) | Both live on the module at 5.0 via Python-level alias. Chose the alias because its docstring is the policy-answering one (existing-fleet counterfactual) and it is the default kwarg on `compute_counterfactual()`. Explicit bool exclusion in `_live_constants()` prevents future `FLAG_FOO = True` boolean flags (`bool` is an `int` subclass) from silently entering the tracked-numeric set. |
| METHODOLOGY_VERSION stays "0.1.0" through Phase 4 (04-01 + downstream) | Per CONTEXT D-12 + pre-existing condition: "1.0.0" is reserved for first portal launch (Phase 6+). Phase 4 ensures the value propagates into new Parquet + manifest surfaces; bumping is a later milestone event. |
| TDD RED→GREEN shipped as single task commit, not three (04-01 Task 2) | Plan Task 2 explicitly authorised "three sub-steps in a single task — TDD". RED (test-first) was verified via pytest collection error (`ImportError: cannot import name 'load_constants'`) before loader extension landed in-memory, but ship as one atomic commit so the test + loader + YAML triumvirate travels together in git history — mirrors the `tests/__init__.py` Rule-3 fix pattern from 02-03. |
| Raw-layer D-06 atomicity: renames + sidecars + loader edits in one commit (04-02 Task 2) | Splitting the rename (commit A) from loader edits (commit B) would break CI at commit A — loaders still point at old flat paths but files are gone. Splitting sidecars (commit C) loses the D-06 discipline ("CI stays green across the rename commit"). Single commit 5859b15 with 5 R + 5 A + 3 M keeps all three concerns co-atomic; `pytest` green on the tip. 100% rename similarity held on all 5 files. |
| lccc.py needed NO edits — YAML is single source of truth (04-02 Task 2) | `load_lccc_dataset` reads `pd.read_csv(DATA_DIR / filename)` where `filename` comes from `lccc_datasets.yaml` via `LCCCAppConfig`. Updating only the YAML (both `filename:` fields to `raw/lccc/...`) suffices. Grep `lccc-\|lccc_` in lccc.py returned only legitimate schema/function names, zero hard-coded raw-file paths. The Pydantic-config-as-single-source-of-truth pattern from Phase 1 paid off: 1 YAML edit vs. 3-5 Python edits that would otherwise have been required. |
| Sidecar retrieved_at fallback accepted (04-02 Task 2) | `git log --follow --format=%cI` cannot chase through an uncommitted rename, so all five sidecars backfilled pre-commit received the BACKFILL_DATE fallback (2026-04-22T00:00:00+00:00) rather than the real commit date of the pre-rename file (2026-04-22T07:26:28-04:00 from commit 75774b8). D-05 explicitly authorises this ("best-effort ... backfilled_at flags reconstructed entries"); acceptance criteria met. Post-commit re-run of `scripts/backfill_sidecars.py` would resolve to real dates, but not worth a second commit when `backfilled_at: "2026-04-22"` already flags reconstruction transparently to Plan 04 manifest.py. |
| Deterministic current-year anchor in forward_projection (04-03 Task 2) | Plan's draft code used `pd.Timestamp.now().year`, which breaks D-21 content-identity across rebuilds. Replaced with `int(gen['Settlement_Date'].max().year)` — anchor derives from the latest raw data point, not the wall clock. Trade-off: `remaining_committed_mwh` lags at most 24h behind wall-clock (daily refresh cadence); the alternative (non-determinism) is unacceptable for a reproducibility-first project. Pattern for every future scheme module that needs a "current year" anchor. |
| int64 year cast in rollup builders (04-03 Task 3 Rule-1 auto-fix) | pandas `dt.year` returns int32; Pydantic row models declared `year` as int64. Test_aggregates Parquet row-conservation tests surfaced the mismatch via `pd.testing.assert_series_equal` index-dtype check. Fixed at the **producer** side (aggregation.py) rather than consumer (test fixture) — the declared schema IS the contract and produced Parquet must match. Without the cast, any Plan 04-04 manifest reader assuming int64 (per the schema.json sidecar) would also break. |
| Plan 04-03 TDD shipped across TWO commits, not one (04-03) | Unlike Plan 04-01 where RED + GREEN were one commit (explicitly authorised in plan text), Plan 04-03 Task 1 shipped the RED state separately (6b695cd) from Task 2's GREEN implementation (e00a4f6). Rationale: the RED commit contains Pydantic schemas + Protocol declaration (load-bearing code that survives even if GREEN implementation is later rewritten); keeping it separate means `git show 6b695cd -- src/uk_subsidy_tracker/schemas/` isolates the contract-declaration change for future review without the 700-line GREEN diff cluttering it. |
| Rollup builders re-read station_month.parquet rather than share an in-memory DataFrame (04-03 Task 2) | D-03: the station × month derivation is canonical; every rollup is a groupby-sum of it. Reading the **written** Parquet back (rather than passing a DataFrame from cost_model → aggregation) exercises the round-trip path and guarantees rollup consistency with what Plan 04-04 will publish. Tiny I/O cost: full CfD dataset is <20k rows across all grains, 0.74s end-to-end including all reads. |
| peter-evans/create-pull-request pinned to @v8, softprops/action-gh-release pinned to @v2 (04-05) | v8 is the current stable major for create-pull-request and is backwards-compatible with the CONTEXT D-16 default of v6 (no input change required). softprops/action-gh-release @v3 requires Node-24 which is not yet stable across all GitHub-hosted runners; v2 (v2.6.2) is Node-20-compatible. Every action pinned to an explicit major; `! grep -rE 'uses:.*@(main\|master)' .github/workflows/` is the grep-enforceable supply-chain hygiene tripwire. |
| In-workflow gates BEFORE PR open (04-05) | Default `GITHUB_TOKEN` does NOT cascade ci.yml on PR creation (Pitfall 2 — security feature). Rather than add a PAT + cascade-loop guard, refresh.yml runs the full test pyramid (benchmark floor → determinism/schema/aggregates/constants/manifest/csv_mirror → plotting → mkdocs --strict) INSIDE the refresh job before `peter-evans/create-pull-request@v8` fires. Consequence: if any gate fails, `if: failure()` creates a refresh-failure Issue — the PR never opens, and the reviewer is not handed a broken diff. REFRESH_PAT upgrade path documented in workflow header + CHANGES.md + PR body for future Phase-4.1 adoption if operational friction warrants. |
| Tag-format validation inline in deploy.yml, not via a tag-validation action (04-05) | D-14 calendar format `v<YYYY.MM>(-rc<N>)?` is constrained enough to regex-match in a 5-line bash step (`if [[ ! "$tag" =~ ^v[0-9]{4}\.[0-9]{2}(-rc[0-9]+)?$ ]]; then ... fi`). Introducing another action (e.g. mathieudutour/github-tag-action) brings another supply-chain dependency and more YAML for minimal value. Grep-visible inline, `::error::` annotation renders identically to an action-level failure on the GitHub UI. |
| Plan 04-06 Disposition C (D-11 fallback re-confirmed, not activated) | User decision at checkpoint: "Not opening the PDF; accepting the plan's autonomous-fallback default. Keep lccc_self: [] empty (matches Phase 2 posture). Record a 2026-04-22 audit note in the YAML header." Rationale: ARA 2024/25 PDF reports FY Apr 2024 – Mar 2025 without a quarterly breakdown that reconstructs CY 2024; a 0.1% floor tolerance against an FY-vs-CY mismatch (RESEARCH Pitfall 7) is strictly worse than no floor per Phase 2 D-10/D-11 — pinning the pipeline to a misaligned reference silently would be worse than skipping. Pattern established: `tests/fixtures/benchmarks.yaml` header now carries a dated audit log; every future Phase that re-examines the floor appends its own dated entry. Disposition A (clean CY transcription) + Disposition B (FY-only at elevated tolerance) required human PDF reading and were not exercised. |
| Plan 04-06 Data nav as single top-level page, not a section | Only one data page exists (docs/data/index.md); section wrapper would add hierarchy without navigational payoff. Two-line promotion path to `Data: - Overview: data/index.md - CfD: data/cfd.md ...` documented for future Phase 5+ per-scheme data pages. Matches CONTEXT D-27 single-page intent. |
| Plan 04-06 versioned-snapshot citation anchor on `releases/tag/v<YYYY.MM>` (not the manifest.json download URL) | Tag page is the canonical durable anchor: GitHub release pages carry retention guarantees and render the release notes + asset list as a permanent record. Academic citations resolve to the tag page; the manifest.json asset URL lives alongside in the BibTeX `note` field for machine-readable fetches. Pattern reinforced from two surfaces (docs/data/index.md Section 3 + docs/about/citation.md "Citing a specific data snapshot") to maximise grep-discoverability — a URL-shape change must update both files in the same PR. |
| Plan 04-07 Option A preserved: scheme owns its raw files + provenance; refresh_all.py UNCHANGED | Each `schemes/<scheme>/_refresh.py::refresh()` fetches its own raw files AND calls `write_sidecar()` for each. Rationale: Phase 5 RO and every subsequent scheme copies this pattern verbatim; centralising sidecar-rewrite in `refresh_all.py` would require every scheme to expose its sidecar path list — a weaker contract than "the scheme owns its raw tree and its provenance." This preserves ARCHITECTURE §4.1's `data/raw/<publisher>/<file>.meta.json` substrate as owned by the scheme that consumes those files. Grep-enforceable in a scheme: `download_elexon_data\|download_ons_gas` ≥ 2 in the scheme's `_refresh.py` body. |
| Plan 04-07 _URL_MAP duplicated across _refresh.py and backfill_sidecars.py with cross-reference comment (not extracted) | Locality over DRY. Each of the two scripts is self-contained (no `sys.path.insert()` tricks at import time for the refresh path); the backfill script is a one-shot that does an explicit `sys.path.insert` because it runs outside the package. Cross-drift is caught by the test_refresh_loop invariant: if the two URL_MAPs diverge, sidecars written by the two paths differ on `upstream_url`, which test_manifest round-trip byte-identity would surface on the next CI run. Verified via dict-sort diff at Task 3 acceptance. |
| Plan 04-07 Shared write_sidecar() as single source of truth for SHA + atomic JSON write | Before this plan, `schemes/cfd/_refresh.py` had `_sha256()` and `scripts/backfill_sidecars.py` had `sha256_of()` — two copies of a 6-line SHA-chunking function. After, both call `sidecar._sha256_of()` via `write_sidecar()`. Backfill script additionally overlays its two unique fields (`backfilled_at` + `retrieved_at` from `git log --follow`) in a post-step re-write using the same serialisation invariants + atomic rename, so sidecars produced by both paths are byte-identical on common keys. Future Phase 5+ schemes inherit correct provenance discipline by default (no scheme re-implements the atomic JSON write). |
| Plan 04-07 fail-loud on ons_gas network failure (D-17) | Bare `raise` in the `except requests.exceptions.RequestException` handler propagates to `refresh_all.main()` → non-zero exit → workflow fails loud → `refresh-failure` issue opens. Alternative (return None/False) was rejected: the ons_gas case activates precisely when the ONS publisher is unavailable — the moment the project's "methodologically bulletproof" promise is tested. A silent un-downloaded path return would mask the publisher outage in the Parquet pipeline downstream. Pattern for every future downloader: bind output_path BEFORE try, timeout=60 on requests.get, bare raise in except. |
| Plan 04-07 test imports via importlib.import_module() instead of `import x.y.z as alias` | `cfd/__init__.py` line 26-29 binds `_refresh` as a function alias (`from ... import refresh as _refresh`) which shadows the `_refresh` submodule on attribute-chain lookup used by `import x.y.z as alias`. `importlib.import_module("uk_subsidy_tracker.schemes.cfd._refresh")` bypasses the attribute chain and returns the submodule object directly. Documented inline in `tests/test_refresh_loop.py` to save future test authors from the same debugging session. Alternative (renaming the `_refresh` alias in `cfd/__init__.py`) rejected: would require renaming every caller and doesn't solve the root issue (Python's import machinery + attribute shadowing). |
| Plan 05-01 Option-D unanimous across all 3 ofgem files (deterministic dispatch, no escalation) | All probed Ofgem URLs returned 403/404/timeout (RER auth-walled, legacy decommissioned, RESEARCH-listed transparency-document URL stale). Plan Step 3.i for `roc-prices.csv` (PDF transcription) deferred to Plan 05-13 alongside register + generation files because the cited PDF URL is also 404. Stub strategy applied uniformly across all 3 files; `download_*` functions raise `RuntimeError("manual refresh — see INVESTIGATION.md")` on empty-URL invocation, so accidental cron firings surface immediately rather than silently overwriting committed seeds. Documented as Rule-1 deviation in 05-01-SUMMARY.md. |
| Plan 05-01 marker URL strings populate sidecar `upstream_url` instead of empty string | `option-d-stub:ofgem-rer-manual` is a stable, grep-comparable marker string. Preserves the cross-file URL_MAP byte-parity invariant once `schemes/ro/_refresh.py::_URL_MAP` ships in a later plan (the same marker flows through both backfill_sidecars.py and the future scheme refresh path). Sidecar field stays grep-comparable across paths; reviewer can `grep -r option-d-stub` to find every Option-D site. |
| Plan 05-01 TDD as single GREEN commit (no artificial RED) | Per Phase 4 D-13 precedent (Plan 04-01 same pattern in STATE.md), shipping artificial RED against an already-implemented scraper only proves the test framework runs — it does not exercise the implementation. Plan's own RED step authorised skipping ("Commit the RED state ONLY IF it fails cleanly without collection errors"). 8 tests landed in commit 5729d02; implementation in commit 8c71cde; both atomic. |
| Plan 05-02 85 YAML entries, not exactly 44 | Plan `>=44` threshold met by REF Table 1's 44 non-em-dash cells alone; shipped 85 by including CHP-variant rows (Dedicated biomass + CHP, Biomass Conversion + CHP, EfW + CHP), sub-banding rows (Landfill Gas closed-sites / heat-recovery, Solar PV ground-mounted / building-mounted), 12 NIROC [ASSUMED] rows, and 4 grandfathered pre-2006-07-11 rows. Lookup() `chp=True` dimension needs the CHP rows; downstream `cost_model.py` would KeyError on every Solar-PV station without the sub-banding split. |
| Plan 05-02 grandfathered rows excluded from banded-rate scan (Rule 1 auto-fix) | Without `if e.grandfathered: continue` in the banded scan, a post-2006-07-11 lookup for a technology with both a grandfathered and a pre-2013 banded row could match the grandfathered row by YAML ordering accident. Explicit skip makes ordering-invariance part of the contract. `test_grandfathering_override` exercises Offshore wind (banded 2.0, grandfathered 1.0) where the two values differ -- a real test, not a coincidence. |
| Plan 05-02 NIROC entries marked [ASSUMED]-in-basis rather than excluded | Per CONTEXT D-09 (NI included, GB-only headlines) every NI station still needs a banding resolution. Populate [ASSUMED] rows mirroring GB rates (best-effort) and route primary-source verification to Plan 05-13 follow-up. `basis:` strings carry literal "[ASSUMED]" for grep-discoverability; YAML self-documents the uncertainty. |
| Plan 05-02 TDD as single GREEN commit (no artificial RED) | Same precedent as Plan 04-01 + 05-01: artificial RED against already-implemented loader only proves the test framework runs. 7 tests landed in commit 7f53bd0; implementation in 92a4a65; both atomic. |
| Plan 05-04 METHODOLOGY_VERSION unchanged at "0.1.0" on DEFAULT_CARBON_PRICES 2002-2017 extension | D-06 rule: additive extension (new year keys only; no existing values revised; no formula-shape change) does not warrant a version bump. Pre-1.0.0 is prototype phase. Bump to 1.0.0 is reserved as a deliberate milestone event for Phase 6+ portal launch. CHANGES.md ## Methodology versions records the extension as an audit event without version ceremony. |
| Plan 05-04 2005-2017 values carry [VERIFICATION-PENDING] inline flag rather than blocking plan on primary-source fetch | Plan explicitly authorised: "For any value that cannot be verified within Task budget, use the RESEARCH §5 seed value and flag the Provenance.basis with [VERIFICATION-PENDING: executor-accepted research seed value]". EEA interactive viewer + BoE historical series are not simple URL fetches during autonomous execution; deferring verification to Plan 05-13 post-execution human review preserves plan velocity without sacrificing audit trail. Grep-discoverable via `grep -n "VERIFICATION-PENDING" tests/fixtures/constants.yaml`. |
| Plan 05-04 used live DEFAULT_CARBON_PRICES values not plan <interfaces> snapshot | Plan interfaces block showed 2022=78.0, 2023=56.0, 2024=39.0, 2025=39.0, 2026=39.0 but live counterfactual.py has 2022=73.0, 2023=45.0, 2024=36.0, 2025=42.0, 2026=40.0. Plan frontmatter directive "Existing 2018-2026 values unchanged" forced preserving live values — the live counterfactual.py source defines "existing". Plan's interfaces block was stale; accepting its snapshot would have tripped the drift tripwire on the same commit it was introduced. |
| Plan 05-04 closed Phase 4 SEED-001 partial-coverage gap in same plan (2018-2020 + 2024-2026 YAML + _TRACKED additions) rather than scope-split | Plan Task 2 Step 1 explicitly authorised co-extension ("Also add the 6 missing existing-year entries — drift-test completion per Phase 4 STATE note"). Cleaner to close Phase-4 debt in the same commit boundary that introduces Phase-5 additions. Drift tripwire now enumerates all 25 DEFAULT_CARBON_PRICES year keys (was only 3: 2021-2023); any future silent edit to any year key fails test_yaml_value_matches_live. |
| Plan 05-06 manifest.py refactor via Option 3 (scheme-awareness inside one manifest) | Option 1 (scheme_name kwarg) forces N invocations and wouldn't surface multi-scheme datasets in one file; Option 2 (per-scheme manifest files) breaks ARCHITECTURE §4.3 one-manifest contract. Option 3 iterates `schemes: Iterable[tuple[str, Any]]` + `derived_root: Path`; `GRAIN_SOURCES/TITLES/DESCRIPTIONS` become nested dicts keyed by scheme × grain. Filesystem-driven grain discovery via `derived_root/<scheme>/*.parquet` glob; per-grain B-02 provenance contract preserved. Plan 05-07 now needs only one-line `("ro", ro)` SCHEMES append; Phase 7+ schemes inherit multi-scheme publishing for free. All 8 pre-existing test_manifest.py tests pass byte-identical (CfD URL pattern `/data/latest/cfd/*.parquet` unchanged). |
| Plan 05-06 test_manifest fixture migrated to new signature rather than back-compat wrapper | The fixture is test infrastructure (not a public contract); the 8 CfD tests assert behaviour on manifest output (round-trip identity, absolute URLs, sha256, methodology_version D-12 chain, generated_at stability Pitfall 3) — not call-signature. All 8 pass unchanged. Keeping a deprecated `derived_dir=` kwarg overload would have forced maintaining parallel code paths through Phase 7+. |
| Plan 05-07 RO manifest provenance maps landed as Rule 2 auto-fix in GREEN phase rather than a separate commit | Plan 05-06 refactored the iterator to be scheme-parametric but left `GRAIN_SOURCES/GRAIN_TITLES/GRAIN_DESCRIPTIONS` as CfD-only dicts — `_build_dataset_entry` returns None when `_grain_sources("ro", grain)` is missing, so the multi-scheme iteration produced a CfD-only manifest (5 datasets). Plan 05-07's must_have truth ("manifest.json with 10 Dataset entries") is unreachable without filling the RO half. The fill is scoped inside `src/uk_subsidy_tracker/publish/manifest.py` — the same file Plan 05-06 flagged in PATTERNS.md line 858 as requiring population ("Without this refactor, RO grains silently fail to publish to site/data/manifest.json"). Landing it as the GREEN gate for Task 2's failing Test 2 keeps the change atomic and traceable: RED commit 86bf146 surfaces the gap; GREEN commit 6bb38b7 fills it. |
| Plan 05-07 invariant-pinning tests may pass on first commit and still belong in a `test(...)` RED commit | `test_ro_refresh_converges_on_unchanged_upstream` passes immediately because `schemes/ro/_refresh.py::refresh()` shipped in 05-05 already satisfies the D-18 invariant (it writes sidecars via the shared `sidecar.write_sidecar()` helper). The test is still shipped as a `test(...)` commit ahead of any implementation change because it ADDS the invariant gate — future refactors that break the "refresh() → upstream_changed() == False" contract now fail CI. Distinct from classical TDD RED where the feature under test does not yet exist; this is an invariant-pinning RED where the feature exists but the gate does not. |
| Plan 05-10 RO test parametrisations use INDEPENDENT module-scoped fixtures alongside CfD (PATTERNS.md directive — NOT merged) | PATTERNS.md "Do NOT merge CfD + RO into one parametrisation — keep separate for clarity and independent module-scoped fixtures." Each scheme rebuilds into its own tmp dir; cross-scheme ordering assumption is impossible. Naming convention: scheme-A keeps existing names (`derived_dir`, `GRAINS`); scheme-B prefixes with the scheme key (`ro_derived_dir`, `RO_GRAINS`). FiT (Phase 7) and onwards inherit the same naming pattern by default. |
| Plan 05-10 D-11 stub-data fallback skip helper for RO row-conservation tests | pandas `assert_series_equal` rejects empty MultiIndex levels because `inferred_type` differs between an empty groupby result (carries column dtype, e.g. `'string'`) and an empty `set_index` result (reports `'empty'`). Same shape, different metadata. Cleanest fix: extract `_skip_if_empty_ro_station_month()` helper called at the top of every parametrisation; reason string mentions "real RER data wired" for grep-discoverability when seed-stub data is replaced. Tests re-arm automatically the moment a single non-empty Ofgem RER row arrives — no test code change required. |
| Plan 05-10 D-10 column-order assertion runs unconditionally on empty Parquets | Plan's literal `test_ro_parquet_grain_schema` placed `pytest.skip()` for empty data BEFORE the column-order assertion. Reordered so column-order check runs first (always), per-row Pydantic validation skips on empty. Parquet column metadata is a file-header property meaningful even with zero rows — if a future plan accidentally reordered RO row-model fields, the regression surfaces immediately rather than waiting for real data. Same robustness bias as the D-11 helper above: lose as little contract coverage as possible to stub-data conditions. |

### Blockers

None currently.

### Todos

- [x] Plan Phase 1 (`/gsd-plan-phase 1`)
- [x] Execute Phase 1 (`/gsd-execute-phase 1`) — 4/4 plans, 5/5 success criteria, MIT licence
- [ ] Rename GitHub repo `cfd-payment` → `uk-subsidy-tracker` (manual UI step; `correction` label already created and will travel on rename)
- [x] Plan Phase 2 (`/gsd-plan-phase 2`)
- [x] Execute Phase 2 Plan 01 — counterfactual pin + GOV-04 seed
- [x] Execute Phase 2 Plan 05 — scope-correction bookkeeping (ROADMAP + REQUIREMENTS + CHANGES + ARCHITECTURE §11 P1 amendment)
- [x] Execute Phase 2 Plan 02 — pandera schemas on elexon/ons_gas + `tests/test_schemas.py` + `tests/test_aggregates.py` pre-Parquet scaffolding
- [x] Execute Phase 2 Plan 03 — `tests/test_benchmarks.py` + `tests/fixtures/benchmarks.yaml` (D-11 fallback shipped)
- [x] Execute Phase 2 Plan 04 — GitHub Actions CI workflow green on push + pull_request (TEST-06; runs 24775720044 + 24775750701)
- [x] Rename GitHub repo (resolved via fresh `richardjlyon/uk-subsidy-tracker` + archive of `cfd-payment`; retrospective Phase-1 shortfall surfaced and closed in Plan 02-04)
- [x] Plan Phase 3 (`/gsd-plan-phase 3`) — Chart Triage Execution (4 plans planned)
- [x] Execute Phase 3 Plan 01 — Wave 1 cleanup: TRIAGE-01 (scissors + bang_for_buck_old deleted) + mkdocs.yml D-06 theme features + validation: block
- [x] Execute Phase 3 Plan 02 — Wave 2: five-theme docs tree + nav: rewrite (TRIAGE-03 shipped; mkdocs --strict clean)
- [x] Execute Phase 3 Plan 03 — Wave 3: PROMOTE chart pages filled with D-01 content (TRIAGE-02 + GOV-01 shipped; mkdocs --strict clean; 7 pages 127-172 lines)
- [x] Execute Phase 3 Plan 04 — Wave 4: full regen + CI strict-build gate (TRIAGE-04)
- [x] Plan Phase 4 (`/gsd-plan-phase 4`) — Publishing Layer (6 plans planned)
- [x] Execute Phase 4 Plan 01 — Wave 0: pyarrow + duckdb deps; SEED-001 Tier 2 constants drift tripwire
- [x] Execute Phase 4 Plan 02 — Raw-layer migration (`data/*` → `data/raw/<publisher>/<file>` + .meta.json sidecars; loader path updates; 100% rename similarity; 3 atomic commits 3f0d037 / 5859b15 / ed4c258)
- [x] Execute Phase 4 Plan 03 — Derived layer: `src/uk_subsidy_tracker/schemas/` Pydantic + `schemes/cfd/*` derivation shipped; 5 Parquet + 5 schema.json siblings emitted in 0.74s; TEST-02/03/05 formally closed on Parquet output (`test_determinism.py` 10/10 via `pyarrow.Table.equals()`); `isinstance(cfd, SchemeModule)` smoke green; GOV-02 substrate (methodology_version column) shipped. Four commits 6b695cd / e00a4f6 / 1a0575f / 79ccb2b.
- [x] Execute Phase 4 Plan 04 — Publishing layer: `src/uk_subsidy_tracker/publish/{manifest,csv_mirror,snapshot}.py` + `refresh_all.py` shipped. manifest.py: Pydantic Manifest/Dataset/Source models (D-07 verbatim), per-grain GRAIN_SOURCES provenance (B-02 mitigation), content-addressed generated_at (Pitfall 3), upstream_url:str not HttpUrl (Pitfall 6), raw-file sha256 re-computed on build (W-05). csv_mirror.py: pinned pandas args (D-10). snapshot.py CLI: --version/--output/--dry-run (D-13/D-14). refresh_all.py per-scheme dirty-check + short-circuit (D-16/D-18). 15 new tests (8 manifest + 7 csv_mirror); 69 passed + 4 skipped. Five commits 2e90d11 / f6afd73 / a024c99 / 14125f4 / 3e94bf4. PUB-01/02/03/05/06 + GOV-02 + GOV-06 closed.
- [x] Execute Phase 4 Plan 05 — Workflows: `.github/workflows/refresh.yml` (daily cron + PR-based commit-back) + `deploy.yml` (tag-push release asset upload) + `.github/refresh-failure-template.md`. Three atomic commits f3b9e33 / bbb421d / 20b7701. GOV-03 + PUB-03 closed; GOV-06 operationalised end-to-end. 69 passed + 4 skipped; mkdocs --strict green. User setup deferred to user (dashboard-only: "Allow GitHub Actions to create and approve pull requests" toggle + `daily-refresh`/`refresh-failure` label creation).
- [x] Execute Phase 4 Plan 06 — Docs + benchmark floor: `docs/data/index.md` (144 lines, 6 D-27 sections, pandas + DuckDB + R snippets + BibTeX/APA citation templates + SHA-256 integrity check) + mkdocs.yml Data nav tab + docs/index.md homepage link + docs/about/citation.md versioned-snapshot pattern + tests/fixtures/benchmarks.yaml 2026-04-22 audit note (Disposition C per user: D-11 fallback re-confirmed — ARA 2024/25 PDF reports FY only, RESEARCH Pitfall 7 prevents 0.1% floor activation) + CHANGES.md consolidation. Four atomic commits 880c41e / caf7a41 / c4e80f2 / 2a91a75. PUB-04 closed. 69 passed + 4 skipped; mkdocs --strict green. **Phase 4 COMPLETE.**
- [x] Execute Phase 4 Plan 07 — Refresh-loop closure (gap-closure from 04-VERIFICATION.md): new `src/uk_subsidy_tracker/data/sidecar.py` shared atomic writer + `schemes/cfd/_refresh.py::refresh()` wires all 3 downloaders + writes 5 sidecars + `ons_gas.download_dataset()` UnboundLocalError fix + timeout=60 + fail-loud raise + `tests/test_refresh_loop.py` + `tests/test_ons_gas_download.py` + `scripts/backfill_sidecars.py` refactored to delegate to write_sidecar() + CHANGES.md entries. Five atomic commits 29b5524 / ac9675a / 42c8c3e / 3497296 / 14e2138. 74 passed + 4 skipped (+5 new). GOV-03 + PUB-05 fully satisfied; refresh-loop invariant algebraically locked. **Phase 4 remains COMPLETE (7/7 plans).**
- [ ] User: enable "Allow GitHub Actions to create and approve pull requests" at https://github.com/richardjlyon/uk-subsidy-tracker/settings/actions (Plan 04-05 user_setup)
- [ ] User: create GitHub labels `daily-refresh` (blue) + `refresh-failure` (red) at https://github.com/richardjlyon/uk-subsidy-tracker/labels (Plan 04-05 user_setup; distinct from pre-existing `correction` label)
- [ ] Phase 3/4: transcribe LCCC ARA 2024/25 calendar-year CfD aggregate into `tests/fixtures/benchmarks.yaml::lccc_self` to activate the mandatory D-10 floor check (folded into Phase 4 Plan 06)
- [ ] Phase 3/4: populate external benchmark anchors (OBR EFO calendar-year translation, DESNZ, HoC, NAO) as transcribable figures surface
- [ ] Phase 4+: Author `docs/abbreviations.md` glossary (CfD, AR1, CCGT, MWh, ETS, SCC, LCCC) to populate `content.tooltips` Material feature (OQ5 seed from Phase 3 Plan 03-04 — feature enabled, glossary empty)
- [ ] Phase 4+: Add CF-formula pin test — `tests/test_capacity_factor.py` freezing `CF = generation / (capacity × hours)` per Reliability theme methodology (OQ4 seed from Phase 3 Plan 03-04 — capacity-factor-seasonal chart caveat tracks this)
- [x] Plan Phase 5 (`/gsd-plan-phase 5`) — RO Module (13 plans planned)
- [x] Execute Phase 5 Plan 01 — Ofgem RO scrapers + seed raw/ofgem/ tree via Option-D fallback. Two atomic commits 8c71cde (Task 1: scrapers + seeds + sidecars + investigation report) / 5729d02 (Task 2: 8 mocked tests). 82 passed + 4 skipped (+8 new). RO-01 closed. All 6 Plan 05-13 follow-up items recorded in `.planning/phases/05-ro-module/05-01-TASK-1-INVESTIGATION.md` `## Plan 05-13 Follow-Ups` section for post-execution review.
- [x] Execute Phase 5 Plan 02 — RO bandings YAML (85 entries: 69 GB banded + 12 NI [ASSUMED] + 4 grandfathered) + `src/uk_subsidy_tracker/data/ro_bandings.py` two-layer Pydantic loader + barrel re-export + 7 unit tests. Two atomic commits `92a4a65` (Task 1: feat yaml + loader + __init__) / `7f53bd0` (Task 2: test 7 cases). 89 passed + 4 skipped (+7 new). Partial RO-02 progress — foundation for Plan 05-05 cost_model.py.
- [x] Execute Phase 5 Plan 12 — CHANGES.md consolidation: [Unreleased] reorganised into Keep-a-Changelog canonical order with `#### Phase 5 — Renewables Obligation Module` and `#### Phase 4 — Publishing Layer (carry-over)` sub-headings; `## Methodology versions` H3 ladder grew from 2 to 4 entries with REF Constable adoption (Plan 05-09; sentinel-gated D-14 hard block) + Mutualisation scope correction SY 2021-22 only (Plan 05-05) + publish/manifest.py scheme-parametric refactor (Plan 05-06) on top of the existing DEFAULT_CARBON_PRICES extension entry from Plan 05-04. One atomic commit `22bb8c5` (1 file, 379 insertions, 208 deletions). Test count unchanged 163 passed + 12 skipped + 22 xfailed. All 9 plan `<verify>` grep gates pass. Phase 5 12/13 plans complete; only Plan 05-13 PEHR remains. RO-01..RO-06 marked complete in REQUIREMENTS.md.
- [x] Execute Phase 5 Plan 07 — Wave 3 lead-off: `refresh_all.SCHEMES` += `("ro", ro)` one-line append + `publish/manifest.py` GRAIN_SOURCES/TITLES/DESCRIPTIONS RO sub-dicts (Rule 2 auto-fix for Plan 05-06 provenance-dict gap) + 2 new RO refresh-loop invariant tests (importlib submodule-shadow bypass; mirrors Plan 04-07 pattern). Three atomic commits `8b2fc1d` (Task 1 feat SCHEMES) / `86bf146` (Task 2 RED test) / `6bb38b7` (Task 2 GREEN manifest provenance). 153 passed + 4 skipped (+2 new). RO-02 + RO-03 end-to-end refresh path closed; Plan 05-08 (RO chart files) unblocked.
- [ ] Plan 05-13 review: 6 follow-up items from Plan 05-01 INVESTIGATION (replace 3 stub raw files with real Ofgem exports, plumb OFGEM_RER_* secrets if Playwright path approved, transcribe roc-prices.csv from public PDFs, decide on Option-C pdfplumber middle-ground, re-examine RESEARCH §2 stale URL templates)
- [ ] Plan 05-13 review: transcribe NIROC primary source to replace the 12 [ASSUMED] entries in `src/uk_subsidy_tracker/data/ro_bandings.yaml` with verified Utility Regulator NI banding factors (drop "[ASSUMED]" markers from `basis:` and cite authoritative NISR statutory instrument numbers)

### Notes

- Brownfield rename COMPLETE: `src/cfd_payment/` → `src/uk_subsidy_tracker/` via `git mv` (commits 7b5538b, 596594c)
- Python floor bumped 3.11 → 3.12 in pyproject.toml + CLAUDE.md (B-3 resolved)
- MkDocs theme swapped `readthedocs` → `material` with palette toggle; `docs/methodology/gas-counterfactual.md` now canonical (was `docs/technical-details/`)
- Root docs committed: ARCHITECTURE.md, RO-MODULE-SPEC.md, CHANGES.md, CITATION.cff, LICENSE (MIT), README.md
- `scissors.py` and `bang_for_buck_old.py` deleted via `git rm` in Plan 03-01 (history preserved; commit c5cc229). Orchestrator now imports 14 chart modules (was 15). Stale PNG/HTML at `docs/charts/html/subsidy_scissors*` remain gitignored and will be purged via Plan 03-04's full regen.
- `mkdocs.yml` primed with D-06 theme features (10 entries) + root-level `validation:` block (nav.omitted_files: warn + anchor + absolute_links warnings) in Plan 03-01 (commit 575b57d). `nav:` block rewritten to 22-entry 5-theme D-04 tree in Plan 03-02 (commit f17f215); first clean `mkdocs build --strict` of Phase 3 (0 warnings, 0.39s build).
- 5 theme directories created under `docs/themes/` (cost/recipients/efficiency/cannibalisation/reliability) via Plan 03-02 — each carries `index.md` (D-02 hybrid: adversarial claim + grid-cards gallery + cross-theme pointers + methodology link) + `methodology.md` (D-05a theme-specific; Cost + Efficiency link OUT to shared `../../methodology/gas-counterfactual.md`). 3 existing CfD chart pages relocated to `docs/themes/cost/` via `git mv` at 97-98% similarity. `docs/charts/index.md` deleted; `docs/charts/subsidy/` directory removed. `docs/index.md` link-rot fixed with 5-theme "Explore by theme" section.
- 7 PROMOTE chart pages filled with full D-01 content in Plan 03-03 (commits 7f38d46, 3e55b22, 4d57874, 30638bf, 3656da3, b75d36d, f527479): lorenz, cfd-payments-by-category, subsidy-per-avoided-co2-tonne, capture-ratio, capacity-factor-seasonal, generation-heatmap, rolling-minimum — each 127-172 lines, all 6 D-01 H2 sections in canonical order, GOV-01 four-way coverage (PNG + methodology link + ≥1 test permalink + Python source permalink) grep-verifiable per page. D-05 shared gas-counterfactual link present on Efficiency flagship alongside pre-existing Cost-theme pages. OQ4 Phase-4 CF-formula pin-test seed recorded in `capacity-factor-seasonal.md` Caveats. Adversarial rhetorical payloads locked per RESEARCH §7.
- CI is now live: `.github/workflows/ci.yml` single-job pytest on push/PR, pinned `astral-sh/setup-uv@v8.1.0` + `actions/checkout@v5`, green on both trigger paths
- GitHub repo lives at `github.com/richardjlyon/uk-subsidy-tracker` (old `cfd-payment` repo archived; origin URL updated locally)
- Raw data files (5 files, each ≤100MB) migrated from flat `data/*.{csv,xlsx}` to canonical `data/raw/<publisher>/<file>` layout per ARCHITECTURE §4.1 in Plan 04-02 (commit 5859b15; 100% rename similarity; `git log --follow` chases back through the pre-rename 75774b8 → 5859b15 history end-to-end). Sibling `.meta.json` sidecars carry SHA-256 + upstream URL + retrieved_at + `backfilled_at: "2026-04-22"` for each file. `scripts/backfill_sidecars.py` retained for future re-runs (commit 3f0d037). Pre-rename flat `data/*.{csv,xlsx}` paths no longer exist.
- **Derived layer lives.** `data/derived/cfd/` populated by `cfd.rebuild_derived()` with 5 Parquet + 5 schema.json siblings (3,624 total rows; 0.74s end-to-end) per Plan 04-03 (commits 6b695cd / e00a4f6 / 1a0575f / 79ccb2b). Gitignored per D-22 (generated artefact, not committed). Every row carries `methodology_version` column (GOV-02 substrate). `src/uk_subsidy_tracker/{schemas,schemes}/` are the new packages; `schemes/cfd/` is the first real §6.1 implementation template for Phase 5+ scheme modules.
- **Publishing layer lives.** `src/uk_subsidy_tracker/publish/` package (Plan 04-04, commits 2e90d11 / f6afd73 / a024c99 / 14125f4 / 3e94bf4) emits `site/data/manifest.json` + `site/data/latest/cfd/<grain>.{parquet,csv,schema.json}` (15 artefacts per scheme). manifest.py is the D-07 verbatim public contract; snapshot.py CLI assembles a versioned release-asset tree for deploy.yml tag-push upload. refresh_all.py per-scheme dirty-check orchestrator short-circuits when upstream unchanged (Pitfall 3 — non-noisy daily PR). `.gitignore` restructured to carve out `site/data/` from the blanket `site/` mkdocs-build ignore (Git does not support re-including paths under an ignored directory, so explicit per-subtree ignores replace the blanket rule). D-12 chain closed end-to-end: counterfactual.METHODOLOGY_VERSION → DataFrame column → Parquet column → validate() check → manifest top-level field. PUB-01/02/03/05/06 + GOV-02 + GOV-06 (snapshot URL portion) now closed.
- Wave 0 deps live: `pyarrow==24.0.0` + `duckdb==1.5.2` in `uv.lock` (commit b9c8233). Both wheel-resolved for Python 3.13 on macOS-arm64; linux-x86_64 standard.
- SEED-001 Tier 2 drift tripwire live: `tests/test_constants_provenance.py` 13 cases (6 name-in-yaml + 6 value-matches-live + 1 audits-not-overdue warn) wired against `tests/fixtures/constants.yaml` through `tests/fixtures/__init__.py::load_constants` (commit f2548df). SEED-001 Tier 3 (auto-rendered provenance doc page) deferred per CONTEXT D-25.
- `_TRACKED` allowlist in `tests/test_constants_provenance.py` = 6 constants (CCGT_EFFICIENCY, GAS_CO2_INTENSITY_THERMAL, DEFAULT_NON_FUEL_OPEX, DEFAULT_CARBON_PRICES_{2021,2022,2023}). Adding a new regulator-sourced constant to `counterfactual.py` requires adding to both `_TRACKED` AND `constants.yaml` for the tripwire to fire on it.
- **Workflows live.** `.github/workflows/refresh.yml` (89 lines) + `.github/workflows/deploy.yml` (65 lines) + `.github/refresh-failure-template.md` (19 lines) shipped in Plan 04-05 (commits f3b9e33 / bbb421d / 20b7701). refresh.yml fires at 06:00 UTC daily + on `workflow_dispatch`; in-workflow gates (benchmark floor → determinism/schema/aggregates/constants/manifest/csv_mirror pytest → plotting → mkdocs --strict) run BEFORE PR open; `peter-evans/create-pull-request@v8` opens `daily-refresh`-labelled PR on `refresh/<run_id>` branch; `peter-evans/create-issue-from-file@v6` opens `refresh-failure`-labelled Issue on any step failure. deploy.yml fires on `git push --tags` matching `v*`; validates D-14 calendar-tag format inline; runs `uk_subsidy_tracker.publish.snapshot --version ${{ github.ref_name }} --output snapshot-out/`; uploads via `softprops/action-gh-release@v2` with 4-glob allowlist (manifest.json + **/*.parquet + **/*.csv + **/*.schema.json). All actions pinned to explicit majors; zero `@main`/`@master` refs. Permissions least-privilege: refresh.yml = contents/pr/issues:write; deploy.yml = contents:write only. GOV-03 + PUB-03 closed; GOV-06 operationalised end-to-end.
- **Journalist/academic-facing data docs live.** `docs/data/index.md` (144 lines) shipped in Plan 04-06 (commit 880c41e). Six canonical D-27 sections: What we publish / How to use it / How to cite it / Provenance guarantees / Known caveats and divergences / Corrections and updates. Copy-pasteable snippets: pandas (with hashlib SHA-256 integrity check), DuckDB (INSTALL httpfs + read_parquet over HTTPS), R (arrow + httr + jsonlite). BibTeX + APA citation templates both anchored on `releases/tag/v<YYYY.MM>`. Cross-links to methodology/gas-counterfactual.md, about/corrections.md, CITATION.cff, CHANGES.md, tests/test_benchmarks.py. Wired into 23-entry top-level nav via `mkdocs.yml` `- Data: data/index.md` tab between Reliability and About (commit caf7a41). docs/index.md gains "For journalists and academics → Use our data" homepage link. docs/about/citation.md gains "Citing a specific data snapshot" section with versioned-snapshot BibTeX template — GOV-06 documented from two surfaces. Closes PUB-04 end-to-end; demonstrates PUB-06 with executable fetch snippets.
- **D-11 lccc_self fallback re-confirmed with 2026-04-22 audit note (Plan 04-06 Disposition C, commit c4e80f2).** `tests/fixtures/benchmarks.yaml` header extended with 12-line audit block above `lccc_self: []`: Phase 4 Plan 06 re-examined the D-11 posture per CONTEXT D-26; ARA 2024/25 reports FY Apr 2024 – Mar 2025 without a quarterly breakdown; 0.1% floor tolerance against FY-vs-CY mismatch (RESEARCH Pitfall 7) strictly worse than no floor per Phase 2 D-10/D-11; awaiting future LCCC quarterly publication for clean CY aggregate. Matches the plan's `<autonomous_fallback>` clause verbatim. `test_lccc_self_reconciliation_floor` continues to skip cleanly with D-11-citing reason string. Pattern established: `tests/fixtures/benchmarks.yaml` now carries a dated audit log; every future Phase that re-examines the floor appends its own dated entry.
- **Phase 4 exit complete (7/7 plans shipped including 04-07 gap-closure).** All five ROADMAP Phase-4 success criteria delivered: (1) three-layer pipeline `data/raw/` → `data/derived/` → `site/data/` operational for CfD (04-02/03/04); (2) test_determinism.py 10/10 via `pyarrow.Table.equals` (04-03); (3) manifest.json full provenance per dataset, GOV-02 closed (04-04); (4) daily refresh + tagged-release CI operational, GOV-03 + PUB-03 closed (04-05) + GOV-03 robustness locked in 04-07 (refresh-loop invariant test); (5) docs/data/index.md + LCCC floor re-examination with explicit audit evidence, PUB-04 closed (04-06). 74 passed + 4 skipped; `mkdocs build --strict` green. Phase 5 (Renewables Obligation module) fully unblocked: schemes/cfd/ is the §6.1 Protocol-conformance template; refresh_all.SCHEMES is a one-line append; manifest.py iterates SCHEMES; docs/data/index.md snippets work unchanged for new schemes.
- **Phase 5 Plan 01 shipped (Option-D fallback for Ofgem RO scrapers).** RO-01 closed via deterministic Option-D dispatch after Task-1 probing showed RER returns HTTP 403 (SharePoint-OIDC walled), legacy `renewablesandchp.ofgem.gov.uk` decommissioned, and all RESEARCH §2 transparency-document URL templates returning 404 (incl. the public-PDF path Step 3.i targeted for `roc-prices.csv`). Two atomic commits: `8c71cde` (Task 1: `src/uk_subsidy_tracker/data/ofgem_ro.py` 170 lines + `roc_prices.py` 115 lines + 3 header-only stub raw files under `data/raw/ofgem/` + 3 sidecars via shared `write_sidecar()` + `scripts/backfill_sidecars.py::URL_MAP` extended with 3 `option-d-stub:ofgem-rer-manual` marker entries + `.planning/phases/05-ro-module/05-01-TASK-1-INVESTIGATION.md` + `data/raw/ofgem/README.md`); `5729d02` (Task 2: `tests/data/test_ofgem_ro.py` 183 lines, 8 mocked tests covering RequestException + timeout=60 + happy path + source-grep D-17 guard + Option-D RuntimeError guard for both modules). All scrapers honour Phase 4 D-17: `output_path` bound BEFORE `try:`, `timeout=60` on `requests.get()`, bare `raise` in except. Under Option D the `download_*` functions raise `RuntimeError("manual refresh — see INVESTIGATION.md ...")` when `_REGISTER_URL` / `_GENERATION_URL` / `_PRICES_URL` are empty, so accidental cron firings fail-loud rather than silently overwriting committed seeds. Loader-owned pandera validation (`strict=False`) tolerates the 0-row stub DataFrames. `INVESTIGATION.md ## Plan 05-13 Follow-Ups` records 6 human-decision items routed to Plan 05-13 Task 5 post-execution review (replace 3 stubs with real exports, plumb `OFGEM_RER_*` secrets if Playwright path approved, transcribe `roc-prices.csv` from public PDFs, decide on Option-C `pdfplumber` middle-ground, re-examine RESEARCH §2 stale URL templates). One Rule-1 deviation documented in `05-01-SUMMARY.md`: Plan Step 3.i prescribed PDF-transcribed `roc-prices.csv` ("PUBLIC DATA, NO AUTH REQUIRED") but the cited URL returns 404 in 2026-04, so stub strategy applied uniformly across all 3 files. 82 passed + 4 skipped (was 74 + 4; +8 new). `mkdocs --strict` unchanged.
- **Phase 5 Plan 02 shipped (RO bandings YAML + Pydantic loader).** `src/uk_subsidy_tracker/data/ro_bandings.yaml` transcribes REF "Notes on the Renewable Obligation" Table 1 into 85 (technology x commissioning-window x country) cells: 69 GB banded across 5 commissioning windows (pre-2013, 2013/14, 2014/15, 2015/16, 2016/17) + 12 NIROC [ASSUMED] entries + 4 grandfathered (pre-2006-07-11 1 ROC/MWh) rows for Onshore wind / Offshore wind / Hydroelectric / Landfill Gas. Every row carries full Provenance (source SI + url + basis + retrieved_on); NIROC entries flag `[ASSUMED]` in basis for grep-discoverability (verification routed to Plan 05-13 follow-up). `src/uk_subsidy_tracker/data/ro_bandings.py` (138 lines) mirrors `LCCCDatasetConfig` + `LCCCAppConfig` two-layer Pydantic pattern from Phase 2 D-07: `RoBandingEntry` + `RoBandingTable` + `load_ro_bandings()`. `RoBandingTable.lookup(technology, country, commissioning_date, chp=False) -> float` handles four dimensions with an explicit pre-2006-07-11 grandfathering short-circuit above the banded-rate scan; banded-rate scan `continue`s past `grandfathered=true` rows so YAML ordering cannot change behaviour (Rule 1 auto-fix applied during Task 1). `src/uk_subsidy_tracker/data/__init__.py` gains `from .ro_bandings import RoBandingEntry, RoBandingTable, load_ro_bandings` so Plan 05-05 `schemes/ro/cost_model.py` imports directly from `uk_subsidy_tracker.data`. 7 new unit tests (`tests/data/test_ro_bandings.py`, 79 lines): load succeeds + >=44 entries; every entry has Provenance fields; 4 lookup cases (GB pre-2013, GB 2013/14, NI pre-2013, unknown-cell KeyError); grandfathering override (Offshore wind banded=2.0 vs grandfathered=1.0 proves short-circuit does real work). Two atomic commits: `92a4a65` (Task 1: feat ro_bandings.yaml + ro_bandings.py + __init__.py barrel re-export) + `7f53bd0` (Task 2: test 7 cases as single GREEN -- Plan 04-01 / 05-01 precedent). 89 passed + 4 skipped (was 82 + 4; +7 new). Partial RO-02 progress; §6.1 Protocol contract lands in Plan 05-05. Grep tripwires verified post-commit: `grep -c "^  source:" ro_bandings.yaml` = 85; `grep -c "retrieved_on:" ro_bandings.yaml` = 85; `grep -c "\[ASSUMED\]" ro_bandings.yaml` = 16; `grep -c "grandfathered: true" ro_bandings.yaml` = 4.
- **Refresh-loop algebraically pinned (Plan 04-07).** Gap #1 + #2 from 04-VERIFICATION.md both closed. `src/uk_subsidy_tracker/data/sidecar.py` (78 lines; commit 29b5524) is the shared atomic sidecar writer — .tmp + os.replace; sort_keys=True + indent=2 + trailing newline; byte-identical serialisation across both call sites (`schemes/cfd/_refresh.py::refresh()` and `scripts/backfill_sidecars.py`). `refresh()` now fetches LCCC + Elexon + ONS and rewrites all 5 sidecars in-line (commit 42c8c3e); `_URL_MAP` byte-matches backfill URL_MAP (verified via dict-sort diff). `ons_gas.download_dataset()` fixed: output_path bound before try + timeout=60 + bare raise on failure (commit ac9675a; TDD RED→GREEN). `tests/test_refresh_loop.py` 2 tests lock the invariant "after refresh(), upstream_changed() returns False; generated_at advances once, stays stable on unchanged second run" (commit 3497296; fully mocked, no network). `tests/test_ons_gas_download.py` 3 tests regression-guard the error-path (commit ac9675a). `scripts/backfill_sidecars.py` refactored to delegate to write_sidecar() + overlay `backfilled_at` + `retrieved_at` from git log (commit 14e2138). Re-running backfill preserved sha256 byte-identity on all 5 sidecars (0 diff); only `retrieved_at` refreshed from BACKFILL_DATE fallback to real `git log --follow` values — strict provenance-accuracy improvement documented in STATE.md as expected behaviour. CHANGES.md [Unreleased] gained 3 Added + 2 Fixed (new section) + 1 Changed bullet. Option A architecture preserved: `refresh_all.py` UNCHANGED. Pattern for Phase 5+: each scheme's `_refresh.py::refresh()` fetches its own raw files and calls `write_sidecar()` for each — no scheme re-implements SHA chunking or atomic JSON write.

---

## Session Continuity

**To resume:** Read `.planning/STATE.md` (this file), then `.planning/ROADMAP.md` for phase structure, then `ARCHITECTURE.md §11` for authoritative exit criteria.

**Next command:** `/gsd-execute-phase 5 --plan 13` (Phase 5 Plan 12 COMPLETE — CHANGES.md consolidation shipped; Phase 5 audit trail now grep-verifiable with 6 RO requirement closures + 4 ## Methodology versions H3 audit entries). Plan 05-12 shipped 1 atomic commit (`22bb8c5`), 1 file modified (CHANGES.md), 1 file created (05-12-SUMMARY.md), ~4 min duration. Test count unchanged: **163 passed + 12 skipped + 22 xfailed**. RO-01..RO-06 marked complete in REQUIREMENTS.md; ROADMAP.md plan-progress row reflects 12/13. Phase 5 progress: 12/13 plans. Plan 05-13 (Post-Execution Human Review) is the only remaining plan — handles 8 follow-up items: replace 3 ofgem stub raws with real RER exports, plumb OFGEM_RER_* secrets if Playwright path approved, transcribe roc-prices.csv from public PDFs, decide on Option-C pdfplumber middle-ground, re-examine RESEARCH §2 stale URL templates, transcribe NIROC primary source for 12 [ASSUMED] entries, audit 2005-2017 DEFAULT_CARBON_PRICES against EEA + BoE primary sources, delete `.planning/phases/05-ro-module/05-09-DIVERGENCE.md` sentinel to re-arm REF reconciliation D-14 hard-block. **Earlier:** `/gsd-execute-phase 5 --auto --plan 12` (Plan 11 COMPLETE). | Earlier: `/gsd-execute-phase 5 --auto --plan 08` (Phase 5 Plan 07 COMPLETE — Wave 3 lead-off shipped; RO end-to-end refresh path closed). Plan 05-07 shipped 3 atomic commits (`8b2fc1d`, `86bf146`, `6bb38b7`), 3 files modified (refresh_all.py, manifest.py, test_refresh_loop.py), ~4 min duration. 153 passed + 4 skipped (+2 new RO refresh-loop tests; zero regressions). Phase 5 progress: 7/13 plans. `refresh_all.SCHEMES` now iterates both CfD and RO under D-18 per-scheme dirty-check; `manifest.build()` emits 10 Dataset entries (5 CfD + 5 RO) once `data/derived/ro/*.parquet` is rebuilt from non-stub raw data. Plan 05-08 (RO chart files: ro_dynamics, ro_by_technology, ro_concentration, ro_forward_projection) is next. Earlier: `/gsd-execute-phase 5 --auto --plan 07` (Phase 5 Plan 06 COMPLETE — manifest.py multi-scheme scaffolding); `/gsd-execute-phase 5 --auto --plan 03` (Phase 5 Plan 02 COMPLETE — partial RO-02 progress, bandings foundation shipped). Earlier: `/gsd-execute-phase 5 --auto --plan 02` (Phase 5 Plan 01 COMPLETE — RO-01 closed via Option-D fallback). Plan 05-01 shipped 2 atomic commits (8c71cde, 5729d02), 11 created files, 1 modified, 12-file file count, 7-min duration. 82 passed + 4 skipped (+8 new from `tests/data/test_ofgem_ro.py`). Phase 5 progress: 1/13 plans. RO-01 closed; remaining requirements (RO-02..RO-06) routed to Plans 05-02 through 05-13. Plan 05-13 will pick up the 6 Plan 05-01 follow-ups during post-execution review (replace 3 ofgem stubs with real exports, plumb `OFGEM_RER_*` secrets if Playwright path approved, transcribe `roc-prices.csv`, decide on `pdfplumber` middle-ground, re-examine RESEARCH §2 stale URLs). | Earlier: Phase 4 closed 7/7 plans: 04-01 (wave-0 deps + SEED-001 Tier-2 drift tripwire), 04-02 (raw-layer migration), 04-03 (derived layer + schemes/cfd/), 04-04 (publishing layer manifest + csv_mirror + snapshot), 04-05 (refresh.yml + deploy.yml + refresh-failure-template.md), 04-06 (docs/data/index.md + nav tab + citation versioned-snapshot pattern + D-11 lccc_self audit note Disposition C), 04-07 (refresh-loop closure: sidecar.write_sidecar + refresh() wires all 3 downloaders + rewrites all 5 sidecars + ons_gas fail-loud fix + refresh-loop invariant test + backfill script refactor). All five ROADMAP Phase-4 success criteria delivered. PUB-04 closed by 04-06; PUB-01/02/03/05/06 + GOV-02/03/06 closed by 04-04/05; GOV-03 robustness + PUB-05 end-to-end loop locked by 04-07. D-11 fallback preserved with explicit 2026-04-22 audit evidence per user-selected Disposition C (ARA 2024/25 FY-only limitation, RESEARCH Pitfall 7). 74 passed + 4 skipped; `mkdocs build --strict` green; METHODOLOGY_VERSION stays "0.1.0" (bump is Phase 6+). Phase 5 is unblocked: schemes/cfd/ is the §6.1 Protocol template for schemes/ro/ (now including a complete refresh-loop reference implementation that writes sidecars via the shared `sidecar.write_sidecar()` helper); `refresh_all.SCHEMES` is a one-line append; manifest iteration is SCHEMES-driven; docs/data/index.md works unchanged for new schemes. Outstanding user setup (deferred to user, dashboard-only, does not block Phase 5 planning): "Allow GitHub Actions to create and approve pull requests" toggle + `daily-refresh`/`refresh-failure` label creation + (from 04-06 follow-up) LCCC ARA CY-aggregate transcription when a future quarterly publication surfaces.

**Stopped at:** Completed 05.2-02-PLAN.md

---
*State initialized: 2026-04-21 after roadmap creation*
