---
phase: 05.2-ro-data-reconstruction-aggregate-grain
plan: 06
type: execute
wave: 6
depends_on: [03, 04, 05]
files_modified:
  - .planning/phases/05-ro-module/05-09-DIVERGENCE.md
  - .planning/REQUIREMENTS.md
  - CHANGES.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - RO-04
  - RO-05
  - RO-06
tags:
  - ro
  - phase-exit
  - ci-gate
  - governance
user_setup: []

must_haves:
  truths:
    - "The Phase 5 sentinel .planning/phases/05-ro-module/05-09-DIVERGENCE.md is DELETED from the working tree after REF reconciliation confirms 22 passes"
    - "tests/test_benchmarks.py::test_ref_constable_ro_reconciliation reports 22 passed (not xfailed) after the sentinel delete; D-14 HARD CI BLOCK is live"
    - ".planning/REQUIREMENTS.md marks RO-04 S4+S5 as deferred-data-gated (NOT invalidated) with cross-reference to backlog 999.1 + SEED-002 in planning-internal artefacts only (NOT in docs/)"
    - "CHANGES.md [Unreleased] documents the full audit trail: Added (aggregate pipeline + transcribed data + §7 Data access + Data access methodology subsection); Changed (schemas/ro field nullability; refresh short-circuit); Fixed (REF reconciliation hard block re-armed)"
    - "The final uv run mkdocs build --strict passes with zero warnings and zero errors (phase-exit gate)"
    - "The final uv run pytest tests/ -q reports zero failed, zero error, the baseline skipped count plus new dormant skips, and the 22 REF-year tests report PASSED (no xfailed for RO REF reconciliation)"
    - ".planning/ROADMAP.md Phase 05.2 row is marked Complete and the Plans section enumerates all 6 plans with [x] checkboxes"
  artifacts:
    - path: ".planning/phases/05-ro-module/05-09-DIVERGENCE.md"
      provides: "DELETED — this is the sentinel file Phase 05.2 removes"
      contains: "(file must NOT exist at phase exit)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated RO-04 status with deferred-data-gated qualifier"
      contains: "RO-04"
    - path: "CHANGES.md"
      provides: "[Unreleased] section with Phase 05.2 audit trail"
      contains: "[Unreleased]"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 05.2 progress entry marked Complete"
  key_links:
    - from: ".planning/phases/05-ro-module/05-09-DIVERGENCE.md absence"
      to: "tests/test_benchmarks.py::test_ref_constable_ro_reconciliation xfail block"
      via: "Sentinel existence check at test_benchmarks.py:263"
      pattern: "if _DIVERGENCE_SENTINEL.exists()"
    - from: ".planning/REQUIREMENTS.md RO-04"
      to: "backlog 999.1 / SEED-002"
      via: "Internal planning cross-reference only (NOT in docs/)"
      pattern: "deferred-data-gated"
    - from: "CHANGES.md [Unreleased]"
      to: "Phase 05.2 full audit trail"
      via: "Keep-a-Changelog format (Added / Changed / Fixed subsections)"
      pattern: "## \\[Unreleased\\]"
---

<objective>
Close Phase 05.2 with the governance ritual: (1) delete the `05-09-DIVERGENCE.md` sentinel, converting 22 xfailed REF reconciliation tests into 22 passed (D-14 HARD CI block re-armed); (2) update `.planning/REQUIREMENTS.md` to mark RO-04's S4/S5 components as `deferred-data-gated` (NOT invalidated) with internal cross-reference to backlog 999.1 + SEED-002; (3) write the full Phase 05.2 audit trail into `CHANGES.md [Unreleased]`; (4) update `.planning/ROADMAP.md` to mark Phase 05.2 Complete with the 6-plan enumeration; (5) run the final `uv run mkdocs build --strict` + `uv run pytest tests/ -q` phase-exit gate — must be fully green.

Purpose: This is the wave that turns Phase 05.2 from "plans executed" into "phase complete". The REF reconciliation un-xfail is the single most load-bearing step — if it fails (any REF year exceeds ±3%), the phase does NOT ship; the sentinel is re-written with a root-cause note and Wave 3 pipeline is iterated. Assuming Waves 3 + 5 produced correct reconstructions, this wave is ~20-30 min of governance work.

Output: 1 file deleted (DIVERGENCE.md); 3 files updated (REQUIREMENTS.md, CHANGES.md, ROADMAP.md); phase-exit gate commands green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.2-ro-data-reconstruction-aggregate-grain/05.2-CONTEXT.md
@.planning/phases/05.2-ro-data-reconstruction-aggregate-grain/05.2-PATTERNS.md
@.planning/phases/05-ro-module/05-09-DIVERGENCE.md
@.planning/REQUIREMENTS.md
@CHANGES.md
@.planning/ROADMAP.md
@tests/test_benchmarks.py

<interfaces>
From .planning/phases/05-ro-module/05-09-DIVERGENCE.md (lines 119-143 per CONTEXT.md `<canonical_refs>`):
The "Re-arm verification" block at the tail of the sentinel file IS the procedure this wave follows. Three steps:
  1. Confirm non-stub derived Parquet (Wave 3 has already done this via annual_summary.parquet)
  2. Delete the sentinel: `rm .planning/phases/05-ro-module/05-09-DIVERGENCE.md`
  3. Re-run `uv run pytest tests/test_benchmarks.py -q -k ref_constable` — expected: 22 passed within ±3%.

From tests/test_benchmarks.py:
- REF_TOLERANCE_PCT: float = 3.0 (the hard block)
- Sentinel check at line ~263: `if _DIVERGENCE_SENTINEL.exists(): pytest.xfail(...)`
- When sentinel is absent, `pytest.xfail()` is not called → actual ±3% assertion runs.

From .planning/ROADMAP.md:
- Phase 05.2 entry at lines ~125-144
- Plans line currently reads "**Plans**: TBD" — becomes "**Plans:** 6/6 plans complete"
- Plans enumeration becomes `- [x] 05.2-01-PLAN.md — ...` through `- [x] 05.2-06-PLAN.md — ...`
- Progress table row for Phase 05.2 updates Completed column to today's date
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sanity-check REF reconciliation readiness, delete the 05-09-DIVERGENCE.md sentinel, and verify all 22 parametrisations move from xfailed to passed</name>
  <files>.planning/phases/05-ro-module/05-09-DIVERGENCE.md (DELETE)</files>
  <read_first>
    - .planning/phases/05-ro-module/05-09-DIVERGENCE.md (full file — re-read the "Re-arm verification" bash block at lines 119-143; this is the governing procedure)
    - tests/test_benchmarks.py (lines 218-295 — REF reconciliation test + sentinel xfail block; confirm fixture adaptation from Plan 03 is in place)
    - data/derived/ro/annual_summary.parquet (Wave 3 output — confirm non-stub + has GB data across at least the 22 REF years)
    - .planning/phases/05.2-ro-data-reconstruction-aggregate-grain/05.2-CONTEXT.md (SC #2 — sentinel delete is the load-bearing phase-exit step; D-14 hard block re-arm)
    - tests/fixtures/benchmarks.yaml (ref_constable section — confirm 22 years transcribed + tolerance config intact)
  </read_first>
  <action>
    **Step 1 — Pre-flight: confirm fixture loads non-empty data.** Re-run the Wave 3 Task 3 sanity check:
    ```bash
    uv run python -c "
    import pyarrow.parquet as pq
    from uk_subsidy_tracker.schemes import ro
    df = pq.read_table(ro.DERIVED_DIR / 'annual_summary.parquet').to_pandas()
    gb = df[df['country'] == 'GB']
    totals = gb.groupby('year')['ro_cost_gbp'].sum() / 1e9
    print(f'GB years present: {sorted(totals.index.tolist())}')
    print(f'Year range: {totals.index.min()} to {totals.index.max()}')
    assert len(totals) >= 22, f'Need at least 22 GB years for REF reconciliation; have {len(totals)}'
    print('Pre-flight OK — proceed to sentinel delete')
    "
    ```
    If this fails, DO NOT proceed — the pipeline is incomplete. Go back to Wave 3 (Plan 03) and investigate.

    **Step 2 — Execute the DIVERGENCE.md re-arm verification bash block verbatim** (lines 119-143 of the sentinel file). It is the authoritative procedure — do not improvise:
    ```bash
    # 1. Confirm non-stub derived Parquet (adapted for aggregate grain — use annual_summary instead of station_month)
    uv run python -c "
    from uk_subsidy_tracker.schemes import ro
    import pyarrow.parquet as pq
    t = pq.read_table(ro.DERIVED_DIR / 'annual_summary.parquet')
    print('num_rows:', t.num_rows)
    assert t.num_rows > 0, 'still stub — RO data not landed'
    "

    # 2. Delete this sentinel
    rm .planning/phases/05-ro-module/05-09-DIVERGENCE.md

    # 3. Re-run REF reconciliation (HARD BLOCK now active)
    uv run pytest tests/test_benchmarks.py -q -k ref_constable
    ```

    **Expected outcome: `22 passed` within ±3% for every REF-transcribed year.**

    **Step 3 — If ANY year fails the ±3% check, HALT.** Per the DIVERGENCE.md Option A ladder + CONTEXT.md Claude's Discretion "REF reconciliation behaviour under aggregate-grain divergence":
    - Do NOT restore the sentinel unless explicitly instructed.
    - Analyse the failure using the aggregate-grain version of the D-14 ladder: (a) REF scope vs D-12 GB-only scope (e.g. REF may include NI); (b) REF scope on mutualisation (REF may exclude SY21-22 mutualisation figures that our reconstruction includes); (c) gas counterfactual CY-averaging artefact; (d) per-technology rollup mismatches.
    - Report the failing year(s) + the delta magnitude to the user via the checkpoint mechanism.
    - If the divergence is legitimate scope difference (e.g. NI inclusion/exclusion), adjust the pipeline filter in Wave 3 and re-run — do NOT adjust `REF_TOLERANCE_PCT` without a corresponding `CHANGES.md ## Methodology versions` entry (per CONTEXT.md Claude's Discretion + Phase 2 D-07).

    **Step 4 — Verify all 22 tests PASSED (not xfailed):**
    ```bash
    uv run pytest tests/test_benchmarks.py -q -k ref_constable 2>&1 | tail -3
    ```
    Expected output last line form: `22 passed in Ns`. If it reads `22 xfailed` or `X passed, Y failed`, STOP and report — something is wrong.

    **Step 5 — Verify the sentinel file is gone and the test file's sentinel reference becomes dead code:**
    ```bash
    test ! -e .planning/phases/05-ro-module/05-09-DIVERGENCE.md && echo "sentinel gone"
    grep -n "_DIVERGENCE_SENTINEL" tests/test_benchmarks.py
    ```
    The `_DIVERGENCE_SENTINEL` reference in `test_benchmarks.py` is still in the file — that's OK. It's dead code (the `if _DIVERGENCE_SENTINEL.exists()` check now always evaluates to False so `pytest.xfail()` never fires). Do not remove it in this wave; a future phase can clean it up if needed. Its presence doesn't affect behaviour.

    **Step 6 — Confirm broader phase-exit test suite state:**
    ```bash
    uv run pytest tests/ -q 2>&1 | tail -5
    ```
    Expected: `N passed, M skipped, 0 xfailed` (or 0 xfailed for RO REF; if other xfailed tests exist in the repo, they are unrelated to Phase 05.2). No `failed` lines. No `error` lines.
  </action>
  <verify>
    <automated>test ! -e .planning/phases/05-ro-module/05-09-DIVERGENCE.md && uv run pytest tests/test_benchmarks.py -q -k ref_constable 2>&1 | tail -3 | grep -E "^22 passed" | wc -l | awk '$1 == 1 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e .planning/phases/05-ro-module/05-09-DIVERGENCE.md` exits 0 (sentinel deleted)
    - `uv run pytest tests/test_benchmarks.py -q -k ref_constable 2>&1 | tail -3 | grep -c "22 passed"` equals 1
    - `uv run pytest tests/test_benchmarks.py -q -k ref_constable 2>&1 | tail -3 | grep -c "xfailed"` equals 0 (no residual xfails for REF reconciliation)
    - `uv run pytest tests/ -q 2>&1 | tail -3 | grep -E "^[0-9]+ failed"` returns no output (no outright failures)
    - `uv run pytest tests/ -q 2>&1 | tail -3 | grep -E "^[0-9]+ error"` returns no output
    - `git status .planning/phases/05-ro-module/05-09-DIVERGENCE.md 2>&1 | grep -E "deleted:|not found"` returns at least 1 match (git sees the deletion)
  </acceptance_criteria>
  <done>
    Sentinel deleted. 22 REF reconciliation parametrisations report PASSED (no xfailed). Broader test suite has zero failures + zero errors. D-14 HARD CI BLOCK is live — any future pipeline regression that breaks a REF-year reconciliation will fail CI immediately.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update .planning/REQUIREMENTS.md to mark RO-04 S4/S5 as deferred-data-gated + cross-reference backlog 999.1 + SEED-002 (internal only)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 41-49 — RO-04 current status row; Traceability table at lines 192-197 for RO entries)
    - .planning/phases/05.2-ro-data-reconstruction-aggregate-grain/05.2-CONTEXT.md (SC #9 — RO-04 deferred-data-gated qualifier with cross-reference to backlog 999.1 + SEED-002 in planning artefacts ONLY, NOT in docs/)
    - .planning/ROADMAP.md Backlog section (lines 259-297 — Phase 999.1 entry confirms this is the backlog identifier)
  </read_first>
  <action>
    **Step 1 — Open `.planning/REQUIREMENTS.md`** and locate the RO-04 line (around line 47):
    ```
    - [x] **RO-04**: RO S2 dynamics chart (4-panel), S3 cost by technology, S4 concentration/Lorenz, S5 forward commitment published
    ```

    **Step 2 — Rewrite the RO-04 line** to reflect the partial completion:
    ```markdown
    - [~] **RO-04**: RO S2 dynamics chart (4-panel) + S3 cost by technology published from aggregate grain (Phase 05.2); S4 concentration/Lorenz + S5 forward commitment `deferred-data-gated` per Phase 05.2 — station-level data access blocked by Ofgem Public Reports Dashboard withdrawal 2025-05-14; unlock paths tracked in backlog 999.1 (credentialed RER scraper) + SEED-002 (REF collaboration follow-up).
    ```

    Note: the `[~]` checkbox style indicates partial completion (not used elsewhere in the file — feel free to use `[x]` with the qualifier baked into the text if `[~]` renders oddly). Confirm rendering works in mkdocs + plain Markdown readers.

    **Step 3 — Update the Traceability table** (around lines 192-197). Locate the RO-04 row:
    ```
    | RO-04 | Phase 5 | Complete |
    ```

    Change to:
    ```
    | RO-04 | Phase 5 | Partial (S2+S3 shipped Phase 5 → re-shipped from aggregate grain Phase 05.2; S4+S5 deferred-data-gated, tracked backlog 999.1 + SEED-002) |
    ```

    **Step 4 — Verify that the references to 999.1 + SEED-002 are ONLY in `.planning/REQUIREMENTS.md`** (internal) and NOT in `docs/`. The requirements file is an internal planning artefact; the reference is permitted there. The D-15 forbidden-link discipline applies only to public docs (docs/**/*.md). Run the check:
    ```bash
    grep -rn "999\\.1\\|SEED-002" docs/ 2>/dev/null | wc -l
    ```
    Expected: `0` (no leakage into public docs).

    **Step 5 — Do NOT re-run the full test suite in this task** (Task 1 already did). Just re-read the file to confirm it parses cleanly:
    ```bash
    uv run python -c "from pathlib import Path; txt = Path('.planning/REQUIREMENTS.md').read_text(); assert 'deferred-data-gated' in txt; assert '999.1' in txt; assert 'SEED-002' in txt; print('REQUIREMENTS.md updated correctly')"
    ```
  </action>
  <verify>
    <automated>grep -c "deferred-data-gated" .planning/REQUIREMENTS.md | awk '$1 >= 1 {exit 0} {exit 1}' && grep -c "999.1" .planning/REQUIREMENTS.md | awk '$1 >= 1 {exit 0} {exit 1}' && grep -c "SEED-002" .planning/REQUIREMENTS.md | awk '$1 >= 1 {exit 0} {exit 1}' && [ $(grep -rn "999\.1\|SEED-002" docs/ 2>/dev/null | wc -l) -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "deferred-data-gated" .planning/REQUIREMENTS.md` returns at least 1 match
    - `grep -n "999.1" .planning/REQUIREMENTS.md` returns at least 1 match (backlog cross-reference)
    - `grep -n "SEED-002" .planning/REQUIREMENTS.md` returns at least 1 match (seed cross-reference)
    - `grep -n "RO-04" .planning/REQUIREMENTS.md` returns at least 2 matches (RO-04 line + traceability table row)
    - `grep -rn "999\\.1\\|SEED-002" docs/ 2>/dev/null | wc -l` equals 0 (D-15 discipline: internal artefacts not in public docs)
    - `grep -c "Partial.*S2.S3.*S4.S5 deferred" .planning/REQUIREMENTS.md` returns at least 1 (traceability table update)
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md marks RO-04 as partial with explicit deferred-data-gated qualifier + cross-references to 999.1 + SEED-002. Neither identifier appears in docs/ (D-15 discipline preserved). Traceability table mirrors the qualifier.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write CHANGES.md [Unreleased] audit trail + update ROADMAP.md Phase 05.2 completion + run the final mkdocs + pytest phase-exit gate</name>
  <files>CHANGES.md, .planning/ROADMAP.md</files>
  <read_first>
    - CHANGES.md (current state — find the `## [Unreleased]` section header; confirm Keep-a-Changelog format conventions)
    - .planning/ROADMAP.md (lines 125-144 — Phase 05.2 row; lines 237-253 — Progress Table)
    - .planning/phases/05.2-ro-data-reconstruction-aggregate-grain/05.2-CONTEXT.md (SC #10 — CHANGES.md audit trail: Added / Changed / Fixed; SC #11 mkdocs --strict gate; SC #12 Phase 6 portal unblocked)
    - Plan 01 / Plan 02 / Plan 03 / Plan 04 / Plan 05 PLAN.md files (to cross-reference what each plan delivered — input to the CHANGES entry)
  </read_first>
  <action>
    **Step 1 — Open `CHANGES.md`** and locate the `## [Unreleased]` section. If it does not exist, create it at the top of the file following Keep-a-Changelog format (immediately after the header block / changelog intro).

    **Step 2 — Add the Phase 05.2 audit trail** under `## [Unreleased]`. Use the exact structure below (Added / Changed / Fixed subsections — match the Keep-a-Changelog convention visible elsewhere in the file):

    ```markdown
    ## [Unreleased]

    ### Added

    - Phase 05.2: `src/uk_subsidy_tracker/data/ofgem_aggregate.py` — Ofgem 12-year XLSX download + pandera-validated CSV loaders for the aggregate grain.
    - Phase 05.2: `src/uk_subsidy_tracker/schemes/ro/aggregate_model.py` — aggregate-grain pipeline emitting `data/derived/ro/annual_summary.parquet` + `by_technology.parquet`.
    - Phase 05.2: `data/raw/ofgem/rocs_report_2006_to_2018_20250410081520.xlsx` — Ofgem 12-year RO aggregate (primary pre-2019 source) with single-URL `.meta.json` sidecar.
    - Phase 05.2: `data/raw/ofgem/ro-annual-aggregate.csv` — SY17–SY23 annual totals transcribed from 7 Ofgem annual-report PDFs with per-row `# Provenance:` header + multi-source `.meta.json` sidecar (`sources[]` array).
    - Phase 05.2: `data/raw/ofgem/roc-prices.csv` — 22 obligation-year rows (buyout + recycle + eroc + mutualisation) transcribed from Ofgem transparency-document PDFs (replaces the Phase 5 header-only stub).
    - Phase 05.2: `data/raw/ofgem/ro-generation.csv` — regenerated from the 12-year XLSX via `parse_xlsx_to_monthly()`; sidecar chains derivation back to the XLSX sha256.
    - Phase 05.2: `tests/conftest.py` — registers `@pytest.mark.dormant` pytest marker with standardised auto-skip reason "dormant per Phase 05.2; un-skip on backlog 999.1".
    - Phase 05.2: `tests/schemes/test_ro_aggregate_model.py` — unit tests for the aggregate pipeline (8 tests including Protocol conformance + determinism + dormant short-circuit).
    - Phase 05.2: `tests/data/test_ofgem_aggregate.py` — mocked-network scraper tests for the new loader module.
    - Phase 05.2: `tests/test_docs_ro_headline_sync.py` — regression guard asserting `docs/schemes/ro.md` headline GBP bn figure matches `annual_summary.parquet` GB-total to 1 decimal place.
    - Phase 05.2: `docs/schemes/ro.md` §7 Methodology `### Data access` subsection — documents the 2025-05-14 Ofgem Public Reports Dashboard withdrawal + what remains publicly downloadable + what is SharePoint-OIDC gated + unlock paths (clinical tone per D-13, no references to internal planning artefacts per D-15).
    - Phase 05.2: S4 + S5 DEFERRED admonitions on `docs/schemes/ro.md` (first use of `!!! warning` in the repo; extensions already declared in `mkdocs.yml`).
    - Phase 05.2: Multi-source sidecar schema extension — `sidecar.write_sidecar()` gains optional `sources: list[dict] | None = None` parameter; backward-compatible with existing 8 single-URL sidecars.
    - Phase 05.2: Dormancy discipline — `# dormant: true` line-1 marker convention introduced on 5 src/ modules (schemes/ro/cost_model.py + forward_projection.py + data/ofgem_ro.py + 2 plotting/subsidy/ro_*.py chart modules); `plotting/__main__.py` auto-skips dormant chart modules; derived Parquet artefacts `station_month.parquet` + `by_allocation_round.parquet` + `forward_projection.parquet` removed from working tree; corresponding PNG/HTML chart artefacts removed from `docs/charts/html/`.

    ### Changed

    - Phase 05.2: `src/uk_subsidy_tracker/schemas/ro.py` — `RoAnnualSummaryRow.ro_cost_gbp_eroc` and `RoByTechnologyRow.ro_cost_gbp_eroc` changed from `float` to `float | None` (default `None`) to support aggregate grain where per-station e-ROC clearing dispatch is not available.
    - Phase 05.2: `src/uk_subsidy_tracker/schemes/ro/__init__.py` — added `DORMANT_STATION_LEVEL: bool = True` module-level flag; `rebuild_derived()` short-circuits station-level grain emission when True and routes through `aggregate_model.py` instead.
    - Phase 05.2: `src/uk_subsidy_tracker/schemes/ro/_refresh.py` — `refresh()` short-circuits station-level downloader calls when dormant; daily cron fetches only the XLSX.
    - Phase 05.2: `tests/test_benchmarks.py::ro_annual_totals_gbp_bn` fixture — reads `annual_summary.parquet` (aggregate grain) instead of `station_month.parquet` (dormant).
    - Phase 05.2: Station-level RO tests (`tests/data/test_ofgem_ro.py` module-level + station-grain parametrisations in `tests/test_schemas.py` + `tests/test_aggregates.py` + `tests/test_determinism.py`) now carry `@pytest.mark.dormant` and auto-skip.
    - Phase 05.2: `docs/schemes/ro.md` — full rewrite preserving SCHEMEPAGE-01 8-section structure; headline recomputed from `annual_summary.parquet`; S4 + S5 sections replaced with DEFERRED admonitions linking to `#data-access`.

    ### Fixed

    - Phase 05.2: REF Constable 2025 Table 1 reconciliation hard block re-armed. `tests/test_benchmarks.py::test_ref_constable_ro_reconciliation` 22-year parametrisation moves from `xfailed` to `passed`; the Phase 5 `.planning/phases/05-ro-module/05-09-DIVERGENCE.md` sentinel is deleted. D-14 CI HARD BLOCK is live: any future pipeline regression that breaks a REF-year reconciliation will fail CI immediately.

    ### Deferred

    - Phase 05.2: RO-04 S4 (concentration/Lorenz) + S5 (forward projection) charts marked `deferred-data-gated` in `.planning/REQUIREMENTS.md`. Station-level data access blocked by Ofgem Public Reports Dashboard withdrawal 2025-05-14; unlock paths tracked in backlog 999.1 (credentialed RER scraper) + SEED-002 (REF collaboration follow-up). Dormant code paths preserved in tree; re-activated by flipping `DORMANT_STATION_LEVEL` to False + removing `@pytest.mark.dormant` marks.
    ```

    If the CHANGES.md file has an existing `## Methodology versions` section, do NOT add a new entry unless `REF_TOLERANCE_PCT` was widened — per CONTEXT.md Claude's Discretion, widening is only permitted when REF-vs-reconstruction divergence is structurally impossible to close with pipeline work. Assume no methodology-version bump in this wave (METHODOLOGY_VERSION stays "0.1.0" per Phase 5 D-05; Phase 6+ reserves the 1.0.0 bump).

    **Step 3 — Update `.planning/ROADMAP.md`:**

    Locate the Phase 05.2 entry (lines 125-144). Change the `**Plans**: TBD` line to:
    ```markdown
    **Plans:** 6/6 plans complete

    Plans:
    - [x] 05.2-01-PLAN.md — Sidecar sources[] extension + data/ofgem_aggregate.py loader skeleton + tests/conftest.py pytest.mark.dormant registration (RO-03)
    - [x] 05.2-02-PLAN.md — Ofgem 12-year XLSX download + SY17-SY23 annual-aggregate CSV transcription + 22-row roc-prices CSV transcription + multi-source sidecars + ro-generation.csv regeneration + ro-register.xlsx dormant-stub deletion (RO-03)
    - [x] 05.2-03-PLAN.md — schemas/ro.py aggregate-grain nullability + schemes/ro/aggregate_model.py pipeline + DORMANT_STATION_LEVEL short-circuit + test_benchmarks.py fixture adaptation (RO-03, RO-06)
    - [x] 05.2-04-PLAN.md — Dormancy discipline: `# dormant: true` line-1 markers on 5 src/ modules + plotting/__main__.py dormant-skip logic + `@pytest.mark.dormant` applied across station-level tests + dormant Parquet + chart artefacts removed (RO-04)
    - [x] 05.2-05-PLAN.md — docs/schemes/ro.md rewrite with real S2+S3 charts + DEFERRED S4/S5 admonitions + §7 Data access subsection + headline-sync regression test + theme-page link updates + mkdocs --strict (RO-04, RO-05)
    - [x] 05.2-06-PLAN.md — 05-09-DIVERGENCE.md sentinel delete + REF reconciliation un-xfail + REQUIREMENTS.md deferred-data-gated update + CHANGES.md audit trail + ROADMAP.md bookkeeping + phase-exit gate (RO-04, RO-05, RO-06)
    ```

    Locate the Progress Table (around line 237). Add a new row for Phase 05.2 OR update the existing row (depending on how the table currently handles sub-phases). Add the completion date as today's UTC date (2026-04-24 per the system reminder; replace with actual today if different). Example entry:
    ```markdown
    | 05.2. RO Data Reconstruction (Aggregate-Grain) | 6/6 | Complete    | 2026-04-24 |
    ```

    **Step 4 — Run the phase-exit gate:** two commands must both come back green.
    ```bash
    # Gate 1: mkdocs --strict
    uv run mkdocs build --strict 2>&1 | tail -5

    # Gate 2: full pytest
    uv run pytest tests/ -q 2>&1 | tail -5
    ```

    Expected:
    - `mkdocs build --strict`: last line something like "INFO - Documentation built in N.NN seconds" with no WARNING or ERROR in the preceding lines.
    - `pytest tests/ -q`: final summary line with `N passed, M skipped, 0 xfailed` (or 0 RO-REF xfails; any unrelated xfails elsewhere in the repo are acceptable per phase scope). No `failed`, no `error`.

    **Step 5 — If either gate fails, DO NOT commit — halt and report.** Possible failure modes:
    - mkdocs warning: usually a broken link. Check the theme-page changes from Plan 05 Task 3.
    - pytest failure: a test somewhere in the tree that used to pass now fails. Likely candidate: test_aggregates.py or test_determinism.py for a grain that should have been dormant-marked in Plan 04 Task 3 but wasn't. Add the mark, re-run.

    **Step 6 — Summary output.** Record in the SUMMARY file:
    - REF reconciliation final state: "22 passed (aggregate grain; D-14 HARD BLOCK live)"
    - Full pytest summary counts: pass / skip / xfail / fail
    - mkdocs --strict exit status
    - Sentinel file state: "deleted (last confirmed by `test ! -e`)"
    - Wait to commit everything via git until after running `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit ...` per the standard GSD plan-commit workflow (the executor will run this at summary-write time).
  </action>
  <verify>
    <automated>uv run mkdocs build --strict 2>&1 | tail -5 | grep -iEc "warning|error" | awk '$1 == 0 {exit 0} {exit 1}' && uv run pytest tests/ -q 2>&1 | tail -3 | grep -E "^[0-9]+ failed" | wc -l | awk '$1 == 0 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "## \\[Unreleased\\]" CHANGES.md` returns exactly 1 match
    - `grep -n "Phase 05.2" CHANGES.md` returns at least 5 matches (multiple Added/Changed/Fixed/Deferred entries reference the phase)
    - `grep -c "^### Added\\|^### Changed\\|^### Fixed\\|^### Deferred" CHANGES.md` returns at least 4 (all four subsections under Unreleased)
    - `grep -n "REF Constable 2025 Table 1 reconciliation hard block re-armed" CHANGES.md` returns exactly 1 match
    - `grep -n "05.2-01-PLAN.md\\|05.2-02-PLAN.md\\|05.2-03-PLAN.md\\|05.2-04-PLAN.md\\|05.2-05-PLAN.md\\|05.2-06-PLAN.md" .planning/ROADMAP.md | wc -l` returns at least 6 (all 6 plans enumerated)
    - `grep -c "\\[x\\] 05.2-" .planning/ROADMAP.md` returns at least 6 (all 6 plans checked off)
    - `grep -n "6/6 plans complete" .planning/ROADMAP.md` returns exactly 1 match
    - `uv run mkdocs build --strict 2>&1 | grep -iEc "warning|error"` equals 0 (phase-exit gate 1)
    - `uv run pytest tests/ -q 2>&1 | tail -3 | grep -E "^[0-9]+ failed" | wc -l` equals 0 (phase-exit gate 2 — no failures)
    - `uv run pytest tests/test_benchmarks.py -q -k ref_constable 2>&1 | tail -3 | grep -c "22 passed"` equals 1 (final re-confirmation)
    - `test ! -e .planning/phases/05-ro-module/05-09-DIVERGENCE.md` exits 0 (sentinel gone)
  </acceptance_criteria>
  <done>
    CHANGES.md [Unreleased] carries the full Phase 05.2 audit trail (Added / Changed / Fixed / Deferred subsections). ROADMAP.md Phase 05.2 entry lists all 6 plans as [x] complete + progress table marks Complete with today's date. Both phase-exit gates (`mkdocs build --strict` + `pytest tests/ -q`) pass green. 22 REF reconciliation tests pass. Phase 05.2 is shippable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sentinel file existence check | `test_benchmarks.py` reads the filesystem for `.planning/phases/05-ro-module/05-09-DIVERGENCE.md`; tamper-evidence is the git diff, not runtime verification |
| Methodology-version bump discipline | Widening REF_TOLERANCE_PCT without a CHANGES.md entry would silently relax the ±3% guarantee; enforced by convention + Task 1 Step 3 halt-on-failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-06-01 | Tampering | Sentinel file deleted but REF reconciliation still silently xfailing (e.g. if the test code carries a second guard) | mitigate | Task 1 acceptance criterion explicitly counts `22 passed` + requires `0 xfailed` for REF reconciliation |
| T-05.2-06-02 | Repudiation | CHANGES.md audit trail incomplete or inaccurate — future reader cannot reconstruct what shipped | mitigate | Task 3 acceptance criteria grep for specific artefact entries; plan-commit step preserves the full file history |
| T-05.2-06-03 | Integrity | ROADMAP.md marks phase Complete but a success criterion is actually unmet | mitigate | Both phase-exit gates (mkdocs + pytest) + 22 REF passes are the CI-level truth; ROADMAP.md is governance bookkeeping on top of them |
| T-05.2-06-04 | Information disclosure | REQUIREMENTS.md cross-references (999.1, SEED-002) accidentally leak into public site build | mitigate | Task 2 Step 4 grep explicitly checks `docs/` for the strings; D-15 discipline on internal artefacts holds |
| T-05.2-06-05 | Denial of Service | Tolerance widening via REF_TOLERANCE_PCT change | accept | Only the user can approve a tolerance change; convention-level mitigation via CHANGES.md required entry |
</threat_model>

<verification>
Phase-exit gate (end-of-Plan-06 state):

```bash
# Sentinel deleted
test ! -e .planning/phases/05-ro-module/05-09-DIVERGENCE.md

# REF reconciliation hard block live
uv run pytest tests/test_benchmarks.py -q -k ref_constable 2>&1 | tail -3 | grep "22 passed"

# Full test suite green
uv run pytest tests/ -q 2>&1 | tail -3 | grep -E "failed|error" | grep -v warning   # should return nothing

# Docs build clean
uv run mkdocs build --strict 2>&1 | grep -iE "warning|error"   # should return nothing

# Governance updates
grep "deferred-data-gated" .planning/REQUIREMENTS.md
grep "## \[Unreleased\]" CHANGES.md
grep "6/6 plans complete" .planning/ROADMAP.md

# D-15 discipline preserved
grep -rn "999\.1\|SEED-002" docs/ 2>/dev/null | wc -l   # should be 0
```

All eight checks must pass for phase exit.
</verification>

<success_criteria>
1. `.planning/phases/05-ro-module/05-09-DIVERGENCE.md` is deleted from the working tree.
2. `tests/test_benchmarks.py::test_ref_constable_ro_reconciliation` reports `22 passed` (not xfailed); D-14 HARD CI BLOCK is live.
3. `.planning/REQUIREMENTS.md` marks RO-04 S4/S5 as `deferred-data-gated` with cross-references to backlog 999.1 + SEED-002 (only in the planning artefact — not in docs/).
4. `CHANGES.md [Unreleased]` contains Added / Changed / Fixed / Deferred subsections with specific artefact references for every plan in Phase 05.2.
5. `.planning/ROADMAP.md` marks Phase 05.2 as 6/6 Complete with today's date in the Progress Table.
6. `uv run mkdocs build --strict` passes with zero warnings/errors (phase-exit gate 1).
7. `uv run pytest tests/ -q` passes with zero failed/error (phase-exit gate 2).
8. D-15 discipline preserved: `grep -rn "999\\.1\\|SEED-002" docs/` returns zero matches.
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-ro-data-reconstruction-aggregate-grain/05.2-06-SUMMARY.md`.
</output>
