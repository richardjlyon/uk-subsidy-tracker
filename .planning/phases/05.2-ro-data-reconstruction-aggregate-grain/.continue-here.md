---
context: phase
phase: 05.2-ro-data-reconstruction-aggregate-grain
task: 2
total_tasks: 3
status: paused
last_updated: 2026-04-25T00:46:07.484Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Fabricated URL slugs in plan artefacts | Plan 05.2-02 task 2 prep landed seven SY17–SY23 PDF URLs under the `transparency-document/` stem, all of which 404. The slugs were plausible-looking pattern extrapolations (derived from the 12-year XLSX filename), not URLs verified to resolve. The executor committed CSV header blocks listing those URLs as `source_pdf_url`, polluting provenance with fake links until the human checkpoint surfaced the 404s. | blocking | Before any plan artefact records an external URL as authoritative, the planner or executor MUST verify the URL resolves (HEAD/GET 200, or a documented 4xx-with-explanation). Add a `verify-urls` step to plan execution: enumerate every URL in the plan's `<interfaces>` / data manifest blocks, fetch each, fail-loud on 4xx/5xx before continuing. This applies especially to data-ingestion plans where URLs become provenance facts. |
| Plan based on assumed-public data without confirming the actual hosting model | Plan 05.2-02 assumed Ofgem aggregate-level annual reports were publicly downloadable while only station-level was RER-gated. Reality (per Ofgem's RO public-reports landing page, 2026-04-24): the public-reports page links only the 12-year XLSX; annual-report PDFs/XLSXes live at `/publications/...` URLs and are NOT linked from the public-reports landing. The premise survived discuss-phase and plan-phase undetected. | advisory | When a phase scope-paragraph asserts "X is publicly downloadable", verify by actually fetching at least one representative URL during the discuss-phase research step (gsd-phase-researcher). Don't ship the assumption to a plan that downstream commits will treat as fact. |

<current_state>
Phase 05.2 paused after Wave 1 (Plan 05.2-01) completed cleanly and Plan 05.2-02 paused at its first human-action checkpoint. The pause is **not** due to missing human input — it's due to discovery (during the checkpoint research) that the plan's transcription premise is wrong: 6 of 7 SY17–SY23 reports ship machine-readable XLSX datasets, so the right move is download+parse, not human transcription. The plan needs structural revision, not human transcription work.

Working tree is clean. All commits are atomic. Wave 1 stands as final. Wave 2 has two commits — `d224044` (12-year XLSX + sidecar — KEEP, canonical raw data) and `a720ae7` (transcription-skeleton CSVs — SUPERSEDED by re-plan). The CONTEXT.md commit `b934492` captures the full SY17–SY23 URL inventory and re-plan implications.
</current_state>

<completed_work>

Completed Tasks:
- Plan 05.2-01: Foundations — Done
  - sidecar.write_sidecar() extended with optional `sources[]` kwarg (commit 7dc303f)
  - data/ofgem_aggregate.py skeleton landed (commit 74a6c15)
  - tests/conftest.py registers @pytest.mark.dormant auto-skip hook (commit 9c9d2c6)
  - Plan metadata + SUMMARY.md (commit 66934f0)
- Plan 05.2-02 task 1: Done
  - Ofgem 12-year XLSX downloaded with single-URL sidecar, dormant ro-register stub deleted (commit d224044)
- Plan 05.2-02 task 2 prep: SUPERSEDED
  - Transcription-skeleton CSVs (ro-annual-aggregate.csv + roc-prices.csv) with `# Provenance:` headers (commit a720ae7)
  - These are no longer the right shape — XLSX-driven CSV emission will replace them in the revised plan
- CONTEXT.md SY17–SY23 discovery section appended (commit b934492)
</completed_work>

<remaining_work>

- Plan 05.2-02 (REVISED — needs re-plan): download SY18–SY23 XLSX dataset companions + sidecars + parse via openpyxl + emit deterministic CSV. Decide SY17 fate (PDF-only) — transcribe or defer.
- Plan 05.2-03: aggregate-grain pipeline → annual_summary.parquet + by_technology.parquet
- Plan 05.2-04: dormancy discipline — `# dormant: true` marker propagation across station-level src/ and tests
- Plan 05.2-05: docs/schemes/ro.md rewrite with S4/S5 deferred admonitions + §7 Data access
- Plan 05.2-06: phase close-out — DIVERGENCE sentinel deletion + REQUIREMENTS/CHANGES/ROADMAP updates
</remaining_work>

<decisions_made>

- Pause + re-plan over inline plan revision — Plan/code drift is worse than a clean re-plan cycle for a structurally-different data-ingestion premise.
- Keep Wave 1 commits final — sidecar `sources[]` extension, ofgem_aggregate.py skeleton, and `@pytest.mark.dormant` hook are all substrate the revised Plan 05.2-02 still consumes.
- Keep commit `d224044` (12-year XLSX + sidecar) — canonical raw data, sha256-pinned, useful regardless of revision.
- Mark commit `a720ae7` (transcription-skeleton CSVs) as SUPERSEDED — re-plan decides revert-vs-replace-in-place.
- SY17 fate is open — only year without an XLSX companion. Either transcribe (one PDF; manageable) or defer alongside station-level data. Re-plan must record the choice.
</decisions_made>

<blockers>
- Plan 05.2-02 needs re-plan: re-run `/gsd-plan-phase 05.2` (or manually rewrite the plan file) before resuming `/gsd-execute-phase 05.2`. CONTEXT.md commit `b934492` carries the full URL inventory + re-plan implications — the planner agent should consume that section directly.
</blockers>

## Required Reading (in order)

1. `.planning/phases/05.2-ro-data-reconstruction-aggregate-grain/05.2-CONTEXT.md` — especially the new "SY17–SY23 annual-report discovery (added 2026-04-24 mid-execution)" section. This is the canonical handoff for the re-plan.
2. `.planning/phases/05.2-ro-data-reconstruction-aggregate-grain/05.2-02-PLAN.md` — the plan file that needs revision. Read to understand what's currently there before the re-plan rewrites it.
3. `.planning/phases/05.2-ro-data-reconstruction-aggregate-grain/05.2-01-SUMMARY.md` — Wave 1 outputs the revised Plan 05.2-02 still consumes (sidecar `sources[]` kwarg, `ofgem_aggregate.py` skeleton with stubbed functions + `XLSX_URL` constant + comment-aware CSV readers, `@pytest.mark.dormant` registration).
4. `.planning/notes/ro-data-strategy-option-a1.md` — the Phase 05.2 strategy document. The XLSX-companion discovery doesn't change the strategy choice (A1 still beats B and C); it only changes the implementation shape within A1.

## Critical Anti-Patterns (do NOT repeat these)

- ANTI-PATTERN: ship plan artefacts containing URLs that haven't been verified to resolve. → Add URL-verification step before any plan that lists external URLs is committed; fetch each URL, fail loud on non-200.
- ANTI-PATTERN: assume "X is publicly downloadable" without checking the canonical landing page. → During discuss-phase research, fetch at least one representative URL to confirm the hosting model. Document the fetch (URL + status + timestamp) in CONTEXT.md as evidence.

## Infrastructure State

- Working tree: clean (all changes committed)
- Branch: main (no phase branch — `branching_strategy: none`)
- Last commit: b934492 (docs(05.2): record SY17–SY23 XLSX-companion discovery; pause Plan 05.2-02 for re-plan)
- Test suite: 175 passed + 14 skipped + 22 xfailed (Wave 1 baseline; Wave 2 added no tests)
- STATE.md status: executing (Phase 05.2 in progress) — note: Plan 05.2-02 is in-progress not failed; resumption via re-plan, not rollback
- Worktrees: none (workflow.use_worktrees=false; sequential execution on main)

## Pre-Execution Critique Required

Re-plan must address:

- **Resume signal protocol.** Plan 05.2-02's checkpoint section enumerated three resume signals (`transcribed:`, `abort:`, `defer-to-REF-transcription:`). The revised plan removes `transcribed:` (not needed for XLSX path), keeps `abort:` semantics, and may add `xlsx-downloads-verified:` or similar. Define the protocol before execution.
- **SY17 fate.** Single PDF-only year — choose transcribe-or-defer and record in plan frontmatter (`must_haves` or similar). If defer: REQUIREMENTS.md gets a SY17-specific deferred-data-gated marker; if transcribe: keep one PDF transcription task and one human-action checkpoint, much narrower scope than the original 7-PDF design.
- **a720ae7 disposition.** Either revert (clean restart) or amend the column shape in-place (faster, slight history-mess). The re-plan picks; record in plan frontmatter.
- **Test surface.** Plan 05.2-01 left a SKIP-marked placeholder for XLSX-parse-determinism (`tests/data/test_ofgem_aggregate.py`); re-plan should expand that placeholder into 6 (or 7) parametrised year-specific tests.
- **Sidecar `sources[]` shape under XLSX path.** D-03 was designed for multi-PDF transcription provenance. Under XLSX path, each year's CSV emission has one canonical XLSX upstream — `sources[]` is still correct (records the chain) but populated from XLSX URLs instead of PDF URLs. Re-plan must spell this out so the loader emits the right shape.

<context>
Phase 05.2's design assumed Ofgem aggregate-level data was publicly downloadable while only station-level was RER-gated. That assumption was partially wrong — annual reports are publicly downloadable, but at /publications/ URLs not /transparency-document/ ones, AND every (recent) report ships an XLSX dataset companion. The plan was structured around manual PDF transcription before that discovery surfaced. The right move is to revise Plan 05.2-02 around XLSX downloads (much stronger provenance, no transcription error risk, automatable) and continue.

The 16 D-decisions in CONTEXT.md still hold. Wave structure (W1 foundations, W2 raw landing, W3 aggregate pipeline, W4 dormancy, W5 docs, W6 close-out) still holds. Only Plan 05.2-02's task structure changes.
</context>

<next_action>
Start with: run `/gsd-plan-phase 05.2` (the planner will read the new CONTEXT.md section and regenerate Plan 05.2-02). When the re-plan finishes, run `/gsd-execute-phase 05.2` to resume — execute-phase will discover Plan 05.2-01 already complete (SUMMARY.md exists), and start Wave 2 with the revised Plan 05.2-02. Wave 1 commits and the d224044 12-year XLSX commit are not affected.
</next_action>
