---
phase: 05.2-ro-data-reconstruction-aggregate-grain
plan: 05
type: execute
wave: 5
depends_on: [03, 04]
files_modified:
  - docs/schemes/ro.md
  - docs/themes/cost/index.md
  - docs/themes/recipients/index.md
  - tests/test_docs_ro_headline_sync.py
autonomous: true
requirements:
  - RO-04
  - RO-05
tags:
  - ro
  - docs
  - mkdocs
  - methodology
user_setup: []

must_haves:
  truths:
    - "docs/schemes/ro.md is fully rewritten with the SCHEMEPAGE-01 8-section structure preserved exactly (H1 + 7 H2 sections + 1 optional Headline FAQ)"
    - "The S2 Cost dynamics and S3 By technology sections embed real chart artefacts produced from data/derived/ro/annual_summary.parquet + by_technology.parquet"
    - "The S4 Concentration and S5 Forward commitment sections contain '!!! warning' admonition blocks with the exact text 'S4 Concentration — DEFERRED: data-gated' and 'S5 Forward commitment — DEFERRED: data-gated' respectively, each linking to #data-access anchor"
    - "A new '### Data access' subsection exists under '## Methodology' with ~250-400 words of mildly-pointed prose (D-13 tone: dates + facts, no adjectives)"
    - "The lead paragraph's headline GBP bn figure matches data/derived/ro/annual_summary.parquet GB-all-years-sum to 1 decimal place (regression test enforces this)"
    - "§7 Data access subsection contains NO reference to: REF Constable PDF, SEED-002, 999.1, credentialed RER scraper, REF collaboration, outreach, or any relational language about collaborators-in-waiting (D-15 forbidden-link discipline enforced by grep in acceptance)"
    - "REF Constable is mentioned only as a clinical tolerance anchor ('test_ref_constable_ro_reconciliation ±3% per scheme year') — no peer-publisher framing (D-16)"
    - "mkdocs build --strict passes with zero warnings and zero errors"
  artifacts:
    - path: "docs/schemes/ro.md"
      provides: "Fully rewritten RO scheme page — headline + 8-section structure + DEFERRED admonitions + Data access subsection"
      min_lines: 200
    - path: "tests/test_docs_ro_headline_sync.py"
      provides: "Regression test asserting the ro.md headline number matches annual_summary.parquet to 1 decimal place"
  key_links:
    - from: "docs/schemes/ro.md ## Concentration (Chart S4)"
      to: "#data-access"
      via: "In-page anchor link inside admonition"
      pattern: "\\[.7 Data access\\]\\(#data-access\\)"
    - from: "docs/schemes/ro.md lead paragraph"
      to: "data/derived/ro/annual_summary.parquet"
      via: "Headline GBP bn sum validated by tests/test_docs_ro_headline_sync.py"
      pattern: "Headline number in prose == parquet sum"
    - from: "docs/schemes/ro.md ## Methodology"
      to: "tests/test_benchmarks.py::test_ref_constable_ro_reconciliation"
      via: "Clinical tolerance-anchor reference"
      pattern: "test_ref_constable_ro_reconciliation.*±3%"
---

<objective>
Rewrite `docs/schemes/ro.md` so every reader — skimmer, academic, and hostile adversary — gets the full picture of what the portal can show (S2 + S3 real charts + reconstructed headline) and what it cannot show yet (S4 + S5 DEFERRED admonitions pointing to a new §7 Data access subsection that documents Ofgem's 2025-05-14 Public Reports Dashboard withdrawal clinically — dates and facts only). The lead paragraph's headline GBP bn figure is recomputed from `annual_summary.parquet` GB totals; a regression test enforces Markdown-prose-matches-Parquet to 1 decimal place (per CONTEXT.md "Claude's Discretion — Headline £67bn update discipline"). Cross-link updates in `docs/themes/cost/index.md` + `docs/themes/recipients/index.md` ensure no stale links to the removed dormant chart pages.

Purpose: This is the public face of Phase 05.2 — the artefact a reader opens. It must (a) make the £67bn figure reproducible from `git clone + uv sync + one command` per CLAUDE.md core value; (b) transparently explain why S4+S5 are deferred without mentioning any internal planning artefacts (D-15 + user memory `feedback_internal_artefacts_off_public_docs.md`); (c) preserve SCHEMEPAGE-01 8-section symmetry with `docs/schemes/cfd.md` (Phase 05.1 template); (d) render cleanly under `mkdocs build --strict`.

Output: Rewritten `docs/schemes/ro.md` (~250-400 lines); minor updates to 2 theme pages to remove stale links; 1 new regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.2-ro-data-reconstruction-aggregate-grain/05.2-CONTEXT.md
@.planning/phases/05.2-ro-data-reconstruction-aggregate-grain/05.2-PATTERNS.md
@docs/schemes/cfd.md
@docs/schemes/ro.md
@docs/themes/cost/index.md
@docs/themes/recipients/index.md
@mkdocs.yml
@data/derived/ro/annual_summary.parquet

<interfaces>
From .planning/phases/05.1-cfd-scheme-page/05.1-CONTEXT.md (SCHEMEPAGE-01 validated template):
- 8-section structure: Lead (H1 + punch paragraph) + What is X? + Cost dynamics (S2) + By technology (S3) + Concentration (S4) + Forward commitment (S5) + Methodology + Data & code (GOV-01 four-way coverage) + optional Headline FAQ
- H2 anchor slugs (MUST preserve verbatim): #what-is-the-ro, #cost-dynamics-chart-s2, #by-technology-chart-s3, #concentration-chart-s4, #forward-commitment-chart-s5, #methodology, #data-code-gov-01-four-way-coverage, #headline-faq

From mkdocs.yml (lines 85-123):
- `admonition` + `pymdownx.details` extensions declared
- Material theme renders `!!! warning "Title"` as yellow-bordered callout

From data/derived/ro/annual_summary.parquet (Plan 03 output):
- Columns: year, country, ro_generation_mwh, ro_cost_gbp, ro_cost_gbp_eroc (nullable), gas_counterfactual_gbp, premium_gbp, mutualisation_gbp, methodology_version
- GB-only total (sum of ro_cost_gbp where country=='GB' across all years) is the headline GBP figure

D-15 FORBIDDEN links / content (DO NOT include in docs/schemes/ro.md):
- REF Constable PDF link (ref.org.uk)
- SEED-002 reference
- backlog 999.1 reference
- "credentialed RER scraper" language
- "REF collaboration" / "outreach" language
- Any relational language about collaborators-in-waiting

D-15 ALLOWED links in §Data access:
- Ofgem XLSX URL (`rocs_report_2006_to_2018_20250410081520.xlsx`)
- Ofgem RO public reports landing page
- Ofgem transparency-document PDFs (annual-report URLs)
- `docs/about/corrections.md`
- GitHub `correction` label URL
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write headline-sync regression test (RED first — before the rewrite introduces the headline prose)</name>
  <files>tests/test_docs_ro_headline_sync.py</files>
  <read_first>
    - docs/schemes/ro.md (current file — the lead paragraph GBP 67bn figure is the target)
    - data/derived/ro/annual_summary.parquet (Plan 03 output — GB-only sum is the authority)
    - .planning/phases/05.2-ro-data-reconstruction-aggregate-grain/05.2-CONTEXT.md (Claude's Discretion — Headline £67bn update discipline; regression-test-as-guard option is the simpler path chosen here)
  </read_first>
  <behavior>
    - Test 1 (headline matches parquet): Read annual_summary.parquet; compute sum of ro_cost_gbp where country == 'GB' across all years; divide by 1e9 to get GBP bn; round to 1 decimal. Read docs/schemes/ro.md; regex-match the first paragraph after the H1 for a number in the form "£NN.N bn" or "GBP NN.N bn". Assert rounded prose number equals rounded parquet sum.
    - Test 2 (annual_summary.parquet exists): If the Parquet file is missing, the test SHOULD skip with a clear reason ("Wave 3 has not run yet") rather than fail. This prevents CI churn during phase execution — it flips to a hard assertion in Plan 06 gates.
  </behavior>
  <action>
    Create `tests/test_docs_ro_headline_sync.py` with the following body:

    ```python
    """Regression guard: docs/schemes/ro.md headline number matches
    data/derived/ro/annual_summary.parquet GB total (Phase 05.2 D-Claude's-Discretion).

    The lead paragraph's "£NN.N bn" figure is the reconstructed GB headline;
    this test enforces that prose tracks data to 1 decimal place. If the
    aggregate pipeline emits a different figure on the next rebuild, this
    test fails and forces either (a) a prose update or (b) a CHANGES.md
    `## Methodology versions` audit entry per Phase 2 D-07.
    """
    from __future__ import annotations

    import re
    from pathlib import Path

    import pytest

    PROJECT_ROOT = Path(__file__).parent.parent
    RO_MD = PROJECT_ROOT / "docs" / "schemes" / "ro.md"
    ANNUAL_SUMMARY_PARQUET = PROJECT_ROOT / "data" / "derived" / "ro" / "annual_summary.parquet"

    _HEADLINE_RE = re.compile(r"£\s*(\d+(?:\.\d+)?)\s*bn", re.IGNORECASE)


    def _parquet_gb_total_gbp_bn() -> float:
        import pyarrow.parquet as pq
        df = pq.read_table(ANNUAL_SUMMARY_PARQUET).to_pandas()
        gb = df[df["country"] == "GB"]
        total_gbp = float(gb["ro_cost_gbp"].sum())
        return round(total_gbp / 1e9, 1)


    def _prose_headline_gbp_bn() -> float | None:
        text = RO_MD.read_text(encoding="utf-8")
        # Look at the first ~30 lines after the H1 for the headline figure
        first_chunk = "\n".join(text.splitlines()[:40])
        m = _HEADLINE_RE.search(first_chunk)
        if m is None:
            return None
        return round(float(m.group(1)), 1)


    def test_ro_headline_prose_matches_parquet_total_to_one_decimal() -> None:
        """D-(Headline £67bn update discipline): prose GBP bn == parquet sum to 1dp."""
        if not ANNUAL_SUMMARY_PARQUET.exists():
            pytest.skip(
                "data/derived/ro/annual_summary.parquet absent — "
                "Wave 3 rebuild_derived() has not run yet"
            )
        parquet_bn = _parquet_gb_total_gbp_bn()
        prose_bn = _prose_headline_gbp_bn()
        assert prose_bn is not None, (
            "No '£NN.N bn' headline figure found in first 40 lines of "
            "docs/schemes/ro.md — rewrite MUST include a computed GBP-bn "
            "headline in the lead paragraph."
        )
        assert prose_bn == parquet_bn, (
            f"Headline figure mismatch: prose says £{prose_bn}bn, "
            f"annual_summary.parquet GB-sum is £{parquet_bn}bn. "
            f"Either update the prose OR record a CHANGES.md "
            f"## Methodology versions entry explaining the divergence."
        )
    ```

    TDD order: write the test first (this task). Before Task 2 runs, the test will most likely be SKIPPED (if annual_summary.parquet exists but no "£NN.N bn" in current ro.md) OR FAIL (if a literal `£67 bn` is already in the file and it happens to match/not match). After Task 2, the rewritten prose carries a real computed figure and the test PASSES.

    Verify it runs (pass OR skip OR fail — just not error):
    ```bash
    uv run pytest tests/test_docs_ro_headline_sync.py -v 2>&1 | tail -5
    ```
  </action>
  <verify>
    <automated>uv run pytest tests/test_docs_ro_headline_sync.py -v 2>&1 | tail -5 | grep -E "passed|failed|skipped"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/test_docs_ro_headline_sync.py` exits 0
    - `grep -n "def test_ro_headline_prose_matches_parquet_total_to_one_decimal" tests/test_docs_ro_headline_sync.py` returns exactly 1 match
    - `grep -n "_HEADLINE_RE" tests/test_docs_ro_headline_sync.py` returns at least 2 matches (regex declared + used)
    - `uv run pytest tests/test_docs_ro_headline_sync.py -v 2>&1 | tail -5 | grep -E "passed|failed|skipped"` returns at least 1 match (test runs, does not error)
    - `grep -n "Headline figure mismatch" tests/test_docs_ro_headline_sync.py` returns exactly 1 match (helpful failure message present)
  </acceptance_criteria>
  <done>
    Headline-sync regression test exists. Running it either skips (Parquet missing) or fails with a clear message (current prose doesn't match recomputed figure) — both acceptable states before Task 2; only PASS is acceptable after Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite docs/schemes/ro.md — headline + 8-section structure + S4/S5 DEFERRED admonitions + §7 Data access subsection</name>
  <files>docs/schemes/ro.md</files>
  <read_first>
    - docs/schemes/ro.md (current state — confirm what existing prose to preserve, especially any "What is the RO?" mechanism explanation)
    - docs/schemes/cfd.md (SCHEMEPAGE-01 validated template — preserve 8-section structure + H2 anchor slugs + four-way coverage manifest shape)
    - .planning/phases/05.2-ro-data-reconstruction-aggregate-grain/05.2-PATTERNS.md (Pattern #6 — DEFERRED admonition template; Pattern #7 — full SCHEMEPAGE-01 template with headline + 8 sections; §7 Methodology + ### Data access subsection pattern; §8 Data & code GOV-01 manifest pattern)
    - .planning/phases/05.2-ro-data-reconstruction-aggregate-grain/05.2-CONTEXT.md (D-09 admonition discipline; D-10 short-admonition + full-in-§7; D-12 preserve 8-section structure; D-13 mildly-pointed tone dates+facts no adjectives; D-14 moderate depth 250-400 words; D-15 forbidden-link list; D-16 REF clinical tolerance anchor only)
    - data/derived/ro/annual_summary.parquet (compute the real GBP bn figure)
    - docs/charts/html/subsidy_ro_dynamics.html (active chart — MUST embed)
    - docs/charts/html/subsidy_ro_by_technology.html (active chart — MUST embed)
    - mkdocs.yml (lines 85-123 — admonition extension declarations confirm rendering support)
    - .planning/phases/05.1-cfd-scheme-page/05.1-CONTEXT.md (SCHEMEPAGE-01 authoritative structure + anchor slugs — these MUST match cfd.md verbatim)
  </read_first>
  <action>
    **Step 1 — Compute the real headline GBP bn figure from annual_summary.parquet:**
    ```bash
    uv run python -c "
    import pyarrow.parquet as pq
    df = pq.read_table('data/derived/ro/annual_summary.parquet').to_pandas()
    gb = df[df['country'] == 'GB']
    total_bn = gb['ro_cost_gbp'].sum() / 1e9
    print(f'GB all-years RO cost: £{total_bn:.1f} bn')
    print(f'Year range: {gb.year.min()} to {gb.year.max()}')
    "
    ```
    Record the figure — call it `HEADLINE_BN` — it replaces the literal `£67 bn` in the lead paragraph.

    **Step 2 — Rewrite `docs/schemes/ro.md` from scratch** following PATTERNS.md Pattern #7 exactly. The file MUST have (in order):

    **A. H1 + lead paragraph:**
    ```markdown
    # UK Renewables Obligation (RO)

    **The Renewables Obligation is the scheme you've never heard of, twice the size of the one you have. Between 2002 and 2024, British electricity consumers paid approximately £HEADLINE_BN bn in RO subsidies to renewable generators — roughly double the cumulative cost of the more widely discussed Contracts for Difference scheme over the same period.**

    Unlike CfD, where payments flow from a central counterparty, RO costs are embedded directly in retail electricity bills via supplier obligations. The scheme closed to new accreditations in 2017 but continues to pay out until 2037 under 20-year support windows locked in during the 2002–2017 build-out phase.
    ```
    Replace `HEADLINE_BN` with the Step 1 computed figure (e.g. "67.3").

    **B. `## What is the RO?`** (anchor `#what-is-the-ro`) — preserve or rewrite from existing `docs/schemes/ro.md` if a mechanism explanation exists there. If the current file is stub-only, write ~150 words on: supplier obligation mechanism; ROC issuance per MWh; bandings (not listed — abstract); buyout + recycle + mutualisation; 2017 closure to new entrants; 2037 support-window cliff.

    **C. `## Cost dynamics (Chart S2)`** (anchor `#cost-dynamics-chart-s2`) — embed the active chart from `docs/charts/html/subsidy_ro_dynamics.html`:
    ```markdown
    ## Cost dynamics (Chart S2)

    --8<-- "docs/charts/html/subsidy_ro_dynamics.html"

    The S2 panel traces annual RO cost, generation, and cost-per-MWh from scheme launch through the latest obligation year. The vertical band at 2017 marks closure to new accreditations; costs continue to rise as existing accredited stations remain on 20-year support windows until 2037.
    ```
    Use the same embed pattern that `docs/schemes/cfd.md` uses for its S2 chart (grep `cfd.md` for the exact include syntax — likely `--8<--` or iframe-style).

    **D. `## By technology (Chart S3)`** (anchor `#by-technology-chart-s3`) — embed `docs/charts/html/subsidy_ro_by_technology.html`:
    ```markdown
    ## By technology (Chart S3)

    --8<-- "docs/charts/html/subsidy_ro_by_technology.html"

    Cost decomposition by banded technology (offshore wind, onshore wind, biomass, solar, etc.). The ordering reflects banding-factor differentials rather than strict generation volumes: biomass and offshore wind attract higher ROC/MWh multipliers than onshore wind or solar.
    ```

    **E. `## Concentration (Chart S4)`** (anchor `#concentration-chart-s4`) — DEFERRED admonition per PATTERNS.md Pattern #6:
    ```markdown
    ## Concentration (Chart S4)

    !!! warning "S4 Concentration — DEFERRED: data-gated"

        Station-level concentration analysis (Lorenz curve, top-N attribution)
        requires per-station ROC-issuance data that is no longer publicly
        downloadable. See [§7 Data access](#data-access) for the full timeline
        and unlock paths.
    ```

    **F. `## Forward commitment (Chart S5)`** (anchor `#forward-commitment-chart-s5`) — DEFERRED admonition:
    ```markdown
    ## Forward commitment (Chart S5)

    !!! warning "S5 Forward commitment — DEFERRED: data-gated"

        Per-station accreditation-end projections to the 2037 scheme close
        require the station register (accreditation-date + 20-year window).
        See [§7 Data access](#data-access).
    ```

    **G. `## Methodology`** (anchor `#methodology`) — methodology prose + new `### Data access` subsection per PATTERNS.md Pattern #7 "§6 Methodology + new ### Data access subsection pattern":

    ```markdown
    ## Methodology

    - **Primary cost:** computed from rocs_issued × buyout_gbp_per_roc + mutualisation adjustments (SY21-22 only).
    - **Gas counterfactual:** shared with CfD. See [gas-counterfactual methodology](../methodology/gas-counterfactual.md).
    - **Scope:** GB-only headline; NI rows structurally preserved but null-valued under aggregate grain.
    - **Calendar year axis:** aggregation is by scheme year end (SY17 ends calendar 2018, etc.); chart x-axes use calendar years.
    - **Methodology version:** `counterfactual.METHODOLOGY_VERSION = "0.1.0"` stamped on every derived Parquet row.
    - **Benchmark:** reconciled against REF Constable 2025 Table 1 via `tests/test_benchmarks.py::test_ref_constable_ro_reconciliation` — reconstruction totals fall within ±3% per scheme year of REF's published figures.

    ### Data access

    On 2025-05-14 Ofgem's Public Reports Dashboard was withdrawn. Station-level ROC issuance and the station register are now accessible only via `rer.ofgem.gov.uk`, which requires SharePoint-OIDC authentication. This section documents how the scheme total is reconstructed from the publicly-downloadable alternatives Ofgem continues to publish.

    **What remains publicly downloadable** (no authentication required):

    - The 12-year aggregate XLSX: [`rocs_report_2006_to_2018_20250410081520.xlsx`](https://www.ofgem.gov.uk/sites/default/files/2025-05/rocs_report_2006_to_2018_20250410081520.xlsx) (last modified 2025-05-21; 3 sheets, GB aggregate by technology × month × country).
    - The Ofgem RO [public reports landing page](https://www.ofgem.gov.uk/renewables-obligation-ro/contacts-guidance-and-resources/public-reports-and-data-ro) — the annual-report PDF index.
    - Individual annual-report PDFs (latest: [SY23-24](https://www.ofgem.gov.uk/transparency-document/renewables-obligation-annual-report-scheme-year-23-1-april-2024-31-march-2025)).

    **What is SharePoint-OIDC gated** (deferred):

    - The station register (accreditation dates + 20-year support windows, required for the S5 forward-projection chart).
    - Monthly per-station ROC issuance (required for the S4 concentration analysis).

    **Unlock paths:**

    Station-level charts S4 and S5 are deferred pending either (a) a future collaboration with an independent analyst holding station-level data, or (b) a future credentialed-access implementation.

    Corrections and additional data sources welcome — see [corrections](../about/corrections.md) or file a GitHub issue with the [`correction`](https://github.com/richardjlyon/uk-subsidy-tracker/issues?q=label%3Acorrection) label.
    ```

    **H. `## Data & code (GOV-01 four-way coverage)`** (anchor `#data-code-gov-01-four-way-coverage`) — adapt PATTERNS.md Pattern #7 §8 manifest. Fill in with:
    - **Primary source:** Ofgem 12-year XLSX + Ofgem RO public reports landing + ONS System Average Price of gas + UK ETS auction results (links provided in PATTERNS.md).
    - **Chart source code:** `plotting/subsidy/ro_dynamics.py` + `ro_by_technology.py` GitHub blob links; note `(S4 and S5 chart modules dormant — see [§7 Data access](#data-access).)`.
    - **Pipeline source code:** `schemes/ro/`, `schemes/ro/aggregate_model.py`, `counterfactual.py`, `data/ofgem_aggregate.py` GitHub blob links.
    - **Test:** `tests/test_benchmarks.py::test_ref_constable_ro_reconciliation` — clinical tolerance-anchor framing only (D-16).
    - **Reproduce** bash block (4 lines): `git clone ...`, `cd uk-subsidy-tracker`, `uv sync`, `uv run python -c "from uk_subsidy_tracker.schemes import ro; ro.refresh(); ro.rebuild_derived()"`, `uv run python -m uk_subsidy_tracker.plotting`.

    **I. `## Headline FAQ`** (anchor `#headline-faq`) — optional, but preserve if exists in current ro.md. Add 1 new Q on reconstruction methodology:
    ```markdown
    **Q: Why is the £HEADLINE_BN bn figure computed from a reconstruction rather than cited from a single source?**

    A: Reproducibility. Under the project's core value ("every headline number reproducible from `git clone + uv sync + one command`"), every pound must trace through the repository's transformation chain rather than be cited from a PDF. The reconstructed figure is independently validated against REF Constable 2025 Table 1 per scheme year (±3% tolerance, enforced as a CI hard-block by `tests/test_benchmarks.py::test_ref_constable_ro_reconciliation`).
    ```

    **Step 3 — Verify forbidden content absence (D-15):** The file must NOT contain any of the forbidden strings. Run:
    ```bash
    # These greps MUST return zero matches in docs/schemes/ro.md
    grep -ci "SEED-002\|999\\.1\|credentialed RER\|REF collaboration\|awaiting reply\|outreach" docs/schemes/ro.md
    grep -ci "ref\.org\.uk" docs/schemes/ro.md   # REF Constable PDF link forbidden
    grep -ci "collaborator\|collaborators-in-waiting" docs/schemes/ro.md
    ```
    All four commands must return `0`. If any returns > 0, that string must be removed before Wave 6.

    **Step 4 — Run `mkdocs build --strict`** to confirm zero warnings:
    ```bash
    uv run mkdocs build --strict 2>&1 | tail -20
    ```
    Expected: "INFO - Documentation built in N.NN seconds" with no WARNING or ERROR lines. If a WARNING surfaces, it is typically a broken link — fix before proceeding to Task 3.

    **Step 5 — Re-run the headline-sync test** from Task 1 — this time it MUST pass:
    ```bash
    uv run pytest tests/test_docs_ro_headline_sync.py -v 2>&1 | tail -5
    ```
    Expected: `1 passed`.
  </action>
  <verify>
    <automated>uv run mkdocs build --strict 2>&1 | tail -5 | grep -Eic "warning|error" | awk '$1 == 0 {exit 0} {exit 1}' && uv run pytest tests/test_docs_ro_headline_sync.py -v 2>&1 | grep -c "PASSED" | awk '$1 == 1 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^# UK Renewables Obligation (RO)" docs/schemes/ro.md` returns exactly 1 match
    - `grep -n "^## What is the RO?" docs/schemes/ro.md` returns exactly 1 match
    - `grep -n "^## Cost dynamics (Chart S2)" docs/schemes/ro.md` returns exactly 1 match
    - `grep -n "^## By technology (Chart S3)" docs/schemes/ro.md` returns exactly 1 match
    - `grep -n "^## Concentration (Chart S4)" docs/schemes/ro.md` returns exactly 1 match
    - `grep -n "^## Forward commitment (Chart S5)" docs/schemes/ro.md` returns exactly 1 match
    - `grep -n "^## Methodology" docs/schemes/ro.md` returns exactly 1 match
    - `grep -n "^### Data access" docs/schemes/ro.md` returns exactly 1 match
    - `grep -n "^## Data & code" docs/schemes/ro.md` returns exactly 1 match
    - `grep -c '!!! warning "S4 Concentration — DEFERRED: data-gated"' docs/schemes/ro.md` equals 1
    - `grep -c '!!! warning "S5 Forward commitment — DEFERRED: data-gated"' docs/schemes/ro.md` equals 1
    - `grep -c "2025-05-14 Ofgem" docs/schemes/ro.md` returns at least 1 match (withdrawal date cited)
    - `grep -c "rocs_report_2006_to_2018_20250410081520.xlsx" docs/schemes/ro.md` returns at least 1 match (XLSX linked in Data access)
    - `grep -c "test_ref_constable_ro_reconciliation" docs/schemes/ro.md` returns at least 1 match (clinical REF tolerance-anchor reference)
    - `grep -ci "SEED-002\\|999\\.1\\|credentialed RER\\|REF collaboration\\|awaiting reply\\|outreach" docs/schemes/ro.md` equals 0 (D-15 forbidden content absent)
    - `grep -ci "ref\\.org\\.uk" docs/schemes/ro.md` equals 0 (D-15 REF Constable PDF link forbidden)
    - `uv run mkdocs build --strict 2>&1 | grep -iEc "warning|error"` equals 0 (mkdocs gate)
    - `uv run pytest tests/test_docs_ro_headline_sync.py -v 2>&1 | grep -c "PASSED"` equals 1 (headline matches parquet)
    - `wc -l docs/schemes/ro.md | awk '{print $1}'` returns a number greater than 150 (non-trivial rewrite)
  </acceptance_criteria>
  <done>
    docs/schemes/ro.md fully rewritten with 8-section SCHEMEPAGE-01 structure; real computed headline; S2 + S3 active charts embedded; S4 + S5 DEFERRED admonitions linking to #data-access; §Data access subsection with ~250-400 words clinical prose; GOV-01 manifest complete; no forbidden content; mkdocs --strict green; headline-sync test passing.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update docs/themes/cost/ + docs/themes/recipients/ index pages to remove stale links to dormant RO charts</name>
  <files>docs/themes/cost/index.md, docs/themes/recipients/index.md</files>
  <read_first>
    - docs/themes/cost/index.md (search for any reference to ro_concentration or ro_forward_projection)
    - docs/themes/recipients/index.md (same)
    - docs/schemes/ro.md (Task 2 output — the DEFERRED admonition + §Data access anchors are the link targets)
    - .planning/phases/05.2-ro-data-reconstruction-aggregate-grain/05.2-CONTEXT.md (Files to modify — "docs/themes/cost/index.md + docs/themes/recipients/index.md — RO S4 concentration card may need DEFERRED marker; planner verifies")
  </read_first>
  <action>
    **Step 1 — Grep both theme pages** to identify any references to the dormant RO charts:
    ```bash
    grep -n "ro_concentration\|ro_forward_projection\|subsidy_ro_concentration\|subsidy_ro_forward_projection" docs/themes/cost/index.md docs/themes/recipients/index.md 2>&1
    ```

    If matches exist, they are stale links to deleted artefacts. Decide per match:
    - **If it's a chart embed (e.g. `--8<--` include or `<iframe>` or `![](...)`)** — replace with a one-line DEFERRED admonition linking to `docs/schemes/ro.md#data-access`:
      ```markdown
      !!! warning "RO station-level charts — DEFERRED: data-gated"

          See [RO scheme page §Data access](../../schemes/ro.md#data-access).
      ```
    - **If it's a narrative link** (e.g. "See the [RO concentration chart](...)") — replace with a link to the DEFERRED admonition on the scheme page:
      ```markdown
      See [RO §Concentration](../../schemes/ro.md#concentration-chart-s4) (currently deferred — data-gated).
      ```
    - **If no matches exist on either page**, no modification needed — document that fact in the Summary.

    **Step 2 — Re-run `mkdocs build --strict`** to confirm zero broken links:
    ```bash
    uv run mkdocs build --strict 2>&1 | tail -10
    ```
    Expected: no WARNING or ERROR lines. Any new WARNING typically indicates a relative-link path error — fix by verifying the `../../schemes/ro.md#anchor` depth is correct for each theme page's location.

    **Step 3 — Verify anchor slugs land correctly** — open the built site (locally) and confirm clicking "RO §Data access" from a theme page lands at the #data-access subsection:
    ```bash
    # Build + spot-check anchor rendering
    uv run mkdocs build --strict 2>&1 | tail -3
    grep -n "data-access" site/themes/cost/index.html 2>/dev/null || true
    grep -n "data-access" site/themes/recipients/index.html 2>/dev/null || true
    grep -n "data-access" site/schemes/ro/index.html 2>/dev/null | head -3
    ```
    The anchor should resolve to `#data-access` in the rendered HTML for `docs/schemes/ro.md`.
  </action>
  <verify>
    <automated>grep -n "ro_concentration\|ro_forward_projection\|subsidy_ro_concentration\|subsidy_ro_forward_projection" docs/themes/cost/index.md docs/themes/recipients/index.md | grep -v "DEFERRED\|data-gated\|#data-access\|#concentration-chart-s4" | wc -l | awk '$1 == 0 {exit 0} {exit 1}' ; uv run mkdocs build --strict 2>&1 | tail -10 | grep -iEc "warning|error" | awk '$1 == 0 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ro_concentration\\|ro_forward_projection" docs/themes/cost/index.md docs/themes/recipients/index.md | grep -v "DEFERRED\\|data-gated\\|#data-access" | wc -l` equals 0 (any surviving references must now be DEFERRED-flagged)
    - `uv run mkdocs build --strict 2>&1 | tail -10 | grep -iEc "warning|error"` equals 0 (no broken links)
    - `test -f site/schemes/ro/index.html` exits 0 (ro.md builds)
  </acceptance_criteria>
  <done>
    Theme pages have no dangling references to dormant RO chart artefacts; any remaining mention is wrapped in a DEFERRED admonition linking to the scheme page's #data-access anchor; mkdocs --strict green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown -> HTML rendering | mkdocs-material extensions (admonition, pymdownx.details) parse user-written Markdown; trust is repo-committers (no public submission path) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-05-01 | Information disclosure | Accidental leak of internal planning state (SEED-002, 999.1) in public docs | mitigate | Task 2 acceptance criteria include grep-based tests for every forbidden string; CI mkdocs-strict gate catches link-level violations |
| T-05.2-05-02 | Tampering | Headline figure drifts away from parquet sum on a future pipeline change | mitigate | Task 1 regression test (tests/test_docs_ro_headline_sync.py) fails on any 1dp mismatch; forces prose update or CHANGES.md methodology-version entry |
| T-05.2-05-03 | Repudiation | Reader cannot verify the withdrawal-date claim (2025-05-14 Ofgem Public Reports Dashboard withdrawal) | mitigate | §Data access cites the Ofgem RO public reports landing page + the 12-year XLSX URL + publisher_last_modified=2025-05-21 — hostile reader can follow the links and confirm independently |
| T-05.2-05-04 | Integrity | Theme-page link rot (dormant chart refs live after Task 1 fix) | mitigate | Task 3 grep + mkdocs --strict double-check |
</threat_model>

<verification>
End-of-wave state:
- `docs/schemes/ro.md` is 8-section SCHEMEPAGE-01-compliant with real headline + real S2/S3 embeds + DEFERRED S4/S5 admonitions + §Data access subsection
- Zero forbidden strings (SEED-002, 999.1, credentialed RER, REF collaboration, ref.org.uk, outreach, collaborators-in-waiting)
- REF Constable referenced clinically as tolerance anchor only
- `tests/test_docs_ro_headline_sync.py` passes
- Theme pages have no stale dormant-chart links
- `uv run mkdocs build --strict` green
</verification>

<success_criteria>
1. docs/schemes/ro.md contains the 8-section SCHEMEPAGE-01 structure with exact H2 anchor slugs matching cfd.md (H1 + What is / S2 / S3 / S4 / S5 / Methodology / Data & code / Headline FAQ).
2. S2 + S3 sections embed real chart artefacts; S4 + S5 sections contain `!!! warning` admonitions with the exact DEFERRED text + #data-access link.
3. `### Data access` subsection documents the 2025-05-14 Ofgem withdrawal + what's public + what's gated + unlock paths (clinically, without naming internal planning artefacts).
4. Headline GBP bn figure is reconstructed from annual_summary.parquet; tests/test_docs_ro_headline_sync.py passes to 1 decimal place.
5. D-15 forbidden content grep returns zero matches across all forbidden strings.
6. Theme pages (docs/themes/cost/index.md + docs/themes/recipients/index.md) have no stale links to dormant RO charts.
7. `uv run mkdocs build --strict` passes with zero warnings/errors.
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-ro-data-reconstruction-aggregate-grain/05.2-05-SUMMARY.md`.
</output>
