# Phase 05.2: RO Data Reconstruction (Aggregate-Grain) — Context

**Gathered:** 2026-04-24
**Status:** Ready for planning

---

<domain>
## Phase Boundary

Replace the Phase 5 Option-D zero-row RO stubs with real numbers driven by publicly-downloadable Ofgem aggregates, reconstruct the £67bn headline reconciled against REF Constable 2025 Table 1 within ±3% per scheme year, and ship `docs/schemes/ro.md` with real S2 + S3 charts and transparently-deferred S4 + S5 slots. Un-xfail `tests/test_benchmarks.py::test_ref_constable_ro_reconciliation` by deleting the `05-09-DIVERGENCE.md` sentinel. Refactor `schemes/ro/` to add an aggregate-grain pipeline while preserving the station-level code paths as dormant, grep-discoverable, re-activatable code ready for a future credentialed-access phase (backlog 999.1) or a REF-collaboration unlock (SEED-002).

**In scope:**

- **Ofgem 12-year XLSX ingestion.** Download `rocs_report_2006_to_2018_20250410081520.xlsx` into `data/raw/ofgem/` with a real sidecar (upstream URL + sha256 + retrieved_at). Parse via openpyxl through a new loader.
- **SY17–SY23 annual transcription.** 7 scheme years of annual totals transcribed from Ofgem annual report PDFs into `data/raw/ofgem/ro-annual-aggregate.csv`. Every row's source PDF URL lands in a per-row Provenance block (CSV header comment) + sources[] array in the sibling `.meta.json` sidecar.
- **ROC prices transcription.** `data/raw/ofgem/roc-prices.csv` filled with 22 rows (`obligation_year` + `buyout` + `recycle` + `eroc` + `mutualisation`) transcribed from Ofgem transparency-document PDFs. Multi-source sidecar discipline matches annual-aggregate.
- **Regenerated monthly aggregate.** `data/raw/ofgem/ro-generation.csv` is regenerated from the XLSX as a deterministic monthly-aggregate file (stays in `data/raw/ofgem/` per D-A1, sidecar points back to XLSX sha256 as upstream).
- **New loader module.** `src/uk_subsidy_tracker/data/ofgem_aggregate.py` owns XLSX download/parse + aggregate CSV loading + ROC-prices loading. Mirrors `data/lccc.py` / `data/elexon.py` pattern.
- **Aggregate-grain pipeline.** New `src/uk_subsidy_tracker/schemes/ro/aggregate_model.py` emits `data/derived/ro/annual_summary.parquet` + `data/derived/ro/by_technology.parquet` using the existing `RoAnnualSummaryRow` + `RoByTechnologyRow` Pydantic schemas, with station-level fields nulled/zeroed.
- **Dormant station-level code paths.** `schemes/ro/cost_model.py`, `schemes/ro/forward_projection.py`, per-station aggregation logic in `schemes/ro/aggregation.py` marked `dormant: true` at module level + docstring. Grep-discoverable via `grep -rn "^dormant: true$"`. Preserved in tree; re-activated on 999.1.
- **Refresh short-circuit.** `schemes/ro/__init__.refresh()` skips dormant scraper calls (ofgem_ro.py station-level paths) when dormant flag true; fetches XLSX only.
- **Test dormancy mark.** `@pytest.mark.dormant` registered in `conftest.py` with `skip reason='dormant per Phase 5.2; un-skip on 999.1'`. Applied to existing station-level RO tests; tests stay in tree, visible as skipped.
- **docs/schemes/ro.md refactor.** Headline recomputed from `annual_summary.parquet` (may remain £67bn if reconstruction matches REF). S2 + S3 charts populate with real data. S4 + S5 slots replaced with mkdocs-material `!!! warning "DEFERRED: data-gated"` admonition blocks linking to §7 Data access. Existing `docs/charts/html/subsidy_ro_concentration{.png,.html}` + `subsidy_ro_forward_projection{.png,.html}` deleted; regeneration skipped while dormant.
- **§7 Data access subsection.** New Methodology-section subsection documenting: (i) 2025-05-14 Ofgem Public Reports Dashboard withdrawal; (ii) what remains publicly downloadable (XLSX URL + annual-report landing page + transparency-doc PDFs); (iii) what is SharePoint-OIDC-gated (station register, per-station ROC issuance, per-station accreditation dates); (iv) unlock paths stated clinically — **without** naming SEED-002 or backlog 999.1 or linking the REF Constable PDF. Tone: mildly-pointed, dates + facts, no adjectives. Length: ~250-400 words. Links out to Ofgem source URLs + `docs/about/corrections.md` + GitHub `correction` label.
- **REF reconciliation hard block.** Delete `.planning/phases/05-ro-module/05-09-DIVERGENCE.md` sentinel. `test_ref_constable_ro_reconciliation` moves from 22 xfailed to 22 passed (within ±3% per year).
- **REQUIREMENTS.md update.** RO-04 marked `deferred-data-gated` for S4 + S5 components with cross-reference to backlog 999.1 and SEED-002 in internal planning artefacts only (not in `docs/`). S2 + S3 remain Complete.
- **CHANGES.md [Unreleased] audit trail.** Full record of what shipped + what was deferred + why.
- **`mkdocs build --strict`** passes with zero warnings post-commit.

**Out of scope (belongs elsewhere):**

- **Credentialed RER scraper** (Playwright + SharePoint-OIDC + GitHub Secrets) — backlog 999.1. Explicitly deferred by Option A1 per `.planning/notes/ro-data-strategy-option-a1.md`.
- **REF Constable data-sharing collaboration follow-up** — SEED-002. Separate outreach trigger; not a Phase 05.2 deliverable.
- **Phase 6 portal X1/X2/X3 cross-scheme charts** — Phase 6.
- **Portal homepage populated RO tile** — Phase 6 reads `data/derived/ro/annual_summary.parquet` (which Phase 05.2 produces); RO-tile population itself is Phase 6 work.
- **METHODOLOGY_VERSION bump to 1.0.0** — Phase 6+ portal launch milestone (Phase 5 D-06).
- **Phase 7+ scheme modules** — strict phase boundary; no FiT / Constraints / CM / Balancing / Grid / SEG work.
- **Un-deferring NIRO under aggregate grain** — Ofgem 12-year XLSX is GB-only; NIROC data lives in NI Annual Reports, which are not in the Phase 05.2 transcription scope. D-09 NIRO inclusion in Parquet is inherited from Phase 5; columns persist with null/zero rows under aggregate grain. Revisit in a later phase if NI data access warrants.
- **Headline recomputation scope negotiation.** The lead paragraph's £67bn figure currently reads verbatim from REF Constable 2025 Table 1; Phase 05.2 recomputes it from `annual_summary.parquet` — if the reconstruction matches REF within ±3%, the figure may remain £67bn; if it diverges legitimately under a different scope convention, the figure updates with a CHANGES.md `## Methodology versions` audit entry. Planner-level decision, not gray-area.

</domain>

<decisions>
## Implementation Decisions

### Raw→derived data plumbing

- **D-01 Regenerated ro-generation.csv stays in `data/raw/ofgem/`.** XLSX + ro-generation.csv + ro-annual-aggregate.csv + roc-prices.csv all colocated in `data/raw/ofgem/`. Mild fiction that a derived-from-XLSX CSV is "raw" is outweighed by: (a) preserves Phase 4/5 loader shape (downstream code consumes from `data/raw/ofgem/`), (b) makes `data/raw/ofgem/` the single "everything Ofgem-sourced" directory, (c) no `data/derived/ro/*.csv` intermediate to reason about. Sidecar on regenerated `ro-generation.csv` carries `upstream_url` pointing back to the XLSX's sha256, making the derivation chain grep-discoverable.
- **D-02 New `data/ofgem_aggregate.py` loader module.** Dedicated module alongside (now-dormant) `data/ofgem_ro.py`, mirroring `data/lccc.py` + `data/elexon.py` pattern. Public API: `download_twelve_year_xlsx()`, `parse_xlsx_to_monthly()`, `load_annual_aggregate_csv()`, `load_roc_prices_csv()`. Keeps dormant station-level scraper untouched — zero risk of silent regression. Aggregate pipeline is isolated + independently testable. `data/ofgem_ro.py` retains the `dormant: true` module flag per D-08 and is not touched during 05.2.
- **D-03 Per-row Provenance block in CSV header + multi-source sidecar for transcribed files.** `ro-annual-aggregate.csv` + `roc-prices.csv` open with a Provenance: block (one line per source PDF: URL + retrieved_on + sha256), matching the user-memory `constant_provenance_pattern` discipline that governs `src/` constants. `.meta.json` sidecar carries `upstream_url: "transcribed:ofgem-annual-reports"` (or equivalent canonical pseudo-URL) + a new `sources: [ { url, sha256, retrieved_on, notes }, ... ]` array enumerating each PDF. Grep-discoverable, auditable. Sidecar schema gains the optional `sources[]` field — non-breaking addition; existing single-URL sidecars unaffected. The 12-year XLSX sidecar remains single-URL (one real upstream).

### Aggregate-grain schema + dormancy

- **D-04 Reuse existing `RoAnnualSummaryRow` + `RoByTechnologyRow` Pydantic schemas.** `schemas/ro.py` models stay as-is structurally. Aggregate pipeline populates computable fields (`year`, `technology`, `generation_mwh`, `ro_cost_gbp`, `gas_counterfactual_gbp`, `premium_gbp`, `mutualisation_gbp`); station-level fields (`station_count`, top-N aggregations, anything station-id-derived) become `0` (for count-shaped fields) or `None` (for nullable-fields). Some field nullability in `schemas/ro.py` may need adjustment — planner's call, minimal touch. Parametrisation of `test_schemas.py` + `test_aggregates.py` + `test_determinism.py` keeps working with no parametrisation-surface changes. Manifest.json schema URLs unchanged.
- **D-05 `station_month.parquet` removed from refresh pipeline until 999.1 fires.** `schemes/ro/__init__.rebuild_derived()` stops calling `build_station_month()` (which lives in the dormant `cost_model.py`). `data/derived/ro/station_month.parquet` + `station_month.schema.json` deleted from working tree. Manifest.json loses the `station_month` entry for RO. Reader-facing semantics: "no file = no data" (beats Phase 5's zero-row sentinel which led to the 22 xfails). Phase 5's `test_schemas` / `test_aggregates` / `test_determinism` `station_month` parametrisations for RO also move under dormancy — see D-06.
- **D-06 Register `@pytest.mark.dormant` in `conftest.py` with skip-with-reason.** New pytest mark at the repo `conftest.py` level: `pytest.register_mark("dormant")` + a fixture / hook that auto-skips any test carrying the mark, with `reason="dormant per Phase 5.2; un-skip on backlog 999.1"`. Apply to: existing station-level RO tests (any `test_*` that imports `schemes.ro.cost_model` or `schemes.ro.forward_projection` or exercises `station_month` grain). Tests stay in `tests/` tree — grep-discoverable, visible in CI as "dormant — skipped", survive refactors. On 999.1 activation, strip the mark; tests re-activate unchanged.
- **D-07 `schemes/ro/__init__.refresh()` short-circuits dormant paths on daily cron.** `refresh()` checks a module-level `DORMANT_STATION_LEVEL = True` constant (or reads the dormant-flag on `schemes.ro.cost_model`); if True, skips all `ofgem_ro.py` station-level scraper calls, fetches only the XLSX + validates the already-committed transcribed CSVs. Daily cron stays quiet for RO (XLSX last-modified check; re-download on change). Matches D-09 in CONTEXT framing: dormant = silent. `refresh_all.SCHEMES` keeps `schemes.ro` registered — no change there.
- **D-08 Module-level `dormant: true` flag + docstring on dormant code paths.** ROADMAP SC #7 discipline. Applies to: `schemes/ro/cost_model.py`, `schemes/ro/forward_projection.py`, the per-station-Lorenz aggregation code path in `schemes/ro/aggregation.py`, and `data/ofgem_ro.py`. Each gets a top-of-file `dormant: true` comment + docstring explaining the dormancy reason + link (in comment, not code) to `.planning/notes/ro-data-strategy-option-a1.md`. Grep discipline: `grep -rn "^# dormant: true$"` must find every dormant module after 05.2.

### DEFERRED chart slot presentation

- **D-09 MkDocs-material `!!! warning` admonition at the S4 + S5 slots.** Where the Phase 5 chart would embed, render a block like `!!! warning "S4 Concentration — DEFERRED: data-gated"` with a short paragraph naming the 2025-05-14 Ofgem withdrawal and an in-page link to §7 Data access. Same treatment for S5. Prose-only — **no placeholder PNG**. Readers unmistakably see absence is deliberate; visual rhythm of CfD page preserved via the 8-section skeleton.
- **D-10 Inline short + detailed in §7 Data access.** S4/S5 admonition: 1-2 sentences max + link anchor. Full forensic timeline + URLs live once in §7 Data access. Adversarial-proof: skimmers see the absence with reason; deep readers get the full audit trail; no duplication.
- **D-11 Delete Phase 5 zero-row chart artefacts; skip regeneration.** Remove `docs/charts/html/subsidy_ro_concentration.html`, `subsidy_ro_concentration_twitter.png`, `subsidy_ro_forward_projection.html`, `subsidy_ro_forward_projection_twitter.png` from the working tree. `plotting/subsidy/ro_concentration.py` + `plotting/subsidy/ro_forward_projection.py` get the `dormant: true` flag per D-08; `plotting/__main__.py` skips dormant modules in the regeneration loop. Clean: no misleading zero-row artefacts in the published site; chart modules re-activate on 999.1.
- **D-12 Keep 8-section structure; S4 + S5 headings remain.** Section order unchanged: Headline / What is the RO? / S2 Cost dynamics / S3 By technology / S4 Concentration / S5 Forward commitment / §7 Methodology / §8 Data & code (GOV-01). S4 + S5 headings carry the DEFERRED admonition in place of a chart embed. SCHEMEPAGE-01 template (Phase 05.1) preserved — reader comparing CfD vs RO sees identical skeleton, with the asymmetry clearly marked. Once 999.1 lands, S4 + S5 charts populate in place — zero structural churn.

### Data access section scope + tone

- **D-13 Mildly-pointed tone: dates + facts, no adjectives.** Narrative voice reports observable facts: "On 2025-05-14 Ofgem's Public Reports Dashboard was withdrawn. Station-level ROC issuance and the station register are now accessible only via `rer.ofgem.gov.uk`, which requires SharePoint-OIDC authentication. This section documents how the scheme total is reconstructed from the publicly-downloadable alternatives Ofgem continues to publish." Facts land; no editorialising; hostile readers can't accuse bias; adversarial readers can't accuse concealment.
- **D-14 Moderate depth (~250-400 words): withdrawal + public + gated + unlock paths.** Subsection covers: (i) the 2025-05-14 withdrawal as a one-paragraph event; (ii) what's publicly downloadable (the 12-year XLSX URL + last-modified date; the annual-report landing page; the transparency-document PDF pattern); (iii) what's SharePoint-OIDC-gated (station register, monthly per-station ROC issuance, per-station accreditation dates); (iv) unlock paths described clinically ("station-level charts S4 + S5 are deferred pending either (a) a future collaboration with an independent analyst holding station-level data, or (b) a future credentialed-access phase"). Length matches CfD-page methodology depth. **Full-forensic URL-and-sha256 manifest is deferred to `docs/data/index.md` + manifest.json** (out of this subsection's scope).
- **D-15 Link out to Ofgem source URLs + corrections channel only.** Allowed out-links from §7 Data access: (a) Ofgem XLSX URL; (b) Ofgem annual-report landing page; (c) Ofgem transparency-document PDF pattern or search URL; (d) `docs/about/corrections.md`; (e) GitHub `correction` label URL. **Explicitly forbidden:** (i) REF Constable PDF link — REF is cited clinically as a test-file tolerance anchor, not as a peer publisher; (ii) `.planning/seeds/SEED-002-*.md` or "REF collaboration" narrative — internal outreach state stays off public docs; (iii) backlog `999.1` or "credentialed RER scraper" language — internal roadmap state stays off public docs; (iv) any relational language about collaborators-in-waiting. See user memory `feedback_internal_artefacts_off_public_docs.md` for the governing principle: methodology-section tone stays data-first and clinical.
- **D-16 REF Constable treatment: clinical tolerance anchor only.** In §7 Data access and §7 Methodology summary prose, REF Constable 2025 Table 1 is named as "the independent benchmark anchor for `tests/test_benchmarks.py::test_ref_constable_ro_reconciliation` — reconstruction totals must fall within ±3% per scheme year of REF's published figures". No peer-publisher framing, no 15-year-track-record acknowledgement, no data-sharing allusion. Turver Net Zero Watch may be cited as peer cross-check per Phase 5 D-15 discipline; same clinical framing applies.

### Claude's Discretion

- **Sidecar schema `sources[]` extension.** Exact JSON shape of the multi-source sidecar (D-03) is planner-decided. Must: (a) remain backward-compatible with single-URL sidecars (existing ~10 sidecars unaffected); (b) validate via Pydantic if one exists at write-time (check `sidecar.write_sidecar()` for existing schema discipline); (c) survive the manifest.json provenance-chain discipline so downstream `publish/manifest.py` handles it without code changes — or with minimal, atomic code changes.
- **Openpyxl XLSX parsing strategy.** 3 sheets (aggregate by tech × month × country per strategy note). Planner picks column/sheet discovery approach (hard-code sheet names from inspection vs iterate sheets vs schema-validate). Deterministic output discipline (D-21 Phase 4) applies: same XLSX → byte-identical `ro-generation.csv`.
- **Annual transcription row schema.** `ro-annual-aggregate.csv` columns (SY-label / year-int / generation_TWh / ro_cost_gbp / per-tech-breakdown?) planner-decided. Must be richer than minimum so `annual_summary.parquet` is computable without re-reading the XLSX for 2018+. Must carry `obligation_year` that joins cleanly with `roc-prices.csv`.
- **REF reconciliation behaviour under aggregate-grain divergence.** D-14 ±3% is the hard block; the **ladder for aggregate-vs-REF mismatches** differs from Phase 5 D-14 (no banding error axis; no per-station carbon attribution). Planner picks the revised diagnostic ladder in `test_benchmarks.py` failure messages. If the reconstruction diverges legitimately from REF's scope (e.g. REF excludes mutualisation; REF includes/excludes NIROC differently), adjust `REF_TOLERANCE_PCT` ONLY with a `CHANGES.md ## Methodology versions` entry per Phase 2 D-07.
- **Headline £67bn update discipline.** Lead paragraph of `docs/schemes/ro.md` currently reads "£67 bn" verbatim from REF Constable. Planner computes the real figure from `annual_summary.parquet` at build time (not hardcoded in Markdown) — MkDocs macros plugin or inline-mkdocstrings-driven substitution OR a build-time sed pass. If the Markdown must carry a hardcoded number, a regression test asserts `docs/schemes/ro.md`'s headline number matches `annual_summary.parquet`'s GB all-in total to one decimal place. Phase-exit criterion: the figure in prose matches the figure in data.
- **Atomic commit slicing.** Planner decomposes 05.2 into atomic waves per Phase 1 D-16 discipline. Suggested grouping (planner may revise): (W1) new `data/ofgem_aggregate.py` + XLSX download wiring + sidecar schema extension if needed; (W2) transcribed CSV landings (annual + prices) + multi-source sidecars; (W3) `schemes/ro/aggregate_model.py` + schema-field nullability adjustments + `rebuild_derived()` wiring; (W4) dormancy discipline (dormant flags + conftest mark + module-level annotations + delete `station_month.parquet`); (W5) `docs/schemes/ro.md` rewrite (headline + S2 + S3 + DEFERRED admonitions + §7 Data access subsection + delete PNG/HTML artefacts); (W6) sentinel-delete + REF reconciliation un-xfail + REQUIREMENTS / CHANGES / `mkdocs --strict` gates.
- **Whether `ro-generation.csv` sidecar `sources[]` is scoped to "regenerated-from-XLSX" or carries the XLSX's own `sources[]`.** Sidecar inheritance discipline is planner-picked. Option A: `ro-generation.csv.meta.json` has `upstream_url: file://data/raw/ofgem/rocs_report_2006_to_2018_20250410081520.xlsx` + `sources: [ XLSX's-own-source ]`. Option B: a flat `upstream_url: "regenerated-from-xlsx:rocs_report_2006_to_2018.xlsx#sha256=..."` with chain-discoverable semantics.

### Folded Todos

No pending todos from STATE.md matched Phase 05.2 (per `gsd-tools todo match-phase 05.2` returned 0 matches).

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Authoritative strategy (spec for this phase)

- `.planning/notes/ro-data-strategy-option-a1.md` — **the Phase 05.2 design document**. Explains why A1 (Ofgem aggregates primary + REF as ±3% cross-check) beats B (credentialed scraper now) and C (REF as primary source). All 16 D-decisions above trace back to this note.
- `ARCHITECTURE.md` §4 — Three-layer data architecture (raw/derived/published). Substrate for `data/raw/ofgem/`, `data/derived/ro/`, `site/data/latest/ro/`.
- `ARCHITECTURE.md` §6.1 — Scheme-module contract (five functions + DERIVED_DIR). **Load-bearing template** for `schemes/ro/` — must still isomorphically conform after the dormancy refactor.
- `ARCHITECTURE.md` §7.1 — Per-source refresh cadence; aggregate pipeline runs on daily cron via XLSX last-modified check.
- `ARCHITECTURE.md` §9.1 + §9.4 — Provenance + methodology-versioning chain; RO Parquet rows retain `methodology_version` column.
- `RO-MODULE-SPEC.md` — Domain authority. §1 goal, §6 data model (grain schemas), §7 cost-computation formula, §10 R1-R7 risks (R1 and R6 continue to apply under aggregate grain; R2/R3 also). §12 success criteria — partially deferred by Phase 05.2 scope.

### Roadmap + requirements

- `.planning/ROADMAP.md` Phase 05.2 (lines ~125-144) — 12 success criteria. SC #1 real S2+S3 charts + £67bn headline reconciled ±3%; SC #2 REF reconciliation hard CI block; SC #3 S4/S5 DEFERRED markers + §7 Data access; SC #4 XLSX ingestion + ro-generation.csv regeneration; SC #5 SY17-SY23 transcription; SC #6 roc-prices.csv transcription; SC #7 aggregate_model + dormant station-level paths; SC #8 §7 Data access section; SC #9 REQUIREMENTS RO-04 deferred-data-gated; SC #10 CHANGES.md audit; SC #11 mkdocs --strict; SC #12 Phase 6 portal unblocked.
- `.planning/REQUIREMENTS.md` — RO-03 (Parquet tables), RO-04 partial (S2/S3 deliverable under 05.2; S4/S5 move to deferred-data-gated), RO-05 (docs page), RO-06 (REF Constable ±3% reconciliation becomes hard CI block).
- `.planning/PROJECT.md` — Core-value "every headline number reproducible from single git clone + uv sync + one command"; adversarial-proofing as first-class concern; methodological bulletproofness as the constraint shaping every downstream decision.
- `.planning/STATE.md` — Current position: Phase 05.1 complete (SCHEMEPAGE-01 validated); Phase 05.2 starting; full test baseline is `163 passed + 12 skipped + 22 xfailed` (the 22 xfailed are the REF reconciliations 05.2 un-xfails).

### Prior-phase context (locked decisions apply)

- `.planning/phases/05-ro-module/05-CONTEXT.md` — **Phase 5 decision bank**. All inherited D-decisions apply: D-01 Ofgem-primary rocs_issued (dormant), D-02 dual cost columns (dormant), D-03 ro_bandings.yaml (dormant), D-05 carbon-price extension (shipped + still active), D-07 CY axis (shipped), D-09 NIRO inclusion (Parquet carries null NI rows under aggregate), D-10 co-firing inclusion (aggregate preserves technology slice), D-11 mutualisation SY 2021-22 only (preserved in roc-prices + annual aggregate), D-12 all-in GB headline (preserved), D-13/D-14 REF Constable anchor + ±3% hard block (becomes live on 05.2 exit), D-15 8-section docs page (preserved).
- `.planning/phases/05-ro-module/05-09-DIVERGENCE.md` — **the sentinel this phase deletes**. The xfail escape hatch for `test_ref_constable_ro_reconciliation` is live only while this file exists; Phase 05.2 must delete it as part of SC #2. Re-arm verification block at lines 119-143 is the script Phase 05.2 runs.
- `.planning/phases/05-ro-module/05-13-SUMMARY.md` — §HA-01 through §HA-04 human-action register. Phase 05.2 closes §HA-01 (Option-D stubs replaced — via aggregates path, not RER path) and §HA-04 (sentinel delete + hard block re-arm). §HA-02 (NIROC transcription) + §HA-03 (EU ETS audit) remain deferred to their own future sessions. Re-arm verification bash blocks in §HA-04 are the verification script.
- `.planning/seeds/SEED-002-ref-collaboration-followup.md` — REF outreach is live but not replied-to (sent 2026-04-24). Relevant to this phase ONLY because the Data access section must NOT expose this internal state (D-15 + user memory `feedback_internal_artefacts_off_public_docs.md`). The `trigger_when` includes "Phase 05.2 completes" — post-phase operational step.
- `.planning/phases/05.1-cfd-scheme-page/05.1-CONTEXT.md` — SCHEMEPAGE-01 template validated on CfD. D-02 8-section structure + D-03 H2 anchor slugs (`#cost-dynamics-chart-s2` etc.) apply verbatim to Phase 05.2's ro.md refactor. Admonition + DEFERRED treatment (D-09/D-10 here) is the new addition; 05.1 page has no deferrals.
- `.planning/phases/04-publishing-layer/04-CONTEXT.md` — Sidecar discipline (`sidecar.write_sidecar()` atomic helper); three-layer pipeline operational; `refresh_all.SCHEMES` registration; `manifest.py` iterates SCHEMES so aggregate grains auto-publish.
- `.planning/phases/02-test-benchmark-scaffolding/02-CONTEXT.md` — `_TOLERANCE_BY_SOURCE` dispatch pattern for REF_TOLERANCE_PCT; TDD RED→GREEN discipline.

### External references (reproducibility)

- **Ofgem 12-year XLSX:** `https://www.ofgem.gov.uk/sites/default/files/2025-05/rocs_report_2006_to_2018_20250410081520.xlsx` — HTTP 200 as of 2026-04-23, last modified 2025-05-21, 114 KB, 3 sheets (aggregate by tech × month × country). Primary source for D-01 + D-02.
- **Ofgem RO public reports landing:** `https://www.ofgem.gov.uk/renewables-obligation-ro/contacts-guidance-and-resources/public-reports-and-data-ro` — the landing page for annual-report PDF discovery.
- **Latest Ofgem annual report:** `https://www.ofgem.gov.uk/transparency-document/renewables-obligation-annual-report-scheme-year-23-1-april-2024-31-march-2025` — source PDF for SY23-24 annual data (last of the 7-year transcription set).
- **Ofgem pre-2007 archive:** `https://www.ofgem.gov.uk/sites/default/files/docs/2006/11/ro_weblist_archive_0.xls` — HTTP 200 as of 2026-04-23, 210 KB. Pre-2007 station-level archive (not in 05.2 scope; informational for 999.1).
- **REF Constable 2025 "Renewable Subsidies" Table 1:** `https://ref.org.uk/attachments/article/390/renewables.subsidies.01.05.25.pdf` — the 22-year benchmark series already transcribed in `tests/fixtures/benchmarks.yaml::ref_constable`. **Not linked from `docs/schemes/ro.md`** per D-15.

### Files to modify

- `src/uk_subsidy_tracker/schemes/ro/__init__.py` — `refresh()` + `rebuild_derived()` updated per D-07 (short-circuit dormant) + D-04 (aggregate pipeline).
- `src/uk_subsidy_tracker/schemes/ro/cost_model.py` — `dormant: true` module flag + docstring (D-08).
- `src/uk_subsidy_tracker/schemes/ro/forward_projection.py` — `dormant: true` module flag + docstring (D-08).
- `src/uk_subsidy_tracker/schemes/ro/aggregation.py` — per-station-Lorenz aggregation path marked dormant (D-08); annual/by-technology rollup paths stay active + adapted to aggregate grain.
- `src/uk_subsidy_tracker/data/ofgem_ro.py` — `dormant: true` module flag + docstring (D-08); NOT called by `refresh()` when dormant.
- `src/uk_subsidy_tracker/schemas/ro.py` — mild nullability adjustments per D-04; station-level fields nullable / zero-able.
- `src/uk_subsidy_tracker/data/sidecar.py` — if a Pydantic schema exists, extend to support optional `sources[]` array (D-03). Backward-compatible.
- `tests/conftest.py` — register `@pytest.mark.dormant` + auto-skip hook (D-06).
- `tests/test_schemas.py` / `tests/test_aggregates.py` / `tests/test_determinism.py` — apply `@pytest.mark.dormant` to `station_month`-grain parametrisations for RO; keep `annual_summary` / `by_technology` / `by_allocation_round` / `forward_projection` parametrisations active (some of the latter may also be dormant depending on aggregate schema — planner decides).
- `tests/test_benchmarks.py` — no code change; behaviour changes: all 22 REF parametrisations move from `xfailed` → `passed` (±3%) when sentinel deletes.
- `tests/data/test_ofgem_ro.py` — existing station-level scraper tests get `@pytest.mark.dormant`.
- `docs/schemes/ro.md` — full rewrite of: headline prose (real computed figure or computed-matches-£67bn), S4 + S5 chart blocks (admonition-only), §7 Methodology + new `### Data access` subsection per D-13-D-16.
- `docs/themes/cost/index.md` + `docs/themes/recipients/index.md` — RO S4 concentration card may need DEFERRED marker; planner verifies.
- `docs/index.md` — portal homepage RO tile headline number; updates at Phase 6 time, not 05.2.
- `mkdocs.yml` — no nav change; verify `--strict` build holds.
- `.planning/REQUIREMENTS.md` — RO-04 marked `deferred-data-gated` for S4/S5 portion; RO-06 re-arm status updated; cross-reference to 999.1 + SEED-002 in internal artefact only (not in `docs/`).
- `CHANGES.md` — `[Unreleased]` full audit trail: Added (aggregate pipeline + transcribed data + §7 Data access); Changed (schemas/ro field nullability; refresh short-circuit); Fixed (REF reconciliation hard block re-armed); `## Methodology versions` entry ONLY if REF_TOLERANCE_PCT widens.
- `.planning/phases/05-ro-module/05-09-DIVERGENCE.md` — **deleted** per SC #2 (after successful pipeline rebuild).

### Files to create

- `src/uk_subsidy_tracker/data/ofgem_aggregate.py` — new loader (D-02).
- `src/uk_subsidy_tracker/schemes/ro/aggregate_model.py` — new aggregate pipeline; produces `annual_summary.parquet` + `by_technology.parquet` (ROADMAP SC #7).
- `data/raw/ofgem/rocs_report_2006_to_2018_20250410081520.xlsx` + `.meta.json` sidecar — replaces stub (D-01 + D-03).
- `data/raw/ofgem/ro-annual-aggregate.csv` + `.meta.json` sidecar with `sources[]` — new (SC #5).
- `data/raw/ofgem/roc-prices.csv` (transcribe into existing stub) + updated `.meta.json` sidecar with `sources[]` (SC #6).
- `data/raw/ofgem/ro-generation.csv` — regenerated from XLSX (D-01) + sidecar pointing back to XLSX.
- `tests/data/test_ofgem_aggregate.py` — mocked-network scraper tests for new loader (Phase 4 no-network-CI discipline).
- `tests/schemes/test_ro_aggregate_model.py` (or similar) — aggregate pipeline unit tests.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets

- **`src/uk_subsidy_tracker/schemes/cfd/`** — Non-dormant §6.1 template. `schemes/ro/__init__.py`'s structure during 05.2 still mirrors this — `refresh()` + `rebuild_derived()` + `regenerate_charts()` + `validate()` + `upstream_changed()` — with the rebuild path routed through `aggregate_model.py` instead of `cost_model.py`.
- **`src/uk_subsidy_tracker/data/lccc.py`** + **`src/uk_subsidy_tracker/data/elexon.py`** — Loader-module templates for the new `data/ofgem_aggregate.py`. Both show: download→validate→return-DataFrame pattern; pandera schema on ingest; try/except around network calls returning `output_path` on failure (Phase 4 discipline).
- **`src/uk_subsidy_tracker/data/sidecar.py::write_sidecar()`** — Shared atomic sidecar writer (`.tmp` + `os.replace`; `sort_keys=True` + `indent=2` + trailing newline). Extend gently for `sources[]` array per D-03; keep current single-URL callers working.
- **`src/uk_subsidy_tracker/counterfactual.py`** — `compute_counterfactual()` + `DEFAULT_CARBON_PRICES` unchanged. `aggregate_model.py` reuses them for per-year gas counterfactual computation (the CY-averaged approach from Phase 5 D-05).
- **`src/uk_subsidy_tracker/data/ro_bandings.yaml`** — Preserved but **not read by aggregate pipeline** — station-level bandings don't apply to pre-aggregated Ofgem totals. Kept in tree for 999.1 re-activation. Its Provenance discipline is the template for the new transcribed-CSV Provenance blocks (D-03).
- **`src/uk_subsidy_tracker/schemes/ro/aggregation.py`** — `build_annual_summary()` + `build_by_technology()` functions already exist (Phase 5 Plan 05-05). `aggregate_model.py` either replaces these or they adapt to read aggregate-source columns instead of station-month columns. Planner decides: extend `aggregation.py` in place (simpler; closer to Phase 5 shape) vs introduce `aggregate_model.py` as a separate computation layer (ROADMAP SC #7 literal wording).
- **`tests/fixtures/benchmarks.yaml::ref_constable`** — 22 REF Table 1 entries already transcribed (Phase 5 Plan 05-09). `REF_TOLERANCE_PCT = 3.0` and `_TOLERANCE_BY_SOURCE` dispatch already wired.
- **`tests/test_benchmarks.py::test_ref_constable_ro_reconciliation`** — Parametrised test ready; sentinel-gated. Deleting `05-09-DIVERGENCE.md` alone un-xfails.
- **`docs/schemes/cfd.md`** — Phase 05.1 template. D-02 8-section structure + D-03 H2 anchor slugs apply verbatim to the `ro.md` rewrite. Reference it for §7 Methodology shape and §8 Data & code GOV-01 manifest.

### Established Patterns

- **Atomic commits per concern** (Phase 1 D-16). Planner decomposes 05.2 into waves; see "Atomic commit slicing" in Claude's Discretion.
- **TDD RED → GREEN** where pragmatic (Phase 4 Plans 01/03/07). Aggregate-model tests land RED against missing module first.
- **Loader-owned pandera validation** — `ofgem_aggregate.py` loaders carry `.validate()` inside function bodies.
- **Two-layer Pydantic + source-key injection** (Phase 2 D-07). Extends to sidecar schema if needed for `sources[]`.
- **Provenance: docstring discipline** (user memory `constant_provenance_pattern.md`). Transcribed CSV rows carry equivalent Provenance: blocks at file-header level (CSV comment lines), not Python docstrings — D-03 adapts the discipline to CSV context.
- **Deterministic rebuild discipline** (Phase 4 D-21). Same XLSX + same transcribed CSVs → byte-identical Parquet output.
- **Dormant = silent, grep-discoverable, re-activatable** (new to this phase per D-07/D-08). Future phases (Phase 7+) inherit this discipline if any scheme turns out to be data-gated partway through.
- **Internal artefacts off public docs** (user memory `feedback_internal_artefacts_off_public_docs.md`). D-15 + D-16 codify the pattern for Phase 05.2.

### Integration Points

- **`counterfactual.METHODOLOGY_VERSION` → RO Parquet column → `manifest.json::methodology_version`** — unchanged; aggregate-grain rows still carry the column.
- **`mkdocs build --strict`** — Permanent CI gate. D-09 `!!! warning` admonitions must render without warning; deleted artefacts must not leave broken links in CfD page or theme galleries.
- **`.github/workflows/refresh.yml`** — Daily 06:00 UTC cron; runs `refresh_all.py`. After D-07 short-circuit, RO refresh is cheaper (XLSX only); per-scheme dirty-check still applies.
- **`.github/workflows/deploy.yml`** — On tag push, snapshot.py emits aggregate-grain Parquet alongside CfD; no workflow change.
- **`docs/data/index.md`** — Journalist/academic entry point; works unchanged for RO aggregate grain (schema metadata flows through manifest.json).
- **`docs/about/corrections.md` + `correction` label** — RO aggregate-reconstruction correction channel inherits automatically. §7 Data access links here (D-15).
- **`.planning/seeds/SEED-002-*.md` trigger** — "Phase 05.2 completes" is a listed trigger. Post-phase operational step: re-engage REF with the live page (OUT OF SCOPE for 05.2 itself; post-phase user action).
- **Backlog 999.1 dormancy discipline** — Every dormant module + test in 05.2 re-activates cleanly on 999.1. The `dormant: true` flag + `@pytest.mark.dormant` mark are the single grep-discoverable switches.

### Known pre-existing considerations

- **22 REF reconciliation tests currently xfailed** — 05.2 un-xfails all 22 simultaneously. If the reconstruction misses ±3% on any year, 05.2 does not ship until resolved (D-14 ladder applies).
- **`data/raw/ofgem/ro-generation.csv` + `ro-register.xlsx` + `roc-prices.csv` are Option-D stubs** — 05.2 replaces all three: ro-generation.csv regenerated from XLSX (D-01); ro-register.xlsx deleted (dormant — station-level scope only); roc-prices.csv transcribed (SC #6).
- **`ro-register.xlsx` stub (4.9 KB) needs handling.** Since 05.2 defers station-level entirely, the stub XLSX should be deleted from `data/raw/ofgem/` + sidecar removed, matching dormancy discipline. Planner verifies this is the intended treatment vs keeping a pointer-style sidecar.
- **Phase 05.1 finished yesterday (2026-04-23)** — `docs/schemes/cfd.md` is the live SCHEMEPAGE-01 template. Any structural changes to `docs/schemes/ro.md` in Phase 05.2 must preserve cross-scheme symmetry.
- **METHODOLOGY_VERSION unchanged at `"0.1.0"`** — 05.2 does not bump. Additive extension of `DEFAULT_CARBON_PRICES` in Phase 5 D-05 already logged. 1.0.0 is reserved for Phase 6+ portal launch.
- **Plotly chart HTML + PNG are gitignored.** Deleting `subsidy_ro_concentration*` etc. (D-11) needs two actions: (a) remove from working tree if currently committed; (b) ensure `plotting/__main__.py` skips regeneration for dormant modules so rebuilds don't recreate them.

</code_context>

<specifics>
## Specific Ideas

- **"Ofgem's withdrawal becomes part of the published analysis"** (`.planning/notes/ro-data-strategy-option-a1.md`). D-13/D-14 makes this concrete: the `### Data access` subsection documents the 2025-05-14 withdrawal as on-site evidence rather than an off-site grievance. The published page itself becomes the artefact of record for the access-regime change.
- **"Every pound, every counterfactual, every methodology exposed"** (PROJECT.md core value). 05.2 ships the £67bn figure from reconstruction, not from citation — even though REF published the same figure. The difference is reproducibility: `git clone + uv sync + rebuild_derived()` produces the same £67bn as REF's PDF, via independently transcribed aggregates.
- **"Parquet is the audit trail"** (Phase 5 specifics, still applies). Under aggregate grain the Parquet carries annual × technology rows; a researcher filters to any subset and gets a verifiable total. The reconciliation test is the public commitment that the total is right.
- **"Dormant = grep-discoverable + re-activatable"** (new to 05.2 per D-07/D-08). Single switch (`dormant: true` flag + `@pytest.mark.dormant` mark) activates / deactivates station-level paths across the entire RO scheme. Backlog 999.1 becomes a switch-flip rather than a re-implementation.
- **"Methodology sections cite sources clinically, not relationally"** (new user-memory pattern from D-15/D-16, `feedback_internal_artefacts_off_public_docs.md`). REF Constable is a tolerance anchor, not a collaborator-in-waiting; the SEED-002 outreach is invisible to the published page. Future scheme pages (FiT, Constraints, CM, Balancing, Grid, SEG) inherit this discipline.
- **"The scheme you've never heard of, twice the size of the one you have"** (Phase 5 specifics, from RO-MODULE-SPEC.md §11). Lead paragraph rhetorical payload. Phase 05.2 preserves this; only the computed headline number changes (if it changes) — the prose framing is stable.

</specifics>

<deferred>
## Deferred Ideas

- **Credentialed RER Playwright scraper** — Backlog 999.1 (`.planning/phases/999.1-credentialed-rer-access-automation-playwright-github-secrets/`). Explicit unlock path #1 for S4 + S5 re-activation.
- **REF data-sharing collaboration** — SEED-002. Explicit unlock path #2 for S4 + S5 re-activation. Outreach sent 2026-04-24; no reply yet. **Not cited on any public docs page** per D-15 + feedback memory.
- **NIROC under aggregate grain.** D-09 NIRO inclusion (from Phase 5) persists structurally — `country` column survives; NI rows null/zero under 05.2. Transcription of NIROC from NI Annual Reports is not in 05.2 scope. Revisit when: (a) NI station-level data becomes publicly accessible, OR (b) a GB/NI cross-jurisdiction reader feedback surfaces.
- **Full-forensic data-access manifest** — URLs-and-sha256s-per-row rather than D-14's moderate-depth subsection. Appropriate home would be `docs/methodology/ro-data-reconstruction.md` (separate page) rather than a subsection of `ro.md`. Revisit if reader feedback demands the deeper audit trail.
- **REF Constable peer-publisher framing in `docs/schemes/ro.md`.** D-16 keeps it clinical. Revisit only if SEED-002 collaboration materialises — and even then, only after mutual agreement about public framing.
- **REF reconciliation ladder calibration for aggregate-vs-REF divergence.** Planner-discretion in 05.2 (per Claude's Discretion); a formal D-decision in a future phase if the ladder turns out to need shared structure across schemes (FiT, Constraints, etc. all face similar reconciliation-under-partial-data questions).
- **`sources[]` sidecar schema formalisation as a cross-scheme discipline.** D-03 introduces the shape for Phase 05.2. Formalise as a Pydantic schema + migration discipline only if Phase 7+ schemes also need multi-source sidecars. Otherwise keep it a 05.2-local pattern.
- **Aggregate-model for CfD.** CfD was station-level from the start (Phase 4 + 05.1). Aggregate-path equivalent for CfD would be symmetrical but unnecessary while station-level data remains publicly accessible. Revisit if LCCC ever restricts access.
- **Cross-scheme DEFERRED-admonition macro.** D-09 pattern (mkdocs `!!! warning` block with "DEFERRED: data-gated" + Data-access link) might recur in Phases 7-12 if other schemes have partial data access. Introduce a reusable mkdocs macro only after the pattern fires a second time.

### Reviewed Todos (not folded)

No pending todos from STATE.md matched Phase 05.2 (`gsd-tools todo match-phase 05.2` returned 0 matches).

</deferred>

---

*Phase: 05.2-ro-data-reconstruction-aggregate-grain*
*Context gathered: 2026-04-24*
