---
phase: 03-chart-triage-execution
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - docs/themes/cost/index.md
  - docs/themes/cost/methodology.md
  - docs/themes/cost/cfd-dynamics.md
  - docs/themes/cost/cfd-vs-gas-cost.md
  - docs/themes/cost/remaining-obligations.md
  - docs/themes/recipients/index.md
  - docs/themes/recipients/methodology.md
  - docs/themes/efficiency/index.md
  - docs/themes/efficiency/methodology.md
  - docs/themes/cannibalisation/index.md
  - docs/themes/cannibalisation/methodology.md
  - docs/themes/reliability/index.md
  - docs/themes/reliability/methodology.md
  - docs/charts/subsidy/cfd-dynamics.md
  - docs/charts/subsidy/cfd-vs-gas-cost.md
  - docs/charts/subsidy/remaining-obligations.md
  - docs/charts/index.md
  - docs/index.md
  - mkdocs.yml
autonomous: true
requirements:
  - TRIAGE-03
  - TRIAGE-04
open_questions_resolved:
  OQ1: "cfd-payments-by-category placed under docs/themes/recipients/ per ARCHITECTURE §5.2 (recorded in nav at line 'Payments by technology: themes/recipients/cfd-payments-by-category.md'); Cost theme gallery cross-links to the Recipients page."
nyquist_compliant: true

must_haves:
  truths:
    - "5 theme directories exist under docs/themes/: cost, recipients, efficiency, cannibalisation, reliability"
    - "Each theme has index.md + methodology.md present"
    - "3 existing CfD pages moved to docs/themes/cost/ via git mv (history preserved)"
    - "docs/charts/index.md is deleted"
    - "docs/charts/subsidy/ is removed (empty)"
    - "docs/index.md no longer links to the deleted docs/charts/index.md"
    - "mkdocs.yml nav block matches the 22-entry structure from RESEARCH §Code Examples (post-Phase-3 nav)"
    - "`uv run mkdocs build --strict` exits 0 at end of this plan"
    - "Every theme index.md contains a `<div class=\"grid cards\" markdown>` block (D-02 gallery pattern)"
    - "Each theme index.md has all 4 D-02 sections (Theme argument, Charts, What to look at next, Methodology link)"
  artifacts:
    - path: "docs/themes/cost/index.md"
      provides: "Cost theme index (D-02 hybrid: argument + gallery + next + methodology link)"
      min_lines: 40
    - path: "docs/themes/cost/methodology.md"
      provides: "Cost-specific methodology; links to ../../methodology/gas-counterfactual.md (D-05)"
      min_lines: 20
    - path: "docs/themes/cost/cfd-dynamics.md"
      provides: "git-mv'd from docs/charts/subsidy/; PNG paths rewritten to ../../charts/html/"
      contains: "![CfD dynamics"
    - path: "docs/themes/recipients/index.md"
      provides: "Recipients theme index; flagship = lorenz; cfd-payments-by-category is 2nd card"
      min_lines: 40
    - path: "docs/themes/efficiency/index.md"
      provides: "Efficiency theme index; flagship = subsidy-per-avoided-co2-tonne"
      min_lines: 40
    - path: "docs/themes/cannibalisation/index.md"
      provides: "Cannibalisation theme index; flagship = capture-ratio"
      min_lines: 40
    - path: "docs/themes/reliability/index.md"
      provides: "Reliability theme index; flagship = generation-heatmap (visual hook); 3 PRODUCTION chart cards"
      min_lines: 40
    - path: "mkdocs.yml"
      provides: "22-entry nav block per D-04 + RESEARCH §Code Examples"
      contains: "themes/cost/index.md"
  key_links:
    - from: "docs/index.md"
      to: "docs/themes/cost/index.md etc."
      via: "Homepage 'Explore by theme' links to all 5 theme indices"
      pattern: "themes/cost/index\\.md"
    - from: "mkdocs.yml (nav)"
      to: "docs/themes/<theme>/*.md"
      via: "nav block enumerates every theme index + methodology + PRODUCTION chart page"
      pattern: "themes/(cost|recipients|efficiency|cannibalisation|reliability)/"
    - from: "docs/themes/cost/methodology.md"
      to: "docs/methodology/gas-counterfactual.md"
      via: "D-05 shared-methodology link"
      pattern: "\\.\\./\\.\\./methodology/gas-counterfactual\\.md"
    - from: "docs/themes/efficiency/methodology.md"
      to: "docs/methodology/gas-counterfactual.md"
      via: "D-05 shared-methodology link"
      pattern: "\\.\\./\\.\\./methodology/gas-counterfactual\\.md"
---

<objective>
Wave 2 structural work: create the 5-theme docs tree, `git mv` the three existing CfD chart pages into `docs/themes/cost/`, delete the old `docs/charts/index.md`, rewrite `mkdocs.yml → nav:` to the 22-entry D-04 structure, and fix the one link-rot site in `docs/index.md` that D-04 creates. Leaves 7 PROMOTE chart pages as stubs (Task 4) so the Wave-end `mkdocs build --strict` passes; Plan 03-03 fills the stubs with full D-01 content.

Purpose: Restructure the docs filesystem to match the 5-theme argument model (TRIAGE-03) so every PRODUCTION chart is reachable from exactly one theme index (TRIAGE-04). Produces a first `mkdocs build --strict` green — without this wave producing a clean build, Plan 03-03's chart-page work has no gate.

Output: 5 theme directories with `index.md` + `methodology.md` (full content per D-02 + D-05a); 3 CfD chart pages relocated with rewritten relative paths; 7 PROMOTE chart stubs at their final locations (skeleton with D-01 headers only — content in Plan 03-03); `mkdocs.yml` rewritten nav + fixed homepage links; `mkdocs build --strict` exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-chart-triage-execution/03-CONTEXT.md
@.planning/phases/03-chart-triage-execution/03-RESEARCH.md
@.planning/phases/03-chart-triage-execution/03-VALIDATION.md
@docs/charts/subsidy/cfd-dynamics.md
@docs/charts/subsidy/cfd-vs-gas-cost.md
@docs/charts/subsidy/remaining-obligations.md
@docs/charts/index.md
@docs/index.md
@docs/methodology/gas-counterfactual.md
@mkdocs.yml

<interfaces>
**Target nav block** (RESEARCH §Code Examples lines 819-852 — copy verbatim into mkdocs.yml replacing the current `nav:` block at lines 40-50). Note: `cfd-payments-by-category` placed under Recipients per OQ1 resolution:

```yaml
nav:
  - Home: index.md
  - Cost:
      - themes/cost/index.md
      - CfD dynamics: themes/cost/cfd-dynamics.md
      - CfD vs gas cost: themes/cost/cfd-vs-gas-cost.md
      - Remaining obligations: themes/cost/remaining-obligations.md
      - Methodology: themes/cost/methodology.md
  - Recipients:
      - themes/recipients/index.md
      - Lorenz curve: themes/recipients/lorenz.md
      - Payments by technology: themes/recipients/cfd-payments-by-category.md
      - Methodology: themes/recipients/methodology.md
  - Efficiency:
      - themes/efficiency/index.md
      - Subsidy per tonne CO₂ avoided: themes/efficiency/subsidy-per-avoided-co2-tonne.md
      - Methodology: themes/efficiency/methodology.md
  - Cannibalisation:
      - themes/cannibalisation/index.md
      - Capture ratio: themes/cannibalisation/capture-ratio.md
      - Methodology: themes/cannibalisation/methodology.md
  - Reliability:
      - themes/reliability/index.md
      - Seasonal capacity factor: themes/reliability/capacity-factor-seasonal.md
      - Generation heatmap: themes/reliability/generation-heatmap.md
      - Rolling minimum: themes/reliability/rolling-minimum.md
      - Methodology: themes/reliability/methodology.md
  - Methodology:
      - Gas counterfactual: methodology/gas-counterfactual.md
  - About:
      - Corrections: about/corrections.md
      - Citation: about/citation.md
```

**Path math** (RESEARCH §Git mv Correctness) — every `docs/themes/<theme>/X.md` resolves as:
- PNG: `../../charts/html/<name>_twitter.png`
- HTML: `../../charts/html/<name>.html`
- Shared methodology: `../../methodology/gas-counterfactual.md`
- Sibling chart same theme: `./<slug>.md`
- Cross-theme: `../<other-theme>/index.md`

**Grid-cards gallery syntax** (RESEARCH Pattern 3 lines 751-773) — paste verbatim into each theme index.md Task 2 writes:

```markdown
<div class="grid cards" markdown>

-   [![<alt>](../../charts/html/<name>_twitter.png)](./<slug>.md)

    __[<title>](./<slug>.md)__

    ---

    <one-sentence caption — rhetorical payload of the chart>

</div>
```

**Homepage link-rot site** (RESEARCH §Git mv Correctness lines 581-587):
- `docs/index.md:35` — `charts/subsidy/cfd-dynamics.md` → `themes/cost/cfd-dynamics.md`
- `docs/index.md:38` — `charts/subsidy/cfd-vs-gas-cost.md` → `themes/cost/cfd-vs-gas-cost.md`
- `docs/index.md:41` — `charts/subsidy/remaining-obligations.md` → `themes/cost/remaining-obligations.md`
- `docs/index.md:45` — `charts/index.md` → DELETE that sentence (target is gone) OR rewrite the whole block to point at the 5 theme indices (recommended).
- `docs/index.md:7` — img path `charts/html/subsidy_cfd_dynamics_twitter.png` → unchanged (docs/index.md is still at docs root; `charts/html/...` is still valid relative from there).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv the 3 existing CfD chart pages into docs/themes/cost/ and rewrite their relative paths</name>
  <files>docs/themes/cost/cfd-dynamics.md, docs/themes/cost/cfd-vs-gas-cost.md, docs/themes/cost/remaining-obligations.md, docs/charts/subsidy/cfd-dynamics.md, docs/charts/subsidy/cfd-vs-gas-cost.md, docs/charts/subsidy/remaining-obligations.md, docs/charts/index.md</files>
  <read_first>
    - docs/charts/subsidy/cfd-dynamics.md (current; 105 lines; PNG path `../html/subsidy_cfd_dynamics_twitter.png` needs rewrite)
    - docs/charts/subsidy/cfd-vs-gas-cost.md (current; 169 lines; same path rewrite pattern)
    - docs/charts/subsidy/remaining-obligations.md (current; 114 lines; same)
    - docs/charts/index.md (current; being deleted)
    - .planning/phases/03-chart-triage-execution/03-RESEARCH.md §Git mv Correctness (commands + incoming-link list + PNG path math)
    - .planning/phases/03-chart-triage-execution/03-RESEARCH.md §Pitfall 1 (relative paths after git mv)
  </read_first>
  <action>
Execute git-native file moves + in-place link rewrites. Keep git's rename detection intact by editing AFTER the `git mv` (not before).

**Step 1 — Create the target directory and move the three files:**

```bash
mkdir -p docs/themes/cost
git mv docs/charts/subsidy/cfd-dynamics.md docs/themes/cost/cfd-dynamics.md
git mv docs/charts/subsidy/cfd-vs-gas-cost.md docs/themes/cost/cfd-vs-gas-cost.md
git mv docs/charts/subsidy/remaining-obligations.md docs/themes/cost/remaining-obligations.md
git rm docs/charts/index.md
rmdir docs/charts/subsidy    # should be empty after the three moves
```

Do NOT `rmdir docs/charts/` — it still holds `html/` (gitignored chart output directory).

**Step 2 — Rewrite relative PNG paths in all three moved files.** In each of:

- `docs/themes/cost/cfd-dynamics.md`
- `docs/themes/cost/cfd-vs-gas-cost.md`
- `docs/themes/cost/remaining-obligations.md`

Search and replace (literal, not regex):

| From | To |
|------|----|
| `(../html/` | `(../../charts/html/` |
| `](../html/` | `](../../charts/html/` |

Apply to both the `![alt](../html/xxx_twitter.png)` image lines AND any interactive-version link lines of the form `→ [Interactive version](../html/xxx.html)`.

**Step 3 — Verify sibling cross-links and methodology links are unchanged.** The existing files link to each other via sibling paths (`cfd-vs-gas-cost.md`, `remaining-obligations.md`, `cfd-dynamics.md`) and to the shared methodology via `../../methodology/gas-counterfactual.md`. After move, both sets remain valid (all three siblings in the same new directory; `../../` from `themes/cost/` still resolves to `docs/`). Do NOT edit these.

**Step 4 — Stage deletions.** `docs/charts/index.md` is removed from the working tree via `git rm` (Step 1). `docs/charts/subsidy/` becomes empty; `rmdir` removes the empty directory. After this task, `git status` should show: 3 renames, 1 deletion, 3 modifications (relative-path rewrites inside the renamed files).
  </action>
  <verify>
    <automated>test -f docs/themes/cost/cfd-dynamics.md && test -f docs/themes/cost/cfd-vs-gas-cost.md && test -f docs/themes/cost/remaining-obligations.md && test ! -f docs/charts/subsidy/cfd-dynamics.md && test ! -d docs/charts/subsidy && test ! -f docs/charts/index.md && grep -q "../../charts/html/subsidy_cfd_dynamics_twitter.png" docs/themes/cost/cfd-dynamics.md && ! grep -q "../html/" docs/themes/cost/cfd-dynamics.md && ! grep -q "../html/" docs/themes/cost/cfd-vs-gas-cost.md && ! grep -q "../html/" docs/themes/cost/remaining-obligations.md</automated>
  </verify>
  <acceptance_criteria>
    - V-T02-05: `test -f docs/themes/cost/cfd-dynamics.md && test -f docs/themes/cost/cfd-vs-gas-cost.md && test -f docs/themes/cost/remaining-obligations.md && test ! -f docs/charts/subsidy/cfd-dynamics.md` exits 0
    - V-T03-04: `test ! -f docs/charts/index.md && test ! -d docs/charts/subsidy` exits 0
    - `git status --porcelain | grep -c "^R  docs/charts/subsidy"` returns 3 (three renames tracked)
    - `grep -q "\.\./\.\./charts/html/subsidy_cfd_dynamics_twitter\.png" docs/themes/cost/cfd-dynamics.md` exits 0
    - `! grep -q "\.\./html/" docs/themes/cost/cfd-dynamics.md` exits 0 (no stale `../html/` paths)
    - `! grep -q "\.\./html/" docs/themes/cost/cfd-vs-gas-cost.md` exits 0
    - `! grep -q "\.\./html/" docs/themes/cost/remaining-obligations.md` exits 0
    - `grep -q "\.\./\.\./methodology/gas-counterfactual\.md" docs/themes/cost/cfd-dynamics.md` exits 0 (shared methodology link preserved)
  </acceptance_criteria>
  <done>
    3 files renamed (history preserved); PNG/HTML paths rewritten from `../html/` to `../../charts/html/`; sibling and shared-methodology links unchanged; old `docs/charts/subsidy/` and `docs/charts/index.md` gone.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 5 theme index.md files with D-02 hybrid structure + grid-cards galleries</name>
  <files>docs/themes/cost/index.md, docs/themes/recipients/index.md, docs/themes/efficiency/index.md, docs/themes/cannibalisation/index.md, docs/themes/reliability/index.md</files>
  <read_first>
    - docs/charts/subsidy/cfd-vs-gas-cost.md (169-line argument-heavy reference — match this rhetorical tone)
    - .planning/phases/03-chart-triage-execution/03-CONTEXT.md §D-02 (hybrid structure spec)
    - .planning/phases/03-chart-triage-execution/03-RESEARCH.md §Pattern 2 Theme Index Template (lines 302-343 — section order + gallery example)
    - .planning/phases/03-chart-triage-execution/03-RESEARCH.md §Pattern 3 Chart Gallery (lines 345-377 — grid-cards syntax)
    - .planning/phases/03-chart-triage-execution/03-RESEARCH.md §7 PROMOTE Chart Summaries (one-line captions per chart)
    - .planning/phases/03-chart-triage-execution/03-RESEARCH.md lines 744-773 (complete gallery block code example)
    - ARCHITECTURE.md §5.1 (theme names + adversarial claims — authoritative for `## Theme argument` content)
    - .planning/phases/03-chart-triage-execution/03-RESEARCH.md §Pitfall 7 (no top-level "Themes" umbrella — each theme is its own top-level nav entry)
  </read_first>
  <action>
Create 5 new files. Each MUST follow the D-02 canonical 4-section order exactly:

1. **Theme argument** — H1 + bold one-sentence claim + 2-3 adversarial paragraphs (tone per CONTEXT §Specifics + ARCHITECTURE §5.1).
2. **Charts** — `## Charts` header + `<div class="grid cards" markdown>` block with one card per PRODUCTION chart in the theme. Flagship first.
3. **What to look at next** — `## What to look at next` header + pointer paragraph with inline links to 1-2 adjacent themes.
4. **Methodology** — `## Methodology` header + single-line pointer to `./methodology.md`.

Use the exact grid-cards syntax from RESEARCH §Pattern 3 (reproduced in the `<interfaces>` block above). Every card must have PNG thumbnail + title (both wrap the link) + `---` separator + one-sentence caption.

**File 1 — `docs/themes/cost/index.md` (4 PRODUCTION cards: cfd-dynamics FLAGSHIP, cfd-vs-gas-cost, remaining-obligations, cfd-payments-by-category CROSS-LINKED to Recipients):**

```markdown
# Cost

**Ten years into the scheme, UK consumers have paid £29bn for CfD electricity — roughly £14bn more than the gas fleet would have cost for the same MWh.**

The CfD scheme was sold as consumer protection against volatile gas prices. What it produced is a price floor — a guaranteed premium consumers pay every month, in every year of the scheme's operation, including 2022's gas crisis. The charts in this theme decompose that £14bn premium into its three constitutive numbers: the volume of subsidised generation, the per-MWh price gap between strike and the gas alternative, and the cumulative bill.

## Charts

<div class="grid cards" markdown>

-   [![CfD dynamics preview](../../charts/html/subsidy_cfd_dynamics_twitter.png)](./cfd-dynamics.md)

    __[CfD dynamics — the full mechanism in four panels](./cfd-dynamics.md)__

    ---

    Volume × price gap = bill. Each panel is a direct consequence of the one above. The diagnostic chart for the whole scheme.

-   [![CfD vs gas cost preview](../../charts/html/subsidy_cfd_vs_gas_cost_twitter.png)](./cfd-vs-gas-cost.md)

    __[What consumers paid vs what gas would have cost](./cfd-vs-gas-cost.md)__

    ---

    The same £14bn premium decomposed into the two real cash-flow channels consumers pay through: wholesale and levy.

-   [![Remaining obligations preview](../../charts/html/subsidy_remaining_obligations_twitter.png)](./remaining-obligations.md)

    __[Remaining obligations — what we've already signed for](./remaining-obligations.md)__

    ---

    The contracts already signed will cost another £33bn+. The scheme's bill is largely a forward commitment, not a current choice.

-   [![Payments by technology preview](../../charts/html/subsidy_cfd_payments_by_category_twitter.png)](../recipients/cfd-payments-by-category.md)

    __[Payments by technology category](../recipients/cfd-payments-by-category.md)__

    ---

    Offshore wind and Drax dominate the spend. Full page lives in the Recipients theme — linked here because technology breakdown is also a cost story.

</div>

## What to look at next

Then → [Recipients](../recipients/index.md) for who actually gets the money, or → [Efficiency](../efficiency/index.md) for whether this cost is worth what it buys in avoided emissions.

## Methodology

How every number on this page was computed → [Cost methodology](./methodology.md).
```

**File 2 — `docs/themes/recipients/index.md` (flagship lorenz + cfd-payments-by-category):**

```markdown
# Where the money goes

**Six projects receive 50% of all CfD subsidy. The renewables revolution is in practice a handful of Investment Contract offshore wind farms and Drax biomass.**

CfD spending is not spread broadly across a decarbonising energy system. It is concentrated — extremely — on a small set of large offshore wind stations and one converted coal-to-biomass plant. When politicians celebrate the scheme, they are celebrating a transfer to this specific list of recipients, not a broad-based transition. The Lorenz curve below is the classic inequality diagnostic, applied to CfD payments; `Payments by technology` shows the same story aggregated up to the technology level.

## Charts

<div class="grid cards" markdown>

-   [![Lorenz curve preview](../../charts/html/subsidy_lorenz_twitter.png)](./lorenz.md)

    __[Lorenz curve — 6 projects = 50%](./lorenz.md)__

    ---

    The inequality curve applied to CfD payments. Six projects receive half the £29bn spent; eleven receive 80%. Drax + Hornsea + Beatrice + Walney dominate.

-   [![Payments by technology preview](../../charts/html/subsidy_cfd_payments_by_category_twitter.png)](./cfd-payments-by-category.md)

    __[Payments by technology category](./cfd-payments-by-category.md)__

    ---

    Monthly and cumulative CfD cost stacked by Offshore Wind, Onshore Wind, Biomass, and Other. Offshore and Drax biomass are the two dominant categories.

</div>

## What to look at next

Then → [Cost](../cost/index.md) to see the total the recipients are collecting, or → [Cannibalisation](../cannibalisation/index.md) for why wind's share of the pot keeps growing.

## Methodology

How every number on this page was computed → [Recipients methodology](./methodology.md).
```

**File 3 — `docs/themes/efficiency/index.md` (flagship subsidy-per-avoided-co2-tonne):**

```markdown
# Efficiency

**Even if you accept the climate premise, CfDs are not a cost-effective way to decarbonise — and getting less so as the grid gets cleaner.**

Every pound spent on CfDs produces a quantifiable amount of avoided CO₂ — fewer tonnes each year as the marginal displaced generator is less and less often gas. The £/tCO₂ ratio is rising because the denominator is falling. Against published benchmarks (UK ETS auction prices, DEFRA social cost of carbon), recent allocation rounds sit above both ceilings. The scheme's climate value per pound is decaying structurally.

## Charts

<div class="grid cards" markdown>

-   [![Subsidy per avoided tCO₂ preview](../../charts/html/subsidy_cost_per_avoided_co2_tonne_twitter.png)](./subsidy-per-avoided-co2-tonne.md)

    __[Subsidy per tonne of CO₂ avoided](./subsidy-per-avoided-co2-tonne.md)__

    ---

    £/tCO₂ by allocation round over 2017–2025, with DEFRA Social Cost of Carbon and UK ETS reference lines. Rising trend; AR3 already well above UK ETS.

</div>

## What to look at next

Then → [Cost](../cost/index.md) for the total pound figure the tCO₂ denominator is dividing into, or → [Cannibalisation](../cannibalisation/index.md) for why extra wind delivers diminishing returns on the grid.

## Methodology

How every number on this page was computed → [Efficiency methodology](./methodology.md).
```

**File 4 — `docs/themes/cannibalisation/index.md` (flagship capture-ratio):**

```markdown
# Cannibalisation

**The more wind we build, the more it crashes its own wholesale price — and consumers top up the difference through the CfD levy. Cannibalisation is self-inflicted and baked in.**

The capture price of wind (what the wholesale market pays per MWh when it's generating) has fallen relative to the time-weighted average wholesale price for a decade. As the fleet grows and concentrated wind hours flood the market, each new MWh earns less. CfD guarantees the difference back to the generator; consumers pay that difference via the levy. The mirror-image chart below shows the two lines moving in opposite directions by design — not by accident.

## Charts

<div class="grid cards" markdown>

-   [![Capture ratio preview](../../charts/html/cannibalisation_capture_ratio_twitter.png)](./capture-ratio.md)

    __[Capture ratio — wind cannibalises its own price](./capture-ratio.md)__

    ---

    Capture-price ratio (capture price / time-weighted wholesale) falling over time by technology. AR1 wind levy per MWh rising in lockstep. Two sides of the same mechanism.

</div>

## What to look at next

Then → [Cost](../cost/index.md) to see the levy side of the same equation, or → [Reliability](../reliability/index.md) for why the concentrated-output hours happen in the first place.

## Methodology

How every number on this page was computed → [Cannibalisation methodology](./methodology.md).
```

**File 5 — `docs/themes/reliability/index.md` (3 PRODUCTION: generation-heatmap FLAGSHIP for visual hook, then rolling-minimum, then capacity-factor-seasonal):**

```markdown
# Reliability

**CfD-subsidised wind and solar have multi-week output droughts that no deployed battery can cover. DESNZ's planning capacity-factor assumptions for future CfD rounds are optimistic; the fleet underperforms them today.**

Every CfD MWh represents displaced gas — except in the weeks when wind and solar simultaneously collapse. The heatmap below makes those weeks visible with the naked eye: blue stripes across years, especially in winter. The rolling-minimum chart quantifies them: multiple 21-day spans below 20% fleet CF, each longer than any deployable battery can economically bridge. The seasonal capacity-factor chart benchmarks the observed CfD fleet against DESNZ's published AR7 planning assumptions and finds them consistently short.

## Charts

<div class="grid cards" markdown>

-   [![Generation heatmap preview](../../charts/html/intermittency_generation_heatmap_twitter.png)](./generation-heatmap.md)

    __[Wind + solar daily capacity factor — year × day-of-year heatmap](./generation-heatmap.md)__

    ---

    Two stacked heatmaps (wind top, solar bottom). Blue stripes are dunkelflaute. You can see every multi-week drought back to 2018.

-   [![Rolling minimum preview](../../charts/html/intermittency_rolling_minimum_twitter.png)](./rolling-minimum.md)

    __[21-day rolling minimum — droughts longer than any battery](./rolling-minimum.md)__

    ---

    Wind and solar 21-day rolling-mean CF with drought troughs flagged. Three weeks of structurally low output is the norm, not a tail event.

-   [![Seasonal capacity factor preview](../../charts/html/capacity_factor_seasonal_twitter.png)](./capacity-factor-seasonal.md)

    __[Seasonal capacity factor — observed vs DESNZ assumption](./capacity-factor-seasonal.md)__

    ---

    Calendar-month fleet CF by technology, benchmarked against DESNZ's published planning assumptions for new CfD rounds. Fleet sits meaningfully below the headline numbers.

</div>

## What to look at next

Then → [Cannibalisation](../cannibalisation/index.md) for what happens in the *concentrated* output hours, or → [Cost](../cost/index.md) for the total spend these reliability caveats apply to.

## Methodology

How every number on this page was computed → [Reliability methodology](./methodology.md).
```

For all 5 files: ensure `pymdownx.superfences` is not needed (grid-cards use standard Markdown list syntax inside `<div class="grid cards" markdown>`, which requires `attr_list` + `md_in_html` only — both already enabled in `mkdocs.yml:L54-59`).
  </action>
  <verify>
    <automated>for d in cost recipients efficiency cannibalisation reliability; do test -f "docs/themes/$d/index.md" && grep -q "grid cards" "docs/themes/$d/index.md" && grep -q "^## Charts" "docs/themes/$d/index.md" && grep -q "^## What to look at next" "docs/themes/$d/index.md" && grep -q "^## Methodology" "docs/themes/$d/index.md" || { echo "FAIL: $d"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - V-T03-01: `for d in cost recipients efficiency cannibalisation reliability; do test -d "docs/themes/$d"; done` exits 0
    - V-T03-03: `for d in cost recipients efficiency cannibalisation reliability; do grep -q "grid cards" "docs/themes/$d/index.md"; done` exits 0
    - Each theme `index.md` has H1 + adversarial bold-claim + `## Charts` + `## What to look at next` + `## Methodology` (grep verifies the 3 H2s)
    - Cost index.md has 4 cards (cfd-dynamics, cfd-vs-gas-cost, remaining-obligations, cfd-payments-by-category cross-link to Recipients)
    - Recipients index.md has 2 cards (lorenz flagship, cfd-payments-by-category)
    - Efficiency index.md has 1 card (subsidy-per-avoided-co2-tonne flagship)
    - Cannibalisation index.md has 1 card (capture-ratio flagship)
    - Reliability index.md has 3 cards (generation-heatmap flagship, rolling-minimum, capacity-factor-seasonal)
    - Every PNG thumbnail reference uses `../../charts/html/<name>_twitter.png` relative path
    - Every card link target is a `.md` file in the same or sibling `themes/<t>/` directory
  </acceptance_criteria>
  <done>
    5 theme index.md files written with D-02 hybrid structure, grid-cards galleries, adversarial arguments, cross-theme pointers, and methodology links.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create 5 theme methodology.md files (D-05a — theme-specific content only; Cost + Efficiency link to shared gas counterfactual)</name>
  <files>docs/themes/cost/methodology.md, docs/themes/recipients/methodology.md, docs/themes/efficiency/methodology.md, docs/themes/cannibalisation/methodology.md, docs/themes/reliability/methodology.md</files>
  <read_first>
    - docs/methodology/gas-counterfactual.md (the canonical shared methodology Cost + Efficiency link to per D-05)
    - src/uk_subsidy_tracker/counterfactual.py (for constant names to cite in Cost + Efficiency methodology)
    - .planning/phases/03-chart-triage-execution/03-CONTEXT.md §D-05, D-05a (methodology organisation rules)
    - .planning/phases/03-chart-triage-execution/03-RESEARCH.md lines 775-800 (cross-theme link pattern)
  </read_first>
  <action>
Create 5 methodology.md files. Each is theme-specific only (D-05a); Cost + Efficiency link OUT to `../../methodology/gas-counterfactual.md`; other themes stand alone.

**File 1 — `docs/themes/cost/methodology.md`:**

```markdown
# Cost — methodology

The Cost theme aggregates CfD payments over time and compares them against a gas counterfactual. Three methodology pieces live here: (a) how cost totals are aggregated, (b) allocation-round handling for forward projections, (c) the link to the shared gas-counterfactual.

## Aggregation

Cost totals are monthly sums of `CFD_Payments_GBP + reference_price × CFD_Generation_MWh` (the two cash-flow channels consumers pay through — levy top-ups + wholesale-embedded cost). Weekly/daily aggregation uses calendar-week or calendar-day sums; monthly uses calendar months (`pd.to_datetime(...).dt.to_period("M")`). Allocation round is preserved as a grouping key.

## Allocation round handling

Charts by allocation round group units via `Allocation_round` from the LCCC portfolio snapshot. Each round (Investment Contract, AR1, AR2, AR3, AR4) has its own cost profile because strike prices differ. Remaining-obligations projections extend each unit's current strike forward to its contract end date.

## Gas counterfactual

The "gas alternative" line used across Cost charts is computed identically to Efficiency's denominator. See the single shared source of truth:

→ [Gas counterfactual (shared methodology)](../../methodology/gas-counterfactual.md)

Cited constants (all carry `Provenance:` blocks — `grep -rn "^Provenance:" src/`):

- `CCGT_EFFICIENCY` (0.55) — BEIS *Electricity Generation Costs 2023*
- `GAS_CO2_INTENSITY_THERMAL` (0.184 tCO₂/MWh thermal) — DESNZ GHG factors
- `CCGT_EXISTING_FLEET_OPEX_PER_MWH` (£5/MWh) — BEIS EGC 2023 Table ES.1
- `DEFAULT_CARBON_PRICES` (2018–2026 annual) — UK ETS / EU ETS auction results

Full definitions: [`src/uk_subsidy_tracker/counterfactual.py`](https://github.com/richardjlyon/uk-subsidy-tracker/blob/main/src/uk_subsidy_tracker/counterfactual.py).
```

**File 2 — `docs/themes/recipients/methodology.md`:**

```markdown
# Recipients — methodology

The Recipients theme computes concentration diagnostics on CfD payments. Two methodology pieces: (a) payment attribution per recipient, (b) concentration metrics (Lorenz curve, thresholds).

## Recipient attribution

Payments are attributed to each `Name_of_CfD_Unit` (the LCCC station-level identifier). Multi-phase stations (e.g. Hornsea Phase 1, 2A, 2B) are treated as separate recipients because they have independent strike prices and contract terms. Units with zero or negative cumulative payment over the analysis window are excluded (they are net contributors, not recipients).

## Concentration metrics

The Lorenz curve sorts recipients by cumulative payment ascending, then plots cumulative % of recipients (x) vs cumulative % of £ (y). Threshold markers ("6 projects = 50%", "11 projects = 80%") are computed by finding the smallest recipient count whose cumulative % crosses each threshold.

Grouping keys: `(Name_of_CfD_Unit, Allocation_round)` — a unit contracted under multiple rounds is a single recipient per round (reflects how the contracts are legally written). Technology-level aggregation (for the Payments-by-technology chart) groups by `Technology_Type` from the LCCC portfolio snapshot.
```

**File 3 — `docs/themes/efficiency/methodology.md`:**

```markdown
# Efficiency — methodology

The Efficiency theme computes £ subsidy per tonne of avoided CO₂. Three methodology pieces: (a) the £ numerator, (b) the tCO₂ denominator, (c) the gas counterfactual the carbon-displacement argument rests on.

## £ subsidy per tCO₂ avoided

Numerator: CfD `CFD_Payments_GBP` (the levy-channel subsidy — what consumers top up via the CfD levy). Excludes wholesale-channel payments because those are what consumers would have paid for any electricity. Denominator: LCCC-reported `Avoided_GHG_tonnes_CO2e`, which uses DESNZ's grid-average emission factor for the displaced margin.

## Allocation-round aggregation

Ratios are computed per allocation round (IC, AR1, AR2, AR3) and averaged over the units active in each round by year. 2022 is excluded from the time-series view because negative `CFD_Payments_GBP` values that year (strike < reference during the gas crisis) distort the ratio; the underlying data is still valid but the chart becomes unreadable.

## Reference benchmarks

Two horizontal reference lines on the flagship chart:

- **DEFRA Social Cost of Carbon** (~£280/tCO₂, 2024 central value) — the societal damage cost published for UK policy appraisal.
- **UK ETS auction price** (~£50/tCO₂, 2024-25 average) — the market price of a traded UK emissions allowance.

## Gas counterfactual

The underlying displaced-gas assumption is the same one used in Cost:

→ [Gas counterfactual (shared methodology)](../../methodology/gas-counterfactual.md)
```

**File 4 — `docs/themes/cannibalisation/methodology.md`:**

```markdown
# Cannibalisation — methodology

The Cannibalisation theme computes the gap between fleet-average wholesale prices and technology-specific capture prices. Two methodology pieces: (a) capture-price ratio, (b) the CfD levy per MWh.

## Capture-price ratio

Capture price for technology *T* is the generation-weighted wholesale price during the hours *T* generated:

```
capture_price[T] = sum( market_reference_price[t] × generation[T, t] ) / sum( generation[T, t] )
```

Time-weighted wholesale is the unweighted mean of `Market_Reference_Price_GBP_Per_MWh` across the same period. The ratio is `capture_price[T] / time_weighted_wholesale`. Values < 1 indicate the technology's output correlates with low prices (cannibalisation); values > 1 would indicate output correlates with high prices. Wind trends down from ~0.95 in 2016 to below 0.75 today.

## CfD levy per MWh

For the bottom-panel levy bar chart, aggregation is AR1 wind units only (the allocation round with the longest price-history). Levy `£/MWh = CFD_Payments_GBP / CFD_Generation_MWh`. Positive values (red in the chart) indicate consumer top-up; negative values (green) indicate producer clawback during high-gas periods like 2022.

## Data source

All computation uses LCCC *Actual CfD Generation and avoided GHG emissions* as the single input. No gas counterfactual is needed for this theme — the cannibalisation argument is market-internal (capture price vs time-weighted price, both from the same LCCC reference price field).
```

**File 5 — `docs/themes/reliability/methodology.md`:**

```markdown
# Reliability — methodology

The Reliability theme computes fleet capacity-factor diagnostics. Three methodology pieces: (a) the CF formula, (b) installed-capacity attribution, (c) rolling statistics and drought detection.

## Capacity-factor formula

For a unit *u* on day *d*:

```
CF[u, d] = CFD_Generation_MWh[u, d] / (installed_capacity_MW[u, d] × 24)
```

Fleet-level CF is capacity-weighted:

```
CF_fleet[T, d] = sum( CF[u, d] × installed_capacity_MW[u, d] ) / sum( installed_capacity_MW[u, d] )
```

over the set of units *u* with technology type *T*.

## Installed capacity attribution

Installed capacity per unit over time is derived from the LCCC *CfD Contract Portfolio Status* snapshot. A unit's installed capacity starts at its first-generation date and remains at its nameplate until contract end. Pre-commissioning days are excluded from the fleet average (fleet CF would otherwise be artificially depressed by zero-generation days from yet-to-build units).

## Rolling statistics and drought detection

Rolling-minimum chart uses `pd.Series.rolling(window=21, center=True).mean()` on the daily fleet-CF series. Drought detection uses `scipy.signal.find_peaks` on the negative series with `prominence=15` (pp of CF), filtering for troughs that fall below each technology's reference CF line (20% for wind, 5% for solar — chosen as the published DESNZ-assumption floor less a margin).

## A note on pin tests

No dedicated pytest-level pin test currently freezes the CF formula above; schema validation via `tests/test_schemas.py` covers the underlying LCCC portfolio data. A CF-formula pin test is a candidate for Phase 4 alongside the Parquet variants of TEST-02/03/05. Treat the formula definitions on this page as the human-readable contract until that test lands.
```

All 5 files end without trailing blank lines; H1 is the only `#` header; subsequent headers use `##`.
  </action>
  <verify>
    <automated>for d in cost recipients efficiency cannibalisation reliability; do test -f "docs/themes/$d/methodology.md" || exit 1; done && grep -q "methodology/gas-counterfactual.md" docs/themes/cost/methodology.md && grep -q "methodology/gas-counterfactual.md" docs/themes/efficiency/methodology.md && ! grep -q "gas-counterfactual" docs/themes/recipients/methodology.md && ! grep -q "gas-counterfactual" docs/themes/cannibalisation/methodology.md && ! grep -q "gas-counterfactual" docs/themes/reliability/methodology.md</automated>
  </verify>
  <acceptance_criteria>
    - V-T03-02: `for d in cost recipients efficiency cannibalisation reliability; do test -f "docs/themes/$d/methodology.md"; done` exits 0
    - D-05 Cost link: `grep -q "methodology/gas-counterfactual.md" docs/themes/cost/methodology.md` exits 0
    - D-05 Efficiency link: `grep -q "methodology/gas-counterfactual.md" docs/themes/efficiency/methodology.md` exits 0
    - D-05a no-duplication: Recipients, Cannibalisation, Reliability methodology files do NOT link to `gas-counterfactual.md` (their data paths don't use the gas counterfactual)
    - Cost methodology cites at least 3 `Provenance:`-block constants by name (`CCGT_EFFICIENCY`, `GAS_CO2_INTENSITY_THERMAL`, `CCGT_EXISTING_FLEET_OPEX_PER_MWH`)
    - Reliability methodology contains the "A note on pin tests" paragraph (OQ4 seed)
    - Each methodology.md is ≥20 lines
  </acceptance_criteria>
  <done>
    5 methodology.md files written; D-05/D-05a enforced (no cross-theme duplication); Cost + Efficiency link to shared gas-counterfactual; Reliability includes OQ4 pin-test seed.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create 7 PROMOTE chart-page stubs with D-01 section skeleton (Plan 03-03 fills content)</name>
  <files>docs/themes/recipients/lorenz.md, docs/themes/recipients/cfd-payments-by-category.md, docs/themes/efficiency/subsidy-per-avoided-co2-tonne.md, docs/themes/cannibalisation/capture-ratio.md, docs/themes/reliability/capacity-factor-seasonal.md, docs/themes/reliability/generation-heatmap.md, docs/themes/reliability/rolling-minimum.md</files>
  <read_first>
    - .planning/phases/03-chart-triage-execution/03-CONTEXT.md §D-01 (6-section template spec — exact headings)
    - .planning/phases/03-chart-triage-execution/03-RESEARCH.md §Pattern 1 lines 247-299 (condensed skeleton)
    - .planning/phases/03-chart-triage-execution/03-RESEARCH.md §7 PROMOTE Chart Summaries (per-chart content — don't author content now, just capture PNG path and source module in the stub)
    - docs/charts/subsidy/cfd-dynamics.md (reference structural precedent)
  </read_first>
  <action>
Clarification from OQ1 (planning-context-resolved): `cfd-payments-by-category` lives at `docs/themes/recipients/cfd-payments-by-category.md`. Per Plan 02 nav block. The Cost theme gallery CROSS-LINKS to that Recipients page — no file at `docs/themes/cost/cfd-payments-by-category.md`. If any such stray file exists, delete it. Create exactly 7 NEW files (NOT 8):

1. `docs/themes/recipients/lorenz.md`
2. `docs/themes/recipients/cfd-payments-by-category.md`
3. `docs/themes/efficiency/subsidy-per-avoided-co2-tonne.md`
4. `docs/themes/cannibalisation/capture-ratio.md`
5. `docs/themes/reliability/capacity-factor-seasonal.md`
6. `docs/themes/reliability/generation-heatmap.md`
7. `docs/themes/reliability/rolling-minimum.md`

Each stub has exactly this shape (adjust `<H1>`, `<PNG>`, `<name>` per chart):

```markdown
# <H1 Title — one-sentence adversarial claim>

**<Lead claim bold — rhetorical payload in one sentence>**

![<alt>](../../charts/html/<PNG>_twitter.png)

→ [Interactive version](../../charts/html/<PNG>.html)

## What the chart shows

<Stub — Plan 03-03 Task fills with panel description.>

## The argument

<Stub — Plan 03-03 Task fills with adversarial framing.>

## Methodology

<Stub — Plan 03-03 Task fills with formulas + methodology links.>

## Caveats

- <Stub — Plan 03-03 Task fills with ≥3 caveats.>

## Data & code

- **Chart source** — [`src/uk_subsidy_tracker/plotting/<module_path>.py`](https://github.com/richardjlyon/uk-subsidy-tracker/blob/main/src/uk_subsidy_tracker/plotting/<module_path>.py)

## See also

<Stub — Plan 03-03 Task fills with sibling-theme pointers.>
```

Per-chart PNG + module mapping (RESEARCH §7 PROMOTE Chart Summaries authoritative):

| File | PNG stem | Module path |
|------|----------|-------------|
| recipients/lorenz.md | subsidy_lorenz | subsidy/lorenz |
| recipients/cfd-payments-by-category.md | subsidy_cfd_payments_by_category | subsidy/cfd_payments_by_category |
| efficiency/subsidy-per-avoided-co2-tonne.md | subsidy_cost_per_avoided_co2_tonne | subsidy/subsidy_per_avoided_co2_tonne |
| cannibalisation/capture-ratio.md | cannibalisation_capture_ratio | cannibalisation/capture_ratio |
| reliability/capacity-factor-seasonal.md | capacity_factor_seasonal | capacity_factor/seasonal |
| reliability/generation-heatmap.md | intermittency_generation_heatmap | intermittency/generation_heatmap |
| reliability/rolling-minimum.md | intermittency_rolling_minimum | intermittency/rolling_minimum |

Note PNG stem `subsidy_cost_per_avoided_co2_tonne` (not `subsidy_per_avoided_co2_tonne`) — the builder.save() call in `subsidy/subsidy_per_avoided_co2_tonne.py` outputs with `cost_per` prefix (verified by RESEARCH §7#3 research note). Module path uses `subsidy_per`.

**Stubs exist to make Plan 03-02 `mkdocs build --strict` pass.** Plan 03-03 replaces the `<Stub — ...>` lines with full D-01 content (100-170 lines per page).
  </action>
  <verify>
    <automated>for f in docs/themes/recipients/lorenz.md docs/themes/recipients/cfd-payments-by-category.md docs/themes/efficiency/subsidy-per-avoided-co2-tonne.md docs/themes/cannibalisation/capture-ratio.md docs/themes/reliability/capacity-factor-seasonal.md docs/themes/reliability/generation-heatmap.md docs/themes/reliability/rolling-minimum.md; do test -f "$f" || exit 1; grep -q "^## What the chart shows" "$f" || exit 1; grep -q "^## The argument" "$f" || exit 1; grep -q "^## Methodology" "$f" || exit 1; grep -q "^## Caveats" "$f" || exit 1; grep -q "^## Data & code" "$f" || exit 1; grep -q "^## See also" "$f" || exit 1; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - V-T02-01: All 7 PROMOTE chart page paths exist (one at recipients/lorenz.md, one at recipients/cfd-payments-by-category.md, one at efficiency/, one at cannibalisation/, three at reliability/)
    - V-T02-02: Each of the 7 stubs contains all 6 D-01 section headers (`## What the chart shows`, `## The argument`, `## Methodology`, `## Caveats`, `## Data & code`, `## See also`)
    - V-T02-03: Each of the 7 stubs embeds the correct PNG via `![<alt>](../../charts/html/<stem>_twitter.png)`
    - V-T02-04: Each of the 7 stubs cites the Python source via `https://github.com/richardjlyon/uk-subsidy-tracker/blob/main/src/uk_subsidy_tracker/plotting/<module_path>.py` permalink
    - No file at `docs/themes/cost/cfd-payments-by-category.md` (OQ1 resolution — the chart lives under Recipients)
    - Each stub is ≥20 lines (even with placeholder content — H1 + 6 H2s + minimum skeleton)
  </acceptance_criteria>
  <done>
    7 stub pages exist at their final theme locations with D-01 6-section skeleton + correct PNG embed + source permalink; Plan 03-03 will replace stubs with full D-01 content.
  </done>
</task>

<task type="auto">
  <name>Task 5: Rewrite mkdocs.yml nav block + fix docs/index.md link-rot; verify mkdocs build --strict</name>
  <files>mkdocs.yml, docs/index.md</files>
  <read_first>
    - mkdocs.yml (post-Plan-03-01 state — theme.features and validation block already correct; only `nav:` needs rewrite)
    - docs/index.md (current; L35/38/41 link to old `charts/subsidy/*` paths; L45 links to deleted `charts/index.md`)
    - .planning/phases/03-chart-triage-execution/03-RESEARCH.md §Code Examples lines 817-852 (full post-Phase-3 nav block)
    - .planning/phases/03-chart-triage-execution/03-RESEARCH.md §Pitfall 8 (docs/index.md link to charts/index.md must be fixed or mkdocs --strict fails)
    - .planning/phases/03-chart-triage-execution/03-RESEARCH.md §Git mv Correctness §Incoming links table (all link-rot sites in docs/index.md)
  </read_first>
  <action>
Two hunks plus a strict-build verification step (Hunk 3). Order matters: do the nav rewrite FIRST, then the `docs/index.md` fixes, THEN run the strict build as the gate. `mkdocs build --strict` won't pass until both content hunks are done.

**Hunk 1 — replace `mkdocs.yml` nav block** (current lines 40-50, the 5-entry `Charts` nav). Replace with the 22-entry structure verbatim:

```yaml
nav:
  - Home: index.md
  - Cost:
      - themes/cost/index.md
      - CfD dynamics: themes/cost/cfd-dynamics.md
      - CfD vs gas cost: themes/cost/cfd-vs-gas-cost.md
      - Remaining obligations: themes/cost/remaining-obligations.md
      - Methodology: themes/cost/methodology.md
  - Recipients:
      - themes/recipients/index.md
      - Lorenz curve: themes/recipients/lorenz.md
      - Payments by technology: themes/recipients/cfd-payments-by-category.md
      - Methodology: themes/recipients/methodology.md
  - Efficiency:
      - themes/efficiency/index.md
      - Subsidy per tonne CO₂ avoided: themes/efficiency/subsidy-per-avoided-co2-tonne.md
      - Methodology: themes/efficiency/methodology.md
  - Cannibalisation:
      - themes/cannibalisation/index.md
      - Capture ratio: themes/cannibalisation/capture-ratio.md
      - Methodology: themes/cannibalisation/methodology.md
  - Reliability:
      - themes/reliability/index.md
      - Seasonal capacity factor: themes/reliability/capacity-factor-seasonal.md
      - Generation heatmap: themes/reliability/generation-heatmap.md
      - Rolling minimum: themes/reliability/rolling-minimum.md
      - Methodology: themes/reliability/methodology.md
  - Methodology:
      - Gas counterfactual: methodology/gas-counterfactual.md
  - About:
      - Corrections: about/corrections.md
      - Citation: about/citation.md
```

Each theme section starts with its bare `themes/<theme>/index.md` entry (no title) — Material uses the first entry as the default page when the reader clicks the theme tab.

**Hunk 2 — fix `docs/index.md` link-rot.** Rewrite the "## The three charts" section (current lines ~33-45) from:

```markdown
## The three charts

1. **[CfD dynamics](charts/subsidy/cfd-dynamics.md)** — the mechanism
   exposed in four panels: volume, unit prices, premium, cumulative
   bill.
2. **[CfD vs Gas Cost](charts/subsidy/cfd-vs-gas-cost.md)** — the
   same money decomposed into the two cash-flow channels consumers
   actually pay through.
3. **[Remaining Obligations](charts/subsidy/remaining-obligations.md)**
   — what the contracts already signed will cost from here on
   (£33bn+).

See the [Charts overview](charts/index.md) for how they relate.
```

to:

```markdown
## Explore by theme

The charts are organised into five argument themes:

1. **[Cost](themes/cost/index.md)** — the mechanism: volume × price gap = bill. Starts with the four-panel diagnostic.
2. **[Recipients](themes/recipients/index.md)** — where the money goes. Six projects receive 50% of the pot.
3. **[Efficiency](themes/efficiency/index.md)** — £/tCO₂ avoided against DEFRA SCC and UK ETS benchmarks.
4. **[Cannibalisation](themes/cannibalisation/index.md)** — why wind crashes its own price, and who tops the difference up.
5. **[Reliability](themes/reliability/index.md)** — the drought periods no battery can cover.
```

The homepage hero image at line 7 (`![...](charts/html/subsidy_cfd_dynamics_twitter.png)`) stays put — `docs/index.md` is at docs root, so `charts/html/...` is still a valid relative path. Do NOT rewrite it.

**Hunk 3 — verify `mkdocs build --strict` passes.** Run it. This is the gate for the task:

```bash
uv run mkdocs build --strict
```

Expected clean output. If it fails, the error will point to a specific missing link, orphan page, or missing `nav:` entry. Fix it (typically a typo or missing stub file from Task 4) and rerun until exit 0.
  </action>
  <verify>
    <automated>grep -q "themes/cost/index.md" mkdocs.yml && grep -q "themes/recipients/cfd-payments-by-category.md" mkdocs.yml && grep -q "themes/reliability/generation-heatmap.md" mkdocs.yml && ! grep -q "charts/subsidy/" mkdocs.yml && ! grep -q "charts/index.md" mkdocs.yml && grep -q "themes/cost/index.md" docs/index.md && ! grep -q "charts/subsidy/" docs/index.md && ! grep -q "charts/index.md" docs/index.md && uv run mkdocs build --strict 2>&1 | tee /tmp/mkdocs-build.log && grep -q "INFO.*Documentation built" /tmp/mkdocs-build.log</automated>
  </verify>
  <acceptance_criteria>
    - V-T02-02 (nav enumerates ≥17 theme-scoped entries): `grep -cE "themes/(cost|recipients|efficiency|cannibalisation|reliability)/.*\.md" mkdocs.yml` returns ≥17
    - `mkdocs.yml` nav block contains `themes/recipients/cfd-payments-by-category.md` (OQ1)
    - `mkdocs.yml` no longer contains any `charts/subsidy/` or `charts/index.md` references
    - `docs/index.md` no longer links to `charts/subsidy/*` or `charts/index.md`
    - `docs/index.md` contains 5 theme links (`themes/cost/index.md`, `themes/recipients/index.md`, etc.)
    - V-T04-02 + V-PHASE-02 (dress rehearsal): `uv run mkdocs build --strict` exits 0
    - No console output line starts with `WARNING` or `ERROR` during the build
  </acceptance_criteria>
  <done>
    mkdocs.yml nav matches D-04 22-entry target; docs/index.md link-rot fixed; mkdocs build --strict is green end-to-end for the first time in Phase 3.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs filesystem ↔ mkdocs.yml nav | Every nav entry must resolve to a file; every file outside the nav triggers `omitted_files: warn` (promoted to error by `--strict`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Integrity (broken relative paths after git mv) | moved chart pages in docs/themes/cost/ | mitigate | Task 1 rewrites `../html/` → `../../charts/html/` in the same commit as the rename; Task 5 `mkdocs build --strict` under `validation.links.not_found: warn` would fail if any broken path remained. |
| T-03-06 | Availability (orphan page breaks --strict) | docs/themes/<t>/*.md | mitigate | Plan 03-01 added `nav.omitted_files: warn`; Task 5 nav rewrite enumerates all 22 pages; stub creation in Task 4 ensures no dangling nav entries. |
| T-03-07 | Tampering (external regulator URL redirect) | methodology.md external links | transfer | Phase-4 Internet Archive auto-archival per ARCHITECTURE §8.4; not in scope here. |
</threat_model>

<verification>
- `for d in cost recipients efficiency cannibalisation reliability; do test -d "docs/themes/$d" && test -f "docs/themes/$d/index.md" && test -f "docs/themes/$d/methodology.md"; done` → exit 0
- `test ! -f docs/charts/index.md && test ! -d docs/charts/subsidy` → exit 0
- 7 PROMOTE stub pages exist at final theme paths
- `mkdocs.yml` nav block has no `charts/subsidy/` or `charts/index.md` references
- `uv run mkdocs build --strict` → exit 0 (first phase gate clearance)
- `uv run pytest` → still 16 passed + 4 skipped (no regression)
- `! grep -q "charts/subsidy" docs/index.md && ! grep -q "charts/index.md" docs/index.md` → exit 0
</verification>

<success_criteria>
- 5 theme directories populated with `index.md` + `methodology.md`
- 3 existing CfD chart pages git-mv'd to `docs/themes/cost/` with rewritten relative paths; history preserved
- `docs/charts/subsidy/` and `docs/charts/index.md` removed from working tree
- 7 PROMOTE chart stubs exist with D-01 skeleton (content filled by Plan 03-03)
- `mkdocs.yml` nav matches the 22-entry D-04 structure
- `docs/index.md` no longer references deleted paths; points at the 5 theme indices
- `mkdocs build --strict` exits 0
- `uv run pytest` stays green
</success_criteria>

<output>
After completion, create `.planning/phases/03-chart-triage-execution/03-02-SUMMARY.md` following the summary template.
</output>
