---
phase: 03-chart-triage-execution
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - docs/themes/recipients/lorenz.md
  - docs/themes/recipients/cfd-payments-by-category.md
  - docs/themes/efficiency/subsidy-per-avoided-co2-tonne.md
  - docs/themes/cannibalisation/capture-ratio.md
  - docs/themes/reliability/capacity-factor-seasonal.md
  - docs/themes/reliability/generation-heatmap.md
  - docs/themes/reliability/rolling-minimum.md
autonomous: true
requirements:
  - TRIAGE-02
  - GOV-01
open_questions_resolved:
  OQ1: "cfd-payments-by-category lives under Recipients (authored here); Cost theme gallery cross-links already in place (Plan 03-02)."
  OQ4: "capacity-factor-seasonal cites tests/test_schemas.py (LCCC Contract Portfolio Status schema) + tests/test_aggregates.py; Caveats section contains the Phase-4 pin-test seed (OQ4 resolution: no CF formula pin today, schema validation covers the input)."
nyquist_compliant: true

must_haves:
  truths:
    - "All 7 PROMOTE chart pages are filled (no `<Stub — ...>` placeholders remain)"
    - "Each page is 100-170 lines (D-01 length target; lorenz may be ~95 if genuinely simpler)"
    - "Each page has all 6 D-01 section headers in the canonical order"
    - "Each page embeds its correct PNG + links to its interactive HTML"
    - "Each page cites its Python source via GitHub permalink (GOV-01)"
    - "Each page cites ≥1 test file via GitHub permalink (GOV-01)"
    - "Each page links to a methodology target (either `../../methodology/gas-counterfactual.md` or `./methodology.md` per GOV-01 Coverage Map)"
    - "`mkdocs build --strict` still exits 0 after all 7 pages are filled"
  artifacts:
    - path: "docs/themes/recipients/lorenz.md"
      provides: "Lorenz flagship for Recipients theme — '6 projects = 50%'"
      min_lines: 95
      contains: "6 projects"
    - path: "docs/themes/recipients/cfd-payments-by-category.md"
      provides: "CfD by-technology breakdown; Offshore + Biomass dominance"
      min_lines: 100
    - path: "docs/themes/efficiency/subsidy-per-avoided-co2-tonne.md"
      provides: "Flagship Efficiency page — £/tCO₂ avoided vs SCC + UK ETS"
      min_lines: 120
    - path: "docs/themes/cannibalisation/capture-ratio.md"
      provides: "Flagship Cannibalisation page — capture price falling, levy rising"
      min_lines: 110
    - path: "docs/themes/reliability/capacity-factor-seasonal.md"
      provides: "Seasonal CF vs DESNZ assumption — optimism audit"
      min_lines: 110
    - path: "docs/themes/reliability/generation-heatmap.md"
      provides: "Visual-hook heatmap — dunkelflaute visible to the naked eye"
      min_lines: 100
    - path: "docs/themes/reliability/rolling-minimum.md"
      provides: "21-day rolling minimum — droughts longer than any battery"
      min_lines: 110
  key_links:
    - from: "each chart page"
      to: "GitHub source permalink"
      via: "`https://github.com/richardjlyon/uk-subsidy-tracker/blob/main/src/uk_subsidy_tracker/plotting/<module>.py`"
      pattern: "github\\.com/richardjlyon/uk-subsidy-tracker/blob/main/src/"
    - from: "each chart page"
      to: "GitHub test permalink"
      via: "`https://github.com/richardjlyon/uk-subsidy-tracker/blob/main/tests/test_*.py`"
      pattern: "github\\.com/richardjlyon/uk-subsidy-tracker/blob/main/tests/test_"
    - from: "Efficiency + Cost charts"
      to: "../../methodology/gas-counterfactual.md"
      via: "Shared methodology link per D-05"
      pattern: "\\.\\./\\.\\./methodology/gas-counterfactual\\.md"
---

<objective>
Wave 3 content-filling: replace the 7 PROMOTE chart-page stubs (created by Plan 03-02 Task 4) with full D-01 content (100-170 lines each). Each page satisfies GOV-01 four-way coverage: narrative + methodology link + test permalink + source permalink.

Purpose: Close TRIAGE-02 (7 PROMOTE charts documented) and GOV-01 (four-way coverage). Tasks in this plan are INDEPENDENT per chart — an executor filling `lorenz.md` does not need `capture-ratio.md` to be done. This means tasks can be parallelised within the wave (autonomous: true, `files_modified` scopes never overlap at the chart-file level).

Output: 7 fully-authored chart pages; `mkdocs build --strict` still green; GOV-01 four-way coverage provable via grep on every page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-chart-triage-execution/03-CONTEXT.md
@.planning/phases/03-chart-triage-execution/03-RESEARCH.md
@.planning/phases/03-chart-triage-execution/03-VALIDATION.md
@docs/charts/subsidy/cfd-dynamics.md
@docs/charts/subsidy/cfd-vs-gas-cost.md
@docs/charts/subsidy/remaining-obligations.md
@docs/methodology/gas-counterfactual.md
@src/uk_subsidy_tracker/counterfactual.py

<interfaces>
**GOV-01 Coverage Map** (RESEARCH §GOV-01 Coverage Map, authoritative for test + source citations per chart):

| # | Chart page | Python module | Test file(s) | Methodology link |
|---|------------|---------------|--------------|------------------|
| 1 | recipients/lorenz.md | plotting/subsidy/lorenz.py | test_schemas.py, test_aggregates.py | ./methodology.md |
| 2 | recipients/cfd-payments-by-category.md | plotting/subsidy/cfd_payments_by_category.py | test_schemas.py, test_aggregates.py | ./methodology.md |
| 3 | efficiency/subsidy-per-avoided-co2-tonne.md | plotting/subsidy/subsidy_per_avoided_co2_tonne.py | test_schemas.py, test_aggregates.py | ../../methodology/gas-counterfactual.md + ./methodology.md |
| 4 | cannibalisation/capture-ratio.md | plotting/cannibalisation/capture_ratio.py | test_schemas.py | ./methodology.md |
| 5 | reliability/capacity-factor-seasonal.md | plotting/capacity_factor/seasonal.py | test_schemas.py, test_aggregates.py | ./methodology.md |
| 6 | reliability/generation-heatmap.md | plotting/intermittency/generation_heatmap.py | test_schemas.py | ./methodology.md |
| 7 | reliability/rolling-minimum.md | plotting/intermittency/rolling_minimum.py | test_schemas.py | ./methodology.md |

**GitHub permalink base:** `https://github.com/richardjlyon/uk-subsidy-tracker/blob/main/`

**D-01 canonical section order (exact H2 headings):**

```
## What the chart shows
## The argument
## Methodology
## Caveats
## Data & code
## See also
```

**Argument content per chart** (RESEARCH §7 PROMOTE Chart Summaries — use these as the adversarial payload, not verbatim but as the locked rhetorical claim):

- **lorenz** → "6 projects receive 50% of all CfD subsidy; 11 receive 80%. The renewables revolution is a handful of offshore wind farms and Drax."
- **cfd-payments-by-category** → "Offshore wind and Drax biomass dominate; most CfD spending flows to a handful of technology categories."
- **subsidy-per-avoided-co2-tonne** → "Even if you accept the climate premise, CfDs are not a cost-effective way to decarbonise — and getting less so as the grid gets cleaner."
- **capture-ratio** → "The more wind we build, the more it crashes its own price — and consumers top up the difference via CfD. Cannibalisation is self-inflicted and baked in."
- **capacity-factor-seasonal** → "DESNZ's planning assumptions for new CfD projects (2027-2031) are optimistic; observed fleet capacity factors sit meaningfully below."
- **generation-heatmap** → "Sustained low-output periods are structural, not exceptional. You can see them in the data with the naked eye."
- **rolling-minimum** → "CfD fleet has had multiple 21-day droughts below reference CF. Three weeks is longer than any deployed battery can cover."

**Reference precedent** (match this tone + structure):

- `docs/charts/subsidy/cfd-dynamics.md` (105 lines — 4-panel diagnostic; reference for capacity-factor-seasonal multi-panel shape)
- `docs/charts/subsidy/cfd-vs-gas-cost.md` (169 lines — argument-heavy; reference for lorenz + subsidy-per-avoided-co2-tonne tone)
- `docs/charts/subsidy/remaining-obligations.md` (114 lines — "What's not in this chart" pattern worth generalising to Caveats)

**Line-count targets per D-01:** 100-170 lines (do not pad; lorenz may legitimately end at ~95 if the argument is a single headline).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill lorenz.md (Recipients flagship — '6 projects = 50%')</name>
  <files>docs/themes/recipients/lorenz.md</files>
  <read_first>
    - docs/themes/recipients/lorenz.md (stub from Plan 03-02 — preserve PNG + permalink; replace `<Stub — ...>` placeholders)
    - docs/charts/subsidy/cfd-vs-gas-cost.md (argument-heavy precedent — match tone)
    - src/uk_subsidy_tracker/plotting/subsidy/lorenz.py (understand the computation; cite the threshold logic in Methodology)
    - .planning/phases/03-chart-triage-execution/03-RESEARCH.md §7 PROMOTE Chart Summaries #2 (data inputs + argument)
    - ARCHITECTURE.md §5.1 Theme B (Recipients rhetorical framing)
  </read_first>
  <action>
Replace every `<Stub — ...>` line in `docs/themes/recipients/lorenz.md` with authored content following D-01 section order. Target length 95-120 lines; may end under 100 because the argument is a single headline.

Retain from stub: H1, lead claim in bold, PNG embed, interactive-version link. Everything else gets replaced.

**Authored content spec** (you author — this is the shape, not a paste-in):

- **## What the chart shows:** Explain the Lorenz axes — cumulative % of projects (x) vs cumulative % of £ (y); describe the two threshold annotations (6 projects = 50%, 11 projects = 80%); mention the top-3-recipients annotations (typically Drax, Hornsea, Beatrice/Walney — confirm from the `Name_of_CfD_Unit` field). ~15-20 lines.

- **## The argument:** 2-3 paragraphs of adversarial framing. Lead claim per interfaces above. Explicitly name the dominant recipients. Contrast "renewables revolution" rhetoric against the concentration. Cite the Gini coefficient if visible in the chart (check the PNG for annotations); if not shown, note that the threshold numbers are the adversarial payload. ~25-30 lines.

- **## Methodology:** Describe the aggregation: group by `(Name_of_CfD_Unit, Allocation_round)`, filter positive-payment units, sort ascending by cumulative payment, compute cumulative %. The threshold markers are the smallest recipient count whose cumulative % crosses 50% / 80%. Cite Recipients methodology for the attribution rule. No gas counterfactual is used — Lorenz is pure attribution. ~15-20 lines.

- **## Caveats:** ≥3 bullets: (a) ATF payments and zero-payment years excluded to keep the curve monotonic; (b) Multi-phase stations (e.g. Hornsea Phase 1, 2A, 2B) treated as separate recipients per contract independence; (c) 2022 distortion — negative `CFD_Payments_GBP` during gas crisis treated as £0 for sort-stability, not as producer clawback. ~10-15 lines.

- **## Data & code** (GOV-01 four-way coverage — all four artefacts MUST appear here):
  - LCCC *Actual CfD Generation* URL (primary source)
  - Chart source permalink: `https://github.com/richardjlyon/uk-subsidy-tracker/blob/main/src/uk_subsidy_tracker/plotting/subsidy/lorenz.py`
  - Test permalinks: `.../blob/main/tests/test_schemas.py` and `.../blob/main/tests/test_aggregates.py`
  - "To reproduce" block: `uv run python -m uk_subsidy_tracker.plotting.subsidy.lorenz`
  - ~15-20 lines.

- **## See also:** Link to `./cfd-payments-by-category.md` (same theme sibling), `../cost/index.md` (where the total pound figure lives), and `./methodology.md` (attribution rules). ~5-8 lines.

No trailing blank lines. Keep PNG/HTML relative paths as-is from the stub (`../../charts/html/subsidy_lorenz_twitter.png`).
  </action>
  <verify>
    <automated>test -f docs/themes/recipients/lorenz.md && ! grep -q "Stub —" docs/themes/recipients/lorenz.md && grep -q "^## What the chart shows" docs/themes/recipients/lorenz.md && grep -q "^## The argument" docs/themes/recipients/lorenz.md && grep -q "^## Methodology" docs/themes/recipients/lorenz.md && grep -q "^## Caveats" docs/themes/recipients/lorenz.md && grep -q "^## Data & code" docs/themes/recipients/lorenz.md && grep -q "^## See also" docs/themes/recipients/lorenz.md && grep -q "blob/main/src/uk_subsidy_tracker/plotting/subsidy/lorenz.py" docs/themes/recipients/lorenz.md && grep -q "blob/main/tests/test_schemas.py" docs/themes/recipients/lorenz.md && grep -q "blob/main/tests/test_aggregates.py" docs/themes/recipients/lorenz.md && test $(wc -l < docs/themes/recipients/lorenz.md) -ge 90</automated>
  </verify>
  <acceptance_criteria>
    - V-T02-02 (all 6 D-01 section headers present)
    - V-T02-03 (PNG embed): `grep -q "!\[.*\](\.\./\.\./charts/html/subsidy_lorenz_twitter\.png)" docs/themes/recipients/lorenz.md` exits 0
    - V-T02-04 (source permalink): `grep -q "github\.com/richardjlyon/uk-subsidy-tracker/blob/main/src/uk_subsidy_tracker/plotting/subsidy/lorenz\.py" docs/themes/recipients/lorenz.md` exits 0
    - V-G01-01 four-way coverage: PNG embed ✓ + methodology link ✓ + `tests/test_schemas.py` permalink ✓ + source permalink ✓
    - No `Stub —` placeholder remains
    - Line count 90-130 (lorenz is allowed to be slightly under the 100 floor per D-01 "do not pad if genuinely simpler")
    - Argument section contains the locked claim "6 projects" (or equivalent threshold wording)
    - `uv run mkdocs build --strict` still exits 0
  </acceptance_criteria>
  <done>
    lorenz.md filled with full D-01 content; GOV-01 four-way coverage provable by grep; strict build still green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fill cfd-payments-by-category.md (Recipients — by-technology breakdown)</name>
  <files>docs/themes/recipients/cfd-payments-by-category.md</files>
  <read_first>
    - docs/themes/recipients/cfd-payments-by-category.md (stub from Plan 03-02)
    - src/uk_subsidy_tracker/plotting/subsidy/cfd_payments_by_category.py (understand the panel layout + aggregation)
    - docs/charts/subsidy/cfd-dynamics.md (multi-panel precedent)
    - .planning/phases/03-chart-triage-execution/03-RESEARCH.md §7 PROMOTE Chart Summaries #1
    - ARCHITECTURE.md §5.2 (Theme B placement; triage note)
  </read_first>
  <action>
Replace stub placeholders in `docs/themes/recipients/cfd-payments-by-category.md` with full D-01 content (100-150 lines).

**Authored content spec:**

- **## What the chart shows:** Two-panel stacked chart. Top — monthly CfD cost stacked by technology category (Offshore Wind, Onshore Wind, Biomass, Other). Bottom — cumulative stacked area of same. Describe the colour encoding using `TECHNOLOGY_COLORS` from `src/uk_subsidy_tracker/plotting/colors.py` (offshore: #1f77b4, onshore: #6baed6, biomass: #d62728, solar: #ff7f0e). The stacked total reconciles with the cfd-vs-gas-cost total (both use `reference_price × gen + CFD_Payments_GBP`).

- **## The argument:** Offshore wind and Drax biomass dominate. Frame this as the technology-level mirror of the Lorenz chart: even if you slice by technology rather than by project, the concentration story is the same. Cite the headline cumulative figures visible in the bottom panel.

- **## Methodology:** Monthly aggregation of `reference_price × CFD_Generation_MWh + CFD_Payments_GBP` grouped by `Technology_Type` from the LCCC portfolio join. Four visible categories + "Other". No counterfactual used (this chart is pure where-the-money-goes, not a comparison). Link to `./methodology.md` (Recipients theme methodology — attribution rule for Technology_Type).

- **## Caveats:** ≥3 bullets: (a) Technology classification follows LCCC portfolio snapshot; reclassifications mid-contract are rare but not tracked; (b) "Other" bucket includes small-scale biomass, ACT (Advanced Conversion Technology), and single-units too small to plot; (c) 2022 negative-payment months visible as red slivers inverted below the zero line on the monthly panel.

- **## Data & code** (GOV-01):
  - LCCC *Actual CfD Generation and avoided GHG emissions* URL
  - Chart source: `.../blob/main/src/uk_subsidy_tracker/plotting/subsidy/cfd_payments_by_category.py`
  - Tests: `.../blob/main/tests/test_schemas.py`, `.../blob/main/tests/test_aggregates.py`
  - Reproduce: `uv run python -m uk_subsidy_tracker.plotting.subsidy.cfd_payments_by_category`

- **## See also:** `./lorenz.md` (project-level view of the same concentration story); `../cost/index.md` (total-spend context); `./methodology.md`.

Line target 100-150.
  </action>
  <verify>
    <automated>test -f docs/themes/recipients/cfd-payments-by-category.md && ! grep -q "Stub —" docs/themes/recipients/cfd-payments-by-category.md && for h in "## What the chart shows" "## The argument" "## Methodology" "## Caveats" "## Data & code" "## See also"; do grep -qF "$h" docs/themes/recipients/cfd-payments-by-category.md || exit 1; done && grep -q "blob/main/src/uk_subsidy_tracker/plotting/subsidy/cfd_payments_by_category.py" docs/themes/recipients/cfd-payments-by-category.md && grep -q "blob/main/tests/test_schemas.py" docs/themes/recipients/cfd-payments-by-category.md && grep -q "blob/main/tests/test_aggregates.py" docs/themes/recipients/cfd-payments-by-category.md && test $(wc -l < docs/themes/recipients/cfd-payments-by-category.md) -ge 100</automated>
  </verify>
  <acceptance_criteria>
    - All 6 D-01 section headers present
    - PNG embed uses `../../charts/html/subsidy_cfd_payments_by_category_twitter.png`
    - Python source permalink present for `subsidy/cfd_payments_by_category.py`
    - GOV-01 four-way coverage: PNG + methodology link (`./methodology.md`) + 2 test permalinks + source permalink
    - Line count 100-150
    - `uv run mkdocs build --strict` still exits 0
  </acceptance_criteria>
  <done>
    cfd-payments-by-category.md filled; GOV-01 four-way coverage provable; strict build green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fill subsidy-per-avoided-co2-tonne.md (Efficiency flagship — £/tCO₂ avoided)</name>
  <files>docs/themes/efficiency/subsidy-per-avoided-co2-tonne.md</files>
  <read_first>
    - docs/themes/efficiency/subsidy-per-avoided-co2-tonne.md (stub)
    - src/uk_subsidy_tracker/plotting/subsidy/subsidy_per_avoided_co2_tonne.py (panel layout + formulas)
    - docs/methodology/gas-counterfactual.md (for the SCC + UK ETS context this chart depends on)
    - docs/charts/subsidy/cfd-vs-gas-cost.md (argument-heavy precedent)
    - .planning/phases/03-chart-triage-execution/03-RESEARCH.md §7 PROMOTE Chart Summaries #3
    - ARCHITECTURE.md §5.1 Theme C (Efficiency)
  </read_first>
  <action>
Replace stub placeholders with full D-01 content (120-170 lines — argument-heavy page per D-01 length target).

**Authored content spec:**

- **## What the chart shows:** Two-panel. Top — £/tCO₂ avoided by allocation round (Investment Contract, AR1, AR2 where data exists) over 2017-2025 (2022 excluded). Bottom — fleet-wide tCO₂ avoided per MWh over time (falling curve as grid decarbonises). Two horizontal reference lines on the top panel: DEFRA Social Cost of Carbon (~£280/tCO₂, 2024 central) and UK ETS auction price (~£50/tCO₂, 2024-25 average).

- **## The argument:** Three paragraphs. (1) The £/tCO₂ ratio is the only climate-efficiency metric that matters for CfDs — everything else is input-cost accounting. (2) The ratio is rising because the denominator is falling: as the grid decarbonises, each new renewable MWh displaces less and less gas. (3) Against both reference ceilings — the UK's own traded carbon price AND its published social cost — recent allocation rounds look expensive. AR3 already exceeds UK ETS.

- **## Methodology:** £ numerator = `CFD_Payments_GBP` only (levy channel — what consumers top up; wholesale channel is what they'd pay anyway). tCO₂ denominator = LCCC-reported `Avoided_GHG_tonnes_CO2e` (DESNZ grid-average emission factor for the displaced margin). Ratio = £ / tCO₂ per allocation round per year. 2022 excluded from top panel because negative `CFD_Payments_GBP` inverts the ratio. Reference-line values: DEFRA SCC central 2024 (~£280/tCO₂); UK ETS 2024-25 auction average (~£50/tCO₂). Link to shared gas counterfactual for carbon-price context: `../../methodology/gas-counterfactual.md`. Link to Efficiency theme methodology for the ratio arithmetic: `./methodology.md`.

- **## Caveats:** ≥4 bullets: (a) `Avoided_GHG_tonnes_CO2e` uses grid-average marginal emission factor — different from a strict merit-order model; (b) 2022 excluded from time series, visible as a gap; (c) SCC value is a UK-policy convention, not a market price — interpret as a policy benchmark not an observed price; (d) as the grid decarbonises the displaced margin shifts from gas to other dispatchables (eventually other low-carbon), further compressing avoided-CO₂ per MWh.

- **## Data & code** (GOV-01):
  - LCCC URL (primary); DEFRA SCC URL (`https://www.gov.uk/government/publications/valuing-greenhouse-gas-emissions-in-policy-appraisal`); UK ETS URL (`https://www.gov.uk/government/publications/participating-in-the-uk-ets`)
  - Chart source: `.../blob/main/src/uk_subsidy_tracker/plotting/subsidy/subsidy_per_avoided_co2_tonne.py`
  - Tests: `.../blob/main/tests/test_schemas.py`, `.../blob/main/tests/test_aggregates.py`
  - Reproduce: `uv run python -m uk_subsidy_tracker.plotting.subsidy.subsidy_per_avoided_co2_tonne`

- **## See also:** `./methodology.md` (Efficiency theme methodology); `../../methodology/gas-counterfactual.md` (shared carbon-price context); `../cost/index.md` (total-pound numerator context); `../cannibalisation/capture-ratio.md` (why new wind produces diminishing returns).

Line target 120-170.

**PNG path note:** `docs/charts/html/subsidy_cost_per_avoided_co2_tonne_twitter.png` (the builder.save() prefix differs from the module name — use `subsidy_cost_per_avoided_co2_tonne`, NOT `subsidy_per_avoided_co2_tonne`). This is a deliberate filename difference, not a typo — verified in RESEARCH §7#3.
  </action>
  <verify>
    <automated>test -f docs/themes/efficiency/subsidy-per-avoided-co2-tonne.md && ! grep -q "Stub —" docs/themes/efficiency/subsidy-per-avoided-co2-tonne.md && for h in "## What the chart shows" "## The argument" "## Methodology" "## Caveats" "## Data & code" "## See also"; do grep -qF "$h" docs/themes/efficiency/subsidy-per-avoided-co2-tonne.md || exit 1; done && grep -q "subsidy_cost_per_avoided_co2_tonne_twitter.png" docs/themes/efficiency/subsidy-per-avoided-co2-tonne.md && grep -q "blob/main/src/uk_subsidy_tracker/plotting/subsidy/subsidy_per_avoided_co2_tonne.py" docs/themes/efficiency/subsidy-per-avoided-co2-tonne.md && grep -q "blob/main/tests/test_schemas.py" docs/themes/efficiency/subsidy-per-avoided-co2-tonne.md && grep -q "blob/main/tests/test_aggregates.py" docs/themes/efficiency/subsidy-per-avoided-co2-tonne.md && grep -q "methodology/gas-counterfactual.md" docs/themes/efficiency/subsidy-per-avoided-co2-tonne.md && test $(wc -l < docs/themes/efficiency/subsidy-per-avoided-co2-tonne.md) -ge 110</automated>
  </verify>
  <acceptance_criteria>
    - All 6 D-01 section headers
    - PNG embed uses `subsidy_cost_per_avoided_co2_tonne_twitter.png` (NOT `subsidy_per_avoided_co2_tonne`)
    - Python source permalink for `subsidy/subsidy_per_avoided_co2_tonne.py`
    - Test permalinks: `test_schemas.py`, `test_aggregates.py`
    - Shared-methodology link: `../../methodology/gas-counterfactual.md` present (D-05 — Efficiency theme is one of the two that link out)
    - Theme-methodology link: `./methodology.md` present
    - DEFRA SCC and UK ETS URLs both cited
    - Line count 110-175
    - `uv run mkdocs build --strict` still exits 0
  </acceptance_criteria>
  <done>
    subsidy-per-avoided-co2-tonne.md filled with full D-01 content; GOV-01 four-way coverage + D-05 shared-methodology link both provable; strict build green.
  </done>
</task>

<task type="auto">
  <name>Task 4: Fill capture-ratio.md (Cannibalisation flagship — wind crashes its own price)</name>
  <files>docs/themes/cannibalisation/capture-ratio.md</files>
  <read_first>
    - docs/themes/cannibalisation/capture-ratio.md (stub)
    - src/uk_subsidy_tracker/plotting/cannibalisation/capture_ratio.py (2-panel structure — DO NOT fix the pre-existing Pyright reportIndexIssue at L98 per CONTEXT §Known pre-existing diagnostics)
    - docs/charts/subsidy/cfd-dynamics.md (multi-panel precedent)
    - .planning/phases/03-chart-triage-execution/03-RESEARCH.md §7 PROMOTE Chart Summaries #4
    - ARCHITECTURE.md §5.1 Theme D (Cannibalisation)
  </read_first>
  <action>
Replace stub placeholders with full D-01 content (110-150 lines).

**Authored content spec:**

- **## What the chart shows:** Two-panel. Top — capture-price ratio (capture price / time-weighted wholesale) falling over time, by technology (Offshore Wind, Onshore Wind). Bottom — AR1 wind levy per MWh rising (bar chart; red = positive levy / consumer top-up, green = negative / producer clawback). The two lines mirror each other by construction.

- **## The argument:** Lead with: "The more wind we build, the more it crashes its own price — and consumers top up the difference via CfD. Cannibalisation is self-inflicted and baked in." Two paragraphs expanding: (a) wind's output is concentrated in high-wind hours, which become increasingly-oversupplied as the fleet grows, so wholesale prices collapse exactly when wind generates, pulling the capture price below the time-weighted average; (b) the CfD mechanism guarantees the gap back to the generator, so the falling capture price in panel 1 *must* translate into the rising levy in panel 2 — they are the same number in two presentations.

- **## Methodology:** Capture price for technology T = generation-weighted wholesale during T's generating hours = `sum(market_reference_price × generation[T]) / sum(generation[T])`. Time-weighted wholesale = unweighted mean of `Market_Reference_Price_GBP_Per_MWh` across the same window. Ratio = capture / time-weighted (< 1 means cannibalisation). Bottom-panel levy restricted to AR1 wind units (longest price history); `£/MWh = CFD_Payments_GBP / CFD_Generation_MWh`; positive = top-up, negative = clawback. All data from LCCC *Actual CfD Generation* — no gas counterfactual required (market-internal diagnostic). Link to `./methodology.md` for formal definitions.

- **## Caveats:** ≥3 bullets: (a) Wind-only technology filter — solar has too few CfD-exercising years for this diagnostic; (b) AR1 wind = long dataset, but small-N; once AR2/AR3 mature, the bottom panel can expand; (c) chart depends on LCCC's `Market_Reference_Price_GBP_Per_MWh` — if LCCC ever changes their reference-price source, the ratio shifts abruptly.

- **## Data & code** (GOV-01):
  - LCCC URL
  - Chart source: `.../blob/main/src/uk_subsidy_tracker/plotting/cannibalisation/capture_ratio.py`
  - Test: `.../blob/main/tests/test_schemas.py`
  - Reproduce: `uv run python -m uk_subsidy_tracker.plotting.cannibalisation.capture_ratio`

- **## See also:** `../cost/index.md` (the levy is the Cost theme's concern too); `../reliability/generation-heatmap.md` (why the concentrated hours happen); `./methodology.md`.

Line target 110-150.
  </action>
  <verify>
    <automated>test -f docs/themes/cannibalisation/capture-ratio.md && ! grep -q "Stub —" docs/themes/cannibalisation/capture-ratio.md && for h in "## What the chart shows" "## The argument" "## Methodology" "## Caveats" "## Data & code" "## See also"; do grep -qF "$h" docs/themes/cannibalisation/capture-ratio.md || exit 1; done && grep -q "cannibalisation_capture_ratio_twitter.png" docs/themes/cannibalisation/capture-ratio.md && grep -q "blob/main/src/uk_subsidy_tracker/plotting/cannibalisation/capture_ratio.py" docs/themes/cannibalisation/capture-ratio.md && grep -q "blob/main/tests/test_schemas.py" docs/themes/cannibalisation/capture-ratio.md && test $(wc -l < docs/themes/cannibalisation/capture-ratio.md) -ge 100</automated>
  </verify>
  <acceptance_criteria>
    - All 6 D-01 section headers
    - PNG embed `cannibalisation_capture_ratio_twitter.png`
    - Source permalink for `cannibalisation/capture_ratio.py`
    - Test permalink for `test_schemas.py`
    - GOV-01 four-way coverage: PNG + `./methodology.md` link + test permalink + source permalink
    - Line count 100-155
    - `uv run mkdocs build --strict` still exits 0
  </acceptance_criteria>
  <done>
    capture-ratio.md filled with full D-01 content; Cannibalisation flagship page live; GOV-01 four-way coverage provable; strict build green.
  </done>
</task>

<task type="auto">
  <name>Task 5: Fill capacity-factor-seasonal.md (Reliability — DESNZ-assumption challenge)</name>
  <files>docs/themes/reliability/capacity-factor-seasonal.md</files>
  <read_first>
    - docs/themes/reliability/capacity-factor-seasonal.md (stub)
    - src/uk_subsidy_tracker/plotting/capacity_factor/seasonal.py (3-panel structure — one panel per technology)
    - docs/charts/subsidy/cfd-dynamics.md (multi-panel precedent)
    - .planning/phases/03-chart-triage-execution/03-RESEARCH.md §7 PROMOTE Chart Summaries #5
    - ARCHITECTURE.md §5.1 Theme E (Reliability)
    - ARCHITECTURE.md §5.2 (DESNZ-assumption triage note — the adversarial payload comes from here)
  </read_first>
  <action>
Replace stub placeholders with full D-01 content (110-150 lines).

**Authored content spec:**

- **## What the chart shows:** Three side-by-side panels (Offshore Wind, Onshore Wind, Solar PV). X-axis per panel: calendar month Jan-Dec. Y-axis: capacity-factor % (0-60 range typically). Bold coloured line = fleet average (capacity-weighted across active CfD units); faint grey lines = individual years back to 2017. Two horizontal reference lines per panel: **CfD actual average** (dotted, colour-coded) and **DESNZ planning assumption** (dashed, black — 49% offshore, 36% onshore, 11% solar per published values).

- **## The argument:** Three paragraphs: (1) DESNZ publishes planning capacity factors that feed CfD strike-price modelling and budget projections — if DESNZ assumes 49% offshore CF, the £/MWh strike is engineered around that assumption; (2) the observed CfD fleet runs meaningfully below DESNZ's headline assumption in every month for every technology — seasonal winter peaks hit ~50% offshore, but the annual average sits well below 45%; (3) either the real fleet CF is lower than DESNZ models, or the new AR7 builds will over-perform the existing fleet by ~8-10 percentage points — an optimism that demands evidence.

- **## Methodology:** CF per unit per day = `CFD_Generation_MWh / (installed_capacity_MW × 24)`. Fleet CF = capacity-weighted average across active units. Seasonal aggregation: average daily CF per calendar month across all years. Faint-grey per-year lines use the same per-month aggregation but for a single calendar year. DESNZ reference values (49% / 36% / 11%) are the published planning assumptions for AR7 rounds. Link to `./methodology.md` for the installed-capacity-over-time attribution rule (pre-commissioning days excluded).

- **## Caveats:** ≥4 bullets: (a) CF depends on `installed_capacity_MW` from LCCC portfolio snapshot; mid-construction units have a gradual ramp-up that the chart does NOT model; (b) Fleet composition changes over time — early years are dominated by IC units, recent by AR1-3, so the fleet-average line blends vintages; (c) DESNZ planning assumptions reflect *new build* projections, not the *existing fleet* — the comparison is valid only to the extent future units repeat fleet behaviour; (d) No dedicated pytest pin on the CF formula today; `tests/test_schemas.py` validates the underlying LCCC portfolio schema. A CF-formula pin test is a Phase-4 seed (see `./methodology.md` — "A note on pin tests").

- **## Data & code** (GOV-01):
  - LCCC *Actual CfD Generation* + LCCC *CfD Contract Portfolio Status* URLs
  - DESNZ planning-assumption source: link to DESNZ's *Energy and Emissions Projections* (latest) or the AR7 notice of intent
  - Chart source: `.../blob/main/src/uk_subsidy_tracker/plotting/capacity_factor/seasonal.py`
  - Tests: `.../blob/main/tests/test_schemas.py`, `.../blob/main/tests/test_aggregates.py`
  - Reproduce: `uv run python -m uk_subsidy_tracker.plotting.capacity_factor.seasonal`

- **## See also:** `./generation-heatmap.md` (same CF data, different visualisation); `./rolling-minimum.md` (low-output droughts); `../cannibalisation/capture-ratio.md` (why concentrated-hour output matters for prices); `./methodology.md`.

Line target 110-150.
  </action>
  <verify>
    <automated>test -f docs/themes/reliability/capacity-factor-seasonal.md && ! grep -q "Stub —" docs/themes/reliability/capacity-factor-seasonal.md && for h in "## What the chart shows" "## The argument" "## Methodology" "## Caveats" "## Data & code" "## See also"; do grep -qF "$h" docs/themes/reliability/capacity-factor-seasonal.md || exit 1; done && grep -q "capacity_factor_seasonal_twitter.png" docs/themes/reliability/capacity-factor-seasonal.md && grep -q "blob/main/src/uk_subsidy_tracker/plotting/capacity_factor/seasonal.py" docs/themes/reliability/capacity-factor-seasonal.md && grep -q "blob/main/tests/test_schemas.py" docs/themes/reliability/capacity-factor-seasonal.md && grep -q "DESNZ" docs/themes/reliability/capacity-factor-seasonal.md && test $(wc -l < docs/themes/reliability/capacity-factor-seasonal.md) -ge 100</automated>
  </verify>
  <acceptance_criteria>
    - All 6 D-01 headers
    - PNG embed `capacity_factor_seasonal_twitter.png`
    - Source permalink for `capacity_factor/seasonal.py`
    - Test permalinks for `test_schemas.py` + `test_aggregates.py`
    - `DESNZ` appears in the argument + caveats (authoritative adversarial payload)
    - OQ4 seed present in Caveats ("CF-formula pin test is a Phase-4 seed")
    - GOV-01 four-way coverage
    - Line count 100-155
    - `uv run mkdocs build --strict` still exits 0
  </acceptance_criteria>
  <done>
    capacity-factor-seasonal.md filled; DESNZ-assumption challenge in place; OQ4 Phase-4 seed recorded in Caveats; strict build green.
  </done>
</task>

<task type="auto">
  <name>Task 6: Fill generation-heatmap.md (Reliability — dunkelflaute visible to naked eye)</name>
  <files>docs/themes/reliability/generation-heatmap.md</files>
  <read_first>
    - docs/themes/reliability/generation-heatmap.md (stub)
    - src/uk_subsidy_tracker/plotting/intermittency/generation_heatmap.py (2-stacked-heatmap structure — DO NOT fix pre-existing Pyright reportReturnType at intermittency/__init__.py:58 per CONTEXT)
    - .planning/phases/03-chart-triage-execution/03-RESEARCH.md §7 PROMOTE Chart Summaries #6
  </read_first>
  <action>
Replace stub placeholders with full D-01 content (100-140 lines).

**Authored content spec:**

- **## What the chart shows:** Two stacked heatmaps. Top panel: wind (offshore + onshore combined) daily CF. Rows = year (2017-2025); columns = day-of-year (1-366). Colourscale blue → red (low CF blue, high CF red). Bottom panel: solar (PV), same layout. Dunkelflaute shows up as blue stripes across years, concentrated in winter for solar, more scattered for wind.

- **## The argument:** Lead: "Sustained low-output periods are structural, not exceptional. You can see them in the data with the naked eye." Three paragraphs: (1) The heatmap is a deliberately direct-access visualisation — no aggregation, no smoothing, no summary statistic — just raw daily CF organised into a year-on-year grid; (2) the blue stripes are the empirical record of every multi-day drought the CfD fleet has endured; (3) solar's pattern is starker — every winter is a continuous blue band — but wind's pattern is what matters economically because the fleet is wind-dominant.

- **## Methodology:** Daily CF per unit = `daily_gen / (installed_capacity × 24)`; aggregated to fleet-level via capacity-weighted average, per-technology. Installed capacity at each date derived from the LCCC portfolio snapshot (first-generation date per unit). Plotly heatmap with pandas pivot (`year` on rows, `day_of_year` on columns, `fleet_cf` as colour). Colour scale custom per-panel to maximise contrast. Link to `./methodology.md` for the installed-capacity-over-time attribution rule.

- **## Caveats:** ≥3 bullets: (a) Pre-commissioning days excluded from fleet CF (unit counted from its first generation date, not its contract-start date); (b) Leap-year day-366 is plotted for 2020, 2024 — other years have column-366 blank by design; (c) Colour scale is per-panel, not absolute — a dark blue in the solar panel is a lower CF than a dark blue in the wind panel.

- **## Data & code** (GOV-01):
  - LCCC URLs (both *Actual CfD Generation* and *Contract Portfolio Status*)
  - Chart source: `.../blob/main/src/uk_subsidy_tracker/plotting/intermittency/generation_heatmap.py`
  - Test: `.../blob/main/tests/test_schemas.py`
  - Reproduce: `uv run python -m uk_subsidy_tracker.plotting.intermittency.generation_heatmap`

- **## See also:** `./rolling-minimum.md` (quantifies the blue stripes into drought durations); `./capacity-factor-seasonal.md` (same CF data, month-aggregated); `./methodology.md`.

Line target 100-140.
  </action>
  <verify>
    <automated>test -f docs/themes/reliability/generation-heatmap.md && ! grep -q "Stub —" docs/themes/reliability/generation-heatmap.md && for h in "## What the chart shows" "## The argument" "## Methodology" "## Caveats" "## Data & code" "## See also"; do grep -qF "$h" docs/themes/reliability/generation-heatmap.md || exit 1; done && grep -q "intermittency_generation_heatmap_twitter.png" docs/themes/reliability/generation-heatmap.md && grep -q "blob/main/src/uk_subsidy_tracker/plotting/intermittency/generation_heatmap.py" docs/themes/reliability/generation-heatmap.md && grep -q "blob/main/tests/test_schemas.py" docs/themes/reliability/generation-heatmap.md && test $(wc -l < docs/themes/reliability/generation-heatmap.md) -ge 100</automated>
  </verify>
  <acceptance_criteria>
    - All 6 D-01 headers
    - PNG embed `intermittency_generation_heatmap_twitter.png`
    - Source permalink for `intermittency/generation_heatmap.py`
    - Test permalink for `test_schemas.py`
    - GOV-01 four-way coverage
    - Line count 100-145
    - `uv run mkdocs build --strict` still exits 0
  </acceptance_criteria>
  <done>
    generation-heatmap.md filled; visual-hook argument in place; strict build green.
  </done>
</task>

<task type="auto">
  <name>Task 7: Fill rolling-minimum.md (Reliability — 21-day droughts longer than any battery)</name>
  <files>docs/themes/reliability/rolling-minimum.md</files>
  <read_first>
    - docs/themes/reliability/rolling-minimum.md (stub)
    - src/uk_subsidy_tracker/plotting/intermittency/rolling_minimum.py (2-panel wind/solar time-series; scipy find_peaks trough detection)
    - .planning/phases/03-chart-triage-execution/03-RESEARCH.md §7 PROMOTE Chart Summaries #7
    - ARCHITECTURE.md §5.2 (the "drought / longer than any battery" argument framing)
  </read_first>
  <action>
Replace stub placeholders with full D-01 content (110-150 lines).

**Authored content spec:**

- **## What the chart shows:** Two panels (Wind top, Solar bottom). Each panel a time-series chart 2017-2025 with three layers: (1) faint grey = daily fleet CF; (2) coloured line = 21-day centred rolling-mean CF; (3) red diamond markers = significant drought troughs (scipy `find_peaks` with `prominence=15` pp, filtered below the technology reference CF line). Horizontal reference lines at 20% CF (wind) and 5% CF (solar).

- **## The argument:** Lead: "CfD fleet has had multiple 21-day droughts below reference CF. Three weeks is longer than any deployed battery can cover. Renewables do not remove the need for firm backup." Three paragraphs: (1) the 21-day window is chosen because it maps to roughly the duration below which batteries + demand-response can plausibly bridge, but above which the fleet requires dispatchable generation or imports; (2) the chart enumerates the actual droughts — multiple per technology per year — and each trough is flagged with a red diamond; (3) the policy consequence: "firm" dispatchable capacity cannot be written out of the capacity-adequacy calculation, and the cost of retaining it is a hidden renewable-attributable cost (Phase 10 balancing + Phase 9 capacity market will quantify).

- **## Methodology:** Fleet CF daily series per technology (same source as generation-heatmap). Rolling mean via `pd.Series.rolling(window=21, center=True).mean()`. Trough detection via `scipy.signal.find_peaks(-rolling_mean, prominence=15)` applied to the negated series (peaks in negation = troughs in original). Prominence = 15 percentage points chosen to filter out noise-level dips while catching the economically meaningful droughts. Reference CF lines set conservatively to technology floors (wind 20%, solar 5%). Link to `./methodology.md` for the full prominence-tuning rationale.

- **## Caveats:** ≥3 bullets: (a) 21-day window is a policy-convention choice, not a battery-physics derivation — a 14-day or 30-day window would flag different troughs; (b) prominence threshold 15pp is tuned to the current fleet size; as the fleet grows, absolute MWh at each trough rises, changing the economic stakes without changing the chart; (c) Rolling mean is centred (looks 10 days forward), so the latest 10 days of data are truncated from the trough-detection output — the right edge of the chart is more recent raw CF but less reliable for peak-finding.

- **## Data & code** (GOV-01):
  - LCCC URLs
  - Chart source: `.../blob/main/src/uk_subsidy_tracker/plotting/intermittency/rolling_minimum.py`
  - Test: `.../blob/main/tests/test_schemas.py`
  - Reproduce: `uv run python -m uk_subsidy_tracker.plotting.intermittency.rolling_minimum`

- **## See also:** `./generation-heatmap.md` (the same droughts visible as blue stripes); `./capacity-factor-seasonal.md` (why these droughts happen monthly); `../cost/remaining-obligations.md` (the forward commitment these reliability caveats apply to); `./methodology.md`.

Line target 110-150.
  </action>
  <verify>
    <automated>test -f docs/themes/reliability/rolling-minimum.md && ! grep -q "Stub —" docs/themes/reliability/rolling-minimum.md && for h in "## What the chart shows" "## The argument" "## Methodology" "## Caveats" "## Data & code" "## See also"; do grep -qF "$h" docs/themes/reliability/rolling-minimum.md || exit 1; done && grep -q "intermittency_rolling_minimum_twitter.png" docs/themes/reliability/rolling-minimum.md && grep -q "blob/main/src/uk_subsidy_tracker/plotting/intermittency/rolling_minimum.py" docs/themes/reliability/rolling-minimum.md && grep -q "blob/main/tests/test_schemas.py" docs/themes/reliability/rolling-minimum.md && grep -q "21-day" docs/themes/reliability/rolling-minimum.md && test $(wc -l < docs/themes/reliability/rolling-minimum.md) -ge 100</automated>
  </verify>
  <acceptance_criteria>
    - All 6 D-01 headers
    - PNG embed `intermittency_rolling_minimum_twitter.png`
    - Source permalink for `intermittency/rolling_minimum.py`
    - Test permalink for `test_schemas.py`
    - `21-day` string appears in argument (adversarial payload locked)
    - GOV-01 four-way coverage
    - Line count 100-155
    - `uv run mkdocs build --strict` still exits 0
  </acceptance_criteria>
  <done>
    rolling-minimum.md filled; drought-longer-than-any-battery argument in place; strict build green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chart page ↔ regulator URL | Each page cites external regulator pages; rot risk exists but is not Phase-3 scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering (link rot to hostile content at regulator URLs) | LCCC / ONS / UK ETS / DESNZ / DEFRA URLs cited across 7 chart pages | transfer | Phase 4 adds Internet Archive auto-archival per ARCHITECTURE §8.4; not in scope here. Material renders links as-is with no inline execution. |
| T-03-09 | Integrity (GitHub permalink points to branch HEAD rather than pinned commit) | `blob/main/...` permalinks | accept | `main` is the project's protected branch; permalink semantics mean the page tracks `main` — acceptable for a live-site methodology page. A Phase-4 versioned-snapshot deploy would pin these to a tag. |
| T-03-10 | Availability (PR-author injecting `<script>` tags in chart Markdown) | all 7 authored files | accept | Material escapes HTML by default; Phase 3 introduces no `{=html}` blocks. Review gate for Markdown PRs is the developer-executor themselves. |
</threat_model>

<verification>
- All 7 PROMOTE chart pages have zero `Stub —` placeholder lines
- Each page has all 6 D-01 H2 section headers in canonical order
- Each page has a PNG embed pointing at `../../charts/html/<stem>_twitter.png`
- Each page has a `blob/main/src/uk_subsidy_tracker/plotting/` permalink
- Each page has ≥1 `blob/main/tests/test_*.py` permalink (GOV-01 four-way coverage)
- Efficiency page (+ implicitly the already-moved 3 Cost pages) link to `../../methodology/gas-counterfactual.md` per D-05
- `uv run mkdocs build --strict` → exit 0
- `uv run pytest` → 16 passed + 4 skipped (no regression)
</verification>

<success_criteria>
- 7 PROMOTE chart pages authored to full D-01 template (100-170 lines each, lorenz may trend to ~95)
- Every page satisfies GOV-01 four-way coverage: narrative (self) + methodology link + test permalink + source permalink
- Every page carries its adversarial rhetorical payload per RESEARCH §7
- Efficiency page (+ Cost-theme existing pages) link to shared gas-counterfactual per D-05
- `mkdocs build --strict` remains green
- Pytest remains green
- TRIAGE-02 + GOV-01 closed for Phase 3
</success_criteria>

<output>
After completion, create `.planning/phases/03-chart-triage-execution/03-03-SUMMARY.md` following the summary template.
</output>
