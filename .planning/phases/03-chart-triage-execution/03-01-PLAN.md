---
phase: 03-chart-triage-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/uk_subsidy_tracker/plotting/subsidy/scissors.py
  - src/uk_subsidy_tracker/plotting/subsidy/bang_for_buck_old.py
  - src/uk_subsidy_tracker/plotting/__main__.py
  - src/uk_subsidy_tracker/counterfactual.py
  - mkdocs.yml
autonomous: true
requirements:
  - TRIAGE-01
open_questions_resolved:
  OQ1: "cfd-payments-by-category placed under docs/themes/recipients/ per ARCHITECTURE §5.2; cross-linked from Cost gallery (decision record: PLAN 03-02 mkdocs.yml nav + PLAN 03-03 chart page location)."
  OQ2: "Chart regeneration from scratch is scheduled in Plan 03-04 Wave 4 (rm -rf docs/charts/html/ then regenerate). Scissors stale PNGs will vanish via the full regen; no explicit rm task needed here."
  OQ3: "mkdocs build --strict added as CI gate in Plan 03-04 Wave 4 (fold into .github/workflows/ci.yml as a second job after pytest)."
  OQ4: "capacity-factor-seasonal cites tests/test_schemas.py (LCCC portfolio schema) + tests/test_aggregates.py; chart page Caveats section carries a pointer to a Phase-4 CF-formula pin test seed."
  OQ5: "content.tooltips enabled without a glossary; abbreviations doc is a Phase 4+ todo (added to STATE.md in Plan 03-04 closeout)."
nyquist_compliant: true

must_haves:
  truths:
    - "`scissors.py` is absent from the working tree"
    - "`bang_for_buck_old.py` is absent from the working tree"
    - "`src/uk_subsidy_tracker/plotting/__main__.py` no longer imports or calls scissors"
    - "`src/uk_subsidy_tracker/counterfactual.py` docstring no longer references the scissors chart"
    - "`mkdocs.yml` theme.features contains the 4 D-06 additions and swaps `toc.follow` → `toc.integrate`"
    - "`mkdocs.yml` contains a root-level `validation:` block with `nav.omitted_files: warn`"
    - "No grep match for `scissors` or `bang_for_buck_old` remains in `src/` or `tests/`, excluding `tests/test_docs_structure.py` (the permanent TRIAGE-01 regression guard, added in Plan 03-04, which must name these files to assert their absence)"
    - "`uv run python -m uk_subsidy_tracker.plotting` runs without ImportError (proves the __main__.py edit is consistent)"
    - "`uv run pytest` stays green (16 passed + 4 skipped, no regressions from the cleanup)"
  artifacts:
    - path: "src/uk_subsidy_tracker/plotting/__main__.py"
      provides: "Orchestrator entry with 14 chart modules (was 15 — scissors removed)"
      contains: "cfd_payments_by_category"
    - path: "src/uk_subsidy_tracker/counterfactual.py"
      provides: "Counterfactual module with cleaned docstring at line ~139"
      contains: "Callers who want fuel-only should reference"
    - path: "mkdocs.yml"
      provides: "Material theme features + validation block matching D-06 + RESEARCH §3"
      contains: "validation:"
  key_links:
    - from: "src/uk_subsidy_tracker/plotting/__main__.py"
      to: "src/uk_subsidy_tracker/plotting/subsidy/"
      via: "imports the 14 remaining chart main() functions; NO scissors import"
      pattern: "^from uk_subsidy_tracker\\.plotting\\."
    - from: "mkdocs.yml"
      to: "Material theme + validation gates"
      via: "theme.features list + root-level validation block"
      pattern: "navigation\\.instant|validation:"
---

<objective>
Wave 1 preparation: purge the two CUT chart modules (TRIAGE-01) and prime `mkdocs.yml` with the D-06 feature deltas and the `validation:` block that RESEARCH §3 requires before `mkdocs build --strict` can catch nav orphans (required before Wave 2 can rely on the build gate).

Purpose: Removes 2 obsolete Python modules from the orchestrator so Wave 4's chart regeneration does not regenerate scissors artefacts; lands the minimum `mkdocs.yml` config changes that Wave 2's structural work depends on. Does NOT rewrite `nav:` (the theme tree does not yet exist in Wave 1 — rewriting nav here would produce dangling entries that break the working tree for the next wave).

Output: Two source files deleted; `__main__.py` and `counterfactual.py` freed of scissors references; `mkdocs.yml` features list matches D-06 and carries a root-level `validation:` block promoting `nav.omitted_files: warn`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-chart-triage-execution/03-CONTEXT.md
@.planning/phases/03-chart-triage-execution/03-RESEARCH.md
@.planning/phases/03-chart-triage-execution/03-VALIDATION.md
@src/uk_subsidy_tracker/plotting/__main__.py
@src/uk_subsidy_tracker/counterfactual.py
@mkdocs.yml

<interfaces>
Current `__main__.py` state (scissors references at L18 + L27 — both must be removed):

```python
# L18:
from uk_subsidy_tracker.plotting.subsidy.scissors import main as scissors
# L27:
scissors()
```

All other imports (capture_ratio, price_vs_wind, cf_monthly, cf_seasonal, heatmap,
load_duration, rolling_minimum, bang_for_buck, lorenz, cfd_dynamics, cfd_vs_gas_total,
cfd_payments_by_category, remaining_obligations, subsidy_per_avoided_co2_tonne) stay.
The file executes each main() at module load time — keep that pattern; just drop
the scissors line from the import block and the call block.

Current `counterfactual.py:130-141` docstring (reference literal — the to-be-cleaned hunk):

```
130:    """
131:    Compute the counterfactual gas-only electricity price, daily.
...
138:    counterfactual_total = gas_fuel_cost + carbon_cost + plant_opex.
139:
140:    Callers who want fuel-only (e.g. scissors chart) should reference
141:    gas_fuel_cost directly instead of counterfactual_total.
142:    """
```

Current `mkdocs.yml:19-28` (theme features — what must change):

```yaml
theme:
  name: material
  locale: en
  features:
    - navigation.tabs
    - navigation.sections
    - toc.follow               # REPLACE with toc.integrate (D-06)
    - search.suggest
    - search.highlight
    - content.code.copy
```

Target `theme.features` after this plan (order matches RESEARCH §Material Theme Feature Regressions):

```yaml
    - navigation.tabs
    - navigation.tabs.sticky      # ADD
    - navigation.sections
    - navigation.top              # ADD
    - navigation.instant          # ADD
    - search.suggest
    - search.highlight
    - content.code.copy
    - content.tooltips            # ADD
    - toc.integrate               # REPLACE toc.follow
```

Target `validation:` block (insert at `mkdocs.yml` root level, between `theme:` and `nav:`):

```yaml
validation:
  nav:
    omitted_files: warn        # non-default; required for TRIAGE-04
    not_found: warn            # default; kept explicit for visibility
    absolute_links: warn       # promote from info
  links:
    not_found: warn            # default
    anchors: warn              # promote from info
    absolute_links: warn       # promote from info
    unrecognized_links: info   # default
```

Note: `nav:` rewrite itself is DEFERRED to Plan 03-02 because the theme tree does not exist yet in Wave 1. Adding orphan-detection first is safe: `nav.omitted_files: warn` only fires if there are files outside the nav; the current three-CfD-chart nav still matches the current filesystem.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete scissors.py + bang_for_buck_old.py and purge all working-tree references</name>
  <files>src/uk_subsidy_tracker/plotting/subsidy/scissors.py, src/uk_subsidy_tracker/plotting/subsidy/bang_for_buck_old.py, src/uk_subsidy_tracker/plotting/__main__.py, src/uk_subsidy_tracker/counterfactual.py</files>
  <read_first>
    - src/uk_subsidy_tracker/plotting/__main__.py (confirm L18 + L27 scissors lines, do not touch the other 14 imports)
    - src/uk_subsidy_tracker/counterfactual.py lines 130-145 (the docstring block to edit)
    - .planning/phases/03-chart-triage-execution/03-RESEARCH.md §Runtime State Inventory (the only 3 working-tree refs to scissors are L18 + L27 of __main__.py and L139 of counterfactual.py; bang_for_buck_old has zero code references)
    - .planning/phases/03-chart-triage-execution/03-CONTEXT.md §Files to delete (TRIAGE-01 scope lock)
  </read_first>
  <action>
Perform four file operations in a single commit. Do NOT attempt any partial state — dangling imports break `uv run python -m uk_subsidy_tracker.plotting`.

**Step 1 — Delete the two CUT files via `git rm`:**

```bash
git rm src/uk_subsidy_tracker/plotting/subsidy/scissors.py
git rm src/uk_subsidy_tracker/plotting/subsidy/bang_for_buck_old.py
```

Do NOT use `rm` alone — we want the deletion staged for the TRIAGE-01 commit. History preservation is automatic in git; nothing extra is needed. Gitignored stale PNG/HTML outputs at `docs/charts/html/subsidy_scissors*` are left in place for Wave 4 to clean via `rm -rf docs/charts/html/` before regeneration (per OQ2 resolution).

**Step 2 — Edit `src/uk_subsidy_tracker/plotting/__main__.py`:** Delete exactly two lines.

Current L18 reads:
```python
from uk_subsidy_tracker.plotting.subsidy.scissors import main as scissors
```

Current L27 reads:
```python
scissors()
```

Delete both lines. Leave every other import and every other function call untouched. The resulting file will have 14 `from uk_subsidy_tracker.plotting.*` imports and 14 `*()` calls. Do not alter the section-comment structure (`# Subsidy economics`, `# Capacity factor`, `# Intermittency`, `# Cannibalisation`).

**Step 3 — Edit `src/uk_subsidy_tracker/counterfactual.py` docstring at line 139-141:**

Current lines 139-141 read:
```
    Callers who want fuel-only (e.g. scissors chart) should reference
    gas_fuel_cost directly instead of counterfactual_total.
```

Replace with (drop the parenthetical — no substitute chart is cited):
```
    Callers who want fuel-only should reference gas_fuel_cost directly
    instead of counterfactual_total.
```

Do not modify the `Provenance:` blocks or any formula lines (143-167).

**Step 4 — Verify and stage:** Run the grep + pytest + import sanity checks in the verify block. Any hit on `scissors` or `bang_for_buck_old` under `src/` or `tests/` means a reference was missed.
  </action>
  <verify>
    <automated>test ! -f src/uk_subsidy_tracker/plotting/subsidy/scissors.py && test ! -f src/uk_subsidy_tracker/plotting/subsidy/bang_for_buck_old.py && ! grep -rn --exclude-dir=__pycache__ --exclude=test_docs_structure.py "scissors\|bang_for_buck_old" src/ tests/ && uv run python -c "import uk_subsidy_tracker.plotting.__main__" 2>&1 | grep -v "Saved " | grep -E "Error|Traceback" && exit 1 || uv run pytest -x</automated>
  </verify>
  <acceptance_criteria>
    - V-T01-01: `test ! -f src/uk_subsidy_tracker/plotting/subsidy/scissors.py` exits 0
    - V-T01-02: `test ! -f src/uk_subsidy_tracker/plotting/subsidy/bang_for_buck_old.py` exits 0
    - V-T01-03: `! grep -rn "from uk_subsidy_tracker.plotting.subsidy.scissors\|import scissors\|bang_for_buck_old" src/ tests/` exits 0
    - V-T01-04: `! grep -n "scissors" src/uk_subsidy_tracker/plotting/__main__.py` exits 0
    - V-T01-05: `! grep -n "scissors" src/uk_subsidy_tracker/counterfactual.py` exits 0
    - `git status` shows the two files staged as `D` (deleted)
    - `uv run pytest` exits 0 with 16 passed + 4 skipped (no regression from the cleanup)
    - `grep -c "^from uk_subsidy_tracker.plotting" src/uk_subsidy_tracker/plotting/__main__.py` returns 14 (was 15)
    - `grep -c "^<chartname>()" src/uk_subsidy_tracker/plotting/__main__.py` pattern: exactly 14 bare function calls remain
  </acceptance_criteria>
  <done>
    Two CUT files deleted via `git rm`; scissors references purged from `__main__.py` (2 lines) and `counterfactual.py` docstring (2 lines); pytest still green; zero working-tree grep hits for `scissors` or `bang_for_buck_old` under `src/` and `tests/`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update mkdocs.yml theme.features + add validation block (D-06 + RESEARCH §3)</name>
  <files>mkdocs.yml</files>
  <read_first>
    - mkdocs.yml (current state — features list at L22-28, no `validation:` block exists)
    - .planning/phases/03-chart-triage-execution/03-CONTEXT.md §D-06 (locked feature list)
    - .planning/phases/03-chart-triage-execution/03-RESEARCH.md §Material Theme Feature Regressions (per-feature prerequisite check)
    - .planning/phases/03-chart-triage-execution/03-RESEARCH.md §Nav Orphan Detection (validation block boilerplate)
  </read_first>
  <action>
Edit `mkdocs.yml` in two localised hunks. Do NOT rewrite the `nav:` block — that is Plan 03-02's scope.

**Hunk 1 — `theme.features` list (replace lines 22-28 verbatim).**

Replace the current block:

```yaml
  features:
    - navigation.tabs
    - navigation.sections
    - toc.follow
    - search.suggest
    - search.highlight
    - content.code.copy
```

With:

```yaml
  features:
    - navigation.tabs
    - navigation.tabs.sticky
    - navigation.sections
    - navigation.top
    - navigation.instant
    - search.suggest
    - search.highlight
    - content.code.copy
    - content.tooltips
    - toc.integrate
```

Order must match the list above (RESEARCH §Material Theme Feature Regressions target ordering). `toc.follow` is removed (not commented out — D-06 swap is explicit, the features are mutually exclusive per Pitfall 4 of RESEARCH).

**Hunk 2 — insert `validation:` block at root level.** Insert a new top-level block IMMEDIATELY AFTER the `theme:` stanza ends (after the `palette:` block's final `name: Switch to light mode` line around line 37) and BEFORE the `# Navigation` comment at line 39. Paste exactly:

```yaml

# Validation — --strict gate teeth (RESEARCH §3, required for TRIAGE-04)
validation:
  nav:
    omitted_files: warn
    not_found: warn
    absolute_links: warn
  links:
    not_found: warn
    anchors: warn
    absolute_links: warn
    unrecognized_links: info
```

Keys go at column-0 root alongside `theme:`, `nav:`, `plugins:`. Two-space indent for children of `validation:` matches the rest of the file.

Do NOT modify: `site_name`, `site_url`, `site_description`, `repo_*`, `palette:`, `nav:` (yet), `markdown_extensions:`, `plugins:`, `extra:`, `copyright:`.
  </action>
  <verify>
    <automated>grep -q "navigation.tabs.sticky" mkdocs.yml && grep -q "navigation.top" mkdocs.yml && grep -q "navigation.instant" mkdocs.yml && grep -q "content.tooltips" mkdocs.yml && grep -q "toc.integrate" mkdocs.yml && ! grep -q "toc.follow" mkdocs.yml && grep -q "^validation:" mkdocs.yml && grep -A2 "^validation:" mkdocs.yml | grep -q "omitted_files: warn" && uv run python -c "import yaml; yaml.safe_load(open('mkdocs.yml'))"</automated>
  </verify>
  <acceptance_criteria>
    - V-T06-01: `grep -q "navigation.tabs.sticky" mkdocs.yml && grep -q "navigation.instant" mkdocs.yml && grep -q "content.tooltips" mkdocs.yml && grep -q "toc.integrate" mkdocs.yml` exits 0
    - V-T04-01 (seed): `grep -A8 "^validation:" mkdocs.yml | grep -q "omitted_files: warn"` exits 0
    - `! grep -q "toc.follow" mkdocs.yml` — the old setting is removed, not commented
    - `grep -c "^[a-z_]*:" mkdocs.yml` increases by exactly 1 vs pre-edit (new `validation:` root key)
    - `uv run python -c "import yaml; yaml.safe_load(open('mkdocs.yml'))"` exits 0 (YAML is valid — no indentation mistakes)
    - `nav:` block is unchanged (still lists the old three charts — intentional; Plan 03-02 rewrites it)
  </acceptance_criteria>
  <done>
    theme.features list matches the D-06 target order exactly; root-level `validation:` block present with `nav.omitted_files: warn` + `links.anchors: warn` + `links.absolute_links: warn`; YAML still parses; `nav:` untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build config ↔ docs tree | `mkdocs.yml` is the contract; filesystem must match its `nav:` and feature prerequisites. Trusted first-party edit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Integrity (dangling imports after file deletion) | `__main__.py`, `counterfactual.py` | mitigate | Task 1 deletes files AND purges references in a single commit; Wave-end `uv run python -m uk_subsidy_tracker.plotting` (Plan 03-04) verifies orchestrator runs clean. Pytest stays green as a regression guard. |
| T-03-02 | Availability (mkdocs-material CSS change breaks grid cards) | `mkdocs.yml` + Material theme | accept | Version pinned in `uv.lock` (already committed, commit 7b5538b et al.). Grid cards live in Material ≥9.5.0; our pin is ≥9.7.6. |
| T-03-03 | Availability (stale scissors PNG referenced post-deletion) | `docs/charts/html/subsidy_scissors*` | accept | Files are gitignored; no Markdown page references them. Plan 03-04 Wave 4 `rm -rf docs/charts/html/` + regenerate makes this deterministic. |
| T-03-04 | Tampering (external regulator URL redirect to malicious content) | chart page outbound citations (added in later plans) | transfer | Out of Phase-3 scope; Phase 4 adds Internet Archive auto-archival per ARCHITECTURE §8.4. |
</threat_model>

<verification>
- `test ! -f src/uk_subsidy_tracker/plotting/subsidy/scissors.py` → exit 0
- `test ! -f src/uk_subsidy_tracker/plotting/subsidy/bang_for_buck_old.py` → exit 0
- `! grep -rn --exclude-dir=__pycache__ --exclude=test_docs_structure.py "scissors\|bang_for_buck_old" src/ tests/` → exit 0 (the regression test at `tests/test_docs_structure.py`, added by Plan 03-04, is the intended exception — it names the CUT files to assert their absence)
- `uv run pytest` → 16 passed + 4 skipped, no regressions
- `grep -q "navigation.instant" mkdocs.yml && grep -q "toc.integrate" mkdocs.yml && ! grep -q "toc.follow" mkdocs.yml` → exit 0
- `grep -A2 "^validation:" mkdocs.yml | grep -q "omitted_files: warn"` → exit 0
- `uv run python -c "import yaml; yaml.safe_load(open('mkdocs.yml'))"` → exit 0 (valid YAML)
- `uv run python -c "import uk_subsidy_tracker.plotting.__main__"` → loads without ImportError (proves __main__.py is consistent post-deletion; chart PNGs may regenerate but the test asserts import resolution only)
</verification>

<success_criteria>
- Two CUT files deleted via `git rm`; preserved in history
- `src/uk_subsidy_tracker/plotting/__main__.py` has 14 imports + 14 calls (was 15 + 15)
- `src/uk_subsidy_tracker/counterfactual.py:139` docstring cleaned of scissors reference
- `mkdocs.yml` theme.features list matches D-06 target ordering exactly (4 additions + 1 swap)
- `mkdocs.yml` root-level `validation:` block present with `nav.omitted_files: warn`
- `uv run pytest` stays green
- YAML parses; nav still untouched for Plan 03-02 to rewrite
</success_criteria>

<output>
After completion, create `.planning/phases/03-chart-triage-execution/03-01-SUMMARY.md` following the summary template.
</output>
