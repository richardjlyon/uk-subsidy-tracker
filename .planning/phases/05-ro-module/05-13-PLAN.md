---
phase: 5
plan: 13
type: execute
wave: 6
depends_on: [12]
files_modified:
  - data/raw/ofgem/ro-register.xlsx
  - data/raw/ofgem/ro-generation.csv
  - data/raw/ofgem/ro-register.xlsx.meta.json
  - data/raw/ofgem/ro-generation.csv.meta.json
autonomous: false
autonomous_rationale: "This plan collects every post-completion item that legitimately requires human judgement: (a) optionally replacing Option-D stub Ofgem files with real exports, (b) visual mkdocs-serve inspection of docs/schemes/ro.md, (c) REF Constable transcription audit, (d) any execution-time divergence reports, (e) investigation-report review for follow-up plumbing of real RER credentials, (f) validate() invariants inspection. User tackles this plan AFTER autonomous Waves 1-5 (Plans 01-12) have all completed."
requirements: []
user_setup:
  - service: manual-review
    why: "Post-Phase-5 human verification checklist — five independent items the autonomous chain cannot self-verify"
    dashboard_config:
      - task: "Open each artefact flagged in the task list below; run the referenced commands; record outcome per item"
        location: "local working copy + localhost:8000 (mkdocs serve)"

must_haves:
  truths:
    - "All autonomous plans (05-01 through 05-12) have completed successfully before Plan 13 is opened by the user"
    - "User has visually confirmed docs/schemes/ro.md renders correctly under mkdocs serve (charts embed, cross-links resolve, methodology section cites REF Constable 2025 + gas-counterfactual.md)"
    - "Any Option-D stubs identified in 05-01-TASK-1-INVESTIGATION.md have a documented path forward (either replaced with real exports now OR deferred with explicit backlog ticket)"
    - "If 05-09-DIVERGENCE.md exists, it has been reviewed and resolved via (a) tolerance widening with audit-header note, (b) follow-up pipeline-fix plan, or (c) deferral with backlog ticket"
    - "REF Constable transcription spot-checked against 05-RESEARCH.md §5 for at least 3-4 year entries"
    - "schemes.ro.validate() has been run and its warnings list reviewed"
    - ".planning/phases/05-ro-module/05-13-SUMMARY.md records one-line status (DONE / DEFERRED-TO-BACKLOG / N/A) per checklist item"
  artifacts:
    - path: ".planning/phases/05-ro-module/05-13-SUMMARY.md"
      provides: "Post-review summary listing per-item status + any new backlog items created + final phase-5 close signal"
      min_lines: 20
  key_links:
    - from: "Plan 05-13 Task 1 (checklist)"
      to: ".planning/phases/05-ro-module/05-01-TASK-1-INVESTIGATION.md § Plan 05-13 Follow-Ups"
      via: "Reviewer reads the follow-ups section; each entry maps to a checklist item"
      pattern: "Plan 05-13 Follow-Ups"
    - from: "Plan 05-13 Task 4 (divergence review)"
      to: ".planning/phases/05-ro-module/05-09-DIVERGENCE.md"
      via: "Sentinel file presence controls xfail in test_benchmarks::test_ref_constable_ro_reconciliation"
      pattern: "05-09-DIVERGENCE.md"
---

<objective>
Plan 05-13 is the Post-Execution Human Review gate for Phase 5. Waves 1-5 (Plans 01-12) run fully unattended and produce a complete RO module — scrapers, seed data, schemas, scheme-module contract, Parquet grains, charts, docs site, REF benchmark reconciliation, manifest registration, CHANGES.md consolidation. This final plan collects every item the autonomous chain could not self-verify and presents it as a single human checklist.

Purpose: Preserves all quality gates that require human judgement (visual chart rendering, transcription accuracy, Option-D stub-replacement decisions, divergence triage) without blocking the unattended chain. The user runs this plan interactively after seeing the execute-phase orchestrator confirm Waves 1-5 complete. No autonomous work happens here; this plan opens a checklist, captures decisions, commits any touchups, and closes Phase 5.

Output: `.planning/phases/05-ro-module/05-13-SUMMARY.md` with one-line status per checklist item. Optional: new backlog items via `/gsd-add-backlog` for deferred follow-ups. Optional: replaced Ofgem raw files + regenerated sidecars.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ro-module/05-CONTEXT.md
@.planning/phases/05-ro-module/05-RESEARCH.md
@.planning/phases/05-ro-module/05-VALIDATION.md
@.planning/phases/05-ro-module/05-01-TASK-1-INVESTIGATION.md
@.planning/phases/05-ro-module/05-12-SUMMARY.md
@CHANGES.md
</context>

<threat_model>
## Trust Boundaries

Inherits from upstream plans; Plan 13 introduces no new threat surface.

| Boundary | Description |
|----------|-------------|
| Plan 05-01 credentials (if user plumbs real RER creds during this review) | OFGEM_RER_* secrets cross boundary into GitHub Secrets only |
| Plan 05-09 benchmark tamper (if user widens REF_TOLERANCE_PCT here without audit-header entry) | CHANGES.md `## Methodology versions` entry required — reviewer MUST document any tolerance bump |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5.13-01 | Tampering | Silent acceptance of Option-D stubs as real data | mitigate | Task 1 requires explicit DECISION: replace-now OR defer-with-backlog-ticket. "Accept-silently" is not an option — user must choose |
| T-5.13-02 | Repudiation | REF transcription audit skipped | mitigate | Task 3 requires spot-check of ≥3 year entries; outcome recorded in SUMMARY.md |
| T-5.13-03 | Repudiation | Tolerance widening without CHANGES.md audit entry | mitigate | If Task 4 results in REF_TOLERANCE_PCT bump, Task 7 MUST commit CHANGES.md `## Methodology versions` entry per D-07 rule |
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Ofgem raw files audit — Option-D stub disposition</name>
  <files>data/raw/ofgem/ro-register.xlsx, data/raw/ofgem/ro-generation.csv, data/raw/ofgem/ro-register.xlsx.meta.json, data/raw/ofgem/ro-generation.csv.meta.json</files>
  <what-built>
Plan 05-01 deterministically chose between Options A/B/C (real-scraper path) or Option D (header-only stubs + stub scrapers that raise RuntimeError if invoked). The choice + rationale are in `.planning/phases/05-ro-module/05-01-TASK-1-INVESTIGATION.md`.
  </what-built>
  <how-to-verify>
1. Read `.planning/phases/05-ro-module/05-01-TASK-1-INVESTIGATION.md` — note the adopted option + the `## Plan 05-13 Follow-Ups` section.
2. Inspect raw file sizes:
   ```bash
   ls -la data/raw/ofgem/
   ```
   Any file under ~1 KB is likely an Option-D header-only stub.
3. Inspect each sidecar for stub markers:
   ```bash
   for f in data/raw/ofgem/*.meta.json; do echo "=== $f ==="; cat "$f"; done
   ```
   An `upstream_url` value containing `option-d-stub:` is a definitive stub marker.
4. **DECISION (mandatory, per-file)** — for each stub file:
   - **Option A — Replace now:** Download the real Ofgem export from the URL the investigation report identifies (typically `rer.ofgem.gov.uk` after logging in via SharePoint, or a transparency-document PDF for roc-prices). Save to the exact path (e.g. `data/raw/ofgem/ro-register.xlsx`). Then regenerate all sidecars:
     ```bash
     uv run python scripts/backfill_sidecars.py --force
     ```
     Verify the sidecar `sha256` changes and `bytes` reflects the real file size.
   - **Option B — Defer with backlog ticket:** Run `/gsd-add-backlog` with a description like "Replace Option-D stub `data/raw/ofgem/<file>` with real Ofgem export once RER credentials are plumbed". Record the backlog ID in `05-13-SUMMARY.md`.
5. Record outcome per file in the SUMMARY.md: `DONE-REPLACED` / `DEFERRED-BACKLOG-<id>` / `N/A-real-download-already-landed`.
  </how-to-verify>
  <verify>Manual inspection + optional `uv run python scripts/backfill_sidecars.py --force` if user replaces files.</verify>
  <done>Per-file decision recorded in 05-13-SUMMARY.md; if any files replaced, sidecars regenerated and diff committed under `chore(05-13): replace Ofgem stub with real export + refresh sidecars`.</done>
  <resume-signal>Type "task1-done" after recording per-file decision</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: mkdocs visual inspection of docs/schemes/ro.md</name>
  <files>docs/schemes/ro.md (inspection only; no edits unless defects found)</files>
  <what-built>
Plan 05-11 shipped `docs/schemes/ro.md` with the D-15 8-section structure, 4 chart embeds (S2 dynamics, S3 by-technology, S4 concentration, S5 forward projection), GOV-01 four-way coverage manifest, and cross-links to `/themes/cost/` + `/themes/recipients/` + `gas-counterfactual.md` methodology. `mkdocs build --strict` passes in the autonomous chain, but chart readability + narrative coherence are human-judgement checks.
  </what-built>
  <how-to-verify>
1. Launch the dev server:
   ```bash
   uv run mkdocs serve
   ```
2. Open `http://localhost:8000/schemes/ro/` in a browser.
3. Confirm visually:
   - [ ] Adversarial headline renders verbatim in the opening section: "The scheme you've never heard of, twice the size of the one you have"
   - [ ] Four charts (S2-S5) are visible and interactive (Plotly hover tooltips respond)
   - [ ] Cross-link to `/themes/cost/` works (click it; verify it navigates)
   - [ ] Cross-link to `/themes/recipients/` works
   - [ ] Methodology section cites REF Constable 2025 AND links to the `gas-counterfactual.md` methodology page
   - [ ] GOV-01 four-way coverage manifest at the bottom is intact (primary Ofgem URL + chart source permalinks + test permalink + reproduce bash block)
   - [ ] Nav tab "Schemes" is present at the top level
4. If any check fails: note the specific defect in `05-13-SUMMARY.md` and decide — fix now (commit under `fix(05-13): ...`) OR log as a backlog ticket via `/gsd-add-backlog`.
5. Kill the dev server (Ctrl-C).
  </how-to-verify>
  <verify>Manual browser inspection; optional `git commit` for any fix.</verify>
  <done>Visual-check outcome recorded in 05-13-SUMMARY.md; any defects either fixed-in-place or logged as backlog items.</done>
  <resume-signal>Type "task2-done" after browser inspection complete</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: REF Constable transcription audit</name>
  <files>tests/fixtures/benchmarks.yaml (inspection + optional amendment)</files>
  <what-built>
Plan 05-09 Task 1 transcribed REF Constable 2025-05-01 Table 1 into `tests/fixtures/benchmarks.yaml::ref_constable` (≥12 per-year entries for 2011-2022; targeting full 22-year 2002-2023 coverage). The autonomous chain cannot verify transcription fidelity — the reviewer must spot-check against the researcher's verbatim transcription in 05-RESEARCH.md §5.
  </what-built>
  <how-to-verify>
1. Open both files side-by-side:
   - `tests/fixtures/benchmarks.yaml::ref_constable`
   - `.planning/phases/05-ro-module/05-RESEARCH.md` §5 (REF Table 1 transcription lines ~1101-1122)
2. Spot-check at least 3-4 years — pick a mix (e.g. 2013, 2017, 2020, 2022). For each:
   - Compare `value_gbp_bn` in benchmarks.yaml vs the research-transcribed figure.
   - Confirm identical to at least 1 decimal place (£0.1 bn).
3. If any discrepancy found:
   - Amend `tests/fixtures/benchmarks.yaml` with the correct value.
   - Re-run:
     ```bash
     uv run pytest tests/test_benchmarks.py -k ref_constable -q
     ```
   - Expect the reconciliation test to either re-pass or surface a new divergence. Handle per Task 4 if divergence appears.
   - Commit as `fix(05-13): correct REF Constable transcription for year <YYYY>`.
4. Record outcome in 05-13-SUMMARY.md: `VERIFIED-CLEAN` / `AMENDED-<n>-entries` / `DEFERRED-BACKLOG-<id>`.
  </how-to-verify>
  <verify>Manual spot-check; optional `uv run pytest tests/test_benchmarks.py -k ref_constable -q` after amendments.</verify>
  <done>Audit result recorded in SUMMARY.md; any amendments committed atomically.</done>
  <resume-signal>Type "task3-done" after spot-check complete</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Divergence report review (if 05-09-DIVERGENCE.md exists)</name>
  <files>.planning/phases/05-ro-module/05-09-DIVERGENCE.md (conditional — only present if Plan 09 detected >3% drift)</files>
  <what-built>
If Plan 05-09 Task 2 found REF reconciliation divergence >3%, the executor did NOT stop or escalate — instead it wrote `.planning/phases/05-ro-module/05-09-DIVERGENCE.md` with the per-year divergence table + root-cause hypothesis + suggested remediation, and `test_ref_constable_ro_reconciliation` xfails while this sentinel file exists. If no divergence was found, this file does not exist and Task 4 is a no-op.
  </what-built>
  <how-to-verify>
1. Check for the sentinel:
   ```bash
   ls -la .planning/phases/05-ro-module/05-09-DIVERGENCE.md 2>/dev/null && echo "EXISTS" || echo "NOT PRESENT — skip Task 4"
   ```
2. If NOT PRESENT — record `N/A — no divergence` in SUMMARY.md and proceed to Task 5.
3. If EXISTS — read the full report. Decide on one of three remediation paths:
   - **(a) Widen tolerance with audit-header note** — if the divergence is a known scope difference (e.g. REF excludes a category our pipeline includes), update `tests/fixtures/benchmarks.yaml` audit header with the scope note, and bump `REF_TOLERANCE_PCT` in `tests/test_benchmarks.py` to a justifiable level (e.g. 5.0%). MUST also add `CHANGES.md ## Methodology versions` entry per D-07. Delete the sentinel: `rm .planning/phases/05-ro-module/05-09-DIVERGENCE.md`. Re-run `uv run pytest tests/test_benchmarks.py -k ref_constable -q` — expect pass.
   - **(b) Follow-up pipeline-fix plan** — if the divergence indicates a genuine pipeline regression (banding error, carbon-price extension bug), run `/gsd-add-backlog` with a description pointing to the divergence file + suggested fix. Leave the sentinel in place (xfail persists). Record backlog ID in SUMMARY.md.
   - **(c) Defer** — if the divergence is small and contextually acceptable (e.g. ±4% for one year with documented scope reason), leave everything as-is and record the rationale in SUMMARY.md + a backlog ticket for future re-investigation.
4. Commit as `refactor(05-13): resolve REF reconciliation divergence per 05-09-DIVERGENCE.md`.
  </how-to-verify>
  <verify>Manual decision; optional `uv run pytest tests/test_benchmarks.py -k ref_constable -q` after remediation; optional `/gsd-add-backlog` for deferred items.</verify>
  <done>Divergence disposition recorded in SUMMARY.md; if path (a) chosen, CHANGES.md `## Methodology versions` entry committed + sentinel deleted.</done>
  <resume-signal>Type "task4-done" (or "task4-na" if sentinel absent)</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Investigation report review — real-scraper backlog decision</name>
  <files>.planning/phases/05-ro-module/05-01-TASK-1-INVESTIGATION.md (inspection only)</files>
  <what-built>
Plan 05-01 Task 1 committed `05-01-TASK-1-INVESTIGATION.md` with Options A/B/C/D assessment + `## Plan 05-13 Follow-Ups` section listing any deferred plumbing. If the autonomous chain adopted Option D, the user now decides whether to invest in the real-scraper path (credentials + Playwright + CI secrets) as a future phase.
  </what-built>
  <how-to-verify>
1. Open `.planning/phases/05-ro-module/05-01-TASK-1-INVESTIGATION.md`.
2. Read the `## Plan 05-13 Follow-Ups` section at the bottom.
3. If the chain landed on Option B/C (real scrapers) — record `N/A — real-scraper already live` in SUMMARY.md and proceed to Task 6.
4. If the chain landed on Option D — decide:
   - **Plumb now**: create a follow-up phase plan for "Ofgem RER programmatic scraper with SharePoint-OIDC auth" via `/gsd-add-backlog` with priority ≥ 999.x (post-milestone). Include: required env vars (OFGEM_RER_EMAIL, OFGEM_RER_SESSION_COOKIE), CI-secret wiring, Playwright dependency, replacement of stub-scraper RuntimeError paths with real logic.
   - **Defer indefinitely**: record rationale (e.g. "stub files adequate until SY23 data horizon advances; revisit in 12 months") in SUMMARY.md; no backlog ticket needed.
5. Record outcome: `BACKLOG-ADDED-<id>` / `DEFERRED-INDEFINITELY` / `N/A`.
  </how-to-verify>
  <verify>Manual read; optional `/gsd-add-backlog` invocation.</verify>
  <done>Outcome recorded in SUMMARY.md; backlog ID noted if applicable.</done>
  <resume-signal>Type "task5-done"</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: validate() invariants inspection</name>
  <files>(no edits — runtime inspection of schemes.ro.validate() output)</files>
  <what-built>
`schemes.ro.validate()` implements 4 D-04 post-rebuild sanity checks (banding divergence, REF drift, methodology_version consistency, forward-projection sanity) per VALIDATION.md §Critical Invariants I-1..I-4. An empty-list return is clean; warnings surface silent quality issues the hard-block tests wouldn't catch.
  </what-built>
  <how-to-verify>
1. Run:
   ```bash
   uv run python -c "from uk_subsidy_tracker.schemes.ro import validate; warnings = validate(); print('\\n'.join(warnings) if warnings else 'OK — no warnings')"
   ```
2. Inspect output:
   - `OK — no warnings` — record `CLEAN` in SUMMARY.md and proceed.
   - Non-empty warning list — read each warning. Each maps to one of:
     - I-1 banding divergence — cross-check with 05-02 ro_bandings.yaml and 05-05 cost_model.py bandings cross-check; if warning reflects real drift, open a fix-up backlog ticket.
     - I-2 REF drift — should already be triaged in Task 4; if still present, re-check sentinel logic.
     - I-3 methodology_version drift — usually indicates a regression; record and likely open a fix-up ticket.
     - I-4 forward-projection sanity — check whether the flagged technology has genuine data issues; open ticket if needed.
3. Record outcome: `CLEAN` / `WARNINGS-<n>-<disposition>` (where disposition = `FIXED` / `BACKLOG-<id>` / `DEFERRED`).
  </how-to-verify>
  <verify>`uv run python -c "from uk_subsidy_tracker.schemes.ro import validate; ..."` — output reviewed by user.</verify>
  <done>validate() output reviewed; outcome recorded in SUMMARY.md.</done>
  <resume-signal>Type "task6-done"</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: Phase 5 close signal + SUMMARY.md + backlog cleanup</name>
  <files>.planning/phases/05-ro-module/05-13-SUMMARY.md (create), CHANGES.md (conditional), .planning/STATE.md (optional update)</files>
  <what-built>
All 6 prior tasks have recorded individual outcomes. Task 7 consolidates into the Phase 5 close-out artefact and commits any remaining touchups.
  </what-built>
  <how-to-verify>
1. Create `.planning/phases/05-ro-module/05-13-SUMMARY.md` with the following structure:
   ```markdown
   # Plan 05-13 — Post-Execution Human Review Summary

   Date: <YYYY-MM-DD>
   Reviewer: <name / handle>

   ## Checklist Outcomes

   | Task | Item | Status | Notes |
   |------|------|--------|-------|
   | 1 | Ofgem raw files audit — ro-register.xlsx | <DONE-REPLACED / DEFERRED-BACKLOG-<id> / N/A> | ... |
   | 1 | Ofgem raw files audit — ro-generation.csv | <...> | ... |
   | 1 | Ofgem raw files audit — roc-prices.csv | <...> | ... |
   | 2 | mkdocs visual inspection | <CLEAN / DEFECTS-FIXED / DEFECTS-BACKLOG-<id>> | ... |
   | 3 | REF Constable transcription audit | <VERIFIED-CLEAN / AMENDED-<n>> | ... |
   | 4 | Divergence report review | <N/A / RESOLVED-(a)/(b)/(c)> | ... |
   | 5 | Investigation report follow-ups | <N/A / BACKLOG-ADDED-<id> / DEFERRED> | ... |
   | 6 | validate() invariants | <CLEAN / WARNINGS-<n>-<disposition>> | ... |

   ## Backlog Items Created

   - <none> OR <list with IDs>

   ## Methodology Changes

   - <none> OR <list with CHANGES.md ## Methodology versions entry references>

   ## Phase 5 Status

   <APPROVED / REVISE>
   ```
2. If any CHANGES.md `## Methodology versions` entries were added (tolerance bump in Task 4 path (a), for example), verify they landed in CHANGES.md.
3. Commit touchups as `chore(05-13): post-review follow-ups + Phase 5 close`.
4. Update `.planning/STATE.md` if desired to mark Phase 5 as fully closed (including post-review):
   ```bash
   node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" state planned-phase --phase "5" --name "RO Module" --plans "13"
   ```
5. If everything is satisfactory: type `approved`. If any revision is required on prior tasks: type `revise <specifics>`.
  </how-to-verify>
  <verify>Manual: SUMMARY.md exists + commit landed.</verify>
  <done>`.planning/phases/05-ro-module/05-13-SUMMARY.md` committed with per-task outcomes; any methodology-version entries in CHANGES.md; user types `approved`.</done>
  <resume-signal>Type "approved" to close Phase 5 (or "revise" with specifics)</resume-signal>
</task>

</tasks>

<verification>
- `test -f .planning/phases/05-ro-module/05-13-SUMMARY.md` → file exists
- `grep -E "^\| [1-6] " .planning/phases/05-ro-module/05-13-SUMMARY.md | wc -l` → ≥8 rows (Task 1 has 3 file rows; Tasks 2-6 one each)
- `grep -q "Phase 5 Status" .planning/phases/05-ro-module/05-13-SUMMARY.md` → found
- If Task 4 chose path (a): `grep -q "REF_TOLERANCE_PCT" CHANGES.md` → found in `## Methodology versions` section
</verification>

<success_criteria>
- [ ] All autonomous Plans 05-01..05-12 completed before opening Plan 13
- [ ] Every checklist item (Tasks 1-6) has a recorded outcome in 05-13-SUMMARY.md
- [ ] Any Option-D stubs have a documented disposition (replaced OR deferred)
- [ ] docs/schemes/ro.md visually approved by user under mkdocs serve
- [ ] REF Constable transcription spot-checked for ≥3 years
- [ ] 05-09-DIVERGENCE.md (if present) triaged via path (a), (b), or (c)
- [ ] validate() warnings reviewed
- [ ] 05-13-SUMMARY.md committed with `APPROVED` status
- [ ] Phase 5 is fully closed (STATE.md reflects completion if updated)
</success_criteria>

<output>
After completion, `.planning/phases/05-ro-module/05-13-SUMMARY.md` is the Phase 5 close-out artefact. Phase 6 planning cites this file + the Plan 05-12 summary as the Phase 5 ledger.
</output>
</content>
</invoke>