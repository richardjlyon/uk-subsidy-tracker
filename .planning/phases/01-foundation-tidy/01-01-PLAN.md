---
phase: 01-foundation-tidy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - CLAUDE.md
  - src/cfd_payment/__init__.py
  - src/cfd_payment/counterfactual.py
  - src/cfd_payment/data/__init__.py
  - src/cfd_payment/data/elexon.py
  - src/cfd_payment/data/lccc.py
  - src/cfd_payment/data/ons_gas.py
  - src/cfd_payment/data/utils.py
  - src/cfd_payment/plotting/__init__.py
  - src/cfd_payment/plotting/__main__.py
  - src/cfd_payment/plotting/chart_builder.py
  - src/cfd_payment/plotting/colors.py
  - src/cfd_payment/plotting/theme.py
  - src/cfd_payment/plotting/utils.py
  - src/cfd_payment/plotting/cannibalisation/__init__.py
  - src/cfd_payment/plotting/cannibalisation/capture_ratio.py
  - src/cfd_payment/plotting/cannibalisation/price_vs_wind.py
  - src/cfd_payment/plotting/capacity_factor/__init__.py
  - src/cfd_payment/plotting/capacity_factor/monthly.py
  - src/cfd_payment/plotting/capacity_factor/seasonal.py
  - src/cfd_payment/plotting/intermittency/__init__.py
  - src/cfd_payment/plotting/intermittency/generation_heatmap.py
  - src/cfd_payment/plotting/intermittency/load_duration.py
  - src/cfd_payment/plotting/intermittency/rolling_minimum.py
  - src/cfd_payment/plotting/subsidy/__init__.py
  - src/cfd_payment/plotting/subsidy/bang_for_buck.py
  - src/cfd_payment/plotting/subsidy/bang_for_buck_old.py
  - src/cfd_payment/plotting/subsidy/cfd_dynamics.py
  - src/cfd_payment/plotting/subsidy/cfd_payments_by_category.py
  - src/cfd_payment/plotting/subsidy/cfd_vs_gas_cost.py
  - src/cfd_payment/plotting/subsidy/lorenz.py
  - src/cfd_payment/plotting/subsidy/remaining_obligations.py
  - src/cfd_payment/plotting/subsidy/scissors.py
  - src/cfd_payment/plotting/subsidy/subsidy_per_avoided_co2_tonne.py
  - tests/data/test_lccc.py
  - tests/data/test_ons.py
  - demo_dark_theme.py
autonomous: true
requirements:
  - FND-01
tags: [rename, packaging, imports, brownfield]

must_haves:
  truths:
    - "uv run python -c 'import uk_subsidy_tracker' succeeds (ROADMAP success criterion 1)"
    - "No source file, test, or repo-root script references the old `cfd_payment` package path"
    - "pyproject.toml declares name = uk-subsidy-tracker and packages = [src/uk_subsidy_tracker]"
    - "pyproject.toml declares requires-python = >=3.12 (matches CLAUDE.md 'Python 3.12+ only' constraint)"
    - "CLAUDE.md ## Runtime section agrees with pyproject.toml on the >=3.12 floor"
    - "Entry point uv run python -m uk_subsidy_tracker.plotting resolves to the renamed package"
  artifacts:
    - path: "src/uk_subsidy_tracker/__init__.py"
      provides: "Renamed top-level package with PROJECT_ROOT, DATA_DIR, OUTPUT_DIR constants"
      contains: "PROJECT_ROOT = Path(__file__).parent.parent.parent"
    - path: "src/uk_subsidy_tracker/plotting/__main__.py"
      provides: "Renamed batch chart entry point"
      contains: "from uk_subsidy_tracker.plotting"
    - path: "pyproject.toml"
      provides: "Project metadata pointing at the renamed package with >=3.12 Python floor"
      contains: 'name = "uk-subsidy-tracker"'
    - path: "uv.lock"
      provides: "Regenerated lockfile matching >=3.12 requires-python"
      contains: "uk-subsidy-tracker"
  key_links:
    - from: "src/uk_subsidy_tracker/plotting/__main__.py"
      to: "uk_subsidy_tracker.plotting.subsidy.*"
      via: "from uk_subsidy_tracker.plotting.subsidy.<module> import main as <alias>"
      pattern: "from uk_subsidy_tracker\\.plotting"
    - from: "pyproject.toml"
      to: "src/uk_subsidy_tracker/"
      via: "[tool.hatch.build.targets.wheel] packages"
      pattern: 'packages = \\["src/uk_subsidy_tracker"\\]'
    - from: "tests/data/test_lccc.py and tests/data/test_ons.py"
      to: "uk_subsidy_tracker.data"
      via: "test imports"
      pattern: "from uk_subsidy_tracker\\.data"
    - from: "CLAUDE.md ## Runtime"
      to: "pyproject.toml requires-python"
      via: "single-source-of-truth agreement on Python floor"
      pattern: 'requires-python = ">=3.12"'
---

<objective>
Hard-cut rename the Python package from `cfd_payment` to `uk_subsidy_tracker` across the entire working tree in a single atomic commit, per D-13/D-14/D-15. Every `from cfd_payment` import, every module-path string in `pyproject.toml` and `demo_dark_theme.py`, and the `git mv` of the package directory itself land together so `uv run python -c 'import uk_subsidy_tracker'` succeeds at phase exit.

This plan also resolves the pre-existing contradiction between CLAUDE.md's Constraints block ("Python 3.12+ only") and its Runtime section ("Python 3.11 or later"). The user-confirmed decision is BUMP TO >=3.12: `pyproject.toml` requires-python is bumped, the lockfile is regenerated, and CLAUDE.md's Runtime section is updated to match.

Purpose: FND-01 is the Phase 1 gate — all downstream work in Phase 2+ assumes `uk_subsidy_tracker` is the package name. No backward-compatibility alias. This plan does ONLY the mechanical rename + Python-floor bump; mkdocs.yml identity, root-doc commits, and the `about/` page are separate plans in Wave 2.

Output: Renamed `src/uk_subsidy_tracker/` tree (history preserved via `git mv`), updated `pyproject.toml` (name + packages + requires-python), regenerated `uv.lock`, CLAUDE.md Runtime section aligned to >=3.12, all 24 `from cfd_payment` imports rewritten, `demo_dark_theme.py` imports rewritten, both test files rewritten.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-tidy/01-CONTEXT.md
@.planning/phases/01-foundation-tidy/01-PATTERNS.md
@CLAUDE.md
@ARCHITECTURE.md
@pyproject.toml

<interfaces>
<!-- Extracted from src/cfd_payment/__init__.py — this content is preserved verbatim; only directory path changes. -->

```python
# src/cfd_payment/__init__.py  →  src/uk_subsidy_tracker/__init__.py (identical content)
from pathlib import Path

PROJECT_ROOT = Path(__file__).parent.parent.parent
DATA_DIR = PROJECT_ROOT / "data"
OUTPUT_DIR = PROJECT_ROOT / "docs" / "charts" / "html"
```

Absolute-import call-site shape preserved across rename (CLAUDE.md Import Organization):
```python
# before
from cfd_payment import DATA_DIR
from cfd_payment.data import load_gas_price
from cfd_payment.plotting.subsidy.cfd_dynamics import main as cfd_dynamics

# after
from uk_subsidy_tracker import DATA_DIR
from uk_subsidy_tracker.data import load_gas_price
from uk_subsidy_tracker.plotting.subsidy.cfd_dynamics import main as cfd_dynamics
```

Relative imports within adjacent submodules (e.g., `from .lccc import ...`) are unaffected and must NOT be rewritten.

CLAUDE.md ## Runtime target state (line 35 currently reads `>=3.11`):
```markdown
## Runtime
- Python 3.12 or later (declared in `pyproject.toml`: `requires-python = ">=3.12"`)
- Currently running on Python 3.13 (based on `.venv` structure)
```
The other `Python 3.11+` mentions in CLAUDE.md at lines 30, 79, 126, 180, 208 are descriptive/historical — leave them for a Phase-3 housekeeping pass. Only line 35 is the "single source of truth" declaration that contradicts pyproject.toml.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: git mv src/cfd_payment → src/uk_subsidy_tracker; bump pyproject.toml (name, packages, requires-python); regenerate uv.lock; align CLAUDE.md Runtime section</name>
  <files>
    src/uk_subsidy_tracker/ (via git mv from src/cfd_payment/),
    pyproject.toml,
    uv.lock,
    CLAUDE.md
  </files>
  <read_first>
    - .planning/phases/01-foundation-tidy/01-CONTEXT.md (D-13, D-14: hard cut, git mv)
    - .planning/phases/01-foundation-tidy/01-PATTERNS.md § "pyproject.toml (config, self-edit)"
    - pyproject.toml (lines 1-30 — current state, especially line 5 `requires-python = ">=3.11"`)
    - CLAUDE.md lines 14 (Constraints "Python 3.12+ only") and 35 (Runtime "Python 3.11 or later") — confirm the contradiction before editing
    - src/cfd_payment/__init__.py (confirm content before move; content is preserved)
  </read_first>
  <action>
    Step 1. Run `git mv src/cfd_payment src/uk_subsidy_tracker` from repo root. Do NOT use `mv` or copy-then-delete — `git mv` preserves file history per D-14. Verify with `git status` that every file appears as `renamed:` (not `deleted` + `new file`).

    Step 2. Edit `pyproject.toml` with these exact changes:
    - Line 2: `name = "cfd-payment"` → `name = "uk-subsidy-tracker"`
    - Line 4: `description = "Analysis of UK Contracts for Difference (CfD) renewable energy subsidy payments"` → `description = "Independent, open, data-driven audit of UK renewable electricity subsidy costs"`
    - Line 5: `requires-python = ">=3.11"` → `requires-python = ">=3.12"` (per user decision BUMP TO >=3.12; matches CLAUDE.md Constraints "Python 3.12+ only")
    - Line 29: `packages = ["src/cfd_payment"]` → `packages = ["src/uk_subsidy_tracker"]`
    - Leave `version = "0.1.0"` unchanged (matches CITATION.cff seed per CONTEXT.md Claude's Discretion).
    - Leave all `dependencies` unchanged.

    Step 3. Regenerate the lockfile. The rename of the project name and the requires-python bump both require a fresh lock:
    ```bash
    uv lock
    ```
    This rewrites `uv.lock` in place. Then verify the environment still resolves:
    ```bash
    uv sync
    ```
    Both commands must exit 0. If `uv lock` fails because of the >=3.12 bump (e.g., a transitive dependency still claims <3.12), surface the error — do not silently fall back to 3.11.

    Step 4. Edit `CLAUDE.md` line 35 to align with the new Python floor. Exact change:
    - Before: `- Python 3.11 or later (declared in `pyproject.toml`: `requires-python = ">=3.11"`)`
    - After:  `- Python 3.12 or later (declared in `pyproject.toml`: `requires-python = ">=3.12"`)`

    Do NOT touch the other `Python 3.11+` mentions in CLAUDE.md (lines 30, 79, 126, 180, 208) — those are descriptive bullets in non-authoritative sections and are out of scope for this plan; Phase 3 housekeeping can sweep them if desired. The ONLY line that must change is line 35, the Runtime single-source-of-truth declaration that currently contradicts Constraints line 14.

    Do NOT yet edit any Python file imports, docs, mkdocs.yml, or README — those are separate tasks in this plan and in Plan 02/03.
  </action>
  <verify>
    <automated>git status --porcelain | grep '^R' | grep -c 'src/cfd_payment -> src/uk_subsidy_tracker' ; test -d src/uk_subsidy_tracker && test ! -d src/cfd_payment ; grep -q 'name = "uk-subsidy-tracker"' pyproject.toml ; grep -q 'packages = \["src/uk_subsidy_tracker"\]' pyproject.toml ; grep -q 'requires-python = ">=3.12"' pyproject.toml ; grep -q 'requires-python = ">=3.12"' CLAUDE.md ; uv sync 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `test -d src/uk_subsidy_tracker` exits 0
    - `test ! -d src/cfd_payment` exits 0 (old directory gone)
    - `git status` shows renamed entries for every file under the old path (no `deleted:` + `new file:` pairs)
    - `grep -c 'name = "uk-subsidy-tracker"' pyproject.toml` returns 1
    - `grep -c 'name = "cfd-payment"' pyproject.toml` returns 0
    - `grep -c 'packages = \["src/uk_subsidy_tracker"\]' pyproject.toml` returns 1
    - `grep -c 'packages = \["src/cfd_payment"\]' pyproject.toml` returns 0
    - `grep pyproject.toml -cE '^requires-python = ">=3.12"$'` returns 1
    - `grep -c 'requires-python = ">=3.11"' pyproject.toml` returns 0
    - `grep -c 'requires-python = ">=3.12"' CLAUDE.md` returns 1 (Runtime section aligned — B-3 fix)
    - `grep -c 'Python 3.12 or later' CLAUDE.md` returns 1 (prose also aligned)
    - `uv sync` exits 0 (environment resolves against the bumped floor)
    - `uv.lock` has been regenerated (timestamp newer than pre-task state; `git diff --stat uv.lock` shows changes)
    - No imports edited yet — at this point `uv run python -c 'import uk_subsidy_tracker'` will FAIL because submodules still contain `from cfd_payment` statements; that is fixed in Task 2.
  </acceptance_criteria>
  <done>Package directory renamed via git mv with history preserved; pyproject.toml declares the new package name, wheel path, and >=3.12 Python floor; uv.lock regenerated; CLAUDE.md Runtime section aligned with Constraints on >=3.12.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite all cfd_payment imports across Python sources, tests, and repo-root scripts</name>
  <files>
    src/uk_subsidy_tracker/counterfactual.py,
    src/uk_subsidy_tracker/data/__init__.py,
    src/uk_subsidy_tracker/data/elexon.py,
    src/uk_subsidy_tracker/data/lccc.py,
    src/uk_subsidy_tracker/data/ons_gas.py,
    src/uk_subsidy_tracker/data/utils.py,
    src/uk_subsidy_tracker/plotting/__init__.py,
    src/uk_subsidy_tracker/plotting/__main__.py,
    src/uk_subsidy_tracker/plotting/chart_builder.py,
    src/uk_subsidy_tracker/plotting/colors.py,
    src/uk_subsidy_tracker/plotting/theme.py,
    src/uk_subsidy_tracker/plotting/utils.py,
    src/uk_subsidy_tracker/plotting/cannibalisation/capture_ratio.py,
    src/uk_subsidy_tracker/plotting/cannibalisation/price_vs_wind.py,
    src/uk_subsidy_tracker/plotting/capacity_factor/monthly.py,
    src/uk_subsidy_tracker/plotting/capacity_factor/seasonal.py,
    src/uk_subsidy_tracker/plotting/intermittency/generation_heatmap.py,
    src/uk_subsidy_tracker/plotting/intermittency/load_duration.py,
    src/uk_subsidy_tracker/plotting/intermittency/rolling_minimum.py,
    src/uk_subsidy_tracker/plotting/subsidy/bang_for_buck.py,
    src/uk_subsidy_tracker/plotting/subsidy/bang_for_buck_old.py,
    src/uk_subsidy_tracker/plotting/subsidy/cfd_dynamics.py,
    src/uk_subsidy_tracker/plotting/subsidy/cfd_payments_by_category.py,
    src/uk_subsidy_tracker/plotting/subsidy/cfd_vs_gas_cost.py,
    src/uk_subsidy_tracker/plotting/subsidy/lorenz.py,
    src/uk_subsidy_tracker/plotting/subsidy/remaining_obligations.py,
    src/uk_subsidy_tracker/plotting/subsidy/scissors.py,
    src/uk_subsidy_tracker/plotting/subsidy/subsidy_per_avoided_co2_tonne.py,
    tests/data/test_lccc.py,
    tests/data/test_ons.py,
    demo_dark_theme.py
  </files>
  <read_first>
    - .planning/phases/01-foundation-tidy/01-PATTERNS.md § "src/cfd_payment/ → src/uk_subsidy_tracker/ (package rename)" (full file list + absolute-vs-relative import rule)
    - CLAUDE.md § "Import Organization" (absolute imports from package root; relative within adjacent submodules)
    - src/uk_subsidy_tracker/plotting/__main__.py (canonical alias-as-main pattern — confirm shape before editing)
    - demo_dark_theme.py (lines 8-10 — three cfd_payment imports)
    - tests/data/test_lccc.py and tests/data/test_ons.py (confirm existing cfd_payment references)
  </read_first>
  <action>
    Mechanical substitution across every file in the files list. Two exact token replacements per file, applied with the Edit tool file-by-file (do NOT use `sed -i` or similar — this project does not permit unsupervised bulk sed, and the agent must verify each file post-edit):

    Replacement A (import statement prefix):
    - `from cfd_payment` → `from uk_subsidy_tracker`
    - Applies to both `from cfd_payment import X` and `from cfd_payment.submodule import Y` forms.

    Replacement B (dotted module path in code, rare — only where code references the string `cfd_payment.` outside an import, e.g., `importlib.import_module("cfd_payment.plotting")` if present):
    - `cfd_payment.` → `uk_subsidy_tracker.`
    - Only apply if preceded by a module-access context (not inside prose strings that legitimately describe the "CfD" scheme name).

    DO NOT change:
    - Relative imports `from .lccc import ...`, `from .utils import ...` — these are adjacent-submodule relative imports (CLAUDE.md convention) and are unaffected by the package rename.
    - Function names, class names, variable names, docstring prose describing "CfD" as a scheme (per CONTEXT.md Claude's Discretion: "leave narrative text describing the 'CfD' scheme untouched").
    - Constants like `CCGT_EFFICIENCY`, `TECHNOLOGY_COLORS`.

    Per-file process:
    1. Read file.
    2. Locate every `from cfd_payment` occurrence.
    3. Replace with `from uk_subsidy_tracker` preserving the rest of the line exactly.
    4. Write file.
    5. After all files edited, run the verify command to confirm zero residual matches.

    Edge cases to handle explicitly:
    - `demo_dark_theme.py` lines 8-10: three imports — `from cfd_payment.plotting import ChartBuilder`, `from cfd_payment.plotting.colors import GENERATION_COLORS`, `from cfd_payment.plotting.theme import register_cfd_dark_theme` — all rewrite to `from uk_subsidy_tracker.plotting...`
    - `tests/data/test_lccc.py` and `tests/data/test_ons.py`: both contain cfd_payment references per PATTERNS.md; rewrite imports.
    - `src/uk_subsidy_tracker/plotting/__main__.py`: contains ~15 alias-as-main imports (the `from cfd_payment.plotting.<subpkg>.<mod> import main as <alias>` pattern). Every one rewrites to `from uk_subsidy_tracker.plotting...`.

    After all files edited, run `uv sync` (no-op if Task 1's sync is still fresh) and then `uv run python -c 'import uk_subsidy_tracker'` to confirm the package imports cleanly.
  </action>
  <verify>
    <automated>! grep -r "from cfd_payment" src/ tests/ demo_dark_theme.py 2>/dev/null && ! grep -rE "\\bcfd_payment\\." src/ tests/ demo_dark_theme.py --include="*.py" 2>/dev/null && uv run python -c 'import uk_subsidy_tracker' && uv run python -c 'from uk_subsidy_tracker import DATA_DIR, OUTPUT_DIR, PROJECT_ROOT; print(DATA_DIR)'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "from cfd_payment" src/ tests/ demo_dark_theme.py` returns zero matches (exit code 1)
    - `grep -rE "\bcfd_payment\." src/ tests/ demo_dark_theme.py --include="*.py"` returns zero matches in Python code (docstring prose describing the CfD scheme is excluded; grep on `.py` files only)
    - `uv run python -c 'import uk_subsidy_tracker'` exits 0
    - `uv run python -c 'from uk_subsidy_tracker import DATA_DIR, OUTPUT_DIR, PROJECT_ROOT'` exits 0
    - `uv run python -c 'from uk_subsidy_tracker.counterfactual import compute_counterfactual'` exits 0
    - `uv run python -c 'from uk_subsidy_tracker.plotting.chart_builder import ChartBuilder'` exits 0
    - `uv run python -c 'from uk_subsidy_tracker.data import load_gas_price'` exits 0
    - Every relative import (`from .`) in the rewritten tree is unchanged — spot-check `src/uk_subsidy_tracker/data/__init__.py` still uses `from .lccc import ...`.
    - `git status` shows modifications only to the files listed in `files_modified` — no stray changes to docs/, mkdocs.yml, or root docs (those are Plan 02/03 scope).
  </acceptance_criteria>
  <done>Package imports cleanly at phase gate; ROADMAP success criterion 1 satisfied; no residual `cfd_payment` module references anywhere in the Python source tree, tests, or repo-root scripts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → repo | Mechanical rename touches 30+ files; risk of introducing syntax errors or missed references that cascade to downstream scheme work |
| local → GitHub | Package import declarations will later be read by CI (Phase 2); a silent partial rename would cause green local imports but red CI |
| pyproject.toml ↔ CLAUDE.md | Two "single source of truth" declarations for Python version floor; drift between them silently misleads downstream readers about runtime support |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Python source tree (cfd_payment → uk_subsidy_tracker sweep) | mitigate | Use `git mv` (D-14) to preserve file history; grep-based acceptance criteria enforce zero residual `from cfd_payment` matches; `uv run python -c 'import uk_subsidy_tracker'` is the hard gate in Task 2's verify |
| T-01-02 | Denial of Service | Downstream phases (Phase 2+ depend on uk_subsidy_tracker package) | mitigate | Single atomic commit per CONTEXT.md D-15; no backward-compat alias (D-13) — fail fast if any site missed, rather than limp along on a shim |
| T-01-03 | Information Disclosure | pyproject.toml + uv.lock | accept | Package name, version, and Python floor are public metadata; no secrets in scope for these files |
| T-01-04 | Tampering | `demo_dark_theme.py` at repo root | mitigate | Included explicitly in file list per PATTERNS.md (3 occurrences); grep verification catches any miss |
| T-01-19 | Repudiation | CLAUDE.md Runtime vs Constraints contradiction | mitigate | Task 1 edits CLAUDE.md line 35 to align with Constraints line 14; acceptance criterion greps for `requires-python = ">=3.12"` in BOTH files so drift is caught at verify time |
</threat_model>

<verification>
Phase-level checks after both tasks land:

1. `test -d src/uk_subsidy_tracker && test ! -d src/cfd_payment` exits 0
2. `grep -r "from cfd_payment" src/ tests/ demo_dark_theme.py` returns zero matches
3. `uv run python -c 'import uk_subsidy_tracker'` exits 0 (ROADMAP success criterion 1)
4. `uv run python -c 'from uk_subsidy_tracker.plotting import ChartBuilder'` exits 0
5. `grep -c 'name = "uk-subsidy-tracker"' pyproject.toml` returns 1
6. `grep -c 'requires-python = ">=3.12"' pyproject.toml` returns 1 AND `grep -c 'requires-python = ">=3.12"' CLAUDE.md` returns 1 (B-3 resolved)
7. `uv sync` exits 0 (environment resolves against the bumped floor)
8. `git log --name-status` on the HEAD commit shows `R` (renamed) entries, not parallel `D` + `A` pairs
</verification>

<success_criteria>
- ROADMAP success criterion 1 met: `uv run python -c 'import uk_subsidy_tracker'` succeeds
- FND-01 delivered: Python package path, pyproject.toml, imports all updated to `uk_subsidy_tracker`
- B-3 resolved: pyproject.toml `requires-python` and CLAUDE.md Runtime both declare `>=3.12`, matching CLAUDE.md Constraints ("Python 3.12+ only")
- uv.lock regenerated and `uv sync` exits 0 under the bumped floor
- No `cfd_payment` module references remain in Python source, tests, or repo-root scripts
- File history preserved (all rename entries visible as `R` in git status/log)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-tidy/01-01-SUMMARY.md` describing:
- git mv command used and file count renamed
- list of files where imports were rewritten (final count)
- output of `uv run python -c 'import uk_subsidy_tracker'` verification
- confirmation of the Python-floor bump: before/after `requires-python` values in pyproject.toml; before/after CLAUDE.md line 35 text; `uv lock` + `uv sync` exit codes
- any files where the rename was skipped and why
- hand-off note to Plan 02 (mkdocs.yml still references `cfd_payment.plotting` in its header comment; will be swept there)
- hand-off note to Plan 03 (README.md creation deferred to Plan 03 since it didn't exist before this phase; CHANGES.md 0.1.0 entry must record the Python-floor bump per B-3)
</output>
</output>
