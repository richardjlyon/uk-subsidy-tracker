---
phase: 01-foundation-tidy
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - mkdocs.yml
  - docs/methodology/gas-counterfactual.md
  - docs/index.md
  - docs/charts/index.md
  - docs/charts/subsidy/cfd-dynamics.md
  - docs/charts/subsidy/cfd-vs-gas-cost.md
  - docs/charts/subsidy/remaining-obligations.md
autonomous: true
requirements:
  - FND-02
tags: [mkdocs, material-theme, navigation, docs-ia]

must_haves:
  truths:
    - "MkDocs Material theme is the active theme; readthedocs theme is gone (ROADMAP success criterion 2)"
    - "mkdocs.yml site identity reflects the renamed repo: site_name, site_url, repo_url, repo_name"
    - "Light + dark palette toggle is configured per D-03"
    - "Gas counterfactual page lives under docs/methodology/ (not docs/technical-details/) per D-11"
    - "docs/index.md and docs/charts/index.md no longer contain any `cfd-payment`/`cfd_payment` URL or module-path tokens"
    - "All three chart pages (cfd-dynamics.md, cfd-vs-gas-cost.md, remaining-obligations.md) sweep clean of `cfd-payment`/`cfd_payment` tokens and dead `technical-details/` links"
    - "BOTH instances of the `richlyon/cfd-payment` URL typo are corrected to `richardjlyon/uk-subsidy-tracker` — one at methodology/gas-counterfactual.md:108 and one at cfd-vs-gas-cost.md (lines 162, 163)"
  artifacts:
    - path: "mkdocs.yml"
      provides: "Material theme config, renamed identity, updated nav, palette toggle"
      contains: "name: material"
    - path: "docs/methodology/gas-counterfactual.md"
      provides: "Relocated methodology page; inbound links updated"
      min_lines: 100
    - path: "docs/charts/subsidy/cfd-dynamics.md"
      provides: "Chart narrative page with updated source URLs and methodology link"
      contains: "uk_subsidy_tracker.plotting.subsidy.cfd_dynamics"
    - path: "docs/charts/subsidy/cfd-vs-gas-cost.md"
      provides: "Chart narrative page with updated source URLs, methodology link, and richlyon-typo fix"
      contains: "uk_subsidy_tracker.plotting.subsidy.cfd_vs_gas_cost"
    - path: "docs/charts/subsidy/remaining-obligations.md"
      provides: "Chart narrative page with updated source URLs"
      contains: "uk_subsidy_tracker.plotting.subsidy.remaining_obligations"
  key_links:
    - from: "mkdocs.yml nav"
      to: "docs/methodology/gas-counterfactual.md"
      via: "Gas Counterfactual nav entry"
      pattern: "methodology/gas-counterfactual\\.md"
    - from: "docs/index.md"
      to: "docs/methodology/gas-counterfactual.md"
      via: "inline 'Gas counterfactual' link"
      pattern: "methodology/gas-counterfactual\\.md"
    - from: "docs/charts/index.md"
      to: "docs/methodology/gas-counterfactual.md"
      via: "'compute the gas counterfactual identically' link"
      pattern: "\\.\\./methodology/gas-counterfactual\\.md"
    - from: "docs/charts/subsidy/cfd-dynamics.md"
      to: "docs/methodology/gas-counterfactual.md"
      via: "'See also' methodology link"
      pattern: "\\.\\./\\.\\./methodology/gas-counterfactual\\.md"
    - from: "docs/charts/subsidy/cfd-vs-gas-cost.md"
      to: "docs/methodology/gas-counterfactual.md"
      via: "'Gas alternative' methodology link"
      pattern: "\\.\\./\\.\\./methodology/gas-counterfactual\\.md"
    - from: "docs/methodology/gas-counterfactual.md"
      to: "src/uk_subsidy_tracker/counterfactual.py on GitHub"
      via: "Reproducibility section source link"
      pattern: "https://github\\.com/richardjlyon/uk-subsidy-tracker/blob/main/src/uk_subsidy_tracker/counterfactual\\.py"
---

<objective>
Swap MkDocs from the `readthedocs` theme to `material` with the opinionated baseline config locked in CONTEXT.md decisions D-01 through D-12: enable the six named features, configure the light/dark palette toggle, rename site identity to "UK Renewable Subsidy Tracker" with the new GitHub URL, add the `About → Corrections` and `About → Citation` nav entries, and relocate `docs/technical-details/gas-counterfactual.md` to `docs/methodology/gas-counterfactual.md`. Fix the stale `richlyon/cfd-payment` GitHub URL bug (two instances — one at methodology/gas-counterfactual.md:108 and a second at cfd-vs-gas-cost.md lines 162/163) while sweeping the docs tree. Sweep all five docs pages that contain `cfd-payment`/`cfd_payment` tokens or dead `technical-details/` links: `docs/index.md`, `docs/charts/index.md`, and the three chart pages `docs/charts/subsidy/cfd-dynamics.md`, `docs/charts/subsidy/cfd-vs-gas-cost.md`, `docs/charts/subsidy/remaining-obligations.md`.

This plan does NOT create the `docs/about/corrections.md` or `docs/about/citation.md` page content — that is Plan 03's scope. This plan only wires the nav entries; the content files are created by Plan 03 and must exist before the final build-strict check in Plan 04.

Rationale for extending the sweep to the three chart pages (resolves checker B-1): Phase 1 is a hard-cut rename with no alias, and Plan 04's `mkdocs build --strict` will fail on the dead `../../technical-details/gas-counterfactual.md` links in these pages once Task 1 here moves the target file. Although the chart pages show as modified/new in `git status` under Phase 3 content work, their `cfd_payment`/`cfd-payment` tokens and dead methodology links MUST be swept here in Phase 1 to keep the strict-build gate achievable.

Purpose: FND-02 (Material theme + `mkdocs build --strict` green); delivers ROADMAP success criterion 2 (modulo Plan 03 page content + Plan 04 final build check). Also completes the docs-IA cleanup of `technical-details/` that git status already started (three of four files in that directory are already `D` per `git status`).

Output: `mkdocs.yml` rewritten with Material theme/palette/nav; `docs/technical-details/gas-counterfactual.md` moved to `docs/methodology/gas-counterfactual.md` via `git mv` with internal references and the broken GitHub URL corrected; `docs/index.md`, `docs/charts/index.md`, and the three chart pages all swept for renamed tokens and dead methodology links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-tidy/01-CONTEXT.md
@.planning/phases/01-foundation-tidy/01-PATTERNS.md
@mkdocs.yml
@docs/index.md
@docs/charts/index.md
@docs/technical-details/gas-counterfactual.md
@docs/charts/subsidy/cfd-dynamics.md
@docs/charts/subsidy/cfd-vs-gas-cost.md
@docs/charts/subsidy/remaining-obligations.md

<interfaces>
<!-- The exact YAML target state for mkdocs.yml, derived from CONTEXT.md D-01..D-10 + PATTERNS.md. -->
<!-- Executor should use these verbatim — no synthesis required. -->

Target theme block:
```yaml
theme:
  name: material
  locale: en
  features:
    - navigation.tabs
    - navigation.sections
    - toc.follow
    - search.suggest
    - search.highlight
    - content.code.copy
  palette:
    - scheme: default
      toggle:
        icon: material/weather-night
        name: Switch to dark mode
    - scheme: slate
      toggle:
        icon: material/weather-sunny
        name: Switch to light mode
```

Target site-identity block:
```yaml
site_name: UK Renewable Subsidy Tracker
site_url: https://richardjlyon.github.io/uk-subsidy-tracker/
site_description: Independent, open, data-driven audit of UK renewable electricity subsidy costs
site_author: Richard Lyon

repo_name: uk-subsidy-tracker
repo_url: https://github.com/richardjlyon/uk-subsidy-tracker
```

Target nav block:
```yaml
nav:
  - Home: index.md
  - Charts:
      - Overview: charts/index.md
      - CfD Dynamics: charts/subsidy/cfd-dynamics.md
      - CfD vs Gas Cost: charts/subsidy/cfd-vs-gas-cost.md
      - Remaining Obligations: charts/subsidy/remaining-obligations.md
  - Gas Counterfactual: methodology/gas-counterfactual.md
  - About:
      - Corrections: about/corrections.md
      - Citation: about/citation.md
```

Target extra.social block (URL updated; structure preserved):
```yaml
extra:
  social:
    - icon: fontawesome/brands/github
      link: https://github.com/richardjlyon/uk-subsidy-tracker
```

Keep unchanged:
- `markdown_extensions:` (lines 33-66 in current file)
- `plugins:` (lines 68-70 — `search` only; no social-cards, glightbox, git-revision-date per D-04)
- `copyright:` (line 79 — update only if year needs changing; leave "Copyright © 2026 Richard Lyon" as-is)

Header comment block (lines 1-7) must also update:
- `# MkDocs Configuration for CfD Payment Analysis` → `# MkDocs Configuration for UK Renewable Subsidy Tracker`
- `#   uv run python -m cfd_payment.plotting` → `#   uv run python -m uk_subsidy_tracker.plotting`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Move gas-counterfactual.md to docs/methodology/ and fix broken GitHub URL</name>
  <files>
    docs/methodology/gas-counterfactual.md (via git mv from docs/technical-details/gas-counterfactual.md)
  </files>
  <read_first>
    - docs/technical-details/gas-counterfactual.md (full file — confirm line 108 bug and all module path references)
    - .planning/phases/01-foundation-tidy/01-PATTERNS.md § "docs/technical-details/gas-counterfactual.md → docs/methodology/gas-counterfactual.md" (canonical list of line-level changes)
    - .planning/phases/01-foundation-tidy/01-CONTEXT.md D-11 (move rationale)
  </read_first>
  <action>
    Step 1. From repo root: `mkdir -p docs/methodology` (the directory does not exist yet).

    Step 2. `git mv docs/technical-details/gas-counterfactual.md docs/methodology/gas-counterfactual.md`. Do NOT copy-and-delete — use `git mv` so MkDocs reviewers and future git log can track the page's history.

    Step 3. Edit `docs/methodology/gas-counterfactual.md` with four exact changes (all lines referenced against the current file before move):

    - **Line 108** (Reproducibility section, single-line markdown link):
      Before: `` The full calculation lives in [`src/cfd_payment/counterfactual.py`](https://github.com/richlyon/cfd-payment/blob/main/src/cfd_payment/counterfactual.py). ``
      After:  `` The full calculation lives in [`src/uk_subsidy_tracker/counterfactual.py`](https://github.com/richardjlyon/uk-subsidy-tracker/blob/main/src/uk_subsidy_tracker/counterfactual.py). ``

      Note two fixes on this line: (a) `cfd_payment` → `uk_subsidy_tracker` in both the display path and the URL path; (b) `richlyon` → `richardjlyon` in the URL (PATTERNS.md flags this as a pre-existing bug to fix during the move — this is the FIRST of TWO `richlyon/` typos in the docs tree; the second is in cfd-vs-gas-cost.md and is fixed in Task 3).

    - **Line 121** (code fence under "To reproduce the chart"):
      Before: `uv run python -m cfd_payment.plotting.subsidy.cfd_vs_gas_cost`
      After:  `uv run python -m uk_subsidy_tracker.plotting.subsidy.cfd_vs_gas_cost`

    - **Line 127** (Python code block):
      Before: `from cfd_payment.counterfactual import (`
      After:  `from uk_subsidy_tracker.counterfactual import (`

    - No other lines in the file require changes — the `Gas price / CCGT efficiency / Gas CO₂ intensity` table, the sensitivity scenarios, all caveats, constants table, and sensitivity overrides are all content that remains true after the rename.

    Step 4. If `docs/technical-details/` is now empty (it should be after the move — all other files in that directory were already deleted per `git status`), remove the directory: `rmdir docs/technical-details/` (only if empty; if any untracked/unrelated file lingers, leave it alone).
  </action>
  <verify>
    <automated>test -f docs/methodology/gas-counterfactual.md && test ! -f docs/technical-details/gas-counterfactual.md && ! grep -q 'richlyon/cfd-payment' docs/methodology/gas-counterfactual.md && grep -q 'richardjlyon/uk-subsidy-tracker/blob/main/src/uk_subsidy_tracker/counterfactual.py' docs/methodology/gas-counterfactual.md && ! grep -q 'cfd_payment\.' docs/methodology/gas-counterfactual.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/methodology/gas-counterfactual.md` exits 0
    - `test -f docs/technical-details/gas-counterfactual.md` exits non-zero (file gone)
    - `git log --follow docs/methodology/gas-counterfactual.md` shows history before the rename (confirms `git mv` not copy)
    - `grep -c 'richlyon/cfd-payment' docs/methodology/gas-counterfactual.md` returns 0 (URL bug fixed — first of two instances)
    - `grep -c 'richardjlyon/uk-subsidy-tracker/blob/main/src/uk_subsidy_tracker/counterfactual.py' docs/methodology/gas-counterfactual.md` returns 1 (the replacement GitHub link is present)
    - `grep -c 'cfd_payment\.' docs/methodology/gas-counterfactual.md` returns 0 (module-path references all swept)
    - `grep -c 'uk_subsidy_tracker.plotting.subsidy.cfd_vs_gas_cost' docs/methodology/gas-counterfactual.md` returns at least 1 (reproduction command present)
    - `grep -c 'from uk_subsidy_tracker.counterfactual import' docs/methodology/gas-counterfactual.md` returns 1
    - Prose describing the "CfD" scheme itself (e.g., "CfD-vs-gas chart", "CfD electricity cost") is preserved verbatim — those are scheme-name references, not package-path references.
  </acceptance_criteria>
  <done>Methodology page moved under the new `docs/methodology/` directory with git history preserved; the first of two `richlyon/cfd-payment` URL instances is fixed and all four module-path references are updated to `uk_subsidy_tracker`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite mkdocs.yml with Material theme, renamed identity, updated nav, header comment</name>
  <files>mkdocs.yml</files>
  <read_first>
    - mkdocs.yml (full file — confirm line numbers match PATTERNS.md expectations)
    - .planning/phases/01-foundation-tidy/01-CONTEXT.md D-01 through D-12 (locked decisions for every block)
    - .planning/phases/01-foundation-tidy/01-PATTERNS.md § "mkdocs.yml (config, self-edit — integration point)" (line-by-line before/after)
    - `<interfaces>` block above (full target YAML provided verbatim)
  </read_first>
  <action>
    Rewrite `mkdocs.yml` block-by-block. Use the Edit tool per block (not Write for the whole file — preserves the rest of the structure and lets the executor see each diff).

    Block 1: Header comment (lines 1-7). Replace verbatim:
    ```yaml
    # MkDocs Configuration for UK Renewable Subsidy Tracker
    #
    # IMPORTANT: Before building/serving docs, generate charts:
    #   uv run python -m uk_subsidy_tracker.plotting
    # This populates docs/charts/html/ with chart files (ignored by git)
    #   uv run mkdocs gh-deploy
    # This updates the GitHub Pages site
    ```

    Block 2: Site identity (lines 9-12). Replace verbatim:
    ```yaml
    site_name: UK Renewable Subsidy Tracker
    site_url: https://richardjlyon.github.io/uk-subsidy-tracker/
    site_description: Independent, open, data-driven audit of UK renewable electricity subsidy costs
    site_author: Richard Lyon
    ```

    Block 3: Repository (lines 14-16). Replace verbatim:
    ```yaml
    # Repository
    repo_name: uk-subsidy-tracker
    repo_url: https://github.com/richardjlyon/uk-subsidy-tracker
    ```

    Block 4: Theme (lines 18-21). Replace with the full target theme block from `<interfaces>` above — eight lines of features, six-line palette with light/dark toggle entries. Verbatim, including the two-space indent under `theme:`.

    Block 5: Nav (lines 23-31). Replace with the target nav block from `<interfaces>` above. Note: "CfD Dynamics (4-panel)" in the current nav becomes "CfD Dynamics" (drop the "(4-panel)" suffix — per D-10). The `Gas Counterfactual` entry changes path from `technical-details/gas-counterfactual.md` to `methodology/gas-counterfactual.md`. The `About` section is new.

    Block 6: Plugins (lines 68-70). Leave unchanged — only `- search`. Per D-04, no social-cards, git-revision-date, or glightbox in Phase 1.

    Block 7: Extra.social (lines 73-76). Update only the URL:
    ```yaml
    extra:
      social:
        - icon: fontawesome/brands/github
          link: https://github.com/richardjlyon/uk-subsidy-tracker
    ```

    Block 8: Copyright (line 79). Leave verbatim as `Copyright &copy; 2026 Richard Lyon`.

    DO NOT run `uv run mkdocs build --strict` at the end of this task — the nav entries `about/corrections.md` and `about/citation.md` point to files that don't exist yet (Plan 03 creates them). A strict build here will fail. Plan 04 runs the final build-strict check after Plan 03's files land.

    As a sanity check, do run `uv run mkdocs build 2>&1 | head -30` (non-strict) to confirm the YAML parses and Material theme loads without a fatal error — nav-target-missing warnings are expected and acceptable at this stage.
  </action>
  <verify>
    <automated>grep -E "^  name: material$" mkdocs.yml && grep -q "scheme: default" mkdocs.yml && grep -q "scheme: slate" mkdocs.yml && grep -q "navigation.tabs" mkdocs.yml && grep -q "content.code.copy" mkdocs.yml && grep -q "^site_name: UK Renewable Subsidy Tracker$" mkdocs.yml && grep -q "^repo_name: uk-subsidy-tracker$" mkdocs.yml && grep -q "https://github.com/richardjlyon/uk-subsidy-tracker" mkdocs.yml && grep -q "methodology/gas-counterfactual.md" mkdocs.yml && grep -q "about/corrections.md" mkdocs.yml && grep -q "about/citation.md" mkdocs.yml && ! grep -q "name: readthedocs" mkdocs.yml && ! grep -q "cfd-payment" mkdocs.yml && ! grep -q "cfd_payment" mkdocs.yml && ! (uv run mkdocs build 2>&1 | grep -Ev "(^INFO|nav entry.*not found)" | grep -qE "ERROR|Traceback")</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "name: material" mkdocs.yml` returns exactly 1 (theme block)
    - `grep -c "name: readthedocs" mkdocs.yml` returns 0
    - `grep -c "scheme: default" mkdocs.yml` returns 1 AND `grep -c "scheme: slate" mkdocs.yml` returns 1 (palette toggle wired)
    - `grep -c "navigation.tabs" mkdocs.yml` returns 1 AND `grep -c "content.code.copy" mkdocs.yml` returns 1 (features present)
    - `grep -c "^site_name: UK Renewable Subsidy Tracker$" mkdocs.yml` returns 1
    - `grep -c "^repo_name: uk-subsidy-tracker$" mkdocs.yml` returns 1
    - `grep -c "https://github.com/richardjlyon/uk-subsidy-tracker" mkdocs.yml` returns at least 2 (repo_url + extra.social.link)
    - `grep -c "methodology/gas-counterfactual.md" mkdocs.yml` returns 1
    - `grep -c "technical-details" mkdocs.yml` returns 0 (old path fully removed from nav)
    - `grep -c "about/corrections.md" mkdocs.yml` returns 1
    - `grep -c "about/citation.md" mkdocs.yml` returns 1
    - `grep -c "cfd-payment\|cfd_payment" mkdocs.yml` returns 0 (zero residual tokens in any block including the header comment)
    - Non-strict build has zero ERROR/Traceback lines after filtering INFO and expected nav-target-missing warnings. The verify command uses `! (uv run mkdocs build 2>&1 | grep -Ev "(^INFO|nav entry.*not found)" | grep -qE "ERROR|Traceback")` — the leading `!` inverts the grep success (we want grep to find NO ERROR or Traceback, i.e., exit non-zero). Warnings about `about/corrections.md` and `about/citation.md` being missing nav targets are EXPECTED and accepted at this stage (Plan 03 creates them).
    - `markdown_extensions:` block and `copyright:` line are unchanged (spot-check via grep for any one of their lines, e.g., `grep -c "pymdownx.arithmatex" mkdocs.yml` returns 1).
  </acceptance_criteria>
  <done>mkdocs.yml uses the Material theme with the opinionated baseline config; site identity reflects the renamed repo; nav wires the About section and points Gas Counterfactual at the new methodology path.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Sweep docs/index.md, docs/charts/index.md, and the three chart pages for cfd-payment URL + cfd_payment module tokens and dead technical-details/ links</name>
  <files>
    docs/index.md,
    docs/charts/index.md,
    docs/charts/subsidy/cfd-dynamics.md,
    docs/charts/subsidy/cfd-vs-gas-cost.md,
    docs/charts/subsidy/remaining-obligations.md
  </files>
  <read_first>
    - docs/index.md (current state — especially lines 59, 66-73, 80)
    - docs/charts/index.md (current state — especially lines 52, 59, 65, 68)
    - docs/charts/subsidy/cfd-dynamics.md (current state — especially lines 89, 90, 95, 104)
    - docs/charts/subsidy/cfd-vs-gas-cost.md (current state — especially lines 138, 162, 163, 168)
    - docs/charts/subsidy/remaining-obligations.md (current state — especially lines 101, 106)
    - docs/technical-details/gas-counterfactual.md (URL-bug reference — confirm shape of the first `richlyon/cfd-payment` typo before applying the parallel fix at cfd-vs-gas-cost.md)
    - .planning/phases/01-foundation-tidy/01-PATTERNS.md § "docs/index.md" and § "docs/charts/index.md" (exact line-by-line changes)
  </read_first>
  <action>
    **Part A: `docs/index.md`** — apply these exact changes (line numbers match the current file):

    - **Line 59**: `[Gas counterfactual](technical-details/gas-counterfactual.md).` → `[Gas counterfactual](methodology/gas-counterfactual.md).`
    - **Line 66**: `git clone https://github.com/richardjlyon/cfd-payment` → `git clone https://github.com/richardjlyon/uk-subsidy-tracker`
    - **Line 67**: `cd cfd-payment` → `cd uk-subsidy-tracker`
    - **Line 69**: `uv run python -m cfd_payment.plotting    # regenerate all charts` → `uv run python -m uk_subsidy_tracker.plotting    # regenerate all charts` (preserve the inline comment spacing)
    - **Line 73**: `Source: [github.com/richardjlyon/cfd-payment](https://github.com/richardjlyon/cfd-payment).` → `Source: [github.com/richardjlyon/uk-subsidy-tracker](https://github.com/richardjlyon/uk-subsidy-tracker).`
    - **Line 80**: `Corrections and contributions welcome via [GitHub Issues](https://github.com/richardjlyon/cfd-payment/issues).` → `Corrections and contributions welcome via [GitHub Issues](https://github.com/richardjlyon/uk-subsidy-tracker/issues).`

    DO NOT change:
    - Line 1: `# CfD Payment Analysis` — leave as-is per CONTEXT.md Claude's Discretion ("leave narrative text describing the 'CfD' scheme untouched").
    - Any prose about the CfD scheme itself, strike prices, Contracts for Difference — those are the subject matter, not the package.
    - Links to `charts/subsidy/cfd-dynamics.md` etc. — chart page filenames retain "cfd" because they describe CfD charts.

    **Part B: `docs/charts/index.md`** — apply these exact changes (line numbers match the current file):

    - **Line 52**: `[Gas counterfactual](../technical-details/gas-counterfactual.md).` → `[Gas counterfactual](../methodology/gas-counterfactual.md).`
    - **Line 59**: `uv run python -m cfd_payment.plotting` → `uv run python -m uk_subsidy_tracker.plotting`
    - **Line 65**: `uv run python -m cfd_payment.plotting.subsidy.cfd_dynamics` → `uv run python -m uk_subsidy_tracker.plotting.subsidy.cfd_dynamics`
    - **Line 68**: `Source: [github.com/richardjlyon/cfd-payment](https://github.com/richardjlyon/cfd-payment).` → `Source: [github.com/richardjlyon/uk-subsidy-tracker](https://github.com/richardjlyon/uk-subsidy-tracker).`

    **Part C: `docs/charts/subsidy/cfd-dynamics.md`** — apply these exact changes:

    - **Line 89** (Chart source bullet):
      Before: `` - **Chart source** — [`src/cfd_payment/plotting/subsidy/cfd_dynamics.py`](https://github.com/richardjlyon/cfd-payment/blob/main/src/cfd_payment/plotting/subsidy/cfd_dynamics.py) ``
      After:  `` - **Chart source** — [`src/uk_subsidy_tracker/plotting/subsidy/cfd_dynamics.py`](https://github.com/richardjlyon/uk-subsidy-tracker/blob/main/src/uk_subsidy_tracker/plotting/subsidy/cfd_dynamics.py) ``
    - **Line 90** (Counterfactual model bullet):
      Before: `` - **Counterfactual model** — [`src/cfd_payment/counterfactual.py`](https://github.com/richardjlyon/cfd-payment/blob/main/src/cfd_payment/counterfactual.py) ``
      After:  `` - **Counterfactual model** — [`src/uk_subsidy_tracker/counterfactual.py`](https://github.com/richardjlyon/uk-subsidy-tracker/blob/main/src/uk_subsidy_tracker/counterfactual.py) ``
    - **Line 95** (reproduce code fence):
      Before: `uv run python -m cfd_payment.plotting.subsidy.cfd_dynamics`
      After:  `uv run python -m uk_subsidy_tracker.plotting.subsidy.cfd_dynamics`
    - **Line 104** (See also methodology link):
      Before: `- [Gas counterfactual](../../technical-details/gas-counterfactual.md) —`
      After:  `- [Gas counterfactual](../../methodology/gas-counterfactual.md) —`

    **Part D: `docs/charts/subsidy/cfd-vs-gas-cost.md`** — apply these exact changes:

    - **Line 138** (Gas alternative methodology link):
      Before: `Full methodology in [Gas counterfactual](../../technical-details/gas-counterfactual.md).`
      After:  `Full methodology in [Gas counterfactual](../../methodology/gas-counterfactual.md).`
    - **Line 162** (Chart source bullet — SECOND instance of the `richlyon/` typo; fix both the typo AND the package rename on the same line):
      Before: `` - **Chart source** — [`src/cfd_payment/plotting/subsidy/cfd_vs_gas_cost.py`](https://github.com/richlyon/cfd-payment/blob/main/src/cfd_payment/plotting/subsidy/cfd_vs_gas_cost.py) ``
      After:  `` - **Chart source** — [`src/uk_subsidy_tracker/plotting/subsidy/cfd_vs_gas_cost.py`](https://github.com/richardjlyon/uk-subsidy-tracker/blob/main/src/uk_subsidy_tracker/plotting/subsidy/cfd_vs_gas_cost.py) ``
    - **Line 163** (Counterfactual model bullet — also has `richlyon/` typo; fix both):
      Before: `` - **Counterfactual model** — [`src/cfd_payment/counterfactual.py`](https://github.com/richlyon/cfd-payment/blob/main/src/cfd_payment/counterfactual.py) ``
      After:  `` - **Counterfactual model** — [`src/uk_subsidy_tracker/counterfactual.py`](https://github.com/richardjlyon/uk-subsidy-tracker/blob/main/src/uk_subsidy_tracker/counterfactual.py) ``
    - **Line 168** (reproduce code fence):
      Before: `uv run python -m cfd_payment.plotting.subsidy.cfd_vs_gas_cost`
      After:  `uv run python -m uk_subsidy_tracker.plotting.subsidy.cfd_vs_gas_cost`

    Note: Task 1 already fixed the FIRST `richlyon/cfd-payment` instance at `docs/methodology/gas-counterfactual.md:108`. Task 3 Part D fixes the SECOND and THIRD instances at lines 162 and 163 of cfd-vs-gas-cost.md (same typo, adjacent lines — both need the `richlyon` → `richardjlyon` correction alongside the package rename).

    **Part E: `docs/charts/subsidy/remaining-obligations.md`** — apply these exact changes:

    - **Line 101** (Chart source bullet):
      Before: `` - **Chart source** — [`src/cfd_payment/plotting/subsidy/remaining_obligations.py`](https://github.com/richardjlyon/cfd-payment/blob/main/src/cfd_payment/plotting/subsidy/remaining_obligations.py) ``
      After:  `` - **Chart source** — [`src/uk_subsidy_tracker/plotting/subsidy/remaining_obligations.py`](https://github.com/richardjlyon/uk-subsidy-tracker/blob/main/src/uk_subsidy_tracker/plotting/subsidy/remaining_obligations.py) ``
    - **Line 106** (reproduce code fence):
      Before: `uv run python -m cfd_payment.plotting.subsidy.remaining_obligations`
      After:  `uv run python -m uk_subsidy_tracker.plotting.subsidy.remaining_obligations`

    DO NOT change across all five files:
    - H1 titles — unchanged (narrative references to the CfD scheme).
    - Chart page filenames `subsidy/cfd-dynamics.md`, `subsidy/cfd-vs-gas-cost.md`, `subsidy/remaining-obligations.md` — scheme-name references, preserved.
    - Chart preview image paths `html/subsidy_cfd_dynamics_twitter.png` etc. — the `.gitignore`d html output dir keeps these filenames because they describe CfD charts; regenerated on next `uv run python -m uk_subsidy_tracker.plotting` run.
    - Prose and table content describing the CfD scheme itself.

    Before finishing, run the two repo-wide sanity greps (both MUST return zero matches):
    ```bash
    grep -rn "cfd-payment\|cfd_payment\." docs/ --include="*.md"
    grep -rn "technical-details/" docs/ --include="*.md"
    ```
    Any remaining hits indicate either a file not covered by this plan (planner error — escalate) or a hit inside an `html/` subdirectory (ignore; `.gitignore`d and excluded by the `--include="*.md"` filter in any case).
  </action>
  <verify>
    <automated>! grep -q "cfd-payment\|cfd_payment\." docs/index.md && ! grep -q "cfd-payment\|cfd_payment\." docs/charts/index.md && ! grep -q "cfd-payment\|cfd_payment\." docs/charts/subsidy/cfd-dynamics.md && ! grep -q "cfd-payment\|cfd_payment\." docs/charts/subsidy/cfd-vs-gas-cost.md && ! grep -q "cfd-payment\|cfd_payment\." docs/charts/subsidy/remaining-obligations.md && ! grep -rn "cfd-payment\|cfd_payment\." docs/ --include="*.md" && ! grep -rn "technical-details/" docs/ --include="*.md" && ! grep -q "richlyon/cfd-payment" docs/charts/subsidy/cfd-vs-gas-cost.md && grep -q "methodology/gas-counterfactual.md" docs/index.md && grep -q "\.\./methodology/gas-counterfactual.md" docs/charts/index.md && grep -q "\.\./\.\./methodology/gas-counterfactual.md" docs/charts/subsidy/cfd-dynamics.md && grep -q "\.\./\.\./methodology/gas-counterfactual.md" docs/charts/subsidy/cfd-vs-gas-cost.md && grep -q "uk_subsidy_tracker.plotting" docs/index.md && grep -q "uk_subsidy_tracker.plotting" docs/charts/index.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cfd-payment" docs/index.md` returns 0
    - `grep -c "cfd_payment\." docs/index.md` returns 0
    - `grep -c "cfd-payment" docs/charts/index.md` returns 0
    - `grep -c "cfd_payment\." docs/charts/index.md` returns 0
    - `grep -c "cfd-payment\|cfd_payment\." docs/charts/subsidy/cfd-dynamics.md` returns 0
    - `grep -c "cfd-payment\|cfd_payment\." docs/charts/subsidy/cfd-vs-gas-cost.md` returns 0
    - `grep -c "cfd-payment\|cfd_payment\." docs/charts/subsidy/remaining-obligations.md` returns 0
    - `grep -rn "cfd-payment\|cfd_payment\." docs/ --include="*.md"` returns zero matches (full docs tree swept clean)
    - `grep -rn "technical-details/" docs/ --include="*.md"` returns zero matches (all dead methodology links rewritten; Plan 04 strict build will succeed on this dimension)
    - `grep -c "richlyon/cfd-payment" docs/charts/subsidy/cfd-vs-gas-cost.md` returns 0 (second `richlyon/` typo instance fixed)
    - `grep -c "methodology/gas-counterfactual.md" docs/index.md` returns 1
    - `grep -c "\.\./methodology/gas-counterfactual.md" docs/charts/index.md` returns 1
    - `grep -c "\.\./\.\./methodology/gas-counterfactual.md" docs/charts/subsidy/cfd-dynamics.md` returns 1
    - `grep -c "\.\./\.\./methodology/gas-counterfactual.md" docs/charts/subsidy/cfd-vs-gas-cost.md` returns 1
    - `grep -c "github.com/richardjlyon/uk-subsidy-tracker" docs/index.md` returns at least 3 (clone URL, source link display + URL, Issues link)
    - `grep -c "github.com/richardjlyon/uk-subsidy-tracker" docs/charts/subsidy/cfd-dynamics.md` returns at least 2 (chart source + counterfactual model bullets)
    - `grep -c "github.com/richardjlyon/uk-subsidy-tracker" docs/charts/subsidy/cfd-vs-gas-cost.md` returns at least 2 (chart source + counterfactual model bullets — both formerly had `richlyon/` typo, both now canonical)
    - `grep -c "github.com/richardjlyon/uk-subsidy-tracker" docs/charts/subsidy/remaining-obligations.md` returns at least 1
    - `grep -c "uk_subsidy_tracker.plotting" docs/index.md` returns 1
    - `grep -c "uk_subsidy_tracker.plotting" docs/charts/index.md` returns at least 2 (bulk + per-chart)
    - `grep -c "uk_subsidy_tracker.plotting.subsidy.cfd_dynamics" docs/charts/subsidy/cfd-dynamics.md` returns 1
    - `grep -c "uk_subsidy_tracker.plotting.subsidy.cfd_vs_gas_cost" docs/charts/subsidy/cfd-vs-gas-cost.md` returns 1
    - `grep -c "uk_subsidy_tracker.plotting.subsidy.remaining_obligations" docs/charts/subsidy/remaining-obligations.md` returns 1
    - `grep -c "^# CfD Payment Analysis$" docs/index.md` still returns 1 (H1 narrative title deliberately preserved per discretion note)
  </acceptance_criteria>
  <done>All five docs-page sweeps complete; no residual `cfd-payment` repo references, `cfd_payment.` module-path references, or dead `technical-details/` methodology links anywhere under `docs/` in tracked markdown files; BOTH instances of the `richlyon/cfd-payment` URL typo are corrected; all five pages link to the new `methodology/gas-counterfactual.md` path. Plan 04's `mkdocs build --strict` gate is now unblocked on this dimension.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo-main → Cloudflare Pages / GitHub Pages | mkdocs.yml `site_url` + `repo_url` become canonical public URLs for the new site; a wrong URL here directs readers to a nonexistent GitHub repo or broken preview |
| mkdocs build → deployed site | Material theme is new to this project; misconfigured `features:` or `palette:` blocks can fail silently in dev and break rendering in prod |
| chart pages → methodology page | Dead `../../technical-details/` links would silently 404 in prod; Plan 04's `mkdocs build --strict` is the gate, but Task 3 must preemptively rewrite all of them to avoid a cascade failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Spoofing | mkdocs.yml `repo_url` + `extra.social.link` + docs-tree GitHub URLs | mitigate | Hard-code the canonical URL `https://github.com/richardjlyon/uk-subsidy-tracker` verbatim in interfaces block; acceptance criteria grep for the exact string; BOTH `richlyon/cfd-payment` URL-typo instances are fixed in this plan — the first at methodology/gas-counterfactual.md:108 in Task 1, the second at cfd-vs-gas-cost.md lines 162/163 in Task 3 Part D (PATTERNS.md flagged `richlyon/` as a pre-existing typo that could misdirect to an attacker-controlled handle if squatted). Acceptance criterion `grep -c "richlyon/cfd-payment" docs/charts/subsidy/cfd-vs-gas-cost.md` returns 0 confirms the second instance is caught. |
| T-01-06 | Tampering | docs/methodology/gas-counterfactual.md (renamed/moved file) | mitigate | Use `git mv` to preserve history; Task 1 acceptance criteria confirm `git log --follow` sees pre-move history |
| T-01-07 | Denial of Service | mkdocs build pipeline | mitigate | Non-strict `mkdocs build` sanity check in Task 2 catches YAML parse errors; final `--strict` gate runs in Plan 04 after Plan 03's about-section content lands — nav-target-missing warnings in Task 2's non-strict build are expected and accepted. Task 3's `grep -rn "technical-details/" docs/ --include="*.md"` returning zero is the pre-flight gate that the strict build will not fail on dead methodology links. |
| T-01-08 | Information Disclosure | site_author, repo_url metadata | accept | All values are already public project metadata (author name, GitHub handle) — no sensitive info introduced |
</threat_model>

<verification>
Phase-level checks after all three tasks land:

1. `test -f docs/methodology/gas-counterfactual.md && test ! -f docs/technical-details/gas-counterfactual.md` exits 0
2. `grep -q "name: material" mkdocs.yml && ! grep -q "name: readthedocs" mkdocs.yml` exits 0
3. `grep -c "cfd-payment\|cfd_payment" mkdocs.yml` returns 0
4. `grep -rn "cfd-payment\|cfd_payment\." docs/ --include="*.md"` returns zero matches
5. `grep -rn "technical-details/" docs/ --include="*.md"` returns zero matches (B-1 cascade to Plan 04 pre-empted)
6. `grep -c "richlyon/cfd-payment" docs/methodology/gas-counterfactual.md docs/charts/subsidy/cfd-vs-gas-cost.md` returns 0 (BOTH `richlyon/` URL typos fixed)
7. Non-strict mkdocs build has zero `ERROR`/`Traceback` lines (strict build is Plan 04)
</verification>

<success_criteria>
- FND-02 delivered: Material theme active, readthedocs theme removed, palette toggle wired
- mkdocs.yml site identity reflects the renamed repo (site_name, site_url, repo_name, repo_url, extra.social)
- Nav wires About → Corrections and About → Citation (content files created in Plan 03)
- `docs/methodology/gas-counterfactual.md` exists with git history preserved from the old path
- Both stale `richlyon/cfd-payment` GitHub URL bugs fixed (methodology/gas-counterfactual.md:108 and cfd-vs-gas-cost.md:162-163)
- Zero residual `cfd-payment` / `cfd_payment.` tokens anywhere in tracked `docs/*.md` files or `mkdocs.yml`
- Zero residual `technical-details/` links in tracked `docs/*.md` files — Plan 04's strict build will not fail on dead methodology links
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-tidy/01-02-SUMMARY.md` describing:
- git mv command used for the methodology page
- Which of the 8 mkdocs.yml blocks were edited (header, site_identity, repo, theme, nav, extra, plugins-unchanged, copyright-unchanged)
- Output of the non-strict `mkdocs build` sanity check (expected: "missing nav entry" warnings for about/corrections.md and about/citation.md; no ERROR or Traceback)
- Confirmation that BOTH `richlyon/cfd-payment` GitHub URL-typo instances were fixed (methodology/gas-counterfactual.md:108 + cfd-vs-gas-cost.md lines 162/163)
- List of all five docs pages swept in Task 3 with line-count of edits per page
- Output of the two repo-wide sanity greps: `grep -rn "cfd-payment\|cfd_payment\." docs/ --include="*.md"` and `grep -rn "technical-details/" docs/ --include="*.md"` — both MUST return zero matches
- Hand-off note to Plan 03: "Nav entries `about/corrections.md` and `about/citation.md` are referenced in mkdocs.yml; Plan 03 must create these files before Plan 04's `mkdocs build --strict` gate."
- Hand-off note to Plan 04: "All dead `technical-details/` methodology links are rewritten; strict-build gate is unblocked on that dimension. Final `--strict` build validation goes in Plan 04 after Plan 03 commits."
</output>
</output>
