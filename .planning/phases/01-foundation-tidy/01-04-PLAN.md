---
phase: 01-foundation-tidy
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03]
files_modified: []
autonomous: false
requirements:
  - GOV-05
tags: [verification, github-label, build-strict, phase-exit-gate]

must_haves:
  truths:
    - "uv run mkdocs build --strict passes green (ROADMAP success criterion 2 final gate)"
    - "GitHub `correction` label exists on the uk-subsidy-tracker repo (or is logged as a user follow-up if gh auth unavailable)"
    - "All five ROADMAP success criteria verify green in a single run"
    - "The phase-exit invariants hold: uk_subsidy_tracker imports, Material theme renders, four root docs tracked, corrections page reachable from nav, CITATION.cff valid"
  artifacts:
    - path: "site/"
      provides: "mkdocs build output produced by strict build (regenerated each run, gitignored)"
  key_links:
    - from: "GitHub Issues for the renamed repo"
      to: "the `correction` label"
      via: "gh label create"
      pattern: "label create correction"
    - from: "mkdocs.yml nav"
      to: "every file referenced in nav"
      via: "strict-mode build must find every nav target"
      pattern: "INFO.*Cleaning site directory"
---

<objective>
Run the phase-exit verification gate. Create the GitHub `correction` label so the URL wired into `docs/about/corrections.md` lands on a populated filter view (GOV-05 completion). Run `uv run mkdocs build --strict` to confirm ROADMAP success criterion 2 in its strictest form — no nav-target-missing warnings because Plan 02 wired `methodology/gas-counterfactual.md`, `about/corrections.md`, `about/citation.md` and Plan 03 created the latter two content files. Walk through all five ROADMAP success criteria in a single scripted check to produce a clean audit trail in the SUMMARY.

This plan performs no source code changes and has no `files_modified` entries — it is a pure verification + external-side-effect plan. Its outputs are (a) the `correction` label on GitHub (or a VERIFICATION.md follow-up note if `gh` is not authenticated) and (b) the SUMMARY.md phase-exit checklist.

Purpose: close out GOV-05's automation-eligible work (label creation) and gate the phase exit on all five success criteria.

Output: `correction` label created on GitHub (or logged as follow-up); SUMMARY.md documenting each of the five ROADMAP success criteria with their verify-command exit status.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-tidy/01-CONTEXT.md
@.planning/phases/01-foundation-tidy/01-01-SUMMARY.md
@.planning/phases/01-foundation-tidy/01-02-SUMMARY.md
@.planning/phases/01-foundation-tidy/01-03-SUMMARY.md
@mkdocs.yml
@CITATION.cff
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the GitHub `correction` label (or log as follow-up if gh auth unavailable)</name>
  <files></files>
  <read_first>
    - .planning/phases/01-foundation-tidy/01-CONTEXT.md § Claude's Discretion "GitHub `correction` label creation"
    - docs/about/corrections.md (confirm the filter URL uses `label%3Acorrection`)
  </read_first>
  <action>
    Step 1. Check whether `gh` CLI is authenticated against the renamed repository:
    ```bash
    gh auth status
    ```

    If this returns a non-zero exit or "not logged in", skip to Step 4 (log as follow-up).

    Step 2. Check whether the renamed repository exists on GitHub yet. If the user did the GitHub UI rename before commit time per CONTEXT.md Claude's Discretion note on "GitHub remote rename coordination", the `uk-subsidy-tracker` repo should be the canonical remote:
    ```bash
    gh repo view richardjlyon/uk-subsidy-tracker --json name,url 2>&1 | head -5
    ```

    If this returns 404, the GitHub UI rename has not happened yet. GitHub auto-redirects the old slug, so `gh repo view richardjlyon/cfd-payment` may still succeed. In that case, proceed with Step 3 against whichever slug responds (GitHub applies the label to the same underlying repo regardless of slug), and log a follow-up in Task 3's verification note: "Confirm GitHub UI rename to `uk-subsidy-tracker` is complete."

    Step 3. Create the label:
    ```bash
    gh label create correction \
      --description "Correction to a published number, chart, or methodology" \
      --color "0E8A16" \
      --repo richardjlyon/uk-subsidy-tracker
    ```

    The colour `0E8A16` is GitHub's muted green (documented; distinct from `good first issue` and `enhancement`). If the label already exists, `gh label create` returns a non-zero exit; that is acceptable — confirm existence via `gh label list --repo richardjlyon/uk-subsidy-tracker | grep -E "^correction\b"`.

    Step 4 (fallback if gh auth unavailable or repo not yet renamed). Append a follow-up note to `.planning/phases/01-foundation-tidy/01-04-VERIFICATION.md` (create file if it does not exist):

    ```markdown
    # Phase 1 Verification Notes

    ## User follow-ups

    - [ ] Create GitHub `correction` label on the `uk-subsidy-tracker` repository:
      ```
      gh label create correction \
        --description "Correction to a published number, chart, or methodology" \
        --color "0E8A16" \
        --repo richardjlyon/uk-subsidy-tracker
      ```
      Reason: `gh auth status` did not return an authenticated session at Phase 1 close.
      Without the label, `docs/about/corrections.md`'s filtered-issues URL
      (`?q=is%3Aissue%20label%3Acorrection`) returns an empty filter view
      rather than a populated one. No functional breakage — just a cosmetic
      placeholder until the first correction lands.

    - [ ] (If applicable) Confirm GitHub UI rename `cfd-payment` → `uk-subsidy-tracker`
      is complete. GitHub auto-redirects the old slug so existing URLs work,
      but canonical references in `mkdocs.yml`, `CITATION.cff`, `README.md`,
      and the methodology page all assume the new slug.
    ```

    Do NOT block the phase on label creation — it is a GOV-05 convenience, not a gate. Plan 03's `docs/about/corrections.md` remains valid either way.
  </action>
  <verify>
    <automated>if gh auth status 2>/dev/null >/dev/null; then gh label list --repo richardjlyon/uk-subsidy-tracker 2>/dev/null | grep -qE "^correction\b" || gh label list --repo richardjlyon/cfd-payment 2>/dev/null | grep -qE "^correction\b"; else test -f .planning/phases/01-foundation-tidy/01-04-VERIFICATION.md && grep -q "Create GitHub \`correction\` label" .planning/phases/01-foundation-tidy/01-04-VERIFICATION.md; fi</automated>
  </verify>
  <acceptance_criteria>
    - Either `gh label list --repo richardjlyon/uk-subsidy-tracker` (or the redirected `cfd-payment` slug) prints a line starting with `correction` (tab-separated description follows), OR `.planning/phases/01-foundation-tidy/01-04-VERIFICATION.md` exists with the user-follow-up checklist entry.
    - If label was created: `gh label list --repo <slug> --json name,color | grep 0E8A16` succeeds (colour pinned to `#0E8A16` as specified).
    - If fallback path taken: the VERIFICATION.md file contains both the `gh label create correction` command as a copy-paste-ready code block AND a reason explaining why label creation was deferred.
    - The `docs/about/corrections.md` file from Plan 03 remains unchanged (this task does not edit it).
  </acceptance_criteria>
  <done>`correction` label exists on GitHub for the renamed repo, OR a VERIFICATION.md follow-up note gives the user a copy-pasteable command to create it later. Either outcome is acceptable per CONTEXT.md's "if `gh` auth is unavailable, note in VERIFICATION.md and leave as a user follow-up" discretion.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run `uv run mkdocs build --strict` and assert zero warnings / zero errors</name>
  <files></files>
  <read_first>
    - mkdocs.yml (confirm post-Plan-02 state: Material theme, palette toggle, About-section nav)
    - docs/about/corrections.md, docs/about/citation.md, docs/methodology/gas-counterfactual.md (all three must exist and be referenced by mkdocs.yml nav)
    - .planning/phases/01-foundation-tidy/01-02-SUMMARY.md (for the note about non-strict warnings being expected at Plan 02's boundary; this task confirms those warnings are now gone)
  </read_first>
  <action>
    Step 1. Verify all nav targets exist as files before running strict build:
    ```bash
    for f in docs/index.md docs/charts/index.md docs/charts/subsidy/cfd-dynamics.md docs/charts/subsidy/cfd-vs-gas-cost.md docs/charts/subsidy/remaining-obligations.md docs/methodology/gas-counterfactual.md docs/about/corrections.md docs/about/citation.md; do
      test -f "$f" || echo "MISSING: $f"
    done
    ```
    This should print no output. If any file is missing, that plan did not complete its Wave 2 scope — escalate before running strict build (running it against a missing file wastes time on an expected failure).

    Step 1b (pre-flight sanity grep — W-2 cascade check). Confirm Plan 02 Task 3's docs sweep landed fully. If either of these greps returns a match, Plan 02's sweep regressed (or a new doc file slipped through) and the strict build would produce a confusing "nav entry not found" error. Catching it here gives a clearer error message faster:
    ```bash
    grep -rn "technical-details/" docs/ --include="*.md"   # MUST be empty
    grep -rn "cfd-payment\|cfd_payment\." docs/ --include="*.md"   # MUST be empty
    ```
    If either returns output, stop and escalate — Plan 02 did not sweep the file(s) named in the match. Do not proceed to the strict build until those are cleaned up; otherwise the `mkdocs build --strict` gate will fail on a `target not found` or a residual renamed-token issue rather than the nav-target sanity check we actually want to run here.

    Step 2. Run the strict build:
    ```bash
    uv run mkdocs build --strict 2>&1 | tee /tmp/mkdocs-strict-build.log
    ```

    `--strict` promotes WARNING to ERROR. Success means:
    - Exit code 0
    - Output contains `INFO - Cleaning site directory`
    - Output contains `INFO - Building documentation to directory: .../site`
    - Output contains `INFO - Documentation built in ...s`
    - No line contains `WARNING`, `ERROR`, or `Traceback`

    Expected warnings that would fail `--strict` (all should be gone after Plans 01-03 land):
    - `nav entry points to 'about/corrections.md' which doesn't exist` — Plan 03 created this file.
    - `nav entry points to 'about/citation.md' which doesn't exist` — Plan 03 created this file.
    - `nav entry points to 'methodology/gas-counterfactual.md' which doesn't exist` — Plan 02 moved the file here.
    - `The following pages exist in the docs directory, but are not included in the nav` — any orphaned files under `docs/` that are not in the nav. This would fire if `docs/technical-details/gas-counterfactual.md` still existed (Plan 02 Task 1 moved it and removed the directory). Confirm via `test ! -d docs/technical-details`.

    Step 3. Inspect the `site/` output:
    ```bash
    ls site/ | head -20
    grep -l "Switch to dark mode" site/**/*.html 2>/dev/null | head -1
    ```
    - `site/` should contain generated HTML.
    - At least one HTML file should contain the Material palette-toggle text (confirms Material theme is actually rendering, not just configured).

    Step 4. Confirm the theme identity in built output:
    ```bash
    grep -l "mkdocs-material" site/index.html 2>/dev/null
    ```
    Material theme embeds its own identifier in generated HTML. Presence confirms the theme swap produced built artefacts, not just passed YAML validation.
  </action>
  <verify>
    <automated>! grep -rn "technical-details/" docs/ --include="*.md" && ! grep -rn "cfd-payment\|cfd_payment\." docs/ --include="*.md" && uv run mkdocs build --strict 2>&1 | tee /tmp/mkdocs-strict.log | grep -qE "INFO.*Documentation built" && ! grep -qE "WARNING|ERROR|Traceback" /tmp/mkdocs-strict.log && test -f site/index.html && grep -q "mkdocs-material" site/index.html</automated>
  </verify>
  <acceptance_criteria>
    - Pre-flight grep `grep -rn "technical-details/" docs/ --include="*.md"` returns zero matches (W-2 cascade check — Plan 02 sweep validated before strict build)
    - Pre-flight grep `grep -rn "cfd-payment\|cfd_payment\." docs/ --include="*.md"` returns zero matches (Plan 02 sweep validated)
    - `uv run mkdocs build --strict` exits 0
    - `/tmp/mkdocs-strict.log` contains `INFO - Documentation built in` (or MkDocs' version-specific equivalent)
    - `/tmp/mkdocs-strict.log` contains zero lines matching `WARNING`, `ERROR`, or `Traceback`
    - `site/index.html` exists
    - `grep -c "mkdocs-material" site/index.html` returns at least 1 (Material theme identifier embedded)
    - `grep -c "Switch to dark mode" site/index.html` returns at least 1 (palette toggle rendered — confirms D-03 palette landed)
    - `test ! -d docs/technical-details` exits 0 (old directory removed in Plan 02 Task 1; strict mode would warn if it lingered with unreferenced content)
    - ROADMAP success criterion 2 is now GREEN and demonstrated via a single `--strict` run.
  </acceptance_criteria>
  <done>`mkdocs build --strict` green; Material theme rendering in built HTML; no nav-target-missing warnings because Plans 01-03 wired everything; ROADMAP success criterion 2 verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase-exit verification — walk the five ROADMAP success criteria and confirm the baseline</name>
  <what-built>
    Phase 1 Foundation Tidy is complete at Task 2. This checkpoint gives the user a single dashboard of the five ROADMAP success criteria (ROADMAP.md Phase 1 section) verified in one pass, with the chance to confirm visual rendering before the phase is closed.

    Pre-computed status (from Plans 01-03 + Task 2):
    - Criterion 1: `uv run python -c 'import uk_subsidy_tracker'` — Plan 01 Task 2 verified green.
    - Criterion 2: `uv run mkdocs build --strict` passes with Material theme — Task 2 (this plan) verified green.
    - Criterion 3: ARCHITECTURE.md, RO-MODULE-SPEC.md, CHANGES.md, CITATION.cff committed at repo root — Plan 03 Task 1+2+5 created/staged; confirmed by `git ls-files` at commit time.
    - Criterion 4: docs/about/corrections.md exists and reachable from site navigation — Plan 03 Task 3 created content; Plan 02 Task 2 wired nav; Task 2 (this plan) confirmed strict build resolves the nav entry.
    - Criterion 5: CITATION.cff contains correct author, repository URL, and version metadata — Plan 03 Task 5 created file with `family-names: Lyon`, `email: richlyon@fastmail.com`, `repository-code: https://github.com/richardjlyon/uk-subsidy-tracker`, `version: "0.1.0"`.
  </what-built>
  <how-to-verify>
    Run each of these commands and confirm they exit 0. Record the output in your decision.

    ```bash
    # Criterion 1: Package imports
    uv run python -c 'import uk_subsidy_tracker; print("OK:", uk_subsidy_tracker.DATA_DIR)'

    # Criterion 2: Strict build (Task 2 already ran; re-run if you want)
    uv run mkdocs build --strict

    # Criterion 3: Four root docs tracked
    git ls-files ARCHITECTURE.md RO-MODULE-SPEC.md CHANGES.md CITATION.cff
    # Expected output: four file paths, one per line

    # Criterion 4: Corrections page reachable from nav
    grep -q "about/corrections.md" mkdocs.yml && test -f docs/about/corrections.md && echo "OK: corrections page wired"

    # Criterion 5: CITATION.cff metadata
    grep -E "^(title|family-names|given-names|email|version|repository-code):" CITATION.cff
    ```

    Then open the locally served site to verify visual rendering:

    ```bash
    uv run mkdocs serve
    ```

    Visit http://127.0.0.1:8000/ in a browser and confirm:

    1. The site title shows "UK Renewable Subsidy Tracker" (top-left or tab title).
    2. The light/dark mode toggle appears in the header (sun/moon icon) and clicking it switches the palette.
    3. The left sidebar nav shows the Charts subsections, `Gas Counterfactual`, and an `About` section with `Corrections` + `Citation` children.
    4. Clicking `About → Corrections` loads the corrections page with the "None yet" table row.
    5. Clicking `About → Citation` loads the citation page with the 0.1.0 version and the repo link.
    6. Clicking `Gas Counterfactual` loads the methodology page (now at `/methodology/gas-counterfactual/`, not `/technical-details/...`).
    7. The existing chart pages (`Charts → CfD Dynamics` etc.) still render — palette swap doesn't break Plotly HTML embeds.

    If any check fails, describe the failure. Otherwise approve to close the phase.
  </how-to-verify>
  <files></files>
  <action>BLOCKING CHECKPOINT. Pause execution and present the five-criterion verification dashboard plus the seven visual-rendering checks (listed in how-to-verify) to the user. Run the five verification commands unattended first; attach their outputs to the checkpoint. Then prompt the user to run `uv run mkdocs serve` locally and walk the seven visual checks in a browser. Do NOT proceed to SUMMARY write-out until the user replies with "approved" or "issues: ...". No file modifications in this task.</action>
  <verify>
    <automated>uv run python -c 'import uk_subsidy_tracker' && test -f site/index.html && git ls-files ARCHITECTURE.md RO-MODULE-SPEC.md CHANGES.md CITATION.cff | wc -l | grep -q '^4$' && grep -q "about/corrections.md" mkdocs.yml && test -f docs/about/corrections.md && grep -q "^version: \"0.1.0\"$" CITATION.cff</automated>
  </verify>
  <done>All five ROADMAP Phase 1 success criteria verified green in one pass, all seven browser visual checks passed, and the user has typed "approved". Phase 1 is closed; Phase 2 can start.</done>
  <resume-signal>
    Type "approved" to mark Phase 1 complete and unblock Phase 2 planning. Type "issues:" followed by a description if any of the seven visual checks or five success-criteria commands failed — the orchestrator will route to gap-closure planning (`/gsd-plan-phase 1 --gaps`) to address them.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → GitHub API | `gh label create` requires an authenticated session; without it, the label is not created and the corrections-page URL points to a not-yet-populated filter |
| MkDocs build → deployed site | `--strict` is the last quality gate before the user deploys via `mkdocs gh-deploy`; any warning here becomes a silent breakage in prod |
| checkpoint visual-verify → user judgment | The seven visual checks rely on the user's browser cache not serving a stale pre-rename build; clearing cache (or hard-reload) is implicit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-15 | Elevation of Privilege | `gh` CLI auth scope | accept | Label creation requires `public_repo` scope; if the user's gh session lacks it, Task 1 falls back to VERIFICATION.md follow-up. No elevation attempted without explicit auth. |
| T-01-16 | Tampering | `mkdocs build --strict` output | mitigate | Strict mode promotes all warnings to errors; Task 2 greps the log for WARNING/ERROR/Traceback and fails the task if any appear. Task 2 Step 1b also runs a pre-flight grep for `technical-details/` and `cfd-payment`/`cfd_payment.` tokens in the docs tree so a Plan 02 regression fails earlier with a clearer message. No silent degradation. |
| T-01-17 | Denial of Service | Phase 2 blockage | accept | If Task 3 checkpoint fails, orchestrator routes to `/gsd-plan-phase 1 --gaps`. Phase 2 cannot start until Phase 1 success criteria are green. Intentional — prevents compounding defects. |
| T-01-18 | Information Disclosure | `/tmp/mkdocs-strict.log` | accept | Build logs contain only public docs content; no secrets. |
</threat_model>

<verification>
Phase-exit verification checklist (all must be green before Task 3 resume signal):

1. Plan 01 verified at commit time: `uv run python -c 'import uk_subsidy_tracker'` exits 0
2. Plan 02 verified at commit time: `grep -c "name: material" mkdocs.yml` returns 1
3. Plan 03 verified at commit time: `for f in ARCHITECTURE.md RO-MODULE-SPEC.md CHANGES.md CITATION.cff; do git ls-files "$f" | grep -q .; done` all succeed
4. Plan 04 Task 2: `uv run mkdocs build --strict` exits 0 with zero warnings
5. Plan 04 Task 2 Step 1b pre-flight greps (W-2 cascade check): `grep -rn "technical-details/" docs/ --include="*.md"` AND `grep -rn "cfd-payment\|cfd_payment\." docs/ --include="*.md"` both return zero matches
6. Plan 04 Task 1: `gh label list` contains `correction` OR VERIFICATION.md has the follow-up note
7. User approval via Task 3 resume signal
</verification>

<success_criteria>
- All five ROADMAP Phase 1 success criteria verified green in a single pass
- GitHub `correction` label created (or user-follow-up logged)
- Material theme renders in `site/` build output; palette toggle functional
- Phase 1 ready to hand off to Phase 2 (Test & Benchmark Scaffolding)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-tidy/01-04-SUMMARY.md` with:
- Status of GitHub `correction` label: created / deferred-to-follow-up
- `uv run mkdocs build --strict` exit code and build time
- Pre-flight grep results (W-2 cascade check) — both MUST be empty
- Full text of the five-criterion verification commands with captured output (demonstrates each is green)
- Final file tree diff at repo root: `ls -la *.md *.cff *.toml LICENSE 2>/dev/null`
- Visual-check pass/fail grid from Task 3 checkpoint (recorded from user's approval message)
- Pointer to the next phase: "Phase 2 (Test & Benchmark Scaffolding) can now start. Run `/gsd-plan-phase 2`."
</output>
</output>
