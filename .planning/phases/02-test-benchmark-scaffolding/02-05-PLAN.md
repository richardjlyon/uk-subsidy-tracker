---
phase: 02-test-benchmark-scaffolding
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - CHANGES.md
autonomous: true
requirements:
  - TEST-02
  - TEST-03
  - TEST-05
tags: [bookkeeping, roadmap, requirements, scope-correction]
threat_mitigations:
  - threat: T-02-01
    disposition: "N/A — this plan does not touch the T-02-01 surface (.github/workflows)"
  - threat: T-02-02
    disposition: "N/A — this plan does not touch CI env"
  - threat: T-02-03
    disposition: "N/A — this plan does not touch benchmarks.yaml"
  - threat: T-02-04
    disposition: "N/A — this plan does not modify counterfactual constants"
  - threat: T-02-05
    disposition: "N/A — branch protection out of scope"

must_haves:
  truths:
    - "`.planning/ROADMAP.md` Phase 2 requirements list reads `TEST-01, TEST-04, TEST-06, GOV-04` (TEST-02/03/05 removed)"
    - "`.planning/ROADMAP.md` Phase 4 requirements list includes TEST-02, TEST-03, TEST-05 alongside the existing PUB-* and GOV-* entries"
    - "`.planning/ROADMAP.md` Phase 2 success criterion 1 reads 'four test classes present and passing: test_counterfactual.py, test_schemas.py, test_aggregates.py, test_benchmarks.py' (NOT 'five')"
    - "`.planning/ROADMAP.md` Phase 2 success criterion 3 re-anchored from 'Ben Pile / REF / Turver' to 'LCCC self-reconciliation and any regulator-native external sources the researcher located'"
    - "`.planning/ROADMAP.md` top-line Phase 2 bullet (line 11) reads 'Four test classes' (not 'Five')"
    - "`.planning/REQUIREMENTS.md` traceability rows for TEST-02, TEST-03, TEST-05 show `Phase 4` (not `Phase 2`)"
    - "`CHANGES.md [Unreleased]` records the scope correction"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 2 + Phase 4 requirement lists accurately reflect D-04; success criteria 1 + 3 re-anchored"
      contains: "four test classes present and passing"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table rows for TEST-02/03/05 point to Phase 4"
      contains: "TEST-02 | Phase 4"
    - path: "CHANGES.md"
      provides: "[Unreleased] Changed entry documenting the Phase 2 scope correction"
      contains: "TEST-02, TEST-03, TEST-05 reassigned"
  key_links:
    - from: ".planning/ROADMAP.md Phase 2 requirements line"
      to: ".planning/REQUIREMENTS.md TEST-* traceability rows"
      via: "both documents must agree on which phase owns each REQ-ID"
      pattern: "TEST-02|TEST-03|TEST-05"
    - from: "Phase 2 roadmap success criterion 1"
      to: "tests/test_*.py files shipped by Plans 01/02/03"
      via: "success criterion enumerates exactly the four files that ship"
      pattern: "test_counterfactual|test_schemas|test_aggregates|test_benchmarks"
---

<objective>
Enforce CONTEXT D-04: move formal TEST-02, TEST-03, TEST-05 from Phase 2 to Phase 4 in both `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md`, reword Phase 2 success criterion 1 from "five test classes" to "four test classes present and passing" (enumerating the four file names), and re-anchor Phase 2 success criterion 3 per CONTEXT D-11 from "Ben Pile / REF / Turver" to "LCCC self-reconciliation and any regulator-native external sources the researcher located."

Purpose: Prevent scope drift silent-deferral. Phase 2 ships pre-Parquet scaffolding variants of test_schemas.py + test_aggregates.py today; the Parquet variants arrive in Phase 4. TEST-05 (determinism) has no Phase-2 artefact at all and is fully deferred. Without this bookkeeping, the phase-exit verification would flag a false "missing TEST-05 artefact" failure, and a later reader would find the docs contradict what actually shipped.

Output: Three doc edits — ROADMAP.md (five locations), REQUIREMENTS.md (three traceability rows), CHANGES.md (one `[Unreleased] Changed` entry). No code.

**ARCHITECTURE.md precedence (per user memory):** if §11 text disagrees with the reworded ROADMAP, the spec wins. Spot-check: §11 P1 (Phase 2 entry/exit) does NOT enumerate specific test-class names — it says "green CI on main, benchmark deltas documented." The reword keeps faith with §11 by being more specific, not by contradicting it. §11 P3 (Phase 4) lists the publishing-layer concerns; TEST-02/03/05 slot in as supplemental test-expansion work in the same phase. No ARCHITECTURE.md edit is required. If the executor finds §11 text that DOES contradict this reword, halt and flag back.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-test-benchmark-scaffolding/02-CONTEXT.md
@.planning/phases/02-test-benchmark-scaffolding/02-RESEARCH.md
@.planning/phases/02-test-benchmark-scaffolding/02-PATTERNS.md
@CHANGES.md
@ARCHITECTURE.md

<interfaces>
**Exact text to edit (verbatim, from current ROADMAP.md state):**

Line 11 (top-line phase bullet):
```
- [ ] **Phase 2: Test & Benchmark Scaffolding** — Five test classes, CI green on main; counterfactual formula pinned
```
→ change `Five test classes` to `Four test classes`.

Line 46 (Phase 2 Requirements):
```
**Requirements**: TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06, GOV-04
```
→ change to: `**Requirements**: TEST-01, TEST-04, TEST-06, GOV-04`.

Line 48 (Phase 2 success criterion 1):
```
  1. `uv run pytest` passes green on `main` — all five test classes present and passing
```
→ change to: `1. uv run pytest passes green on main — four test classes present and passing: test_counterfactual.py, test_schemas.py, test_aggregates.py, test_benchmarks.py` (preserve the leading two-space indent + backticks on `uv run pytest` + `main` exactly as in the original markdown; only the trailing clause changes).

Line 50 (Phase 2 success criterion 3):
```
  3. `tests/test_benchmarks.py` documents divergence from Ben Pile (2021 + 2026), REF subset, and Turver aggregate explicitly in output or docstring
```
→ change to: `3. tests/test_benchmarks.py documents divergence from LCCC self-reconciliation and any regulator-native external sources the researcher located (OBR, Ofgem, DESNZ, HoC Library, NAO) explicitly in the fixture notes or test docstring` (preserve backticks on the path).

Line 71 (Phase 4 Requirements — current):
```
**Requirements**: PUB-01, PUB-02, PUB-03, PUB-04, PUB-05, PUB-06, GOV-02, GOV-03, GOV-06 (snapshot URL portion)
```
→ append TEST-02, TEST-03, TEST-05. New line: `**Requirements**: PUB-01, PUB-02, PUB-03, PUB-04, PUB-05, PUB-06, GOV-02, GOV-03, GOV-06 (snapshot URL portion), TEST-02, TEST-03, TEST-05`.

**REQUIREMENTS.md traceability table (lines 167–173 current):**
```
| TEST-01 | Phase 2 | Pending |
| TEST-02 | Phase 2 | Pending |
| TEST-03 | Phase 2 | Pending |
| TEST-04 | Phase 2 | Pending |
| TEST-05 | Phase 2 | Pending |
| TEST-06 | Phase 2 | Pending |
| GOV-04 | Phase 2 | Pending |
```
→ change `Phase 2` → `Phase 4` on the TEST-02, TEST-03, and TEST-05 rows only. TEST-01, TEST-04, TEST-06, GOV-04 stay Phase 2.

**CHANGES.md `[Unreleased]` block (already populated by Plans 02 + 03):** after the final `### Added` bullet, ADD a new `### Changed` sub-section (Keep-a-Changelog 1.1.0 convention — separate section per change category).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ROADMAP.md — five edits (top-line bullet + Phase 2 req list + criterion 1 + criterion 3 + Phase 4 req list)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (whole file — confirm current line numbers match the plan's line-number references; line numbers may shift if prior plans touched the file)
    - .planning/phases/02-test-benchmark-scaffolding/02-CONTEXT.md (D-04 and D-11 verbatim)
    - ARCHITECTURE.md §11 P1 + P3 (confirm spec doesn't contradict the reword)
  </read_first>
  <behavior>
    - Five surgical edits. Line numbers in the plan reference are indicative — the executor SHOULD search-and-replace on the exact source strings below rather than relying on line numbers.
    - Preserve all markdown: indentation, backticks, bold markers, checkboxes.
    - If ARCHITECTURE.md §11 contradicts the reword, STOP and flag back.
  </behavior>
  <action>
Open `.planning/ROADMAP.md`. Make exactly five changes by searching for the source text and replacing with the target text. Do NOT rely on line numbers alone — search by content.

**Edit 1 — top-line phase bullet.** Search:
```
- [ ] **Phase 2: Test & Benchmark Scaffolding** — Five test classes, CI green on main; counterfactual formula pinned
```
Replace with:
```
- [ ] **Phase 2: Test & Benchmark Scaffolding** — Four test classes, CI green on main; counterfactual formula pinned
```

**Edit 2 — Phase 2 Requirements line.** Search:
```
**Requirements**: TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06, GOV-04
```
Replace with:
```
**Requirements**: TEST-01, TEST-04, TEST-06, GOV-04
```

**Edit 3 — Phase 2 success criterion 1.** Search:
```
  1. `uv run pytest` passes green on `main` — all five test classes present and passing
```
Replace with:
```
  1. `uv run pytest` passes green on `main` — four test classes present and passing: `test_counterfactual.py`, `test_schemas.py`, `test_aggregates.py`, `test_benchmarks.py`
```

**Edit 4 — Phase 2 success criterion 3.** Search:
```
  3. `tests/test_benchmarks.py` documents divergence from Ben Pile (2021 + 2026), REF subset, and Turver aggregate explicitly in output or docstring
```
Replace with:
```
  3. `tests/test_benchmarks.py` documents divergence from LCCC self-reconciliation and any regulator-native external sources the researcher located (OBR, Ofgem, DESNZ, HoC Library, NAO) explicitly in the fixture notes or test docstring
```

**Edit 5 — Phase 4 Requirements line.** Search:
```
**Requirements**: PUB-01, PUB-02, PUB-03, PUB-04, PUB-05, PUB-06, GOV-02, GOV-03, GOV-06 (snapshot URL portion)
```
Replace with:
```
**Requirements**: PUB-01, PUB-02, PUB-03, PUB-04, PUB-05, PUB-06, GOV-02, GOV-03, GOV-06 (snapshot URL portion), TEST-02, TEST-03, TEST-05
```

Do NOT modify:
- Any other phase section (Phase 1, Phase 3, Phase 5–12)
- The Progress Table at the bottom
- Phase 2's "Goal", "Depends on", "Plans", or "UI hint" fields
- Phase 2 success criteria 2, 4, 5 (pin test / CI trigger / methodology_version)

**Spec-precedence check.** Before saving, read `ARCHITECTURE.md` §11 P1 (Phase 2) and §11 P3 (Phase 4). Confirm neither enumerates specific TEST-** IDs per phase (RESEARCH.md confirms they don't). If either section DOES pin TEST-02/03/05 to Phase 2 explicitly, STOP — the spec takes precedence per user memory (`project_spec_source.md`), and the scope correction needs to land in ARCHITECTURE.md first. Do not ship ROADMAP edits that contradict ARCHITECTURE.md.
  </action>
  <verify>
    <automated>grep -q 'Four test classes, CI green on main' .planning/ROADMAP.md && grep -q '\*\*Requirements\*\*: TEST-01, TEST-04, TEST-06, GOV-04' .planning/ROADMAP.md && grep -q 'four test classes present and passing' .planning/ROADMAP.md && grep -q 'LCCC self-reconciliation and any regulator-native external sources' .planning/ROADMAP.md && grep -q 'TEST-02, TEST-03, TEST-05' .planning/ROADMAP.md && ! grep -q 'TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06, GOV-04' .planning/ROADMAP.md && ! grep -q 'Five test classes' .planning/ROADMAP.md && ! grep -q 'all five test classes' .planning/ROADMAP.md && ! grep -q 'Ben Pile' .planning/ROADMAP.md && ! grep -q 'Turver aggregate' .planning/ROADMAP.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'Four test classes, CI green on main' .planning/ROADMAP.md` exits 0
    - `grep -q '\*\*Requirements\*\*: TEST-01, TEST-04, TEST-06, GOV-04' .planning/ROADMAP.md` exits 0
    - `grep -q 'four test classes present and passing' .planning/ROADMAP.md` exits 0
    - `grep -q 'test_counterfactual.py.*test_schemas.py.*test_aggregates.py.*test_benchmarks.py' .planning/ROADMAP.md` exits 0 (all four file names on the criterion-1 line)
    - `grep -q 'LCCC self-reconciliation and any regulator-native external sources' .planning/ROADMAP.md` exits 0
    - `grep -q 'OBR, Ofgem, DESNZ, HoC Library, NAO' .planning/ROADMAP.md` exits 0
    - `grep -q 'PUB-01.*PUB-06.*GOV-02.*GOV-03.*GOV-06.*TEST-02, TEST-03, TEST-05' .planning/ROADMAP.md` exits 0 (Phase 4 line includes TEST reassignments)
    - `grep -q 'Five test classes' .planning/ROADMAP.md` exits 1 (removed from top-line bullet)
    - `grep -q 'all five test classes' .planning/ROADMAP.md` exits 1 (removed from criterion 1)
    - `grep -q 'Ben Pile' .planning/ROADMAP.md` exits 1 (removed from criterion 3)
    - `grep -q 'Turver aggregate' .planning/ROADMAP.md` exits 1 (removed from criterion 3)
    - `grep -q 'REF subset' .planning/ROADMAP.md` exits 1 (removed from criterion 3)
    - Phase 1, Phase 3, Phases 5–12 untouched (their line content still matches the pre-edit state)
  </acceptance_criteria>
  <done>
    Five surgical edits applied; Phase 2 and Phase 4 req lists + Phase 2 success criteria 1 + 3 + top-line bullet all reflect CONTEXT D-04/D-11; ARCHITECTURE.md §11 confirmed non-contradictory.
  </done>
</task>

<task type="auto">
  <name>Task 2: REQUIREMENTS.md — move TEST-02, TEST-03, TEST-05 traceability rows to Phase 4</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (whole file — confirm current row format on lines 167–173)
    - .planning/phases/02-test-benchmark-scaffolding/02-CONTEXT.md (D-04 rationale)
  </read_first>
  <behavior>
    - Three single-line edits in the Traceability table.
    - Search by row content, not line number (line numbers may have shifted).
    - TEST-01, TEST-04, TEST-06, GOV-04 rows UNCHANGED.
  </behavior>
  <action>
Open `.planning/REQUIREMENTS.md`. Make three surgical edits to the Traceability table. Preserve table column alignment (pipes + spacing).

**Edit 1.** Search:
```
| TEST-02 | Phase 2 | Pending |
```
Replace with:
```
| TEST-02 | Phase 4 | Pending |
```

**Edit 2.** Search:
```
| TEST-03 | Phase 2 | Pending |
```
Replace with:
```
| TEST-03 | Phase 4 | Pending |
```

**Edit 3.** Search:
```
| TEST-05 | Phase 2 | Pending |
```
Replace with:
```
| TEST-05 | Phase 4 | Pending |
```

Do NOT modify:
- TEST-01, TEST-04, TEST-06, GOV-04 rows (all stay `Phase 2 | Pending`)
- Any other TEST-**, PUB-**, RO-**, FIT-**, CON-**, CM-**, BAL-**, GRID-**, SEG-**, X-**, PORTAL-**, FND-**, TRIAGE-**, or GOV-** row
- The coverage summary at the bottom (v1 requirements: 61 — still 61; phase distribution changes but total is preserved)
- The body text of the requirement lists (lines 1–155) — only the traceability table rows change
  </action>
  <verify>
    <automated>grep -q '| TEST-02 | Phase 4 | Pending |' .planning/REQUIREMENTS.md && grep -q '| TEST-03 | Phase 4 | Pending |' .planning/REQUIREMENTS.md && grep -q '| TEST-05 | Phase 4 | Pending |' .planning/REQUIREMENTS.md && grep -q '| TEST-01 | Phase 2 | Pending |' .planning/REQUIREMENTS.md && grep -q '| TEST-04 | Phase 2 | Pending |' .planning/REQUIREMENTS.md && grep -q '| TEST-06 | Phase 2 | Pending |' .planning/REQUIREMENTS.md && grep -q '| GOV-04 | Phase 2 | Pending |' .planning/REQUIREMENTS.md && ! grep -q '| TEST-02 | Phase 2 | Pending |' .planning/REQUIREMENTS.md && ! grep -q '| TEST-03 | Phase 2 | Pending |' .planning/REQUIREMENTS.md && ! grep -q '| TEST-05 | Phase 2 | Pending |' .planning/REQUIREMENTS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '| TEST-02 | Phase 4 | Pending |' .planning/REQUIREMENTS.md` exits 0
    - `grep -q '| TEST-03 | Phase 4 | Pending |' .planning/REQUIREMENTS.md` exits 0
    - `grep -q '| TEST-05 | Phase 4 | Pending |' .planning/REQUIREMENTS.md` exits 0
    - `grep -q '| TEST-01 | Phase 2 | Pending |' .planning/REQUIREMENTS.md` exits 0 (unchanged)
    - `grep -q '| TEST-04 | Phase 2 | Pending |' .planning/REQUIREMENTS.md` exits 0 (unchanged)
    - `grep -q '| TEST-06 | Phase 2 | Pending |' .planning/REQUIREMENTS.md` exits 0 (unchanged)
    - `grep -q '| GOV-04 | Phase 2 | Pending |' .planning/REQUIREMENTS.md` exits 0 (unchanged)
    - `grep -q '| TEST-02 | Phase 2 | Pending |' .planning/REQUIREMENTS.md` exits 1 (moved)
    - `grep -q '| TEST-03 | Phase 2 | Pending |' .planning/REQUIREMENTS.md` exits 1 (moved)
    - `grep -q '| TEST-05 | Phase 2 | Pending |' .planning/REQUIREMENTS.md` exits 1 (moved)
    - Total row count in the traceability table unchanged (no rows added or deleted)
  </acceptance_criteria>
  <done>
    Three traceability rows moved from Phase 2 to Phase 4; the other four Phase-2 TEST/GOV rows unchanged; document otherwise pristine.
  </done>
</task>

<task type="auto">
  <name>Task 3: CHANGES.md — record the scope correction under [Unreleased] ### Changed</name>
  <files>CHANGES.md</files>
  <read_first>
    - CHANGES.md (current state — `[Unreleased]` block with `### Added` sub-section populated by Plans 02 + 03)
    - .planning/phases/02-test-benchmark-scaffolding/02-CONTEXT.md (D-04 language)
  </read_first>
  <behavior>
    - Adds a new `### Changed` sub-section inside `[Unreleased]`, AFTER the `### Added` sub-section.
    - Keep-a-Changelog 1.1.0 convention: `### Added` and `### Changed` are peer sub-sections of a release; ordering is conventionally Added → Changed → Deprecated → Removed → Fixed → Security.
    - Does NOT touch `### Added` bullets from prior plans.
    - Does NOT touch `## Methodology versions` section (Plan 01 owns it).
    - Does NOT touch `## [0.1.0] — 2026-04-21` released block.
  </behavior>
  <action>
Open `CHANGES.md`. Locate the `## [Unreleased]` section and its `### Added` sub-section (populated by Plans 02 + 03). IMMEDIATELY AFTER the last bullet of `### Added` (which should be the benchmarks-yaml bullet from Plan 03), INSERT a blank line followed by:

```markdown
### Changed
- Phase 2 scope correction (CONTEXT D-04): formal `TEST-02`, `TEST-03`,
  `TEST-05` requirement IDs reassigned from Phase 2 to Phase 4.
  Phase 2 still ships pre-Parquet scaffolding variants of
  `tests/test_schemas.py` and `tests/test_aggregates.py` (today), plus
  CI, pin test, and methodology versioning (TEST-01, TEST-04, TEST-06,
  GOV-04). Phase 4 will add the Parquet-schema + Parquet-aggregate
  variants + `tests/test_determinism.py` to satisfy the three formal
  requirements. `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md`
  updated.
- Phase 2 ROADMAP success criterion 1 reworded from "all five test
  classes" to "four test classes present and passing" enumerating the
  four file names. Success criterion 3 re-anchored from "Ben Pile /
  REF / Turver" to "LCCC self-reconciliation and any regulator-native
  external sources the researcher located" (CONTEXT D-11).
```

Do NOT modify:
- The `### Added` bullets above (Plans 02 + 03 content)
- `## Methodology versions` + the `### 1.0.0 — 2026-04-22` entry (Plan 01 content)
- `## [0.1.0] — 2026-04-21` and its sub-sections (Phase 1 release)
- The file header (lines 1–7 — title, Keep-a-Changelog link, SemVer link)
  </action>
  <verify>
    <automated>uv run python -c "import pathlib; t=pathlib.Path('CHANGES.md').read_text(encoding='utf-8'); assert '### Changed' in t; assert 'Phase 2 scope correction' in t; assert 'TEST-02' in t and 'TEST-03' in t and 'TEST-05' in t; assert 'CONTEXT D-04' in t; assert 'LCCC self-reconciliation' in t; assert '### 1.0.0 — 2026-04-22' in t; assert '## [0.1.0] — 2026-04-21' in t; assert 'tests/test_counterfactual.py' in t; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '### Changed' CHANGES.md` exits 0
    - `grep -q 'Phase 2 scope correction' CHANGES.md` exits 0
    - `grep -q 'CONTEXT D-04' CHANGES.md` exits 0
    - `grep -q 'TEST-02.*TEST-03.*TEST-05' CHANGES.md` exits 0 (or the three IDs on separate lines within the Changed bullet)
    - `grep -q 'LCCC self-reconciliation' CHANGES.md` exits 0
    - `grep -q 'Ben Pile' CHANGES.md` exits 0 (referenced in the re-anchor sentence as the source of the old phrasing) OR exits 1 (if the bullet uses different phrasing — either is acceptable)
    - `grep -q '### 1.0.0 — 2026-04-22' CHANGES.md` exits 0 (Plan 01 Methodology versions entry preserved)
    - `grep -q '## \[0.1.0\] — 2026-04-21' CHANGES.md` exits 0 (Phase 1 release block preserved)
    - `grep -q 'tests/test_counterfactual.py' CHANGES.md` exits 0 (Plan 02 `### Added` bullets preserved)
    - `grep -q 'elexon_agws_schema' CHANGES.md` exits 0 (Plan 02 `### Added` bullets preserved)
    - `grep -q 'tests/fixtures/benchmarks.yaml' CHANGES.md` exits 0 (Plan 03 `### Added` bullets preserved)
  </acceptance_criteria>
  <done>
    `[Unreleased] ### Changed` sub-section documents the scope correction; prior `### Added` bullets intact; `## Methodology versions` + `## [0.1.0]` unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan-time bookkeeping ↔ future-phase execution | ROADMAP + REQUIREMENTS edits are the source of truth the Phase-4 planner will read. Inconsistency between the two files = downstream plan drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 through T-02-05 | varied | N/A | N/A | This plan is documentation-only. No CI, no YAML, no test expectations, no secrets. |

**Documentation-integrity concern (outside STRIDE):** spec precedence. Per user memory (`project_spec_source.md`), ARCHITECTURE.md §11 is the authoritative phase plan — ROADMAP derives from it. Task 1's action explicitly halts if §11 contradicts the reword; RESEARCH.md confirms §11 does not pin specific TEST-** IDs per phase, so no contradiction is expected.
</threat_model>

<verification>
- Five ROADMAP edits verified by grep assertions in Task 1
- Three REQUIREMENTS traceability edits verified by grep in Task 2
- CHANGES.md `### Changed` sub-section verified by grep in Task 3
- Cross-document consistency: `grep -E 'TEST-0(2|3|5)' .planning/ROADMAP.md .planning/REQUIREMENTS.md` shows these IDs only in Phase-4 contexts after edits
- ARCHITECTURE.md §11 unchanged (spec precedence preserved)
</verification>

<success_criteria>
- ROADMAP.md Phase 2 and Phase 4 sections accurately reflect what Phase 2 actually delivers
- REQUIREMENTS.md traceability table agrees with ROADMAP
- CHANGES.md records the correction so a future reader can trace the decision
- Phase 2 can now close cleanly without a false "TEST-05 missing artefact" failure in phase-exit verification
- Phase-4 planning picks up TEST-02, TEST-03, TEST-05 alongside PUB-* work
</success_criteria>

<output>
After completion, create `.planning/phases/02-test-benchmark-scaffolding/02-05-SUMMARY.md` following the summary template. Note in the SUMMARY whether ARCHITECTURE.md §11 spot-check passed (expected: yes) so future auditors have the trail.
</output>
