---
phase: 02-test-benchmark-scaffolding
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - .github/workflows/ci.yml
autonomous: false
requirements:
  - TEST-06
tags: [ci, github-actions, uv, pytest]
threat_mitigations:
  - threat: T-02-01
    disposition: mitigate
    mitigation: "Third-party action `astral-sh/setup-uv` pinned to the specific tag `@v8` (NOT `@main` and NOT `@latest`). `actions/checkout` pinned to `@v5`. Both pins are grep-visible; a future PR upgrading the action must edit this file explicitly."
  - threat: T-02-02
    disposition: mitigate
    mitigation: "Workflow consumes no secrets. `uv run pytest` reads local CSV/XLSX from `data/` — no network calls in the test path (the only network-hitting tests carry `@pytest.mark.skip`). Workflow does not `echo` any env vars."
  - threat: T-02-03
    disposition: "N/A — this plan does not touch the T-02-03 surface (benchmarks.yaml)"
  - threat: T-02-04
    disposition: "N/A — this plan does not modify counterfactual constants"
  - threat: T-02-05
    disposition: "N/A — branch protection deferred per CONTEXT out-of-scope clause"

must_haves:
  truths:
    - "`.github/workflows/ci.yml` exists and is valid YAML"
    - "Workflow triggers on `push` to `main` and on `pull_request`"
    - "Workflow runs a single job on `ubuntu-latest` with Python 3.12"
    - "Workflow uses `astral-sh/setup-uv@v8.1.0` (pinned tag, NOT `@main` or `@latest`) and `actions/checkout@v5`"
    - "Workflow runs `uv sync --frozen` then `uv run --frozen pytest -v`"
    - "No ruff / pyright / mkdocs-strict steps (deferred to Phase 3+)"
    - "No secrets consumed; no env vars echoed"
    - "Locally: `uv run pytest` already exits 0 after Plans 01/02/03 — CI simply reproduces this on a clean runner"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Single-job GitHub Actions workflow running pytest on push/PR"
      contains: "astral-sh/setup-uv@v8.1.0"
    - path: ".github/workflows/ci.yml"
      provides: "pytest step"
      contains: "uv run --frozen pytest"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "uv.lock"
      via: "uv sync --frozen fails if pyproject.toml drifts from uv.lock"
      pattern: "uv sync --frozen"
    - from: ".github/workflows/ci.yml"
      to: "tests/test_{counterfactual,schemas,aggregates,benchmarks}.py"
      via: "uv run --frozen pytest discovers and runs the new test files"
      pattern: "uv run --frozen pytest"
---

<objective>
Ship TEST-06: GitHub Actions CI workflow at `.github/workflows/ci.yml` that runs `uv run --frozen pytest` on every push to `main` and every pull request. Single job, Python 3.12, pinned third-party actions, no secrets, no lint gates.

Purpose: Close the Phase-2 feedback loop. Plans 01–03 authored four test classes and a methodology-versioning mechanism — this plan makes those tests gate every PR against `main`. Any future constant change, schema drift, row-leakage, or benchmark divergence now fails CI before merge.

Output: A single new file `.github/workflows/ci.yml` following the Astral `setup-uv` README example verbatim (RESEARCH.md Pattern 6), plus a human-verification checkpoint confirming the workflow goes green on an actual GitHub run.

**Scope discipline (per CONTEXT "Claude's Discretion"):** no `ruff`, no `pyright`, no `mkdocs build --strict`, no matrix, no secrets, no deploy. Phase 3+ owns the lint/build gates; Phase 4 owns the daily-refresh cron (GOV-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-test-benchmark-scaffolding/02-CONTEXT.md
@.planning/phases/02-test-benchmark-scaffolding/02-RESEARCH.md
@.planning/phases/02-test-benchmark-scaffolding/02-PATTERNS.md
@.planning/phases/02-test-benchmark-scaffolding/02-01-SUMMARY.md
@.planning/phases/02-test-benchmark-scaffolding/02-02-SUMMARY.md
@.planning/phases/02-test-benchmark-scaffolding/02-03-SUMMARY.md
@pyproject.toml

<interfaces>
**No in-repo CI workflow analog** — `.github/` directory does not exist. This plan creates it.

**Exogenous pattern source (verbatim, RESEARCH.md Pattern 6, PATTERNS.md lines 377–416):**

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0
        with:
          enable-cache: true
          python-version: "3.12"

      - name: Install dependencies
        run: uv sync --frozen

      - name: Run tests
        run: uv run --frozen pytest -v
```

**Python floor verification (pyproject.toml):**
- Line 5: `requires-python = ">=3.12"`
- Line 17: `"pytest>=9.0.3"` (dev-dep already pinned)
- No new deps required by Phase 2 per RESEARCH.md §"Standard Stack".

**Expected local state before Wave-4 execution:** `uv run pytest` already exits 0 covering Plans 01–03 tests (counterfactual pin, schemas, aggregates, benchmarks). If local run is red, STOP — do not ship a workflow that codifies a broken suite.

**Pin discipline (T-02-01):** `astral-sh/setup-uv@v8.1.0` (fully-qualified minor+patch tag, NOT `@v8` floating major, NOT `@main`, NOT `@latest`). `actions/checkout@v5` is also pinned. Fully-qualified pinning closes the checker warning about `@v8` being a moving major-version tag; any upgrade must edit this YAML explicitly and appears in review diff. RESEARCH.md justified `@v8`; the bump to `@v8.1.0` is within Claude's Discretion per CONTEXT and strengthens supply-chain hygiene.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm local green + create .github/workflows/ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - pyproject.toml (verify `requires-python = ">=3.12"` on line 5; `pytest>=9.0.3` on line 17)
    - .planning/phases/02-test-benchmark-scaffolding/02-PATTERNS.md lines 377–416 (Pattern 6 — CI workflow)
    - .planning/phases/02-test-benchmark-scaffolding/02-RESEARCH.md §"Pattern 6" (full skeleton with rationale)
    - .planning/phases/02-test-benchmark-scaffolding/02-01-SUMMARY.md (Plan 01 outputs — confirm METHODOLOGY_VERSION landed)
    - .planning/phases/02-test-benchmark-scaffolding/02-02-SUMMARY.md (Plan 02 outputs — confirm schemas + test files landed)
    - .planning/phases/02-test-benchmark-scaffolding/02-03-SUMMARY.md (Plan 03 outputs — confirm benchmarks fixture + test landed)
  </read_first>
  <behavior>
    - Before authoring the workflow: executor runs `uv run pytest -v` locally and verifies exit 0. If red, halt and report back; do not ship CI over a broken suite.
    - Creates `.github/` directory and `.github/workflows/` subdirectory if absent.
    - Creates `.github/workflows/ci.yml` with the exact verbatim content from the action block below.
    - `astral-sh/setup-uv@v8.1.0` — pinned tag. NOT `@main`, NOT `@latest`.
    - `actions/checkout@v5` — pinned tag.
    - Python 3.12, single job, ubuntu-latest.
    - `uv sync --frozen` and `uv run --frozen pytest -v` — `--frozen` (not `--locked`) mirrors the Astral README example and forbids lockfile updates (RESEARCH.md "Alternatives Considered" row 1).
    - No secrets, no env vars, no matrix, no lint steps.
  </behavior>
  <action>
**Step A — verify local green before authoring CI.** From repo root, run:

```bash
uv run pytest -v
```

Expected: exit code 0, all tests from Plans 01–03 pass (counterfactual pin + schemas + aggregates + benchmarks floor, plus existing `tests/data/` tests). If the run is red, STOP — do not proceed to Step B. Report the failure back; diagnose in the failing plan's scope (01, 02, or 03), not here.

**Step B — create directory structure.** From repo root:

```bash
mkdir -p .github/workflows
```

**Step C — create `.github/workflows/ci.yml` with exactly this content:**

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0
        with:
          enable-cache: true
          python-version: "3.12"

      - name: Install dependencies
        run: uv sync --frozen

      - name: Run tests
        run: uv run --frozen pytest -v
```

Do not add:
- `ruff` / `pyright` / `black` / `isort` steps (deferred to Phase 3+)
- `mkdocs build --strict` step (deferred to Phase 3+)
- Matrix over Python versions (the `requires-python = ">=3.12"` floor means 3.12 is the only supported version; newer is redundant until we explicitly add a matrix)
- Schedule / cron triggers (the daily-refresh workflow is a Phase-4 concern, GOV-03, in a separate file)
- Env vars, secrets, deploy steps, artefact uploads
- Workflow-level concurrency limits (not required for Phase 2)

The file is ~20 lines. Keep it that way.
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && uv run python -c "import yaml; d = yaml.safe_load(open('.github/workflows/ci.yml')); assert 'on' in d or True in d; assert 'jobs' in d and 'test' in d['jobs']; steps = d['jobs']['test']['steps']; assert any('astral-sh/setup-uv@v8.1.0' == s.get('uses','') for s in steps); assert any('uv sync --frozen' in s.get('run','') for s in steps); assert any('uv run --frozen pytest' in s.get('run','') for s in steps); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `.github/workflows/ci.yml` exists
    - `grep -q 'astral-sh/setup-uv@v8.1.0' .github/workflows/ci.yml` exits 0 (pinned tag, T-02-01 mitigated)
    - `grep -q 'actions/checkout@v5' .github/workflows/ci.yml` exits 0
    - `grep -Eq 'astral-sh/setup-uv@(main|latest|v8)$' .github/workflows/ci.yml` exits 1 (non-zero = no unpinned or floating-major reference; the fully-qualified `@v8.1.0` tag must be used)
    - `grep -q 'python-version: "3.12"' .github/workflows/ci.yml` exits 0
    - `grep -q 'uv sync --frozen' .github/workflows/ci.yml` exits 0
    - `grep -q 'uv run --frozen pytest' .github/workflows/ci.yml` exits 0
    - `grep -q 'branches: \[main\]' .github/workflows/ci.yml` exits 0
    - `grep -q 'pull_request' .github/workflows/ci.yml` exits 0
    - `grep -iq 'secrets\.' .github/workflows/ci.yml` exits 1 (no secrets consumed, T-02-02 mitigated)
    - `grep -iq 'schedule:' .github/workflows/ci.yml` exits 1 (no cron — daily refresh is Phase 4)
    - `grep -iq 'ruff\|pyright\|mkdocs' .github/workflows/ci.yml` exits 1 (no lint gates — Phase 3+)
    - `uv run python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0 (valid YAML)
    - Local `uv run pytest` was verified green in Step A before file creation (executor notes the pytest output in their task report)
  </acceptance_criteria>
  <done>
    CI workflow exists, valid YAML, pinned actions, scoped correctly (pytest only — no lint, no cron, no deploy), local pytest green before file landed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — CI goes green on first real run</name>
  <files>.github/workflows/ci.yml (observed only — no file edits in this task)</files>
  <what-built>
    `.github/workflows/ci.yml` — single-job GitHub Actions workflow. Runs `uv sync --frozen` + `uv run --frozen pytest -v` on every push to `main` and every pull request. Python 3.12, ubuntu-latest, pinned actions, no secrets.
  </what-built>
  <how-to-verify>
    1. **Commit + push.** The workflow file must be on a branch pushed to `origin`. If Plan 04 is being executed on a feature branch, push that branch now:
       ```bash
       git push -u origin HEAD
       ```
    2. **Open a throwaway PR** (or let the orchestrator's normal merge flow open one). Suggested body: "Phase 2: test + CI scaffolding."
    3. **Observe the CI run on the PR.** From a browser: open the PR page, look for the "CI / test" check. From CLI:
       ```bash
       gh run list --limit 3
       gh run watch  # optional, streams the latest run
       ```
    4. **Expected outcome:** the check reports green. Pipeline runs to completion in ~2–4 minutes. All tests from Plans 01/02/03 pass, plus existing `tests/data/*` (the live-network ones are skipped via `@pytest.mark.skip`).
    5. **Confirm push-to-`main` trigger also fires.** After merge (or on a direct push to `main` if permitted), confirm a run appears for the `main` branch push event:
       ```bash
       gh run list --branch main --limit 3
       ```
    6. **Red-run diagnosis.** If the CI fails:
       - `gh run view <run-id> --log-failed` — read the failing step's log
       - If `uv sync --frozen` fails → `pyproject.toml` drifted from `uv.lock`; regenerate locally with `uv lock` and push
       - If a pytest test fails that passes locally → the test carries a machine-specific assumption (timezone, locale, decimal precision); either fix the test or make the comparison more robust
       - If `astral-sh/setup-uv@v8.1.0` fails to resolve → version tag drift; bump to the current stable tag per https://github.com/astral-sh/setup-uv/releases and update the pin in this file with a CHANGES.md entry
  </how-to-verify>
  <action>
    Push the current branch to origin, open a pull request (or rely on the orchestrator flow to open one), and observe the GitHub Actions CI run on both the PR and on the subsequent push to `main`. See the `<how-to-verify>` block for the exact commands. Do NOT edit `.github/workflows/ci.yml` in this task — that was Task 1's responsibility. This task is purely an observation checkpoint.
  </action>
  <verify>
    Manual (authoritative): human watches the PR + main run complete green per the `<how-to-verify>` steps. This is a checkpoint:human-verify task — the human signal is the contract. No `<automated>` block: `gh run list --branch HEAD` is not valid `gh` syntax (the `--branch` flag expects a branch name), and any automated probe here would either always pass or always fail regardless of CI outcome.
  </verify>
  <done>
    CI run on the PR is green; CI run on push-to-`main` (after merge) is also green; human approves with `approved` signal.
  </done>
  <resume-signal>
    Type `approved` once the CI check is green on BOTH a PR run AND a push-to-`main` run. Or describe the failure mode (with `gh run view` output) if red.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner ↔ repo | Untrusted third-party action code executes against our repo contents. Mitigated by pinning action versions. |
| CI logs ↔ public GitHub | Public repos expose CI logs to anyone. Mitigated by consuming zero secrets in Phase 2 CI and never echoing env. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Supply-chain (third-party action) | `.github/workflows/ci.yml` — `astral-sh/setup-uv`, `actions/checkout` | mitigate | `astral-sh/setup-uv@v8.1.0` + `actions/checkout@v5` — specific tags, NOT `@main` or `@latest`. Pin is grep-visible; any upgrade PR must edit the YAML explicitly. Astral is a trusted maintainer of the Python tooling ecosystem (same org publishing the `uv` binary we already trust via `uv.lock`). |
| T-02-02 | Information Disclosure (CI secrets / env) | `.github/workflows/ci.yml` | mitigate | Workflow consumes zero secrets (`grep -q 'secrets\.' ci.yml` returns non-zero). Test suite reads local `data/` files; no external APIs hit in CI (live-network tests carry `@pytest.mark.skip`). No `env:` block; no `echo $VAR` steps. CI logs contain only pytest output. |
| T-02-03 | Tampering (YAML) | N/A | N/A | This plan does not touch `benchmarks.yaml`. |
| T-02-04 | Tampering (test integrity) | N/A | N/A | This plan does not modify counterfactual constants or test expected values. |
| T-02-05 | EoP (branch protection) | `main` branch | N/A — branch protection deferred per CONTEXT out-of-scope clause | Phase 2 ships the CI signal; enforcement (required check on `main`, required review, etc.) is a GitHub UI step outside the repo. CONTEXT treats this as advisory for Phase 2; formal branch-protection rollout is deferred. |
</threat_model>

<verification>
- File exists, valid YAML, pinned actions → automated grep assertions above
- Local `uv run pytest -v` green before file landed → manual check in Step A
- CI first-run green on PR → human-verify checkpoint
- CI first-run green on push-to-`main` → human-verify checkpoint
- `uv sync --frozen` fails on pyproject/uv.lock drift — intentional, part of reproducibility guarantee
</verification>

<success_criteria>
- `.github/workflows/ci.yml` exists, valid YAML, pinned to `astral-sh/setup-uv@v8.1.0` + `actions/checkout@v5`
- Workflow triggers on push-to-main AND pull_request
- Single `test` job runs `uv sync --frozen` + `uv run --frozen pytest -v` on Python 3.12 / ubuntu-latest
- No lint / build / cron / deploy steps (scope discipline per CONTEXT)
- CI reports green on the first real PR + push-to-`main` run (human-verified)
- Phase 2 close-out gate: all five roadmap success criteria become verifiable (pytest green in CI = criterion 1 + 4; methodology_version present = criterion 5; pin test exists = criterion 2; benchmarks documented = criterion 3 after Plan 05 rewords)
</success_criteria>

<output>
After completion, create `.planning/phases/02-test-benchmark-scaffolding/02-04-SUMMARY.md` following the summary template. Include the first successful CI run URL in the summary for future-reference traceability.
</output>
