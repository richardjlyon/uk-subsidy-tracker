---
phase: 06-flagship-cross-scheme-charts
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/uk_subsidy_tracker/plotting/portal/__init__.py
  - src/uk_subsidy_tracker/plotting/portal/x1_stacked_total.py
  - src/uk_subsidy_tracker/plotting/portal/x2_cumulative_premium.py
  - src/uk_subsidy_tracker/plotting/portal/x3_per_household.py
  - src/uk_subsidy_tracker/plotting/__main__.py
autonomous: true
requirements: [X-01, X-02, X-03]
must_haves:
  truths:
    - "X1 stacked-by-scheme chart renders Twitter PNG (without rangeselector buttons) AND interactive HTML (with 1y/5y/All rangeselector buttons) from cross_scheme.parquet"
    - "X2 cumulative-premium chart renders both Twitter PNG and interactive HTML from cross_scheme.parquet"
    - "X3 per-household chart renders both Twitter PNG and interactive HTML; pre-2014 bars are omitted (per D-Discretion + RESEARCH Q3)"
    - "X1 PNG hero shows 'All-time' static view with NO rangeselector button overlay (per UI-SPEC §2 lock)"
    - "X1 chart subtitle reads 'Covers 2 of 8 schemes — see scheme grid for coverage status.' verbatim (per Copywriting Contract)"
    - "All 3 charts use SCHEME_COLORS palette consistently (CfD = #1f77b4, RO = #d62728)"
    - "X1/X2/X3 main() entry points are wired into plotting/__main__.py charts list"
  artifacts:
    - path: "src/uk_subsidy_tracker/plotting/portal/__init__.py"
      provides: "Plotting subpackage barrel (mirror plotting/subsidy/__init__.py)"
    - path: "src/uk_subsidy_tracker/plotting/portal/x1_stacked_total.py"
      provides: "X1 stacked-by-scheme chart with TWO-figure pattern (PNG no buttons + HTML with rangeselector)"
      contains: "def main"
    - path: "src/uk_subsidy_tracker/plotting/portal/x2_cumulative_premium.py"
      provides: "X2 cumulative premium over gas chart"
      contains: "def main"
    - path: "src/uk_subsidy_tracker/plotting/portal/x3_per_household.py"
      provides: "X3 per-household stacked-by-scheme chart"
      contains: "def main"
    - path: "docs/charts/html/x1_stacked_total_twitter.png"
      provides: "X1 Twitter hero (1200×675, no rangeselector)"
    - path: "docs/charts/html/x1_stacked_total.html"
      provides: "X1 interactive HTML with rangeselector buttons"
    - path: "docs/charts/html/x2_cumulative_premium_twitter.png"
      provides: "X2 Twitter hero"
    - path: "docs/charts/html/x2_cumulative_premium.html"
      provides: "X2 interactive HTML"
    - path: "docs/charts/html/x3_per_household_twitter.png"
      provides: "X3 Twitter hero"
    - path: "docs/charts/html/x3_per_household.html"
      provides: "X3 interactive HTML"
  key_links:
    - from: "src/uk_subsidy_tracker/plotting/portal/x1_stacked_total.py"
      to: "data/derived/portal/cross_scheme.parquet"
      via: "_prepare() reads via portal.DERIVED_DIR"
      pattern: "portal.DERIVED_DIR"
    - from: "src/uk_subsidy_tracker/plotting/portal/x{1,2,3}_*.py"
      to: "src/uk_subsidy_tracker/plotting/colors.py::SCHEME_COLORS"
      via: "from uk_subsidy_tracker.plotting.colors import SCHEME_COLORS"
      pattern: "SCHEME_COLORS"
    - from: "src/uk_subsidy_tracker/plotting/__main__.py"
      to: "src/uk_subsidy_tracker/plotting/portal/x{1,2,3}*.py"
      via: "import + charts list tuple append"
      pattern: "x1_stacked_total"
---

<objective>
Build 3 of the 5 flagship cross-scheme X-charts (X1, X2, X3) per UI-SPEC §"Component Inventory" and Copywriting Contract. All read from `cross_scheme.parquet` (Wave 1 substrate). X1 implements the locked TWO-figure pattern (PNG hero without rangeselector + HTML with native Plotly `rangeselector` 1y/5y/All buttons per D-07). X2 plots combined cumulative premium across schemes. X3 plots per-household stacked-by-scheme with pre-2014 bars omitted (per RESEARCH Q3 recommendation). All wire into `plotting/__main__.py`.

Purpose: PORTAL-01 requires X1 hero on the homepage; X1/X2/X3 are the "P1 flagship" trio per REQUIREMENTS.md. Wave 4 docs/portal/ pages embed these PNGs + HTMLs; Wave 5 docs/index.md embeds X1.

Output: 4 new src/plotting/portal/ files; 1 modified plotting/__main__.py; 6 new chart artefacts (3 PNG + 3 HTML; .div.html siblings auto-emitted by ChartBuilder.save()).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-flagship-cross-scheme-charts/06-CONTEXT.md
@.planning/phases/06-flagship-cross-scheme-charts/06-RESEARCH.md
@.planning/phases/06-flagship-cross-scheme-charts/06-PATTERNS.md
@.planning/phases/06-flagship-cross-scheme-charts/06-UI-SPEC.md
@.planning/phases/06-flagship-cross-scheme-charts/06-01-PLAN.md
@CLAUDE.md

<interfaces>
From src/uk_subsidy_tracker/plotting/chart_builder.py (verified API):
```python
class ChartBuilder:
    def __init__(self, title: str, height: int = 600, width: int = 1200): ...
    def create_basic(self) -> go.Figure: ...
    def create_subplots(self, rows, cols, ...) -> go.Figure: ...
    def format_currency_axis(self, fig, axis="y", suffix=" bn", title=None) -> None: ...
    def save(
        self,
        fig: go.Figure,
        slug: str,
        export_twitter: bool = False,
        export_html: bool = False,
        export_div: bool = False,
    ) -> None:
        """Output dir defaults to OUTPUT_DIR = PROJECT_ROOT / 'docs' / 'charts' / 'html'.
        Twitter PNG = 1200×675 @ scale=2; HTML = full Plotly; div = embeddable .div.html."""
```

From src/uk_subsidy_tracker/plotting/colors.py (Wave 1 output):
```python
SCHEME_COLORS: dict[str, str] = {
    "CfD": "#1f77b4",  # offshore wind blue
    "RO":  "#d62728",  # biomass red
    # 6 reserved slots for Phases 7-12
}
```

From src/uk_subsidy_tracker/schemes/portal/__init__.py (Wave 1 output):
```python
DERIVED_DIR: Path = PROJECT_ROOT / "data" / "derived" / "portal"
SHIPPED_SCHEMES: tuple[str, ...] = ("CfD", "RO")
```

From data/derived/portal/cross_scheme.parquet (Wave 1 output, 7 columns in declaration order):
year (int64), scheme (string='CfD'|'RO'), cost_gbp (float64), premium_gbp (float64),
generation_mwh (float64, nullable), households_uk (int64, nullable for pre-2014),
methodology_version (string)

From src/uk_subsidy_tracker/plotting/__main__.py (current charts list near lines 14-46 + 66-90):
```python
# Existing: 18 chart entries imported and listed in `charts` tuple
# Append imports + 3 new tuples: ("x1_stacked_total", x1_stacked_total), ...
```

From src/uk_subsidy_tracker/plotting/subsidy/ro_dynamics.py (analog for empty-data placeholder + 5-step recipe):
```python
def _prepare() -> pd.DataFrame: ...  # read parquet; return empty DF if absent
def _placeholder(builder: ChartBuilder) -> go.Figure: ...  # graceful degradation
def main() -> None: ...  # builder.save(...); calls _placeholder if df empty
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cross_scheme.parquet read | Trusted (Wave 1 substrate; row-conservation + schema + determinism tests gate it) |
| Plotly + kaleido PNG export | Trusted (already-shipped pattern across 18 prior charts) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | SCHEME_COLORS palette drift | mitigate | Wave 1 Provenance: docstring on SCHEME_COLORS; chart modules import from colors.py (single source of truth); palette tested against PLOT_BG WCAG AA (UI-SPEC §"Per-scheme palette" criterion 4) |
| T-06-02-02 | Information Disclosure | X-chart PNG/HTML reveal subsidy data | accept | All numbers public; sourced from regulator data |
| T-06-02-03 | Denial of Service | Empty cross_scheme.parquet breaks chart regen | mitigate | Each chart's main() short-circuits to _placeholder() if _prepare() returns empty DataFrame (analog: ro_dynamics.py); ensures `python -m uk_subsidy_tracker.plotting` succeeds end-to-end even without data |
| T-06-02-04 | Repudiation | X1 PNG hero secretly carries rangeselector buttons | mitigate | UI-SPEC §"X1 hero embed" lock requires PNG without buttons; implement TWO-figure pattern per RESEARCH §"Plotly rangeselector specifics" Option A; visual smoke check in acceptance criteria |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create plotting/portal/ subpackage barrel + X1 stacked-by-scheme chart with TWO-figure pattern</name>
  <files>
    src/uk_subsidy_tracker/plotting/portal/__init__.py,
    src/uk_subsidy_tracker/plotting/portal/x1_stacked_total.py
  </files>
  <read_first>
    - src/uk_subsidy_tracker/plotting/subsidy/__init__.py (full file — barrel pattern; package docstring)
    - src/uk_subsidy_tracker/plotting/subsidy/ro_dynamics.py (full file — 5-step recipe + _prepare() + _placeholder() + main(); empty-data graceful degradation)
    - src/uk_subsidy_tracker/plotting/subsidy/cfd_dynamics.py (lines 1-100 — single-figure layout discipline)
    - src/uk_subsidy_tracker/plotting/chart_builder.py lines 1-100 + lines 310-400 (ChartBuilder API + save() signature with independent export flags)
    - src/uk_subsidy_tracker/plotting/theme.py (FONT_FAMILY, TITLE_COLOR, TEXT_COLOR, PLOT_BG constants)
    - src/uk_subsidy_tracker/plotting/colors.py (SCHEME_COLORS — Wave 1 output)
    - src/uk_subsidy_tracker/schemes/portal/__init__.py (DERIVED_DIR + SHIPPED_SCHEMES — Wave 1 output)
    - .planning/phases/06-flagship-cross-scheme-charts/06-RESEARCH.md §"Plotly rangeselector specifics" lines 218-280, §"Plotting pattern" lines 716-856
    - .planning/phases/06-flagship-cross-scheme-charts/06-PATTERNS.md §"Plotting modules" §"x1_stacked_total.py" lines 363-475 (CRITICAL deviation: TWO-figure pattern)
    - .planning/phases/06-flagship-cross-scheme-charts/06-UI-SPEC.md §2 X1 hero embed lines 172-191 + Copywriting Contract lines 281-285
  </read_first>
  <action>
    **Step 1.1 — Create `src/uk_subsidy_tracker/plotting/portal/__init__.py`** (barrel; mirror plotting/subsidy/__init__.py):
    - Module docstring: "Cross-scheme portal chart modules (Phase 6, REQUIREMENTS X-01..X-05). Each X-chart reads `data/derived/portal/cross_scheme.parquet` (Wave 1 substrate) and emits Twitter-PNG + interactive HTML + div HTML via ChartBuilder.save(). UI-SPEC §"Component Inventory" + Copywriting Contract are the visual contract."
    - Body: empty (no re-exports needed; main() functions imported via `from uk_subsidy_tracker.plotting.portal.<slug> import main as <slug>` in plotting/__main__.py per Task 4).

    **Step 1.2 — Create `src/uk_subsidy_tracker/plotting/portal/x1_stacked_total.py`** (5-step recipe + TWO-figure pattern):
    - Module docstring: "X1 — Total UK subsidy stacked by scheme, annual. PORTAL-01 P1 flagship chart. Plotly native rangeselector (1y/5y/All) on the interactive HTML; Twitter-PNG hero is a static All-time view. Stacks only schemes with full reconstruction (CfD + RO today). REQUIREMENT X-01."
    - Imports per RESEARCH §"Plotting pattern" lines 720-731:
      ```python
      from __future__ import annotations
      import copy
      import pandas as pd
      import plotly.graph_objects as go
      import pyarrow.parquet as pq
      from uk_subsidy_tracker.plotting import ChartBuilder
      from uk_subsidy_tracker.plotting.colors import SCHEME_COLORS
      from uk_subsidy_tracker.schemes import portal
      ```

    - **`_prepare() -> pd.DataFrame`** (per PATTERNS lines 376-387):
      ```python
      def _prepare() -> pd.DataFrame:
          src = portal.DERIVED_DIR / "cross_scheme.parquet"
          if not src.exists():
              return pd.DataFrame()
          df = pq.read_table(src).to_pandas()
          if len(df) == 0:
              return df
          # Coerce year → datetime for rangeselector compatibility (X1 only).
          # Parquet `year` stays int64 for D-21 determinism; coercion lives at the
          # plotting boundary per RESEARCH §"Plotly rangeselector specifics" line 222-225.
          df["year_dt"] = pd.to_datetime(df["year"], format="%Y")
          # Stack order: biggest scheme at the bottom (UI-SPEC §"Stack order on X1" lines 146).
          # Compute per-scheme total cost; sort schemes ascending so groupby preserves order.
          totals = df.groupby("scheme")["cost_gbp"].sum().sort_values(ascending=True)
          df["scheme"] = pd.Categorical(df["scheme"], categories=list(totals.index), ordered=True)
          return df.sort_values(["year", "scheme"])
      ```

    - **`_placeholder(builder: ChartBuilder) -> go.Figure`** (graceful degradation; per PATTERNS lines 390-403):
      ```python
      def _placeholder(builder: ChartBuilder) -> go.Figure:
          fig = builder.create_basic()
          fig.add_annotation(
              x=0.5, y=0.5, xref="paper", yref="paper",
              text=("<b>No cross-scheme data yet</b><br><br>"
                    "data/derived/portal/cross_scheme.parquet is empty.<br>"
                    "Run schemes.portal.rebuild_derived() first."),
              showarrow=False, font={"size": 14, "color": "#9ca3af"},
          )
          fig.update_xaxes(visible=False)
          fig.update_yaxes(visible=False)
          return fig
      ```

    - **`_build_stacked_figure(df: pd.DataFrame, builder: ChartBuilder) -> go.Figure`** (extract trace + layout logic for reuse across PNG + HTML):
      ```python
      def _build_stacked_figure(df: pd.DataFrame, builder: ChartBuilder) -> go.Figure:
          fig = builder.create_basic()
          for scheme_name, sub in df.groupby("scheme", observed=True, sort=False):
              fig.add_trace(go.Bar(
                  x=sub["year_dt"],
                  y=sub["cost_gbp"] / 1e9,
                  name=str(scheme_name),
                  marker_color=SCHEME_COLORS[str(scheme_name)],
                  hovertemplate="%{x|%Y}<br>" + str(scheme_name) +
                                "<br>£%{y:.2f} bn<extra></extra>",
              ))
          fig.update_layout(
              barmode="stack",
              title=dict(
                  text="<b>Total UK subsidy stacked by scheme</b>",
                  subtitle=dict(
                      text="Covers 2 of 8 schemes — see scheme grid for coverage status.",
                      font=dict(size=12, color="#a0a4b8"),
                  ),
                  x=0.05, xanchor="left",
              ),
          )
          builder.format_currency_axis(fig, axis="y", suffix=" bn", title="Subsidy cost (£bn)")
          return fig
      ```

    - **`main() -> None`** (TWO-figure pattern; CRITICAL per UI-SPEC §"X1 hero embed" lock + PATTERNS lines 453-474):
      ```python
      def main() -> None:
          df = _prepare()
          builder = ChartBuilder(
              title="Total UK subsidy stacked by scheme",
              height=600,
          )
          if df.empty:
              fig = _placeholder(builder)
              builder.save(fig, "x1_stacked_total", export_twitter=True)
              return

          # Build BASE figure (traces + layout + subtitle), no rangeselector
          fig_base = _build_stacked_figure(df, builder)

          # PNG hero — All-time view, NO rangeselector (UI-SPEC §"X1 hero embed" lock)
          fig_png = go.Figure(fig_base)
          fig_png.update_xaxes(type="date", title="Year")
          builder.save(fig_png, "x1_stacked_total",
                       export_twitter=True, export_html=False, export_div=False)

          # HTML interactive — native Plotly rangeselector buttons (1y / 5y / All)
          fig_html = go.Figure(fig_base)
          fig_html.update_xaxes(
              rangeselector=dict(
                  buttons=[
                      dict(count=1, label="1y", step="year", stepmode="backward"),
                      dict(count=5, label="5y", step="year", stepmode="backward"),
                      dict(step="all", label="All"),
                  ],
                  xanchor="left", yanchor="bottom", x=0.0, y=1.02,
                  active=2,  # default active = "All" per UI-SPEC line 185
              ),
              type="date",  # REQUIRED — rangeselector needs a date-typed axis
              title="Year",
          )
          builder.save(fig_html, "x1_stacked_total",
                       export_twitter=False, export_html=True, export_div=True)


      if __name__ == "__main__":
          main()
      ```

    **Step 1.3 — Smoke-test X1 directly**:
    ```bash
    uv run python -m uk_subsidy_tracker.plotting.portal.x1_stacked_total
    ```
    Verify 3 files emitted to `docs/charts/html/`:
    - `x1_stacked_total_twitter.png` (Twitter PNG, ~12-50 KB)
    - `x1_stacked_total.html` (Plotly interactive HTML; contains rangeselector buttons)
    - `x1_stacked_total.div.html` (embeddable div HTML)
  </action>
  <verify>
    <automated>
      uv run python -m uk_subsidy_tracker.plotting.portal.x1_stacked_total &&
      test -f docs/charts/html/x1_stacked_total_twitter.png &&
      test -f docs/charts/html/x1_stacked_total.html &&
      test -f docs/charts/html/x1_stacked_total.div.html &&
      grep -c "rangeselector" docs/charts/html/x1_stacked_total.html &&
      grep -c "rangeselector" docs/charts/html/x1_stacked_total.div.html
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists: `src/uk_subsidy_tracker/plotting/portal/__init__.py`
    - File exists: `src/uk_subsidy_tracker/plotting/portal/x1_stacked_total.py`
    - File exists: `docs/charts/html/x1_stacked_total_twitter.png` (size ≥ 5000 bytes)
    - File exists: `docs/charts/html/x1_stacked_total.html` (size ≥ 50000 bytes — Plotly bundle)
    - File exists: `docs/charts/html/x1_stacked_total.div.html`
    - `grep -c "def main" src/uk_subsidy_tracker/plotting/portal/x1_stacked_total.py` returns 1
    - `grep -c "def _prepare" src/uk_subsidy_tracker/plotting/portal/x1_stacked_total.py` returns 1
    - `grep -c "def _placeholder" src/uk_subsidy_tracker/plotting/portal/x1_stacked_total.py` returns 1
    - `grep -c "def _build_stacked_figure" src/uk_subsidy_tracker/plotting/portal/x1_stacked_total.py` returns 1 (extracted for TWO-figure pattern)
    - `grep -c "rangeselector" src/uk_subsidy_tracker/plotting/portal/x1_stacked_total.py` returns ≥ 1
    - `grep -c "type=.date." src/uk_subsidy_tracker/plotting/portal/x1_stacked_total.py` returns ≥ 1 (date-typed axis required by rangeselector)
    - `grep -c "Covers 2 of 8 schemes — see scheme grid for coverage status\\." src/uk_subsidy_tracker/plotting/portal/x1_stacked_total.py` returns 1 (verbatim subtitle from Copywriting Contract)
    - `grep -c "SCHEME_COLORS" src/uk_subsidy_tracker/plotting/portal/x1_stacked_total.py` returns ≥ 1
    - `grep -c "rangeselector" docs/charts/html/x1_stacked_total.html` returns ≥ 1 (HTML includes buttons)
    - **PNG without rangeselector buttons (TWO-figure proof):** Two separate `builder.save()` calls in main() — `grep -c "builder.save" src/uk_subsidy_tracker/plotting/portal/x1_stacked_total.py` returns 3 (1 placeholder path + 2 in TWO-figure success path)
    - X1 plotting succeeds against the cross_scheme.parquet from Wave 1 (CfD + RO bands visible; titles per Copywriting Contract)
  </acceptance_criteria>
  <done>
    X1 stacked-total chart ships with the TWO-figure pattern (PNG hero static All-time + HTML with rangeselector). 3 artefacts in docs/charts/html/. PNG visually verified to match UI-SPEC.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create X2 cumulative-premium chart + X3 per-household chart (parallelisable with Task 1)</name>
  <files>
    src/uk_subsidy_tracker/plotting/portal/x2_cumulative_premium.py,
    src/uk_subsidy_tracker/plotting/portal/x3_per_household.py
  </files>
  <read_first>
    - src/uk_subsidy_tracker/plotting/subsidy/ro_dynamics.py (full file — _prepare/_placeholder/main pattern; panel-4 cumulative pattern at lines 184-219 for X2 reference)
    - src/uk_subsidy_tracker/plotting/subsidy/cfd_payments_by_category.py (full file — stacked-bar legend pattern for X3 reference)
    - src/uk_subsidy_tracker/plotting/portal/x1_stacked_total.py (Task 1 output — same _prepare()/_placeholder() shape; X2/X3 mirror without rangeselector)
    - src/uk_subsidy_tracker/plotting/colors.py (SCHEME_COLORS)
    - .planning/phases/06-flagship-cross-scheme-charts/06-PATTERNS.md §"x2_cumulative_premium.py" + §"x3_per_household.py" lines 476-512
    - .planning/phases/06-flagship-cross-scheme-charts/06-UI-SPEC.md Copywriting Contract lines 281-294 (X2/X3 chart titles + subtitles + axis labels)
  </read_first>
  <action>
    **Step 2.1 — Create `src/uk_subsidy_tracker/plotting/portal/x2_cumulative_premium.py`**:
    - Module docstring: "X2 — Cumulative premium over gas, combined across covered schemes. PORTAL-01 P1 flagship chart. REQUIREMENT X-02."
    - Same `_prepare() -> pd.DataFrame` shape as X1 (read parquet; coerce year_dt; return empty DF if absent), MINUS the categorical-sort step (X2 sums across schemes per year, no stacking).
    - `_placeholder()` verbatim from X1 (text adjusted to "No cross-scheme premium data yet").
    - `main()` (single figure; no TWO-figure pattern needed since X2 has no rangeselector):
      ```python
      def main() -> None:
          df = _prepare()
          builder = ChartBuilder(title="Cumulative premium over gas — covered schemes", height=600)
          if df.empty:
              fig = _placeholder(builder)
              builder.save(fig, "x2_cumulative_premium", export_twitter=True)
              return

          # Sum premium across schemes per year, then cumulative sum.
          # D-08 conformance: only shipped-scheme rows present in cross_scheme.parquet,
          # so the combined sum naturally reflects the partial-coverage caveat.
          combined = (df.groupby("year")["premium_gbp"].sum()
                      .sort_index().cumsum() / 1e9)
          fig = builder.create_basic()
          fig.add_trace(go.Scatter(
              x=pd.to_datetime(combined.index, format="%Y"),
              y=combined.values,
              mode="lines+markers",
              line={"color": "#d62728", "width": 2.5},
              fill="tozeroy",
              fillcolor="rgba(214,39,40,0.25)",
              hovertemplate="%{x|%Y}<br>£%{y:.1f}bn<extra></extra>",
              name="Cumulative premium",
          ))
          fig.update_layout(
              title=dict(
                  text="<b>Cumulative premium over gas — covered schemes</b>",
                  subtitle=dict(
                      text="Covers 2 of 8 schemes — see scheme grid for coverage status.",
                      font=dict(size=12, color="#a0a4b8"),
                  ),
                  x=0.05, xanchor="left",
              ),
              showlegend=False,
          )
          fig.update_xaxes(type="date", title="Year")
          builder.format_currency_axis(fig, axis="y", suffix=" bn",
                                       title="Cumulative premium (£bn)")
          builder.save(fig, "x2_cumulative_premium",
                       export_twitter=True, export_html=True, export_div=True)


      if __name__ == "__main__":
          main()
      ```

    **Step 2.2 — Create `src/uk_subsidy_tracker/plotting/portal/x3_per_household.py`**:
    - Module docstring: "X3 — Cost per UK household, decomposed by scheme. PORTAL-01 P1 flagship chart. Per-household division uses ONS UK households-count carried per-row in cross_scheme.parquet (D-02). Pre-2014 bars omitted because households_uk is null pre-ONS-Families-and-Households-series start (RESEARCH Q3); methodology page documents. REQUIREMENT X-03."
    - `_prepare()` mirrors X1 _prepare() shape PLUS NaN-filter:
      ```python
      def _prepare() -> pd.DataFrame:
          src = portal.DERIVED_DIR / "cross_scheme.parquet"
          if not src.exists():
              return pd.DataFrame()
          df = pq.read_table(src).to_pandas()
          if len(df) == 0:
              return df
          # Drop rows with missing households_uk (pre-2014; RESEARCH Q3 recommendation
          # — methodology.md documents the omission).
          df = df[df["households_uk"].notna() & (df["households_uk"] > 0)]
          df["per_household_gbp"] = df["cost_gbp"] / df["households_uk"]
          return df
      ```
    - `_placeholder()` verbatim from X1 (text "No per-household data yet").
    - `main()` (single figure; stacked bars by scheme):
      ```python
      def main() -> None:
          df = _prepare()
          builder = ChartBuilder(title="Cost per UK household by scheme", height=600)
          if df.empty:
              fig = _placeholder(builder)
              builder.save(fig, "x3_per_household", export_twitter=True)
              return

          fig = builder.create_basic()
          for scheme_name, sub in df.groupby("scheme", sort=False):
              fig.add_trace(go.Bar(
                  x=sub["year"], y=sub["per_household_gbp"],
                  name=str(scheme_name),
                  marker_color=SCHEME_COLORS[str(scheme_name)],
                  hovertemplate=("%{x}<br>" + str(scheme_name) +
                                 "<br>£%{y:,.0f}/household<extra></extra>"),
              ))
          fig.update_layout(
              barmode="stack",
              title=dict(
                  text="<b>Cost per UK household by scheme</b>",
                  subtitle=dict(
                      text=("Covers 2 of 8 schemes; ONS UK households-count "
                            "denominator — see methodology."),
                      font=dict(size=12, color="#a0a4b8"),
                  ),
                  x=0.05, xanchor="left",
              ),
          )
          fig.update_xaxes(title="Year")
          builder.format_currency_axis(fig, axis="y", title="Cost per household (£)")
          builder.save(fig, "x3_per_household",
                       export_twitter=True, export_html=True, export_div=True)


      if __name__ == "__main__":
          main()
      ```

    **Step 2.3 — Smoke-test X2 + X3 directly**:
    ```bash
    uv run python -m uk_subsidy_tracker.plotting.portal.x2_cumulative_premium
    uv run python -m uk_subsidy_tracker.plotting.portal.x3_per_household
    ```
    Verify 6 new files emitted (PNG + HTML + div HTML × 2 charts).
  </action>
  <verify>
    <automated>
      uv run python -m uk_subsidy_tracker.plotting.portal.x2_cumulative_premium &&
      uv run python -m uk_subsidy_tracker.plotting.portal.x3_per_household &&
      test -f docs/charts/html/x2_cumulative_premium_twitter.png &&
      test -f docs/charts/html/x2_cumulative_premium.html &&
      test -f docs/charts/html/x3_per_household_twitter.png &&
      test -f docs/charts/html/x3_per_household.html
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists: `src/uk_subsidy_tracker/plotting/portal/x2_cumulative_premium.py`
    - File exists: `src/uk_subsidy_tracker/plotting/portal/x3_per_household.py`
    - File exists: `docs/charts/html/x2_cumulative_premium_twitter.png` (size ≥ 5000 bytes)
    - File exists: `docs/charts/html/x2_cumulative_premium.html` (size ≥ 50000 bytes)
    - File exists: `docs/charts/html/x3_per_household_twitter.png` (size ≥ 5000 bytes)
    - File exists: `docs/charts/html/x3_per_household.html` (size ≥ 50000 bytes)
    - `grep -c "def main" src/uk_subsidy_tracker/plotting/portal/x2_cumulative_premium.py` returns 1
    - `grep -c "def main" src/uk_subsidy_tracker/plotting/portal/x3_per_household.py` returns 1
    - `grep -c "cumsum" src/uk_subsidy_tracker/plotting/portal/x2_cumulative_premium.py` returns ≥ 1 (X2 cumulative semantics)
    - `grep -c "households_uk" src/uk_subsidy_tracker/plotting/portal/x3_per_household.py` returns ≥ 2 (NaN filter + division denominator)
    - `grep -c "Cumulative premium over gas — covered schemes" src/uk_subsidy_tracker/plotting/portal/x2_cumulative_premium.py` returns 1 (verbatim title from Copywriting Contract)
    - `grep -c "Cost per UK household by scheme" src/uk_subsidy_tracker/plotting/portal/x3_per_household.py` returns 1 (verbatim title from Copywriting Contract)
    - `grep -c "Covers 2 of 8 schemes; ONS UK households-count denominator" src/uk_subsidy_tracker/plotting/portal/x3_per_household.py` returns 1 (verbatim X3 subtitle from Copywriting Contract)
    - `grep -c "rangeselector" src/uk_subsidy_tracker/plotting/portal/x2_cumulative_premium.py` returns 0 (X2 has no rangeselector per UI-SPEC)
    - `grep -c "rangeselector" src/uk_subsidy_tracker/plotting/portal/x3_per_household.py` returns 0 (X3 has no rangeselector per UI-SPEC)
  </acceptance_criteria>
  <done>
    X2 + X3 charts ship; 6 new artefacts in docs/charts/html/. Per-household NaN guard for pre-2014 in place. Verbatim copy from Copywriting Contract.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire X1/X2/X3 main() entries into plotting/__main__.py</name>
  <files>
    src/uk_subsidy_tracker/plotting/__main__.py
  </files>
  <read_first>
    - src/uk_subsidy_tracker/plotting/__main__.py (full file — existing import pattern lines 14-46 + charts list lines 66-90)
    - .planning/phases/06-flagship-cross-scheme-charts/06-PATTERNS.md §"Plotting orchestrator" lines 594-621
    - .planning/phases/06-flagship-cross-scheme-charts/06-RESEARCH.md §"Step 5 — Wire main() into plotting/__main__.py" lines 819-839
  </read_first>
  <action>
    **Step 3.1 — Add imports** (per RESEARCH lines 822-827; placement: after existing 18 imports, in alphabetical-by-module order):
    ```python
    from uk_subsidy_tracker.plotting.portal.x1_stacked_total import main as x1_stacked_total
    from uk_subsidy_tracker.plotting.portal.x2_cumulative_premium import main as x2_cumulative_premium
    from uk_subsidy_tracker.plotting.portal.x3_per_household import main as x3_per_household
    ```

    **Step 3.2 — Append 3 entries to the charts list** (per RESEARCH lines 829-839; the existing list contains 18 chart tuples):
    ```python
    # Cross-scheme portal flagship charts (Phase 6, REQUIREMENTS X-01..X-03)
    ("x1_stacked_total", x1_stacked_total),
    ("x2_cumulative_premium", x2_cumulative_premium),
    ("x3_per_household", x3_per_household),
    ```
    Place these at the END of the charts list (Phase 6's append; X4/X5 added in Wave 3 by Plan 06-03).

    **Step 3.3 — Smoke-test the orchestrator end-to-end**:
    ```bash
    uv run python -m uk_subsidy_tracker.plotting
    ```
    Verify the orchestrator runs all 21 charts (existing 18 + new 3) without error. Output should report 21 charts OK + 0 ERR.
  </action>
  <verify>
    <automated>
      grep -c "x1_stacked_total\|x2_cumulative_premium\|x3_per_household" src/uk_subsidy_tracker/plotting/__main__.py &&
      uv run python -m uk_subsidy_tracker.plotting 2>&1 | tail -10
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from uk_subsidy_tracker.plotting.portal.x1_stacked_total import main as x1_stacked_total" src/uk_subsidy_tracker/plotting/__main__.py` returns 1
    - `grep -c "from uk_subsidy_tracker.plotting.portal.x2_cumulative_premium import main as x2_cumulative_premium" src/uk_subsidy_tracker/plotting/__main__.py` returns 1
    - `grep -c "from uk_subsidy_tracker.plotting.portal.x3_per_household import main as x3_per_household" src/uk_subsidy_tracker/plotting/__main__.py` returns 1
    - `grep -c '("x1_stacked_total", x1_stacked_total)' src/uk_subsidy_tracker/plotting/__main__.py` returns 1
    - `grep -c '("x2_cumulative_premium", x2_cumulative_premium)' src/uk_subsidy_tracker/plotting/__main__.py` returns 1
    - `grep -c '("x3_per_household", x3_per_household)' src/uk_subsidy_tracker/plotting/__main__.py` returns 1
    - `uv run python -m uk_subsidy_tracker.plotting` exits 0 and the orchestrator output reports ≥ 21 charts processed (existing 18 + new 3 + any X4/X5 if Plan 06-03 already shipped)
    - All 9 new chart artefacts present in `docs/charts/html/`: x{1,2,3}_*_twitter.png + x{1,2,3}_*.html + x{1,2,3}_*.div.html
  </acceptance_criteria>
  <done>
    plotting/__main__.py knows about X1/X2/X3. The full chart-orchestration command runs all charts (CfD + RO + new portal) cleanly. Wave 4/5 docs can now embed the artefacts with confidence the regen pipeline ships them.
  </done>
</task>

</tasks>

<verification>
- All 3 tasks complete; `uv run python -m uk_subsidy_tracker.plotting` exits 0
- 9 chart artefacts present in `docs/charts/html/` (3 PNG + 3 HTML + 3 div.html for X1+X2+X3)
- X1 PNG does NOT show rangeselector buttons (visual smoke check: open PNG; should be a static stacked bar chart only)
- X1 HTML DOES show rangeselector buttons (visual smoke check: open HTML; click 1y/5y/All buttons)
- All 3 X-charts use SCHEME_COLORS for CfD (#1f77b4) + RO (#d62728) consistently
- `uv run pytest tests/` exits 0 (no test regression — Wave 1 tests still green)
</verification>

<success_criteria>
- X-01, X-02, X-03 substrate complete; PNG + HTML artefacts ready for Wave 4 embed pages
- X1 visual contract honoured: PNG no buttons; HTML with native Plotly rangeselector
- Charts read from cross_scheme.parquet; broken-substrate path gracefully degrades to placeholder
- All 3 chart titles + subtitles match Copywriting Contract verbatim
- plotting/__main__.py orchestrator runs end-to-end without error
</success_criteria>

<output>
After completion, create `.planning/phases/06-flagship-cross-scheme-charts/06-02-SUMMARY.md` documenting:
- 4 new src files + 1 modified file + 9 new chart artefacts
- TWO-figure pattern proof for X1 (file sizes; rangeselector grep-count in HTML vs absence in PNG raster — note PNG inspection is visual)
- Any deviations from plan with rationale
</output>
