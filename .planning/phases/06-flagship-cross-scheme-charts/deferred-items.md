# Phase 06 — Deferred Items

Out-of-scope discoveries logged during plan execution. None of these are introduced by Phase 06 work; they are pre-existing untracked items observed at the start of Plan 06-05's session.

## Pre-existing untracked items (Plan 06-05 session)

| Item | Type | Status | Notes |
|------|------|--------|-------|
| `site/` | Directory (mkdocs build output) | Untracked | Generated by `uv run mkdocs build`; should be gitignored project-wide. Not added to git in this plan. Future hygiene: add `site/` to `.gitignore` if not already covered by a parent rule. |
| `test` | Empty file at repo root | Untracked | Pre-existing empty file (not a directory; not a test fixture). Origin unknown. Out of scope for Phase 06. Future cleanup: remove or relocate. |

These items were visible in the environment-block `git status` at session start and are unchanged by Plan 06-05.
