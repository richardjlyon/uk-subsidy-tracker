---
phase: 06-flagship-cross-scheme-charts
plan: 05
type: execute
wave: 5
depends_on: [06-04]
files_modified:
  - docs/index.md
autonomous: true
requirements: [PORTAL-01, PORTAL-02]
must_haves:
  truths:
    - "docs/index.md renders 3 headline cards (Total subsidy / Premium over gas / Per household) above the existing 2×4 scheme grid"
    - "Each headline card carries a hardcoded numeric value driven by latest_fully_reconciled_year (currently 2023) from cross_scheme.parquet"
    - "Caveat line beneath the cards reads italic: '*Covers 2 of 8 schemes; full coverage in Phases 7-12.*'"
    - "X1 Twitter PNG hero is embedded between the caveat and the existing 2×4 scheme grid; Interactive HTML link follows immediately"
    - "CfD + RO scheme-grid tile headline figures are updated to reflect latest fully-reconciled scheme year totals (CfD 2023 + RO 2023; tile prose retained from Phase 05.1)"
    - "6 placeholder tiles unchanged ('Coming in Phase N' label; non-clickable text)"
    - "PORTAL-02 honoured: CfD + RO populated tiles link to schemes/cfd.md / schemes/ro.md (no anchor; per UI-SPEC §3 lock)"
    - "uv run mkdocs build --strict exits 0 with zero warnings"
  artifacts:
    - path: "docs/index.md"
      provides: "Portal homepage with 3 headline cards + caveat + X1 hero embed + 2×4 scheme grid"
      min_lines: 80
  key_links:
    - from: "docs/index.md headline cards"
      to: "data/derived/portal/cross_scheme.parquet (via Wave 6 headline-sync test)"
      via: "Hardcoded prose values; regression test gate (Plan 06-06)"
      pattern: "(?i)Total subsidy.*£"
    - from: "docs/index.md X1 hero"
      to: "docs/charts/html/x1_stacked_total_twitter.png + .html"
      via: "Twitter PNG embed + Interactive link pattern"
      pattern: "x1_stacked_total"
    - from: "docs/index.md scheme tiles"
      to: "docs/schemes/cfd.md + docs/schemes/ro.md"
      via: "PORTAL-02 clickthrough, no anchor"
      pattern: "schemes/cfd\\.md|schemes/ro\\.md"
---

<objective>
Retrofit `docs/index.md` per UI-SPEC §"Component Inventory §1 + §2 + §3": insert 3 headline cards (Total subsidy / Premium over gas / Per household for the latest fully-reconciled scheme year) + italic caveat + X1 Twitter PNG hero + Interactive link, immediately above the existing Phase 05.1 2×4 scheme grid. Update CfD + RO populated tiles' headline figures to reflect the same latest-year reconciliation. The 6 placeholder tiles remain unchanged.

Purpose: PORTAL-01 SC#1 — "Portal homepage renders 3 headline cards, X1 chart with rangeselector tabs, and 2×4 scheme grid." PORTAL-02 — populated tiles link to scheme pages. D-05 + D-06 + D-09 lock the headline-card contract; D-10 locks the placeholder-tile discipline.

Output: 1 modified file (docs/index.md). Hardcoded prose values are anchored by Wave 6 regression test (Plan 06-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-flagship-cross-scheme-charts/06-CONTEXT.md
@.planning/phases/06-flagship-cross-scheme-charts/06-RESEARCH.md
@.planning/phases/06-flagship-cross-scheme-charts/06-PATTERNS.md
@.planning/phases/06-flagship-cross-scheme-charts/06-UI-SPEC.md
@.planning/phases/06-flagship-cross-scheme-charts/06-04-PLAN.md
@CLAUDE.md

<interfaces>
From docs/index.md (current state — Phase 05.1 D-10 2×4 scheme grid already in place):
- Lines 1-18: # UK Renewable Subsidy Tracker + intro paragraph
- Lines 22-76: 2×4 scheme grid (CfD + RO populated; 6 placeholder tiles)
- Lines 78+: "Scheme detail pages" + "For journalists" + Status section

From .planning/phases/06-flagship-cross-scheme-charts/06-UI-SPEC.md:
- §1 Headline-card row (lines 154-170): Material `grid cards` extension; 3 cards stat-style (no body, no link); caveat line beneath as italic single-line
- §2 X1 hero embed (lines 172-191): same Twitter PNG + Interactive HTML link pattern as scheme/theme pages
- §3 Headline figures on existing 2×4 scheme grid (lines 192-205): only CfD + RO tiles update; 6 placeholders unchanged; PORTAL-02 link convention is `schemes/cfd.md` (no anchor)
- Copywriting Contract lines 272-280 (verbatim card label copy + caveat text)

From docs/themes/cost/index.md lines 9-59 (Material grid cards markup exemplar):
```markdown
<div class="grid cards" markdown>

-   **£N.N bn**

    Total subsidy (latest scheme year)

</div>
```

From src/uk_subsidy_tracker/schemes/portal/__init__.py (Wave 1 output — `latest_fully_reconciled_year()` helper to compute the values):
- `latest_fully_reconciled_year() -> int` returns 2023 today (per RESEARCH §"latest_fully_reconciled_year rule").

From data/derived/portal/cross_scheme.parquet (Wave 1 output) — values to look up:
- For year=2023: CfD cost_gbp + RO cost_gbp = headline-card total
- For year=2023: CfD premium_gbp + RO premium_gbp = headline-card premium
- For year=2023: (CfD cost + RO cost) / households_uk[2023] = headline-card per-household

Per Copywriting Contract format rules:
- Card A + B: `**£N.N bn**` (with space, 1 decimal)
- Card C: `**£N,NNN**` (no decimal, comma thousands separator)
- Caveat: `*Covers 2 of 8 schemes; full coverage in Phases 7-12.*`
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hardcoded prose values | Trusted (Wave 6 regression test gate — fails if drift) |
| Material grid cards rendering | Trusted (already-shipped pattern; existing themes/cost/index.md exemplar) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Tampering | Hardcoded headline figures drift from parquet | mitigate | Wave 6 ships `tests/test_headline_sync.py` parametrised over all surfaces (homepage cards + cfd.md + ro.md) with ±£0.05bn tolerance; failing test = update prose + commit per D-09/D-12 cadence |
| T-06-05-02 | Information Disclosure | Per-household card reveals subsidy/household | accept | Public ONS data ÷ public regulator data |
| T-06-05-03 | Repudiation | Tiles link to broken scheme pages | mitigate | mkdocs --strict gate catches `not_found` warnings on link targets |
| T-06-05-04 | Tampering | Placeholder-tile headline figure inserted accidentally | mitigate | D-10 lock: no headline number on placeholder tiles; acceptance criteria grep-checks the 6 placeholders contain only "Coming in Phase N" markers |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Compute latest_fully_reconciled_year values + insert 3 headline cards + caveat + X1 hero on docs/index.md</name>
  <files>
    docs/index.md
  </files>
  <read_first>
    - docs/index.md (full file — lines 1-100; understand current intro + scheme grid layout)
    - docs/themes/cost/index.md lines 9-59 (Material grid cards markup exemplar with `<div class="grid cards" markdown>`)
    - src/uk_subsidy_tracker/schemes/portal/__init__.py (Wave 1 output — `latest_fully_reconciled_year()` helper)
    - data/derived/portal/cross_scheme.parquet (Wave 1 output — actual values for 2023 row)
    - .planning/phases/06-flagship-cross-scheme-charts/06-UI-SPEC.md §1 Headline-card row + §2 X1 hero embed + Copywriting Contract lines 272-285
    - .planning/phases/06-flagship-cross-scheme-charts/06-PATTERNS.md §"docs/index.md MODIFIED" lines 1030-1069
    - docs/charts/html/x1_stacked_total_twitter.png (verify exists from Wave 2)
    - docs/charts/html/x1_stacked_total.html (verify exists from Wave 2)
  </read_first>
  <action>
    **Step 1.1 — Compute the actual headline-card values** (run inline, NOT a script — capture and write into prose):
    ```bash
    uv run python -c "
    import pyarrow.parquet as pq
    from uk_subsidy_tracker.schemes import portal

    df = pq.read_table(portal.DERIVED_DIR / 'cross_scheme.parquet').to_pandas()
    year = portal.latest_fully_reconciled_year()
    row = df[df['year'] == year]
    total_cost_bn = row['cost_gbp'].sum() / 1e9
    total_premium_bn = row['premium_gbp'].sum() / 1e9
    households = row['households_uk'].iloc[0]  # same per-row in given year
    per_household = (row['cost_gbp'].sum()) / households

    print(f'latest_fully_reconciled_year = {year}')
    print(f'Card A (Total subsidy): £{total_cost_bn:.1f} bn')
    print(f'Card B (Premium over gas): £{total_premium_bn:.1f} bn')
    print(f'Card C (Per household): £{per_household:,.0f}')
    print(f'  (= £{int(round(per_household / 100) * 100):,} rounded to nearest £100 if cleaner)')
    "
    ```
    Capture the 3 numeric values; round per Copywriting Contract:
    - Card A: 1 decimal place + " bn" suffix → e.g. `£8.3 bn`
    - Card B: 1 decimal place + " bn" suffix → e.g. `£3.2 bn`
    - Card C: no decimal + comma thousands separator → e.g. `£293`

    **Note:** Card C prose may be £NN (small per-household figure ~£200-400/yr for covered-only). Format MUST follow `£N,NNN` per Copywriting Contract — but if value is < 1000 (no comma needed), render as `£NNN` per existing scheme-page convention. Wave 6 regression test enforces format ±£1.

    **Step 1.2 — Edit `docs/index.md`** to insert the new block above the existing `## Schemes` H2:
    - Find the line `## Schemes` (currently around line 22 per CONTEXT §"Files to modify").
    - Immediately ABOVE it (after the existing intro paragraph + horizontal rule), insert:
      ```markdown
      <div class="grid cards" markdown>

      -   **£X.X bn**

          Total subsidy (latest scheme year)

      -   **£Y.Y bn**

          Premium over gas (latest scheme year)

      -   **£ZZZ**

          Per household (latest scheme year)

      </div>

      *Covers 2 of 8 schemes; full coverage in Phases 7-12.*

      ![Total UK subsidy stacked by scheme — covered schemes only](charts/html/x1_stacked_total_twitter.png)

      [Interactive version](charts/html/x1_stacked_total.html){target="_blank"}

      ---

      ```
    - **CRITICAL substitutions** (use the actual values from Step 1.1):
      - `**£X.X bn**` → actual Card A value (e.g. `**£8.3 bn**`)
      - `**£Y.Y bn**` → actual Card B value (e.g. `**£3.2 bn**`)
      - `**£ZZZ**` → actual Card C value (e.g. `**£293**` or `**£3,200**` depending on magnitude)
    - **Material `grid cards` formatting rules** (UI-SPEC §"Card layout convention" lines 162-170):
      - Each card uses 4 leading spaces for the body text (continuation under the bullet)
      - Blank line between bullet content and next card
      - **NO `---` separator inside a card** (that would render as horizontal rule per docs/themes/cost/index.md exemplar; stat cards skip the separator)

    **Step 1.3 — Update CfD + RO scheme-grid tile headline figures** (UI-SPEC §3):
    - Locate the existing 2×4 scheme grid in `docs/index.md` (Phase 05.1 D-10 layout).
    - For the CfD tile: update the headline figure prose to use latest-fully-reconciled-year framing if it doesn't already. Per Copywriting Contract §"Tile typography contract" lines 200-203, the populated tile title uses `__[Scheme name — £N bn since YYYY](schemes/scheme.md)__` format.
    - **Pre-existing CfD prose pattern** (from Phase 05.1): likely `[**Contracts for Difference (CfD)** — £29bn paid since 2015](schemes/cfd.md)` or similar. Verify and update ONLY the headline number to match latest-year totals from cross_scheme.parquet if drift detected.
    - **Pre-existing RO prose pattern** (from Phase 05.2): likely `[**Renewables Obligation (RO)** — £58.6bn covered](schemes/ro.md)` or similar. Verify and preserve unless drift.
    - **D-10 lock:** the 6 placeholder tiles (FiT / Constraints / Capacity Market / Balancing / Grid / SEG) are UNCHANGED from Phase 05.1: bold-text title (NOT a link), `*Coming in Phase N.* {brief description}` body. **Do NOT** add headline figures to placeholder tiles.

    **Step 1.4 — Status section update** (CONTEXT §"Known pre-existing considerations"):
    - Find the existing "Status" section near the bottom of docs/index.md.
    - If it currently says "Two scheme modules are shipped" or similar, update to reflect cross-scheme aggregation now exists. Suggested update: "Two scheme modules are shipped (CfD + RO); a cross-scheme aggregation layer (`data/derived/portal/cross_scheme.parquet`) joins them for the 5 flagship cross-scheme charts above."
    - Optional addition: link to the new portal section: "See [Portal](portal/index.md) for cross-scheme charts."
    - **No other rewrite of the Status section** — minimal touch.

    **Step 1.5 — Run `uv run mkdocs build --strict`** to gate the change.
  </action>
  <verify>
    <automated>
      uv run python -c "
      import pyarrow.parquet as pq
      from uk_subsidy_tracker.schemes import portal
      df = pq.read_table(portal.DERIVED_DIR / 'cross_scheme.parquet').to_pandas()
      year = portal.latest_fully_reconciled_year()
      row = df[df['year'] == year]
      total = row['cost_gbp'].sum() / 1e9
      premium = row['premium_gbp'].sum() / 1e9
      per_hh = row['cost_gbp'].sum() / row['households_uk'].iloc[0]
      print(f'EXPECTED PROSE — Card A: £{total:.1f} bn / Card B: £{premium:.1f} bn / Card C: £{int(round(per_hh)):,}')
      " &&
      grep -c '<div class="grid cards" markdown>' docs/index.md &&
      grep -c "Total subsidy (latest scheme year)" docs/index.md &&
      grep -c "Premium over gas (latest scheme year)" docs/index.md &&
      grep -c "Per household (latest scheme year)" docs/index.md &&
      grep -c "Covers 2 of 8 schemes; full coverage in Phases 7-12\." docs/index.md &&
      grep -c "x1_stacked_total_twitter\.png" docs/index.md &&
      grep -c "x1_stacked_total\.html" docs/index.md &&
      uv run mkdocs build --strict 2>&1 | tee /tmp/mkdocs-strict.log &&
      ! grep -i "warn\|error" /tmp/mkdocs-strict.log
    </automated>
  </verify>
  <acceptance_criteria>
    - File modified: `docs/index.md`
    - `grep -c '<div class="grid cards" markdown>' docs/index.md` returns ≥ 1 (3-card row inserted)
    - `grep -c "Total subsidy (latest scheme year)" docs/index.md` returns 1 (Card A label verbatim)
    - `grep -c "Premium over gas (latest scheme year)" docs/index.md` returns 1 (Card B label verbatim)
    - `grep -c "Per household (latest scheme year)" docs/index.md` returns 1 (Card C label verbatim)
    - `grep -cE '\*\*£[0-9]+\.[0-9] bn\*\*' docs/index.md` returns ≥ 2 (Card A + Card B billion-format values)
    - `grep -cE '\*\*£[0-9,]+\*\*' docs/index.md` returns ≥ 1 (Card C non-billion-format value; may overlap above grep so use this as a presence check)
    - `grep -c '\*Covers 2 of 8 schemes; full coverage in Phases 7-12\.\*' docs/index.md` returns 1 (caveat line italic-wrapped per UI-SPEC §"Headline-card row" lines 168-170)
    - `grep -c "x1_stacked_total_twitter.png" docs/index.md` returns 1 (X1 PNG hero embed)
    - `grep -c "x1_stacked_total.html" docs/index.md` returns 1 (X1 HTML interactive link)
    - `grep -c '\[Interactive version\].*x1_stacked_total\.html.*{target="_blank"}' docs/index.md` returns 1 (UI-SPEC §2 X1 hero embed pattern)
    - `grep -c "schemes/cfd.md" docs/index.md` returns ≥ 1 (PORTAL-02: CfD tile clickthrough)
    - `grep -c "schemes/ro.md" docs/index.md` returns ≥ 1 (PORTAL-02: RO tile clickthrough)
    - **D-10 placeholder discipline:** `grep -c "Coming in Phase 7\|Coming in Phase 8\|Coming in Phase 9\|Coming in Phase 10\|Coming in Phase 11\|Coming in Phase 12" docs/index.md` returns ≥ 6 (one per placeholder tile; preserved from Phase 05.1)
    - **D-10 placeholder no-headline-figure check:** Around each "Coming in Phase N" mention, no nearby `**£` numeric format should appear (sanity check the placeholders are unchanged; rough proxy: line-count of `**£` matches expected — 3 from cards + 0-2 from CfD/RO tile titles per Phase 05.1 pattern; 6 placeholders should NOT add to this count)
    - `uv run mkdocs build --strict` exits 0 with zero stderr WARNINGs
    - `wc -l < docs/index.md` returns ≥ 80 (existing file was ~76 lines per CONTEXT; now ≥ 80 with insertions)
    - **CRITICAL:** the 3 hardcoded numeric values in the cards MATCH the values computed by `latest_fully_reconciled_year()` from cross_scheme.parquet. Acceptance: print the expected values via the verify-block command and visually compare against the inserted prose. (Wave 6 will add a parametrised regression test to enforce this.)
  </acceptance_criteria>
  <done>
    Portal homepage retrofit complete. 3 headline cards + italic caveat + X1 hero + Interactive HTML link sit above the preserved 2×4 scheme grid. CfD + RO tiles preserve PORTAL-02 clickthrough. 6 placeholder tiles unchanged. mkdocs --strict green.
  </done>
</task>

</tasks>

<verification>
- 1 task complete; docs/index.md modified; mkdocs --strict zero warnings
- Visual smoke check: `uv run mkdocs serve` shows the 3-card row + X1 hero on the homepage
- 3 hardcoded headline numbers match the values computed via `portal.latest_fully_reconciled_year()` against `cross_scheme.parquet`
- 6 placeholder tiles unchanged; 2 populated tiles link to scheme pages
- The portal/* nav block (Wave 4) is reachable from the top nav
</verification>

<success_criteria>
- PORTAL-01 SC#1 satisfied: 3 headline cards + caveat + X1 hero + 2×4 scheme grid all on docs/index.md
- PORTAL-02 satisfied: CfD + RO populated tiles link to schemes/cfd.md / schemes/ro.md (per UI-SPEC §3 lock — no anchor)
- mkdocs --strict permanent gate green
- Wave 6 has a known-shape homepage to write headline-sync regression test against
</success_criteria>

<output>
After completion, create `.planning/phases/06-flagship-cross-scheme-charts/06-05-SUMMARY.md` documenting:
- Modified file (docs/index.md) + line-count delta
- The 3 actual headline values inserted (Card A / B / C with the ints/floats from cross_scheme.parquet)
- mkdocs --strict pass evidence
- Confirmation that placeholder tiles unchanged from Phase 05.1
- Any deviations from plan with rationale
</output>
