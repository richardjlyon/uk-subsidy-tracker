---
phase: 05.1-cfd-scheme-page
plan: 03
type: execute
wave: 2
depends_on:
  - "05.1-01"
files_modified:
  - docs/index.md
autonomous: true
requirements:
  - SCHEMEPAGE-01
tags:
  - docs
  - homepage
  - scheme-grid
  - portal-prestage

must_haves:
  truths:
    - "docs/index.md no longer contains a 'Module in focus: Contracts for Difference' section (prose migrated to docs/schemes/cfd.md in Plan 01)."
    - "docs/index.md no longer contains the 'Data sources' bullet list (migrates to per-scheme GOV-01 §8 blocks — already lives on cfd.md and ro.md)."
    - "docs/index.md no longer contains the 'Code & reproducibility' bash block (migrates to docs/data/index.md and to each scheme page's §8)."
    - "docs/index.md now renders a Material-theme `grid cards` block with exactly 8 tiles: 2 populated (CfD + RO) + 6 placeholders (FiT / Constraints / Capacity Market / Balancing / Grid / SEG)."
    - "Each populated tile links to the matching scheme-detail page (schemes/cfd.md for CfD; schemes/ro.md for RO) and shows its headline figure (£29bn for CfD, £67bn for RO)."
    - "Each placeholder tile is labelled 'Coming in Phase N' with N matching the ROADMAP.md phase numbering (FiT=7, Constraints=8, CM=9, Balancing=10, Grid=11, SEG=12)."
    - "The 'Explore by theme' five-card section is preserved verbatim as the cross-scheme reading path."
    - "The 'For journalists and academics' Use-our-data card is preserved."
    - "The Status section is preserved in footer-style form (per-scheme status moved into the scheme grid tiles)."
    - "uv run mkdocs build --strict exits 0 (the permanent Phase 3 CI gate)."
    - "Grid renders correctly on mobile under Material theme default responsive behaviour (human-spot-check; automated test is strict-build only)."
  artifacts:
    - path: "docs/index.md"
      provides: "Hero + 2×4 scheme grid + theme nav + journalist card + status — pre-stages Phase 6 PORTAL-01 layout"
      contains: "grid cards"
      min_lines: 80
  key_links:
    - from: "docs/index.md"
      to: "docs/schemes/cfd.md"
      via: "populated CfD scheme-grid tile link"
      pattern: "schemes/cfd\\.md"
    - from: "docs/index.md"
      to: "docs/schemes/ro.md"
      via: "populated RO scheme-grid tile link"
      pattern: "schemes/ro\\.md"
    - from: "docs/index.md"
      to: "docs/themes/cost/index.md"
      via: "preserved Explore-by-theme card"
      pattern: "themes/cost/index\\.md"
    - from: "docs/index.md"
      to: "docs/data/index.md"
      via: "preserved For-journalists Use-our-data card"
      pattern: "data/index\\.md"
---

<objective>
Pivot `docs/index.md` from its current "Module in focus: Contracts for Difference" shape into the hero + 2×4 scheme-grid + theme-nav + journalist-card + status shape defined by 05.1 D-10. This pre-stages the Phase 6 PORTAL-01 layout (3 headline cards + X1 chart + tabs land in Phase 6; the grid cells ship empty here with 2 populated + 6 placeholder tiles). The existing homepage prose that migrated elsewhere is deleted in this plan; the existing homepage prose that survives (theme nav, journalist card, Status) is preserved verbatim.

Purpose: Restore CfD↔RO symmetry on the homepage — no more "CfD special-case focus" section; both shipped scheme modules appear as equal tiles in the grid; all 8 v1 scheme modules have a place in the grid so Phases 7-12 slot in without further homepage redesign.

Output: One modified file (`docs/index.md`) + one atomic commit. `mkdocs build --strict` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-cfd-scheme-page/05.1-CONTEXT.md
@.planning/phases/05.1-cfd-scheme-page/05.1-PATTERNS.md
@.planning/phases/05.1-cfd-scheme-page/05.1-01-PLAN.md
@.planning/ROADMAP.md
@CLAUDE.md
@docs/index.md
@docs/themes/cost/index.md
@docs/schemes/cfd.md
@docs/schemes/ro.md
@mkdocs.yml
</context>

<threat_model>
## Trust Boundaries

Docs-only phase. No executable code paths, no data ingress. Only trust boundary: mkdocs content pipeline.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-03-01 | Denial of Service | homepage grid rendering | mitigate | Grid relies on Material theme `grid cards` extension + `md_in_html` markdown extension (mkdocs.yml:100). Verify extension is enabled before committing; `mkdocs build --strict` catches malformed `<div class="grid cards" markdown>` blocks. |
| T-05.1-03-02 | Information Disclosure | homepage headline figures | accept | The £29bn and £67bn figures are hand-written on the tiles per D-Claude's-Discretion. Phase 6 wires these to `manifest.json`; for 05.1 hardcoded inline values match the same numbers published on cfd.md + ro.md scheme pages. No divergence risk at this checkpoint. |

No new attack surface introduced.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite docs/index.md to hero + 2×4 scheme-grid + preserved theme/journalist/status sections</name>
  <files>docs/index.md</files>

  <read_first>
    MANDATORY — read these files before editing:
    - `docs/index.md` (the file being edited — read all 111 lines; the Hero prose at lines 1-19 is preserved minus the "Current coverage" sentence that moves to the grid tiles; "Module in focus" lines 22-50 migrated to cfd.md in Plan 01; "Explore by theme" lines 52-59 preserved; "Scheme detail pages" lines 61-66 replaced by grid; "For journalists and academics" lines 68-70 preserved; "Data sources" lines 72-84 deleted; "Code & reproducibility" lines 86-98 deleted; "Status" lines 100-110 preserved with minor edit)
    - `docs/themes/cost/index.md` lines 9-59 (the existing Material `grid cards` exemplar in use — read this BEFORE writing the new grid to ensure the pattern is reproduced byte-accurate)
    - `.planning/phases/05.1-cfd-scheme-page/05.1-PATTERNS.md` § "docs/index.md (site homepage, reader-entry grid)" — contains the exact populated-tile shape + placeholder-tile shape + phase-numbering table + full skeleton
    - `.planning/ROADMAP.md` lines 134-211 (Phase 6 through Phase 12) — verify placeholder-tile phase numbering matches exactly: FiT=Phase 7, Constraints=Phase 8, CM=Phase 9, Balancing=Phase 10, Grid=Phase 11, SEG=Phase 12. This is load-bearing: if ROADMAP changes the phase numbering, the tiles must stay in sync.
    - `docs/schemes/cfd.md` (Plan 01) — confirm the headline figure and link target are stable before wiring the CfD tile
    - `docs/schemes/ro.md` lines 1-10 — the RO headline (£67bn) matches the tile's claim
    - `mkdocs.yml` lines 94-127 (markdown_extensions) — confirm `attr_list`, `md_in_html`, `pymdownx.superfences` are enabled (Material grid cards require these; all three are present at lines 97, 100, 121)

    MANDATORY PRECONDITION — run this before editing. `docs/charts/html/` is `.gitignore`d and regenerated via `uv run python -m uk_subsidy_tracker.plotting`. The homepage grid embeds PNG thumbnails on the CfD and RO populated tiles — if either PNG is missing, the mkdocs --strict gate at the end of this task will fail with "Doc file 'index.md' contains a link to '...' which is not found". If the check below fails, run `uv run python -m uk_subsidy_tracker.plotting` first, then re-check.

    ```bash
    ls docs/charts/html/subsidy_cfd_dynamics_twitter.png \
       docs/charts/html/subsidy_ro_dynamics_twitter.png \
       >/dev/null 2>&1 || {
      echo "Homepage-tile PNGs missing (CfD or RO dynamics thumbnail). Run: uv run python -m uk_subsidy_tracker.plotting"
      exit 1
    }
    ```
  </read_first>

  <action>
  Rewrite `docs/index.md` to the new D-10 structure. The target shape is laid out in `05.1-PATTERNS.md` § "docs/index.md" and the full skeleton is there — reproduce it, do not ad-lib. The page must be ≥80 lines and contain the following sections in this order:

  **Section 1 — Hero (lines 1-~15):**

  Preserve the existing hero text from `docs/index.md` lines 1-14 substantially verbatim. Edit the "Current coverage" sentence at lines 16-18 — it currently says "the Contracts for Difference module is shipped below. The remaining seven modules are under active development". Replace with a one-sentence pivot that points into the grid:

  ```markdown
  **Current coverage:** the Contracts for Difference and Renewables Obligation modules are shipped. Each remaining module ships as its scheme grid tile below — see the tile for the latest status.
  ```

  Keep the horizontal rule `---` after the hero.

  **Section 2 — `## Schemes` — 2×4 grid (new):**

  After the hero `---`, add the Schemes section with an 8-tile grid. The grid uses Material theme `grid cards` (already in use on `docs/themes/cost/index.md`):

  ```markdown
  ## Schemes

  <div class="grid cards" markdown>

  -   [![CfD dynamics preview](charts/html/subsidy_cfd_dynamics_twitter.png)](schemes/cfd.md)

      __[Contracts for Difference — £29bn since 2015](schemes/cfd.md)__

      ---

      Ten years of strike-price commitments. £14bn premium over the gas counterfactual. 2022 gas-crisis test: CfD electricity still cost 7% more than gas even at crisis peak.

  -   [![RO dynamics preview](charts/html/subsidy_ro_dynamics_twitter.png)](schemes/ro.md)

      __[Renewables Obligation — £67bn since 2002](schemes/ro.md)__

      ---

      The scheme you've never heard of, twice the size of the one you have. ~£6bn/yr forward-committed to 2037; the largest legacy renewables subsidy in the UK.

  -   **Feed-in Tariff (FiT)**

      ---

      *Coming in Phase 7.* Domestic and small-commercial renewables tariff; 800k+ installs.

  -   **Constraint Payments**

      ---

      *Coming in Phase 8.* The growing cost of paying wind farms to switch off when transmission capacity is saturated.

  -   **Capacity Market**

      ---

      *Coming in Phase 9.* Capacity payments to dispatchable generation under EMR; modified methodology (no gas counterfactual).

  -   **Balancing Services**

      ---

      *Coming in Phase 10.* Pre/post-renewables balancing-cost delta — the system-operability cost of high renewable penetration.

  -   **Grid Socialisation**

      ---

      *Coming in Phase 11.* Best-efforts TNUoS attribution with low/central/high sensitivity bounds; the most uncertain cost bucket.

  -   **Smart Export Guarantee (SEG)**

      ---

      *Coming in Phase 12.* Aggregate-only scheme page; no station-level data available from the regulator.

  </div>
  ```

  Tile shape rules (load-bearing; violating any of these breaks the grid rendering or Material theme parity):
  - `<div class="grid cards" markdown>` wrapper — the `markdown` attribute is non-negotiable; it enables md-in-html rendering (requires `md_in_html` extension in mkdocs.yml:100 — confirmed present).
  - Each populated tile has 4 elements in this order: (1) `[![alt](png-path)](link)` image-link; (2) `__[title](link)__` bold-link title; (3) `---` horizontal rule; (4) one-paragraph card body.
  - Each placeholder tile has 3 elements: (1) `**Name**` bold heading (no link — tile is not clickable); (2) `---`; (3) italic "*Coming in Phase N.*" body paragraph.
  - Tile image paths are relative to `docs/` (the homepage is at docs/index.md so paths are `charts/html/…png` NOT `../charts/html/…png`). Compare with theme-gallery pages which use `../../charts/html/` because they are two levels deep.
  - Alt text on populated tiles uses PNG filename convention ("<scheme> dynamics preview") matching the existing pattern in `docs/themes/cost/index.md`.
  - Link targets on populated tiles: `schemes/cfd.md` and `schemes/ro.md` (relative to docs/).

  **Section 3 — `## Explore by theme` (preserved):**

  Reproduce verbatim from current `docs/index.md` lines 51-59:

  ```markdown
  ## Explore by theme

  The charts are organised into five argument themes:

  1. **[Cost](themes/cost/index.md)** — the mechanism: volume × price gap = bill. Starts with the four-panel diagnostic.
  2. **[Recipients](themes/recipients/index.md)** — where the money goes. Six projects receive 50% of the pot.
  3. **[Efficiency](themes/efficiency/index.md)** — £/tCO₂ avoided against DEFRA SCC and UK ETS benchmarks.
  4. **[Cannibalisation](themes/cannibalisation/index.md)** — why wind crashes its own price, and who tops the difference up.
  5. **[Reliability](themes/reliability/index.md)** — the drought periods no battery can cover.
  ```

  **Section 4 — `## For journalists and academics` (preserved):**

  Reproduce from current `docs/index.md` lines 68-70:

  ```markdown
  ## For journalists and academics

  [**Use our data**](data/index.md) — pandas, DuckDB, and R snippets + citation templates + SHA-256 integrity verification. Start at [`manifest.json`](https://richardjlyon.github.io/uk-subsidy-tracker/data/manifest.json) and follow the URLs.
  ```

  **Section 5 — `## Status` (preserved + minor edit):**

  Preserve the Status footer from current `docs/index.md` lines 100-110, but drop the per-scheme enumeration ("all five CfD theme pages..." and "The remaining seven scheme modules are sequenced...") since those states now live in the scheme-grid tiles. Proposed replacement:

  ```markdown
  ## Status

  Version 0.x prototype. Two scheme modules are shipped — Contracts for Difference and Renewables Obligation. The remaining six modules are sequenced in the project [roadmap](https://github.com/richardjlyon/uk-subsidy-tracker/blob/main/.planning/ROADMAP.md) and will populate the grid above as they ship. Corrections and contributions welcome via [GitHub Issues](https://github.com/richardjlyon/uk-subsidy-tracker/issues).
  ```

  **Sections to DELETE (do not reproduce in the rewrite):**
  - Current lines 22-50: "Module in focus: Contracts for Difference" — migrated to `docs/schemes/cfd.md` in Plan 01.
  - Current lines 61-66: "Scheme detail pages" prose — replaced by the scheme grid in Section 2.
  - Current lines 72-84: "Data sources" bullet list — lives on each scheme page's GOV-01 §8 block per D-10.
  - Current lines 86-98: "Code & reproducibility" bash block — lives on `docs/data/index.md` (Phase 4 PUB-04) and on each scheme page's §8 Reproduce block.

  **What NOT to do:**
  - Do NOT retain any "Module in focus" prose on the homepage. It migrated in Plan 01; leaving duplicate prose breaks single-source-of-truth discipline.
  - Do NOT pre-wire the Phase 6 PORTAL-01 features (3 headline cards, X1 stacked chart, Latest/Last-5/All-time tabs). Those land in Phase 6. 05.1 ships the grid cells empty-modulo-CfD+RO.
  - Do NOT add `![image]` thumbnails to the 6 placeholder tiles. They are deliberately text-only to visually distinguish "shipped" from "coming". When each phase ships, the relevant tile gets its thumbnail in that phase's plan.
  - Do NOT change the theme-nav bullets or the journalist card wording. Those are stable v1 pieces preserved here.
  - Do NOT use HTML `<ul>`/`<li>` or Plotly iframes — plain markdown with Material `grid cards` wrapping only.
  - Do NOT fabricate headline figures for placeholder tiles (e.g. "£X bn expected"). The placeholder pattern is "*Coming in Phase N.*" + a one-sentence scheme description; no numbers.
  - Do NOT change Phase numbering. FiT=7, Constraints=8, CM=9, Balancing=10, Grid=11, SEG=12. Verify against ROADMAP.md lines 16-21 before committing.
  </action>

  <verify>
    <automated>
    # 0. PRECONDITION — homepage-tile PNGs exist on disk (CfD + RO dynamics thumbnails)
    ls docs/charts/html/subsidy_cfd_dynamics_twitter.png \
       docs/charts/html/subsidy_ro_dynamics_twitter.png \
       >/dev/null 2>&1 || {
      echo "Homepage-tile PNGs missing (CfD or RO dynamics thumbnail). Run: uv run python -m uk_subsidy_tracker.plotting"
      exit 1
    }

    # File present and has the expected shape
    test -f docs/index.md || { echo "docs/index.md missing (!)"; exit 1; }
    wc -l docs/index.md | awk '$1 >= 80 { exit 0 } { print "homepage too short: " $1 " lines < 80"; exit 1 }'

    # No "Module in focus" — migrated in Plan 01
    ! grep -q "Module in focus" docs/index.md || { echo "Module-in-focus section still present (migrated to cfd.md in Plan 01)"; exit 1; }

    # No Data sources bullet list — dropped per D-10
    ! grep -q "^## Data sources$" docs/index.md || { echo "Data sources section still present — should be removed per D-10"; exit 1; }

    # No Code & reproducibility bash block — dropped per D-10
    ! grep -q "^## Code & reproducibility$" docs/index.md || { echo "Code & reproducibility section still present — should be removed per D-10"; exit 1; }

    # No lingering "Scheme detail pages" section (replaced by the grid)
    ! grep -q "^## Scheme detail pages$" docs/index.md || { echo "Scheme-detail-pages section still present — replaced by the grid per D-10"; exit 1; }

    # Grid cards wrapper present
    grep -q '<div class="grid cards" markdown>' docs/index.md || { echo "Material grid-cards wrapper missing"; exit 1; }

    # Exactly 8 tiles (each tile starts with "-   ")
    TILES=$(awk '/<div class="grid cards" markdown>/,/<\/div>/' docs/index.md | grep -cE "^-   ")
    [ "$TILES" -eq 8 ] || { echo "Expected 8 grid tiles, got $TILES"; exit 1; }

    # 2 populated tiles link to scheme pages
    grep -q "schemes/cfd.md" docs/index.md || { echo "CfD tile link missing"; exit 1; }
    grep -q "schemes/ro.md" docs/index.md || { echo "RO tile link missing"; exit 1; }

    # Populated tiles have chart-image thumbnails
    grep -q "subsidy_cfd_dynamics_twitter.png" docs/index.md || { echo "CfD tile thumbnail missing"; exit 1; }
    grep -q "subsidy_ro_dynamics_twitter.png" docs/index.md || { echo "RO tile thumbnail missing"; exit 1; }

    # Headline figures present on populated tiles
    grep -q "£29" docs/index.md || { echo "CfD headline figure missing"; exit 1; }
    grep -q "£67" docs/index.md || { echo "RO headline figure missing"; exit 1; }

    # 6 placeholder phases present with exact phase numbers
    grep -q "Coming in Phase 7" docs/index.md || { echo "Phase 7 (FiT) placeholder missing"; exit 1; }
    grep -q "Coming in Phase 8" docs/index.md || { echo "Phase 8 (Constraints) placeholder missing"; exit 1; }
    grep -q "Coming in Phase 9" docs/index.md || { echo "Phase 9 (Capacity Market) placeholder missing"; exit 1; }
    grep -q "Coming in Phase 10" docs/index.md || { echo "Phase 10 (Balancing) placeholder missing"; exit 1; }
    grep -q "Coming in Phase 11" docs/index.md || { echo "Phase 11 (Grid) placeholder missing"; exit 1; }
    grep -q "Coming in Phase 12" docs/index.md || { echo "Phase 12 (SEG) placeholder missing"; exit 1; }

    # Preserved sections
    grep -q "^## Explore by theme$" docs/index.md || { echo "Explore-by-theme section missing"; exit 1; }
    grep -q "^## For journalists and academics$" docs/index.md || { echo "For-journalists section missing"; exit 1; }
    grep -q "^## Status$" docs/index.md || { echo "Status section missing"; exit 1; }

    # Theme-nav still has 5 bullets pointing to 5 theme index pages
    THEME_BULLETS=$(awk '/^## Explore by theme$/,/^## For journalists/' docs/index.md | grep -cE "themes/(cost|recipients|efficiency|cannibalisation|reliability)/index\.md")
    [ "$THEME_BULLETS" -eq 5 ] || { echo "Expected 5 theme-nav bullets; got $THEME_BULLETS"; exit 1; }

    # mkdocs build --strict passes against the post-edit tree
    uv run mkdocs build --strict > /tmp/mkdocs_plan03.log 2>&1 || { cat /tmp/mkdocs_plan03.log; echo "mkdocs --strict failed after homepage rewrite"; exit 1; }
    ! grep -E "^WARNING" /tmp/mkdocs_plan03.log || { cat /tmp/mkdocs_plan03.log; echo "mkdocs --strict emitted warnings"; exit 1; }

    echo "Plan 03 homepage rewrite verified."
    </automated>
  </verify>

  <acceptance_criteria>
    - `test -f docs/index.md` returns 0; file is ≥80 lines.
    - `grep -c "Module in focus"` on `docs/index.md` returns 0 (section migrated to cfd.md in Plan 01).
    - `grep -c "^## Data sources$"` returns 0 and `grep -c "^## Code & reproducibility$"` returns 0 (sections deleted per D-10).
    - `grep -c '<div class="grid cards" markdown>'` returns at least 1.
    - Grid contains exactly 8 tiles (counted by `-   ` prefixes inside the grid cards div).
    - 2 populated tiles link to `schemes/cfd.md` and `schemes/ro.md` respectively and embed `subsidy_cfd_dynamics_twitter.png` and `subsidy_ro_dynamics_twitter.png` thumbnails.
    - Chart PNGs were present on disk at execution time (precondition check passed — both `subsidy_cfd_dynamics_twitter.png` and `subsidy_ro_dynamics_twitter.png` found under `docs/charts/html/`; assumes `uv run python -m uk_subsidy_tracker.plotting` was run recently).
    - Headline figures present: `grep "£29"` and `grep "£67"` each return at least 1 hit.
    - 6 placeholder tiles each labelled with the correct phase number: `grep "Coming in Phase 7"` through `"Coming in Phase 12"` each return at least 1 hit.
    - Preserved sections present: `## Explore by theme` (with 5 theme-nav bullets pointing to the 5 theme index pages), `## For journalists and academics` (with Use-our-data link to `data/index.md`), `## Status`.
    - `uv run mkdocs build --strict` exits 0 with zero `WARNING` lines.
    - No references to the four deleted theme pages: `grep -E "themes/cost/cfd-dynamics\.md|themes/cost/remaining-obligations\.md|themes/recipients/cfd-payments-by-category\.md|themes/recipients/lorenz\.md" docs/index.md` returns empty.
  </acceptance_criteria>

  <done>
  `docs/index.md` rewritten to the D-10 layout: hero + 2×4 scheme grid (2 populated + 6 placeholders) + Explore-by-theme (5 cards preserved) + For-journalists card (preserved) + Status (edited to drop per-scheme enumeration). `mkdocs build --strict` green. No lingering references to deleted theme pages, Module-in-focus prose, Data-sources list, or Code-reproducibility block.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Atomic commit — docs(05.1-03): pivot homepage to hero + 2×4 scheme grid</name>
  <files>docs/index.md</files>

  <read_first>
    - `git status --porcelain` to confirm `docs/index.md` is the only modified file (Plans 02 and 03 run in parallel in Wave 2 but touch disjoint files; this commit must not accidentally stage Plan 02's work).
    - `git log --oneline -5` — confirm commit-message cadence.
  </read_first>

  <action>
  Single atomic commit containing only the homepage rewrite. Phase 1 D-16 atomicity: Plan 03's concern is the homepage layout; Plans 01, 02, and 04 are separate atomic commits.

  ```bash
  git add docs/index.md
  # Verify nothing else is staged
  STAGED_COUNT=$(git diff --cached --name-only | wc -l | tr -d ' ')
  [ "$STAGED_COUNT" -eq 1 ] || { echo "Expected 1 staged file, got $STAGED_COUNT"; git diff --cached --name-only; exit 1; }

  git commit -m "$(cat <<'EOF'
  docs(05.1-03): pivot homepage to hero + 2×4 scheme grid

  Restructures docs/index.md per 05.1 D-10: hero + scheme-grid + theme
  nav + journalist card + status. Pre-stages the Phase 6 PORTAL-01
  layout — grid cells ship empty-modulo-CfD+RO here; Phase 6 adds the
  three headline cards + X1 stacked chart + Latest/Last-5/All-time tabs.

  Scheme grid has 8 tiles: 2 populated (CfD with £29bn headline + RO
  with £67bn) + 6 placeholders (FiT Phase 7, Constraints Phase 8, CM
  Phase 9, Balancing Phase 10, Grid Phase 11, SEG Phase 12). Populated
  tiles show Twitter-PNG thumbnail + headline figure + link to the
  matching scheme-detail page.

  Sections removed:
  - "Module in focus: Contracts for Difference" — migrated to
    docs/schemes/cfd.md in Plan 01. CfD↔RO symmetry restored; no more
    special-case homepage prose.
  - "Data sources" bullet list — lives per-scheme on each GOV-01 §8
    block now.
  - "Code & reproducibility" bash block — lives on docs/data/index.md
    (Phase 4 PUB-04) and on each scheme page's §8 Reproduce block.
  - "Scheme detail pages" prose — replaced by the scheme grid.

  Sections preserved verbatim: "Explore by theme" five-card theme nav
  (Cost / Recipients / Efficiency / Cannibalisation / Reliability),
  "For journalists and academics" Use-our-data card, Status (minor
  edit to drop per-scheme enumeration — that state now lives on the
  grid tiles).

  uv run mkdocs build --strict exits 0 with zero warnings.

  Plan 03 of 4 in Phase 05.1. Plan 04 handles CHANGES.md [Unreleased]
  entry + REQUIREMENTS.md SCHEMEPAGE-01 formalisation.
  EOF
  )"
  ```

  **What NOT to do:**
  - Do NOT commit if `git diff --cached --name-only` shows more than just `docs/index.md`. That means Plan 02 or Plan 04 work bled into the staging area.
  - Do NOT skip hooks.
  - Do NOT amend a prior commit to fold the homepage change in. Atomic-linear-commits on main is the Phase 1 D-16 discipline.
  </action>

  <verify>
    <automated>
    # Commit landed
    git log -1 --format='%s' | grep -q "docs(05.1-03): pivot homepage to hero + 2×4 scheme grid" || { echo "Plan 03 commit message missing/wrong"; exit 1; }

    # Single-file commit
    CHANGED=$(git show --stat HEAD --format='' | grep -c '|')
    [ "$CHANGED" -eq 1 ] || { echo "Plan 03 commit touches $CHANGED files, expected 1 (docs/index.md only)"; exit 1; }

    # Tree is clean
    git diff --quiet HEAD || { echo "Uncommitted changes remain"; exit 1; }

    # Post-commit strict build green
    uv run mkdocs build --strict > /tmp/mkdocs_plan03_postcommit.log 2>&1 || { cat /tmp/mkdocs_plan03_postcommit.log; echo "post-commit strict build failed"; exit 1; }
    ! grep -E "^WARNING" /tmp/mkdocs_plan03_postcommit.log || { cat /tmp/mkdocs_plan03_postcommit.log; echo "post-commit strict build warnings"; exit 1; }

    echo "Plan 03 commit + post-commit --strict gate verified."
    </automated>
  </verify>

  <acceptance_criteria>
    - `git log -1 --format='%s'` contains `docs(05.1-03): pivot homepage to hero + 2×4 scheme grid`.
    - `git show --stat HEAD` lists exactly one file: `docs/index.md`.
    - `git status --porcelain` returns empty (clean tree).
    - `uv run mkdocs build --strict` exits 0 on post-commit HEAD with zero `WARNING` lines.
  </acceptance_criteria>

  <done>
  Single atomic commit with message `docs(05.1-03): pivot homepage to hero + 2×4 scheme grid` touching only `docs/index.md`. Post-commit strict build green. Plan 04 can proceed to bookkeeping.
  </done>
</task>

</tasks>

<verification>
Final gate:

```bash
# 1. Phase 3 permanent CI gate
uv run mkdocs build --strict

# 2. Grid visible in rendered HTML (sanity-check — human spot-check expected)
ls site/index.html && grep -c 'class="grid cards"' site/index.html
# Expect: at least 1 hit; confirms the grid cards rendered

# 3. 8 tiles rendered
python3 -c "
import re
html = open('site/index.html').read()
# Each tile in grid cards renders as a <li> or <div> under a <ul class='md-grid__...'>
# Material wraps grid cards in .md-grid__list > .md-grid__item
tiles = html.count('md-grid__item')
print(f'Rendered tiles: {tiles}')
assert tiles >= 8, f'Expected >=8 tiles, got {tiles}'
"
# Note: actual class name may be "grid__item" or similar depending on Material version;
# adjust the grep pattern to match the actual rendered HTML if needed.
```

No pytest run required (docs-only change; no Python code touched).
</verification>

<success_criteria>
- `docs/index.md` rewritten to hero + 2×4 scheme grid + theme nav + journalist card + status.
- Scheme grid has exactly 8 tiles: 2 populated (CfD + RO with thumbnails + headline figures + scheme-page links) + 6 placeholders (FiT Phase 7 through SEG Phase 12).
- Removed sections: Module-in-focus, Data sources, Code & reproducibility, old "Scheme detail pages" prose.
- Preserved sections: Explore-by-theme (5 cards verbatim), For-journalists (Use-our-data card), Status (edited to drop per-scheme enumeration).
- Chart PNGs (CfD + RO dynamics thumbnails) were present on disk at execution time (precondition check passed — prevents the mkdocs --strict gate failing on missing homepage-tile assets).
- `uv run mkdocs build --strict` exits 0 with zero `WARNING` lines.
- Single atomic commit: `docs(05.1-03): pivot homepage to hero + 2×4 scheme grid` touching only `docs/index.md`.
- No references to deleted theme pages (`themes/cost/cfd-dynamics.md` etc.) remain on the homepage.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-cfd-scheme-page/05.1-03-SUMMARY.md` capturing:
- before/after line count of docs/index.md
- the exact wording of each populated-tile card-body paragraph (preserve the final text; note any deviations from the plan's draft wording and rationale)
- the 6 placeholder-tile body paragraph wordings (should be consistent "*Coming in Phase N.*" + 1-sentence scheme description)
- confirmation that mobile responsive rendering works (human spot-check if possible; Material theme default behaviour is expected to just work with `grid cards`)
- any Rule-2 auto-fix made to Status section wording
- confirmation that the 5 theme-nav bullets in Explore-by-theme are byte-identical to the pre-Plan-03 versions
- confirmation that the PNG precondition check passed at execution time (both `subsidy_cfd_dynamics_twitter.png` and `subsidy_ro_dynamics_twitter.png` found under `docs/charts/html/`)
</output>
</content>
