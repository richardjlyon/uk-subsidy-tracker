---
phase: 05.1-cfd-scheme-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/schemes/cfd.md
autonomous: true
requirements:
  - SCHEMEPAGE-01
tags:
  - docs
  - scheme-page
  - cfd

must_haves:
  truths:
    - "docs/schemes/cfd.md exists as a new file at the canonical scheme-page location."
    - "A hostile reader sees both £29bn and £14bn in the opening paragraph without scrolling (D-13 'every component visible')."
    - "The 4 canonical CfD charts (S2 dynamics, S3 by-technology, S4 Lorenz, S5 remaining-obligations) are each embedded as a Twitter-PNG image immediately followed by an 'Interactive version' HTML link (D-01/D-05 verbatim mirror of RO)."
    - "The page reads in the same 8-section order as docs/schemes/ro.md so a reader who has navigated RO finds CfD with no re-training (D-02)."
    - "H2 anchor slugs #cost-dynamics-chart-s2, #by-technology-chart-s3, #concentration-chart-s4, #forward-commitment-chart-s5 resolve — these are the URL targets Plan 02's theme-gallery cards link into (D-03)."
    - "A §8 GOV-01 four-way coverage block is present with all four pillars: primary source URL, chart source-code permalink(s), pipeline source-code permalink, test permalink."
    - "A §9 Headline FAQ is present with the four CfD-specific D-14 questions."
    - "mkdocs build --strict compiles the new page with zero warnings when cfd.md is added to nav (the nav wiring itself is Plan 02; this plan ships the page standalone — the file must be valid Markdown that passes mkdocs strict parsing even before nav is wired)."
  artifacts:
    - path: "docs/schemes/cfd.md"
      provides: "CfD scheme-detail page mirroring ro.md 8-section structure + 4-chart embeds + GOV-01 + Headline FAQ"
      min_lines: 230
      contains: "Cost dynamics (Chart S2)"
  key_links:
    - from: "docs/schemes/cfd.md"
      to: "docs/charts/html/subsidy_cfd_dynamics_twitter.png"
      via: "markdown image embed"
      pattern: "subsidy_cfd_dynamics_twitter\\.png"
    - from: "docs/schemes/cfd.md"
      to: "docs/charts/html/subsidy_cfd_payments_by_category_twitter.png"
      via: "markdown image embed"
      pattern: "subsidy_cfd_payments_by_category_twitter\\.png"
    - from: "docs/schemes/cfd.md"
      to: "docs/charts/html/subsidy_lorenz_twitter.png"
      via: "markdown image embed"
      pattern: "subsidy_lorenz_twitter\\.png"
    - from: "docs/schemes/cfd.md"
      to: "docs/charts/html/subsidy_remaining_obligations_twitter.png"
      via: "markdown image embed"
      pattern: "subsidy_remaining_obligations_twitter\\.png"
    - from: "docs/schemes/cfd.md"
      to: "docs/methodology/gas-counterfactual.md"
      via: "§7 methodology bullet link"
      pattern: "methodology/gas-counterfactual\\.md"
---

<objective>
Create `docs/schemes/cfd.md` — the CfD scheme-detail page — as a direct structural mirror of `docs/schemes/ro.md`. Eight H2 sections + Headline FAQ + See-also + italic footer. Four chart embeds (S2 / S3 / S4 / S5) in the RO-verbatim Twitter-PNG + interactive-HTML pattern. Prose migrates wholesale from the four soon-to-be-deleted theme pages (`themes/cost/cfd-dynamics.md`, `themes/cost/remaining-obligations.md`, `themes/recipients/cfd-payments-by-category.md`, `themes/recipients/lorenz.md`) into §§3/6/4/5 respectively — Phase 3 adversarial prose is preserved, not rewritten. The adversarial lead rewrites to RO single-sentence-punch cadence per D-12 with both £29bn and £14bn visible per D-13. GOV-01 four-way coverage manifest at §8. Headline FAQ §9 with four CfD-specific D-14 questions.

Purpose: Restore CfD↔RO symmetry before Phase 6 PORTAL-01 needs it. A reader who has learnt `docs/schemes/ro.md` finds `docs/schemes/cfd.md` using the same reading pattern, section order, chart-embed convention, and GOV-01 block shape. This is the load-bearing artefact for 05.1; Plans 02-04 sweep up around it.

Output: One new file (`docs/schemes/cfd.md`, ~250 lines) + one atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05.1-cfd-scheme-page/05.1-CONTEXT.md
@.planning/phases/05.1-cfd-scheme-page/05.1-PATTERNS.md
@CLAUDE.md
@docs/schemes/ro.md
@docs/schemes/index.md
@docs/themes/cost/cfd-dynamics.md
@docs/themes/cost/remaining-obligations.md
@docs/themes/recipients/cfd-payments-by-category.md
@docs/themes/recipients/lorenz.md
@docs/index.md
@docs/methodology/gas-counterfactual.md
@mkdocs.yml
</context>

<threat_model>
## Trust Boundaries

Docs-only phase. No new executable code paths, no user-input surfaces, no data ingress. The only trust boundary touched is the content pipeline (Markdown source → `mkdocs build --strict` → published HTML on GitHub Pages).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-01-01 | Information Disclosure | docs/schemes/cfd.md GOV-01 permalinks | accept | Only public regulator URLs (LCCC data portal, ONS, GOV.UK UK ETS) and public GitHub repo permalinks are cited. No secrets, credentials, or PII. |
| T-05.1-01-02 | Tampering | mkdocs build pipeline | mitigate | `mkdocs build --strict` (Phase 3 permanent CI gate) detects broken internal links, missing nav entries, unresolved anchors. Plan 01 ships the page standalone — the `--strict` gate runs in Plan 02 once nav is wired. Plan 01's task-level verify runs `mkdocs build --strict` against a temporary nav shim to confirm the page itself is valid. |

No new attack surface. Existing CI gate sufficient.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create docs/schemes/cfd.md mirroring ro.md 8-section structure</name>
  <files>docs/schemes/cfd.md</files>

  <read_first>
    MANDATORY — read these files before writing a single line of cfd.md:
    - `docs/schemes/ro.md` (THE TEMPLATE — read all 251 lines; cfd.md is a direct structural mirror)
    - `.planning/phases/05.1-cfd-scheme-page/05.1-PATTERNS.md` (exact embed markdown, anchor slug rules, GOV-01 URL shape, prose-migration source→destination table)
    - `.planning/phases/05.1-cfd-scheme-page/05.1-CONTEXT.md` (D-01 through D-14 are locked)
    - `docs/themes/cost/cfd-dynamics.md` (prose source for §3 — four panels explainer, "CfD shield does not work" insight, caveats, formula pseudocode)
    - `docs/themes/cost/remaining-obligations.md` (prose source for §6 — panels explainer, headline-figures table, "what's not in this chart", methodology pseudocode, caveats)
    - `docs/themes/recipients/cfd-payments-by-category.md` (prose source for §4 — what the chart shows, the argument, methodology prose, caveats)
    - `docs/themes/recipients/lorenz.md` (prose source for §5 — what the chart shows, the argument, methodology pseudocode, caveats)
    - `docs/index.md` lines 22-50 (the "Module in focus" section — line 37-50 "What are CfDs?" migrates verbatim into §2; line 22-31 lead migrates then rewrites per D-12)
    - `docs/methodology/gas-counterfactual.md` (§7 methodology links out to this — do not duplicate the formula on cfd.md)

    MANDATORY PRECONDITION — run this before doing anything else. `docs/charts/html/` is `.gitignore`d and regenerated via `uv run python -m uk_subsidy_tracker.plotting`. If the 4 CfD PNGs are missing at execution time, the mkdocs --strict gate at the end of this task will fail on missing assets. If the check below fails, run `uv run python -m uk_subsidy_tracker.plotting` first, then re-check.

    ```bash
    ls docs/charts/html/subsidy_cfd_dynamics_twitter.png \
       docs/charts/html/subsidy_cfd_payments_by_category_twitter.png \
       docs/charts/html/subsidy_lorenz_twitter.png \
       docs/charts/html/subsidy_remaining_obligations_twitter.png \
       >/dev/null 2>&1 || {
      echo "Chart PNGs missing. Run: uv run python -m uk_subsidy_tracker.plotting"
      exit 1
    }
    ```
  </read_first>

  <action>
  Create `docs/schemes/cfd.md` as a new file following the RO-verbatim 8-section template + Headline FAQ + See-also + italic footer. The page MUST contain the following 9 H2 sections in this exact order and with these exact heading strings (anchor slugs are mechanical MkDocs derivations — do not override):

  1. `# UK Contracts for Difference (CfD)` (H1)
  2. **Opening paragraph** — single-sentence bold punch headline per D-12 cadence. MUST contain both `£29 billion` (or `£29bn`) AND `£14 billion` (or `£14bn`) in the same paragraph per D-13. Example shape (refine wording; keep the anchors):

     > **The price floor sold as a shield — ten years in, UK consumers have paid £29 billion for CfD electricity and £14 billion more than the existing gas fleet would have cost for the same MWh.**

     Follow with a 2-3 sentence "every component visible" paragraph referencing the 2022 gas-crisis evidence anchor ("In only one year — 2022, the worst gas crisis in living memory — did CfD electricity come close to matching the gas alternative, and even then it still cost 7% more"). Follow with a third paragraph describing scope disclosures: LCCC data portal as primary source, gas counterfactual shared with RO, early-period exclusion (pre-Jan 2018, ~5.6 TWh, ~£0.4bn omitted from the £14bn — migrated from cfd-dynamics.md caveat #1), structural concentration (6 projects = 50% of £29bn — forward reference to §5).

  3. `## What is the CfD?` → 2-3 paragraph explainer. MIGRATE VERBATIM the "What are CfDs?" block from `docs/index.md` lines 37-50 (two paragraphs: "A **Contract for Difference** guarantees a fixed price per MWh..." through "...no more, no less."). Then add a bulleted mechanism list (strike price, reference price, levy direction, 15-year term / 20-year AR7 / 35-year Hinkley) styled like RO's bullet list at `docs/schemes/ro.md:17-22`. Optionally add 1-2 H3 subsections mirroring RO's "Why X / Why Y" framing — you may use `### Why the CfD dominates the renewables-subsidy conversation` (contrast with RO's "scheme you've never heard of") and `### Why a 15-year strike-price lock matters` — or fold into a single prose block. H3 subsections are optional; main §2 explainer paragraphs are required.

  4. `## Cost dynamics (Chart S2)` → anchor `#cost-dynamics-chart-s2`. Embed pattern (exact, verbatim):

     ```markdown
     ## Cost dynamics (Chart S2)

     ![CfD dynamics — the full mechanism in four panels](../charts/html/subsidy_cfd_dynamics_twitter.png)

     [Interactive version](../charts/html/subsidy_cfd_dynamics.html){target="_blank"}
     ```

     Then migrate the prose from `docs/themes/cost/cfd-dynamics.md`:
     - "The four panels" explainer (source lines 13-36) — lightly reflow into RO-style numbered list if it helps cadence; substantive content preserved.
     - "What the chart reveals" narrative (source lines 38-54) — "The CfD shield does not work" insight migrates verbatim; two-paragraph cadence preserved.
     - "How the numbers stack" pseudocode block (source lines 56-70) — migrate verbatim in a fenced code block.

     The §3 Caveats block from the deleted page does NOT belong in §3 — per the methodology-caveats section pattern at `docs/schemes/ro.md:162-169`, these caveats migrate to a dedicated H3 "Methodology caveats — what this page does not adjust for" subsection inside §7 Methodology. See §7 instructions below.

  5. `## By technology (Chart S3)` → anchor `#by-technology-chart-s3`. Embed block identical shape to §3 but with `subsidy_cfd_payments_by_category_twitter.png` and `subsidy_cfd_payments_by_category.html`. Alt text: `![CfD payments by technology category](...)`. Then migrate prose from `docs/themes/recipients/cfd-payments-by-category.md`:
     - "What the chart shows" (source lines 9-27) — two-panel description with TECHNOLOGY_COLORS convention reference.
     - "The argument" (source lines 29-57) — "Offshore wind and Drax biomass dominate" + 2022-negative-slivers explanation preserved.
     - Methodology prose (source lines 59-94) — collapse the LCCC technology-mapping table and `cfd_cost` formula into a short prose note; the full formula pseudocode can live in §7 Methodology if length is a concern. (Planner preference: keep the LCCC-technology→display-category mapping table inline in §4 because it is a reader-level classification statement, not a methodology-engine statement; put the `cfd_cost = reference_price × gen + CFD_Payments_GBP` formula in §7.)
     - Caveats (source lines 96-113) — migrate only the caveats that are distinctively per-chart (technology classification is LCCC-snapshot-fixed; "Other" aggregation pooled; 2022 negative-payment months visible as inverted slivers; biomass category ≈ Drax). Cross-chart caveats (gas counterfactual shape, early-period exclusion) go to §7.

  6. `## Concentration (Chart S4)` → anchor `#concentration-chart-s4`. Embed block with `subsidy_lorenz_twitter.png` and `subsidy_lorenz.html`. Alt text: `![Lorenz curve — CfD payment concentration](...)`. Migrate prose from `docs/themes/recipients/lorenz.md`:
     - "What the chart shows" (source lines 9-26) — Lorenz construction, threshold annotations, top-3 labelling.
     - "The argument" (source lines 28-54) — "Six projects receive 50%" + Drax+Hornsea+Walney+Beatrice+Ørsted/SSE/Iberdrola/Equinor prose verbatim.
     - Methodology pseudocode (source lines 56-80) — migrate verbatim in fenced code block; preserve `(cum_pct_payments >= threshold).argmax()` formula.
     - Caveats (source lines 82-102) — migrate: positive-payment filter is load-bearing, multi-phase offshore wind farms treated as separate recipients, 2022 gas-crisis distortion, no Gini coefficient plotted.
     - Add a cross-reference sentence at end of §5: "The RO scheme's equivalent concentration view is at [RO Concentration](ro.md#concentration-chart-s4); the curvature differs (RO steeper because Drax + Lynemouth + top offshore farms dominate over 20 years; CfD shallower because the fleet is more uniformly-large offshore wind)." This is the reciprocal to the line being edited in Plan 02 on ro.md:83.

  7. `## Forward commitment (Chart S5)` → anchor `#forward-commitment-chart-s5`. Embed block with `subsidy_remaining_obligations_twitter.png` and `subsidy_remaining_obligations.html`. Alt text: `![Remaining CfD obligations](...)`. Migrate prose from `docs/themes/cost/remaining-obligations.md`:
     - "The four panels" description (source lines 13-27) — flat-vs-NESO-growth × annual-vs-cumulative layout.
     - "Headline figures" table (source lines 29-41) — migrate verbatim: `| Round | Units | Annual cost | Runs to |` with Investment Contracts / AR1 / AR2 / Total rows. The `£33bn` cumulative + £50bn realistic-including-AR3+ sentence migrates too.
     - "What's *not* in this chart" block (source lines 43-59) — AR3+ / AR7 / Hinkley / Drax-biomass-CfD exclusions preserved.
     - Methodology pseudocode (source lines 61-80) — migrate in fenced code block.
     - Caveats (source lines 82-94) — migrate as bulleted list.

  8. `## Methodology` → no chart embed. Follow RO's §7 pattern at `docs/schemes/ro.md:122-169`:
     - Bulleted list of methodology primitives. Include:
       * **Primary cost:** `cfd_cost = reference_price × generation + CFD_Payments_GBP` equivalent to `strike × generation` by construction; link to `counterfactual.py` source.
       * **Gas counterfactual:** shared with RO. See [`methodology/gas-counterfactual.md`](../methodology/gas-counterfactual.md) for the single source of truth on the displaced-gas assumption (CCGT efficiency 0.55, ONS gas wholesale, UK ETS carbon overlay, £5/MWh fixed O&M existing-fleet).
       * **Calendar year axis:** all charts use calendar year. Scheme-year (Apr-Mar) is preserved in raw LCCC settlements but derived grains aggregate to CY.
       * **Scope:** GB-only; CfD is a GB-scheme (no NIRO equivalent).
       * **Wholesale+levy decomposition:** For the breakdown of the £14bn premium into wholesale-price + consumer-levy cash flows, see [what consumers paid vs what gas would have cost](../themes/cost/cfd-vs-gas-cost.md) — the chart stays on its own theme page per D-04.
       * **Methodology version:** `counterfactual.METHODOLOGY_VERSION = "0.1.0"` (see `CHANGES.md ## Methodology versions`); bump to 1.0.0 reserved for Phase 6 portal launch per Phase 5 D-06.
       * **Benchmark:** reconciled against LCCC self-reconciliation floor per `tests/test_benchmarks.py`; external cross-check candidates include Ben Pile 2021 + 2026 totals.

     - H3 `### Methodology caveats — what this page does not adjust for` — migrate the Caveats block from `docs/themes/cost/cfd-dynamics.md` lines 72-86 (early period excluded / gas counterfactual assumes existing-fleet economics / gas-supply assumption at observed wholesale prices). These are cross-chart caveats that apply to every £/MWh and £bn figure on the page.

  9. `## Data & code (GOV-01 four-way coverage)` → follow the RO shape at `docs/schemes/ro.md:171-218` exactly. All four pillars present:

     - **Primary source** bullet — cite the three LCCC / ONS / UK ETS URLs verbatim from `docs/themes/cost/cfd-dynamics.md:89-91`:
       * [LCCC Actual CfD Generation and avoided GHG emissions](https://www.lowcarboncontracts.uk/data-portal/dataset/actual-cfd-generation-and-avoided-ghg-emissions/actual-cfd-generation-and-avoided-ghg-emissions)
       * [LCCC CfD Contract Portfolio Status](https://www.lowcarboncontracts.uk/data-portal/dataset/cfd-contract-portfolio-status) (for S5 forward projection)
       * [ONS System Average Price of gas](https://www.ons.gov.uk/economy/inflationandpriceindices/timeseries/gk36/mm22)
       * [UK ETS auction results (GOV.UK)](https://www.gov.uk/government/publications/participating-in-the-uk-ets)
       * [NESO Future Energy Scenarios](https://www.neso.energy/publications/future-energy-scenarios-fes) (for S5 NESO-growth scenario)

     - **Chart source code** bullet — 4 GitHub permalinks, one per chart. URL shape `https://github.com/richardjlyon/uk-subsidy-tracker/blob/main/src/uk_subsidy_tracker/plotting/subsidy/<file>.py`. Files:
       * `cfd_dynamics.py`
       * `cfd_payments_by_category.py`
       * `lorenz.py`
       * `remaining_obligations.py`

     - **Pipeline source code** bullet — `schemes/cfd/` directory tree URL (`https://github.com/richardjlyon/uk-subsidy-tracker/tree/main/src/uk_subsidy_tracker/schemes/cfd`) + `counterfactual.py` file URL (`https://github.com/richardjlyon/uk-subsidy-tracker/blob/main/src/uk_subsidy_tracker/counterfactual.py`) + `data/lccc.py` + `data/ons_gas.py` + `data/elexon.py` for raw-layer downloaders (link at least the three — match RO's 3-linked-pipeline-file shape).

     - **Test** bullet — link 2 tests per D-Claude's-Discretion:
       * [`tests/test_counterfactual.py`](https://github.com/richardjlyon/uk-subsidy-tracker/blob/main/tests/test_counterfactual.py) — pins the gas counterfactual formula (fuel + carbon + O&M) per Phase 2 TEST-01.
       * [`tests/test_benchmarks.py`](https://github.com/richardjlyon/uk-subsidy-tracker/blob/main/tests/test_benchmarks.py) — LCCC self-reconciliation floor + external anchors (Phase 2 TEST-04).

     - **Reproduce** fenced bash block — the canonical pattern, replacing `ro` with `cfd`:

       ```bash
       git clone https://github.com/richardjlyon/uk-subsidy-tracker
       cd uk-subsidy-tracker
       uv sync
       uv run python -c "from uk_subsidy_tracker.schemes import cfd; cfd.refresh(); cfd.rebuild_derived()"
       uv run python -m uk_subsidy_tracker.plotting
       ```

     - OPTIONAL H3 `### Per-grain derived-data shape` — a table of the 5 CfD Parquet grains (`station_month`, `annual_summary`, `by_technology`, `by_allocation_round`, `forward_projection` per `schemes/cfd/` Phase 4 module), mirroring RO at `docs/schemes/ro.md:196-206`. If you include it, keep to 5 rows. If omitted, add a one-sentence cross-reference to `docs/data/index.md` instead. Planner preference: include the table (cheap; matches RO parity).

  10. `## Headline FAQ` → follow RO pattern at `docs/schemes/ro.md:220-237`. Exactly four `**Q: ...**` questions in this order per D-14:
     - **Q: Is £29bn / £14bn the only figure that matters?** — answer: no; annual levy, per-MWh premium, forward commitment (chart S5) are equal-weight reads.
     - **Q: Why compare to gas rather than to renewables-without-subsidy?** — marginal-displacement question; methodology links out to `methodology/gas-counterfactual.md`; alternative framings (£/tCO2, new-build gas) documented on other site themes.
     - **Q: What about 2022? Didn't the scheme work that year?** — the 2022 gas-crisis test: CfD still cost 7% more than gas even at crisis peak; premium-per-MWh inverts briefly but cumulative bill keeps climbing.
     - **Q: How is £29bn different from LCCC's published CfD figures?** — CY-axis + gas-counterfactual formula convention differences; LCCC publishes levy payments directly, this site derives consumer cost via strike × generation minus gas-counterfactual.

     Each answer = 1-3 sentences; tone matches RO's FAQ answers (informative, not defensive). Answer #2 and Answer #4 MUST contain external links (the counterfactual methodology page and the LCCC data portal respectively).

  11. `## See also` → 6-bullet list mirroring RO at `docs/schemes/ro.md:240-245`:
     - [Cost theme](../themes/cost/index.md) — cross-scheme subsidy cost comparisons.
     - [Recipients theme](../themes/recipients/index.md) — who receives the subsidies.
     - [Gas counterfactual](../methodology/gas-counterfactual.md) — the displaced-gas methodology shared with RO.
     - [Citation](../about/citation.md) — how to cite this dataset.
     - [Corrections](../about/corrections.md) — reporting errors in any CfD chart, number, or methodology claim on this page.
     - [Schemes overview](index.md) — landing page for all per-scheme detail pages; links to [Renewables Obligation (RO)](ro.md) as the sister scheme page.

  12. Horizontal rule `---` then italic footer — two paragraphs per `docs/schemes/ro.md:249-251`:

     > *This page is part of the [Schemes section](index.md). It is the first scheme detail page chronologically — CfD is the prototype scheme this site was built around — and it mirrors the [RO scheme page](ro.md) structure verbatim. The template here — eight sections, GOV-01 four-way coverage at page bottom, Headline FAQ — is the pattern for the remaining scheme detail pages (FiT in Phase 7, constraints / capacity market / balancing / grid / SEG in Phases 8-12).*
     >
     > *Last refreshed: see [`manifest.json`](https://github.com/richardjlyon/uk-subsidy-tracker/blob/main/site/data/manifest.json) `generated_at` field; daily 06:00 UTC cron.*

  **What NOT to do:**
  - Do NOT embed interactive HTML via `pymdownx.tabbed` or iframes — PNG-first + `[Interactive version](...){target="_blank"}` link is the non-negotiable convention per D-05.
  - Do NOT duplicate the gas-counterfactual formula on cfd.md. §7 Methodology cross-links to `methodology/gas-counterfactual.md`; formula authority lives there per D-04.
  - Do NOT embed `cfd_vs_gas_total` chart on cfd.md — it stays on its theme page per D-04; §7 cross-links to it.
  - Do NOT rewrite the Phase 3 prose. The four prose-source files contain adversarial-proofed paragraphs — migrate them, lightly reflow for cadence, preserve content.
  - Do NOT add new scope to cfd.md that doesn't exist on ro.md. Cross-scheme symmetry is first-class; the two pages should read like they were written from the same template.
  - Do NOT use backslash-escaped anchors or Markdown variations; MkDocs slugify `## Cost dynamics (Chart S2)` → `#cost-dynamics-chart-s2` mechanically. Verify against RO's actual slugs in `docs/themes/cost/index.md:43,51` + `docs/themes/recipients/index.md:27`.
  </action>

  <verify>
    <automated>
    # Run from repo root. All must pass.

    # 0. PRECONDITION — chart PNGs exist on disk (regenerated via `uv run python -m uk_subsidy_tracker.plotting`)
    ls docs/charts/html/subsidy_cfd_dynamics_twitter.png \
       docs/charts/html/subsidy_cfd_payments_by_category_twitter.png \
       docs/charts/html/subsidy_lorenz_twitter.png \
       docs/charts/html/subsidy_remaining_obligations_twitter.png \
       >/dev/null 2>&1 || {
      echo "Chart PNGs missing. Run: uv run python -m uk_subsidy_tracker.plotting"
      exit 1
    }

    # 1. File exists and has meaningful size
    test -f docs/schemes/cfd.md || { echo "cfd.md missing"; exit 1; }
    wc -l docs/schemes/cfd.md | awk '$1 >= 230 { exit 0 } { print "cfd.md too short: " $1 " lines < 230"; exit 1 }'

    # 2a. All 9 H2 sections present in correct order (automated diff vs expected sequence)
    awk -F'## ' '/^## / {print $2}' docs/schemes/cfd.md > /tmp/cfd_h2.txt
    cat > /tmp/cfd_h2_expected.txt <<'EXPECTED'
What is the CfD?
Cost dynamics (Chart S2)
By technology (Chart S3)
Concentration (Chart S4)
Forward commitment (Chart S5)
Methodology
Data & code (GOV-01 four-way coverage)
Headline FAQ
See also
EXPECTED
    # grep out optional H3 subsection lines that start with "### " (awk above already filters for "## " exactly — not "### ")
    # But double-check by restricting to lines matching exactly two '#' at start:
    awk '/^## [^#]/ {sub(/^## /, ""); print}' docs/schemes/cfd.md > /tmp/cfd_h2.txt
    diff -u /tmp/cfd_h2_expected.txt /tmp/cfd_h2.txt || {
      echo "H2 section order/names do not match expected sequence. See diff above."
      exit 1
    }

    # 2b. Individual H2 presence checks (belt-and-braces alongside the full-list diff)
    grep -c "^## What is the CfD?$" docs/schemes/cfd.md | grep -q "^1$" || { echo "Missing or wrong §2 heading"; exit 1; }
    grep -c "^## Cost dynamics (Chart S2)$" docs/schemes/cfd.md | grep -q "^1$" || { echo "Missing §3 heading"; exit 1; }
    grep -c "^## By technology (Chart S3)$" docs/schemes/cfd.md | grep -q "^1$" || { echo "Missing §4 heading"; exit 1; }
    grep -c "^## Concentration (Chart S4)$" docs/schemes/cfd.md | grep -q "^1$" || { echo "Missing §5 heading"; exit 1; }
    grep -c "^## Forward commitment (Chart S5)$" docs/schemes/cfd.md | grep -q "^1$" || { echo "Missing §6 heading"; exit 1; }
    grep -c "^## Methodology$" docs/schemes/cfd.md | grep -q "^1$" || { echo "Missing §7 heading"; exit 1; }
    grep -c "^## Data & code (GOV-01 four-way coverage)$" docs/schemes/cfd.md | grep -q "^1$" || { echo "Missing §8 heading"; exit 1; }
    grep -c "^## Headline FAQ$" docs/schemes/cfd.md | grep -q "^1$" || { echo "Missing §9 heading"; exit 1; }
    grep -c "^## See also$" docs/schemes/cfd.md | grep -q "^1$" || { echo "Missing See-also heading"; exit 1; }

    # 3. D-13 both numbers visible in opening block (first 50 lines)
    head -50 docs/schemes/cfd.md | grep -qE "£29 ?(bn|billion)" || { echo "£29bn not in opening block"; exit 1; }
    head -50 docs/schemes/cfd.md | grep -qE "£14 ?(bn|billion)" || { echo "£14bn not in opening block"; exit 1; }

    # 4. All 4 chart PNG embeds present
    grep -q "subsidy_cfd_dynamics_twitter.png" docs/schemes/cfd.md || { echo "Missing S2 chart embed"; exit 1; }
    grep -q "subsidy_cfd_payments_by_category_twitter.png" docs/schemes/cfd.md || { echo "Missing S3 chart embed"; exit 1; }
    grep -q "subsidy_lorenz_twitter.png" docs/schemes/cfd.md || { echo "Missing S4 chart embed"; exit 1; }
    grep -q "subsidy_remaining_obligations_twitter.png" docs/schemes/cfd.md || { echo "Missing S5 chart embed"; exit 1; }

    # 5. All 4 interactive HTML links present with target="_blank"
    grep -q 'subsidy_cfd_dynamics.html).*target="_blank"' docs/schemes/cfd.md || { echo "S2 interactive link malformed"; exit 1; }
    grep -q 'subsidy_cfd_payments_by_category.html).*target="_blank"' docs/schemes/cfd.md || { echo "S3 interactive link malformed"; exit 1; }
    grep -q 'subsidy_lorenz.html).*target="_blank"' docs/schemes/cfd.md || { echo "S4 interactive link malformed"; exit 1; }
    grep -q 'subsidy_remaining_obligations.html).*target="_blank"' docs/schemes/cfd.md || { echo "S5 interactive link malformed"; exit 1; }

    # 6. GOV-01 four pillars present in §8
    grep -q "lowcarboncontracts.uk/data-portal" docs/schemes/cfd.md || { echo "Missing LCCC primary source URL"; exit 1; }
    grep -q "blob/main/src/uk_subsidy_tracker/plotting/subsidy/cfd_dynamics.py" docs/schemes/cfd.md || { echo "Missing chart source permalink"; exit 1; }
    grep -q "tree/main/src/uk_subsidy_tracker/schemes/cfd" docs/schemes/cfd.md || { echo "Missing pipeline source permalink"; exit 1; }
    grep -q "blob/main/tests/test_counterfactual.py" docs/schemes/cfd.md || { echo "Missing test permalink"; exit 1; }

    # 7. FAQ has 4 questions
    FAQ_COUNT=$(awk '/^## Headline FAQ$/,/^## See also$/' docs/schemes/cfd.md | grep -cE "^\*\*Q:")
    [ "$FAQ_COUNT" -eq 4 ] || { echo "FAQ has $FAQ_COUNT questions, expected 4"; exit 1; }

    # 8. Methodology cross-links to gas-counterfactual.md
    grep -q "methodology/gas-counterfactual.md" docs/schemes/cfd.md || { echo "§7 missing gas-counterfactual cross-link"; exit 1; }

    # 9. cfd-vs-gas-cost cross-link present per D-04 (methodology section routes to the wholesale+levy decomposition)
    grep -q "themes/cost/cfd-vs-gas-cost.md" docs/schemes/cfd.md || { echo "§7 missing cfd-vs-gas-cost cross-link per D-04"; exit 1; }

    # 10. mkdocs --strict passes against a temporary nav shim.
    # Plan 01 ships cfd.md WITHOUT the nav entry (that is Plan 02's atom); validate by temporarily inserting a nav entry,
    # running strict build, and reverting. Use a Python script written to a temp file (column-0 content) to avoid
    # heredoc indentation traps — indented Python inside a <<'PY' heredoc raises IndentationError and silently
    # corrupts the shim. Writing to /tmp/ also gives us a trap-cleanup target.
    cp mkdocs.yml mkdocs.yml.bak
    # Trap restores mkdocs.yml even on script failure / early exit / SIGINT
    trap 'mv -f mkdocs.yml.bak mkdocs.yml 2>/dev/null || true' EXIT

    cat > /tmp/nav_shim.py <<'PY'
import re
content = open("mkdocs.yml").read()
# Insert "- Contracts for Difference (CfD): schemes/cfd.md" after "- Overview: schemes/index.md"
new_content, n = re.subn(
    r"(- Overview: schemes/index\.md\n)",
    r"\1      - Contracts for Difference (CfD): schemes/cfd.md\n",
    content,
    count=1,
)
assert n == 1, f"Expected exactly 1 substitution in mkdocs.yml, made {n}"
open("mkdocs.yml", "w").write(new_content)
PY
    python3 /tmp/nav_shim.py || { echo "nav-shim Python script failed (indentation/substitution)"; exit 1; }

    if uv run mkdocs build --strict > /tmp/mkdocs_plan01.log 2>&1; then
      STRICT_OK=1
    else
      STRICT_OK=0
      cat /tmp/mkdocs_plan01.log
    fi
    # Restore mkdocs.yml explicitly (trap will also fire on EXIT as a safety net)
    mv mkdocs.yml.bak mkdocs.yml
    trap - EXIT
    [ "$STRICT_OK" -eq 1 ] || { echo "mkdocs --strict failed with cfd.md in nav"; exit 1; }

    # 10a. Post-run assertion — mkdocs.yml must be byte-identical to pre-shim state
    git diff --quiet -- mkdocs.yml || {
      echo "shim left mkdocs.yml dirty — restore failed, see: git diff mkdocs.yml"
      exit 1
    }

    echo "All Plan 01 verifications passed."
    </automated>
  </verify>

  <acceptance_criteria>
    - `test -f docs/schemes/cfd.md` returns 0.
    - `wc -l docs/schemes/cfd.md` reports at least 230 lines (load-bearing floor given the prose-migration scope; matches RO's 251-line reality).
    - `grep -c "^## " docs/schemes/cfd.md` reports at least 9 (the 9 required H2 sections).
    - `awk '/^## [^#]/ {sub(/^## /, ""); print}' docs/schemes/cfd.md` produces the exact expected 9-line sequence: `What is the CfD?` → `Cost dynamics (Chart S2)` → `By technology (Chart S3)` → `Concentration (Chart S4)` → `Forward commitment (Chart S5)` → `Methodology` → `Data & code (GOV-01 four-way coverage)` → `Headline FAQ` → `See also` (verified by `diff -u` against `/tmp/cfd_h2_expected.txt` in the verify block).
    - `head -50 docs/schemes/cfd.md` contains both the string `£29` and the string `£14`.
    - `grep -c "subsidy_cfd_dynamics_twitter.png\|subsidy_cfd_payments_by_category_twitter.png\|subsidy_lorenz_twitter.png\|subsidy_remaining_obligations_twitter.png" docs/schemes/cfd.md` reports at least 4.
    - `grep -c 'target="_blank"' docs/schemes/cfd.md` reports at least 4 (one per interactive-HTML link).
    - GOV-01 four pillars all present in §8: `grep "lowcarboncontracts.uk"`, `grep "blob/main/src/uk_subsidy_tracker/plotting/subsidy/cfd_dynamics.py"`, `grep "tree/main/src/uk_subsidy_tracker/schemes/cfd"`, `grep "blob/main/tests/test_counterfactual.py"` — each returns at least 1 hit.
    - `awk '/^## Headline FAQ$/,/^## See also$/' docs/schemes/cfd.md | grep -cE "^\*\*Q:"` reports exactly 4.
    - `grep "methodology/gas-counterfactual.md" docs/schemes/cfd.md` returns at least 1 hit (§7 cross-link).
    - `grep "themes/cost/cfd-vs-gas-cost.md" docs/schemes/cfd.md` returns at least 1 hit (§7 D-04 cross-link).
    - `uv run mkdocs build --strict` exits 0 when cfd.md is added to nav via the temporary-shim test in the verify block (original mkdocs.yml restored after).
    - After the nav-shim block runs, `git diff --quiet -- mkdocs.yml` returns 0 (mkdocs.yml was cleanly restored by the trap + explicit restore; no dirty state left behind).
    - No references to the four doomed files inside cfd.md itself (those files are deleted in Plan 02; cfd.md must not link to them): `grep -E "themes/cost/cfd-dynamics|themes/cost/remaining-obligations|themes/recipients/cfd-payments-by-category|themes/recipients/lorenz\.md" docs/schemes/cfd.md` returns empty (the `\.md` anchor prevents false positives on `cfd_dynamics.py` chart-source link in §8).
  </acceptance_criteria>

  <done>
  `docs/schemes/cfd.md` exists with 9 H2 sections in the RO-verbatim order, all 4 chart embeds + interactive-HTML links present, D-13 both-numbers-in-opening-paragraph compliance verified, GOV-01 four-way coverage manifest complete in §8, Headline FAQ §9 has exactly 4 CfD-specific D-14 questions, and `mkdocs build --strict` passes when the page is added to nav (the nav wiring itself is Plan 02; this task validates the page compiles cleanly under strict mode against a temporary nav shim, with trap-based cleanup ensuring mkdocs.yml is restored byte-identically even on failure).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Atomic commit — docs(05.1-01): add CfD scheme-detail page</name>
  <files>docs/schemes/cfd.md</files>

  <read_first>
    - `CHANGES.md` (verify Keep-a-Changelog [Unreleased] section shape — no edits here, that is Plan 04)
    - Recent commit log: `git log --oneline -15` — confirm the commit-message cadence used across the repo (`docs(05-11): ...`, `docs(04-06): ...`, `fix(05-review): ...`).
    - Working tree state: `git status --porcelain` — must show exactly ONE file (`docs/schemes/cfd.md`) and nothing else. If `mkdocs.yml` shows as modified, the nav-shim in Task 1 did not restore cleanly — abort and investigate before committing.
  </read_first>

  <action>
  Stage and commit `docs/schemes/cfd.md` as a single atomic commit. Phase 1 D-16 discipline: one concern per commit. The commit contains the new scheme-detail page with all 8 sections + Headline FAQ + See-also — nothing else. Theme-page deletions, homepage pivot, mkdocs.yml nav, schemes/index.md, CHANGES.md are handled in Plans 02-04 as separate atomic commits so `mkdocs --strict` can be verified at each intermediate tree state.

  **Pre-stage check (defensive guard against Task 1 nav-shim leakage):**

  ```bash
  # mkdocs.yml must be byte-identical to pre-Plan-01 state. The Task 1 nav-shim
  # cp/trap/restore pattern should have left it clean, but verify explicitly
  # before staging anything.
  git diff --quiet -- mkdocs.yml || {
    echo "FATAL: mkdocs.yml has uncommitted changes — nav-shim did not restore cleanly."
    echo "Diff:"
    git diff mkdocs.yml
    exit 1
  }
  ```

  **Commit command:**

  ```bash
  git add docs/schemes/cfd.md
  git commit -m "$(cat <<'EOF'
  docs(05.1-01): add CfD scheme-detail page mirroring ro.md

  Ships docs/schemes/cfd.md — new scheme-detail page following the 8-section
  + Headline FAQ template established by docs/schemes/ro.md (Phase 5 D-15).
  Prose migrated from the 4 overlap theme pages (cfd-dynamics,
  remaining-obligations, cfd-payments-by-category, lorenz) into §§3/6/4/5
  respectively. Adversarial-lead per 05.1 D-12 with both £29bn paid and
  £14bn premium visible in opening paragraph per D-13. GOV-01 four-way
  coverage manifest at §8 (LCCC + ONS + UK ETS + NESO + 4 chart-source
  permalinks + schemes/cfd/ pipeline + test_counterfactual.py +
  test_benchmarks.py + reproduce-from-git-clone bash). Headline FAQ §9
  with 4 CfD-specific D-14 questions. See-also + italic footer matches RO.

  Plan 01 of 4 in Phase 05.1. Plans 02-04 handle:
  - delete the 4 overlap theme pages + migrate inbound links + mkdocs.yml nav
  - homepage pivot to 2×4 scheme grid
  - CHANGES.md [Unreleased] entry + REQUIREMENTS.md SCHEMEPAGE-01

  Theme-page deletions are Plan 02 — this commit deliberately leaves the
  4 doomed files in place so this commit's tree passes mkdocs --strict
  without broken theme-page inbound links.
  EOF
  )"
  ```

  **What NOT to do:**
  - Do NOT combine this commit with Plan 02's deletions. A combined commit would make the intermediate tree un-revertable without losing the scheme page.
  - Do NOT stage mkdocs.yml here — nav wiring is Plan 02's atom. Plan 01's tree must pass `mkdocs --strict` even without cfd.md in the nav (MkDocs tolerates files not referenced by nav provided they exist on disk and validation.nav.omitted_files is set to `warn` per mkdocs.yml:47 — with `--strict`, a warn on omitted_files is promoted to an error, so this expected-to-fail state is the very thing the verify block's temporary-nav-shim pattern works around).
  - Do NOT skip hooks (`--no-verify`).
  - Do NOT proceed if the pre-stage check shows `mkdocs.yml` dirty — the nav-shim in Task 1 failed to restore and must be investigated first.
  </action>

  <verify>
    <automated>
    # Commit landed, tree clean, cfd.md under version control
    git log -1 --format='%H %s' | grep -q "docs(05.1-01): add CfD scheme-detail page" || { echo "Commit message missing or wrong"; exit 1; }
    git diff --quiet HEAD -- docs/schemes/cfd.md || { echo "cfd.md has uncommitted changes"; exit 1; }
    git ls-files docs/schemes/cfd.md | grep -q "docs/schemes/cfd.md" || { echo "cfd.md not tracked"; exit 1; }

    # Only one file changed in the commit — and it MUST be docs/schemes/cfd.md specifically (defence against nav-shim leakage)
    CHANGED_FILES=$(git diff --name-only HEAD~1 HEAD)
    [ "$CHANGED_FILES" = "docs/schemes/cfd.md" ] || {
      echo "Commit touches unexpected files. Expected exactly 'docs/schemes/cfd.md'. Got:"
      echo "$CHANGED_FILES"
      exit 1
    }

    CHANGED_COUNT=$(echo "$CHANGED_FILES" | wc -l | tr -d ' ')
    [ "$CHANGED_COUNT" -eq 1 ] || { echo "Commit touches $CHANGED_COUNT files, expected 1 (cfd.md only)"; exit 1; }

    echo "Plan 01 commit verified."
    </automated>
  </verify>

  <acceptance_criteria>
    - `git log -1 --format='%s'` contains `docs(05.1-01): add CfD scheme-detail page mirroring ro.md`.
    - `git show --stat HEAD` lists exactly one file: `docs/schemes/cfd.md`.
    - `git diff --name-only HEAD~1 HEAD` returns exactly `docs/schemes/cfd.md` — no `mkdocs.yml`, no other files (defence against Task 1 nav-shim leakage).
    - `git diff HEAD~1 HEAD -- docs/schemes/cfd.md` shows the full scheme-page insertion.
    - Working tree is clean: `git status --porcelain` returns empty.
  </acceptance_criteria>

  <done>
  Single atomic commit on HEAD with message `docs(05.1-01): add CfD scheme-detail page mirroring ro.md`, touching only `docs/schemes/cfd.md`. Tree is clean. `git diff --name-only HEAD~1 HEAD` returns exactly one line. Plan 02 executors can now reference `docs/schemes/cfd.md#cost-dynamics-chart-s2` etc. as link targets.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# File present, correct size, under version control
test -f docs/schemes/cfd.md && git ls-files docs/schemes/cfd.md

# The temporary-nav-shim mkdocs --strict verification (inside Task 1) passed
# means the page itself is valid — Plan 02 inherits a clean starting state

# Confirm the commit did NOT accidentally stage mkdocs.yml from the nav-shim
[ "$(git diff --name-only HEAD~1 HEAD)" = "docs/schemes/cfd.md" ] && echo "Single-file commit verified"

# Confirm no accidental overlap with the 4 doomed files' URL slugs inside cfd.md
grep -E "themes/cost/cfd-dynamics\.md|themes/cost/remaining-obligations\.md|themes/recipients/cfd-payments-by-category\.md|themes/recipients/lorenz\.md" docs/schemes/cfd.md
# Must return empty — cfd.md should link to the chart-source .py files,
# not to the doomed .md theme pages
```

Phase-level `mkdocs build --strict` with cfd.md permanently wired into nav is Plan 02's verify gate (once the doomed pages are removed and the nav is updated in a single atomic commit).
</verification>

<success_criteria>
- `docs/schemes/cfd.md` exists, ≥230 lines, 9 H2 sections in RO-verbatim order (verified by automated `diff` against expected section sequence).
- D-13 rule honoured: both `£29` and `£14` appear in the first 50 lines of the file.
- All 4 chart embeds present with PNG + interactive-HTML-link pattern per D-05.
- Chart PNGs were present on disk at execution time (precondition check passed; assumes `uv run python -m uk_subsidy_tracker.plotting` was run recently).
- GOV-01 four-way coverage manifest in §8 complete (primary source + chart source + pipeline source + test).
- Headline FAQ has exactly 4 CfD-specific D-14 questions.
- §7 Methodology cross-links to `methodology/gas-counterfactual.md` (D-04 formula authority stays on methodology page) and `themes/cost/cfd-vs-gas-cost.md` (D-04 wholesale+levy decomposition cross-link).
- Nav-shim mkdocs --strict test passes AND mkdocs.yml is restored byte-identically (trap-protected cleanup).
- Single atomic commit: `docs(05.1-01): add CfD scheme-detail page mirroring ro.md` touching only `docs/schemes/cfd.md` (verified by `git diff --name-only HEAD~1 HEAD` returning exactly that one path).
- Tree at HEAD is clean; Plan 02 can begin with `docs/schemes/cfd.md` as a stable link target.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-cfd-scheme-page/05.1-01-SUMMARY.md` capturing:
- line count of cfd.md and section-by-section prose-migration provenance (which source file fed which §)
- any cadence deviations from D-12 example draft (the adversarial-lead wording is Claude's Discretion; document the final sentence)
- FAQ answer wording choices (especially the LCCC-vs-CfD-site-figures convention-difference answer)
- the H3 subsection decision in §2 (whether the two optional H3s were included or folded)
- confirmation that `grep -E "themes/cost/cfd-dynamics\.md|themes/cost/remaining-obligations\.md|themes/recipients/cfd-payments-by-category\.md|themes/recipients/lorenz\.md" docs/schemes/cfd.md` returns empty
- confirmation that nav-shim trap-cleanup worked: mkdocs.yml unchanged by the end of Task 1 verify block
- confirmation that Plan 01's commit touches exactly `docs/schemes/cfd.md` (via `git diff --name-only HEAD~1 HEAD`)
</output>
</task>
</tasks>
</output>
</content>
</invoke>