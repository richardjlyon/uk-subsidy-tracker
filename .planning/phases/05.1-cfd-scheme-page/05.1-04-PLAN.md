---
phase: 05.1-cfd-scheme-page
plan: 04
type: execute
wave: 3
depends_on:
  - "05.1-01"
  - "05.1-02"
  - "05.1-03"
files_modified:
  - CHANGES.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - SCHEMEPAGE-01
tags:
  - docs
  - bookkeeping
  - changelog
  - requirements

must_haves:
  truths:
    - "CHANGES.md [Unreleased] gains a new `#### Phase 05.1 — CfD Scheme Page Retrofit` subsection under `### Added`, inserted above the existing `#### Phase 5` block (newest-first ordering)."
    - "The CHANGES.md entry documents what shipped (cfd.md + homepage grid) and what was deleted (4 overlap theme pages) with cross-references to the 4 atomic commits from Plans 01-03."
    - "`.planning/REQUIREMENTS.md` gains a new `SCHEMEPAGE-01` requirement ID in a new `### Scheme Page Template` section with the exact wording from the 05.1 context (`Every scheme page mirrors docs/schemes/ro.md 8-section structure + 4-chart embeds + GOV-01 four-way coverage manifest + Headline FAQ`)."
    - "The REQUIREMENTS.md Traceability table gains a row: `| SCHEMEPAGE-01 | Phase 05.1 | Complete |` — the ID is closed by this phase."
    - "The REQUIREMENTS.md v1 coverage counters are updated: 61 → 62 requirements; 61 → 62 mapped; 0 unmapped."
    - "`.planning/ROADMAP.md` Phase 05.1 section updates `Requirements: TBD` to `Requirements: SCHEMEPAGE-01` and marks the phase as complete once Plan 04 lands (i.e., once the fourth plan's atomic commit lands)."
    - "`uv run mkdocs build --strict` still exits 0 on post-commit HEAD (regression guard — CHANGES.md and the .planning/ tree are not served by mkdocs, but we run the gate anyway for paranoia)."
    - "`uv run pytest tests/test_docs_structure.py -q` still passes (regression guard — the 4 deleted theme pages do not break Phase 3 TRIAGE invariants because the invariants enumerate absence-of-CUT-pages and presence-of-PROMOTE-pages; the 4 deleted are PROMOTE pages whose content moved to cfd.md, so the invariant enumeration may need a one-line update that should already have been applied in Plan 02 if triggered)."
  artifacts:
    - path: "CHANGES.md"
      provides: "[Unreleased] entry documenting Phase 05.1 — load-bearing audit trail for a future release tag"
      contains: "Phase 05.1 — CfD Scheme Page Retrofit"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SCHEMEPAGE-01 formalises cross-scheme page-template symmetry as a checkable requirement"
      contains: "SCHEMEPAGE-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 05.1 no longer shows `Requirements: TBD`; closes the REQ-ID placeholder"
      contains: "SCHEMEPAGE-01"
  key_links:
    - from: "CHANGES.md"
      to: "docs/schemes/cfd.md"
      via: "prose reference"
      pattern: "docs/schemes/cfd\\.md"
    - from: ".planning/REQUIREMENTS.md"
      to: "docs/schemes/cfd.md"
      via: "SCHEMEPAGE-01 definition prose"
      pattern: "docs/schemes/cfd\\.md"
    - from: ".planning/REQUIREMENTS.md"
      to: "docs/schemes/ro.md"
      via: "SCHEMEPAGE-01 definition prose (the load-bearing template)"
      pattern: "docs/schemes/ro\\.md"
---

<objective>
Ship the Phase 05.1 bookkeeping: a `CHANGES.md [Unreleased]` entry that documents what landed + a new `SCHEMEPAGE-01` requirement ID in `.planning/REQUIREMENTS.md` formalising cross-scheme page-template symmetry + a `ROADMAP.md` update promoting Phase 05.1's `Requirements: TBD` placeholder to `Requirements: SCHEMEPAGE-01`. Single atomic commit. Post-commit `mkdocs build --strict` still green.

Purpose: Formalise the cross-scheme page-template contract as a checkable REQ-ID that Phases 7-12 inherit. Every future scheme page (FiT, Constraints, CM, Balancing, Grid, SEG) has an acceptance criterion ("does this new page mirror the 8-section + GOV-01 + Headline FAQ template?") that comes straight from SCHEMEPAGE-01. Also lay the audit trail proof — CHANGES.md is the release-notes log; a future `v1.0.0` release tag cuts from `[Unreleased]` into a versioned section, and the Phase 05.1 entry must be there.

Output: 3 modified files + 1 atomic commit. `mkdocs build --strict` green on post-commit HEAD.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-cfd-scheme-page/05.1-CONTEXT.md
@.planning/phases/05.1-cfd-scheme-page/05.1-PATTERNS.md
@.planning/phases/05.1-cfd-scheme-page/05.1-01-PLAN.md
@.planning/phases/05.1-cfd-scheme-page/05.1-02-PLAN.md
@.planning/phases/05.1-cfd-scheme-page/05.1-03-PLAN.md
@CLAUDE.md
@CHANGES.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@docs/schemes/cfd.md
@docs/schemes/ro.md
@docs/index.md
</context>

<threat_model>
## Trust Boundaries

Docs-only phase. Plan 04 edits CHANGES.md + `.planning/REQUIREMENTS.md` + `.planning/ROADMAP.md` — none of these are served by mkdocs; they are repo-internal audit/governance artefacts. No executable code path, no user-input surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-04-01 | Tampering | CHANGES.md audit trail | mitigate | Keep-a-Changelog discipline (canonical order: Added / Changed / Fixed / [Unreleased] → release tag) prevents ambiguous entries. Plan 04 inserts the Phase 05.1 subsection at a specific location (above Phase 5, newest-first); verify block enforces ordering. |
| T-05.1-04-02 | Information Disclosure | .planning/REQUIREMENTS.md Traceability drift | mitigate | Coverage counters (61 → 62 / 61 → 62 / 0 unmapped) provide an algebraic check. If a future edit omits SCHEMEPAGE-01 from the Traceability table but adds it to the list, the `Mapped to phases: NN` line goes stale. Plan 04's verify block runs a grep-count consistency check. |

No new attack surface.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append Phase 05.1 entry to CHANGES.md [Unreleased] Added</name>
  <files>CHANGES.md</files>

  <read_first>
    - `CHANGES.md` lines 1-170 (the existing `[Unreleased]` structure; specifically lines 8-11 `## [Unreleased]` + `### Added` + `#### Phase 5 — Renewables Obligation Module` heading at line 12; read through the existing RO Plan 05-11 entry at lines 145-166 as the closest precedent for the Phase 05.1 entry's shape and tone)
    - `.planning/phases/05.1-cfd-scheme-page/05.1-PATTERNS.md` § "CHANGES.md" — contains the exact draft wording of the new Phase 05.1 subsection, the section-placement rule (insert above `#### Phase 5`), and the no-new-Methodology-version-bump confirmation
  </read_first>

  <action>
  Insert a new `#### Phase 05.1 — CfD Scheme Page Retrofit` subsection under `### Added`, positioned BETWEEN the existing `### Added` line 10 and the existing `#### Phase 5 — Renewables Obligation Module` line 12. Newest-first ordering within each `### <category>` block.

  **Exact insertion point:** After line 10 (`### Added`), before line 12 (`#### Phase 5 — Renewables Obligation Module`). Preserve all other CHANGES.md content byte-identically (do not touch the Phase 4 entries, Fixed section, Methodology versions section, or anything else).

  **Content to insert** (4-6 bullet items following the RO Plan 05-11 entry's shape at CHANGES.md:145-166):

  ```markdown
  #### Phase 05.1 — CfD Scheme Page Retrofit

  - **CfD scheme-detail page** (Plan 05.1-01). `docs/schemes/cfd.md` (~250
    lines, 9 H2 sections) mirrors `docs/schemes/ro.md` 8-section template
    verbatim per 05.1 D-01/D-02: Headline (both £29bn paid + £14bn
    premium visible per D-13) / What is the CfD / Cost dynamics (Chart
    S2) / By technology (Chart S3) / Concentration (Chart S4) / Forward
    commitment (Chart S5) / Methodology / Data & code (GOV-01) + Headline
    FAQ (4 CfD-specific D-14 questions) + See-also. Prose migrated
    wholesale from the four overlap theme pages (Phase 3 adversarial
    prose preserved, not rewritten). All 4 chart embeds wired with PNG +
    interactive HTML link; GOV-01 four-way coverage manifest at §8
    cites LCCC + ONS + UK ETS + NESO sources, 4 chart-source permalinks
    (cfd_dynamics.py + cfd_payments_by_category.py + lorenz.py +
    remaining_obligations.py), `schemes/cfd/` pipeline + `counterfactual.py`
    permalinks, test_counterfactual.py + test_benchmarks.py test
    permalinks, reproduce-from-`git clone` bash block. `uv run mkdocs
    build --strict` green.

  - **Deleted 4 overlap theme pages** (Plan 05.1-02). `docs/themes/cost/cfd-dynamics.md`,
    `docs/themes/cost/remaining-obligations.md`,
    `docs/themes/recipients/cfd-payments-by-category.md`,
    `docs/themes/recipients/lorenz.md` removed via `git rm`. Their
    content overlapped 1:1 with `cfd.md` §§S2/S5/S3/S4 respectively per
    05.1 D-06; prose migrated to cfd.md in Plan 01 so nothing lost.

  - **Inbound-link migration + nav update** (Plan 05.1-02). Theme-
    gallery cards on `docs/themes/cost/index.md` (3 CfD cards) and
    `docs/themes/recipients/index.md` (2 CfD cards) retargeted from the
    deleted pages to `schemes/cfd.md#<anchor>`. Outside-the-gallery
    cases: `docs/themes/reliability/rolling-minimum.md:153` See-also
    bullet and `docs/schemes/ro.md:83` reciprocal cross-link both
    retargeted. `docs/schemes/index.md` promotes CfD from Coming-soon
    to Currently-published (alphabetical: CfD before RO). `mkdocs.yml`
    nav updated: Schemes tab gains `Contracts for Difference (CfD):
    schemes/cfd.md` between Overview and RO; Cost and Recipients tabs
    lose the 4 deleted-page entries. Six non-overlap CfD theme pages
    preserved in Phase 3 D-01 six-section form per 05.1 D-08.

  - **Homepage pivoted to 2×4 scheme grid** (Plan 05.1-03). `docs/index.md`
    restructured per 05.1 D-10: hero + Schemes grid (8 Material `grid
    cards` tiles: 2 populated with CfD + RO thumbnails + headline
    figures + links, 6 placeholders labelled "Coming in Phase 7-12"
    for FiT / Constraints / CM / Balancing / Grid / SEG) + Explore-
    by-theme five-card nav (preserved) + For-journalists Use-our-data
    card (preserved) + Status (edited to drop per-scheme enumeration
    — that state moved onto the grid tiles). Sections removed from
    the old homepage: "Module in focus: Contracts for Difference"
    (migrated to cfd.md), "Data sources" bullets (migrated to per-
    scheme GOV-01 §8 blocks), "Code & reproducibility" bash block
    (migrated to `docs/data/index.md` + each scheme page's §8 Reproduce
    block), "Scheme detail pages" prose (replaced by the grid). Pre-
    stages Phase 6 PORTAL-01 layout — same 8-cell grid, Phase 6 wires
    in headline-figure data-binding + X1 stacked chart + Latest/
    Last-5/All-time tabs.

  - **REQUIREMENTS bookkeeping — SCHEMEPAGE-01 closed** (Plan 05.1-04).
    New `SCHEMEPAGE-01` requirement ID added to `.planning/REQUIREMENTS.md`
    formalising the cross-scheme page-template contract: "Every scheme
    page mirrors `docs/schemes/ro.md` 8-section structure + 4-chart
    embeds + GOV-01 four-way coverage manifest + Headline FAQ". Closed
    by Phase 05.1 via both `cfd.md` and `ro.md` matching the template.
    Phases 7-12 inherit SCHEMEPAGE-01 as an acceptance criterion for
    each new scheme page. Coverage counters bumped 61 → 62 / all
    mapped.

  ```

  **Where exactly to put it:** The file currently reads (lines 8-12):

  ```markdown
  ## [Unreleased]

  ### Added

  #### Phase 5 — Renewables Obligation Module
  ```

  After the edit it reads:

  ```markdown
  ## [Unreleased]

  ### Added

  #### Phase 05.1 — CfD Scheme Page Retrofit

  - **CfD scheme-detail page** (Plan 05.1-01). ...
  [... the 5 bullets above ...]

  #### Phase 5 — Renewables Obligation Module
  ```

  **What NOT to do:**
  - Do NOT touch any other section of CHANGES.md. Phase 4 entries, `### Changed`, `### Fixed`, `## Methodology versions`, `## [1.0.0]` (if present from a prior tag) — all byte-identical.
  - Do NOT add a new `## Methodology versions` H3 entry. Phase 05.1 is docs-only; `counterfactual.METHODOLOGY_VERSION` is unchanged at `0.1.0`.
  - Do NOT rename or restructure the `#### Phase 5` heading above — only insert the new `#### Phase 05.1` block above it.
  - Do NOT fold this bookkeeping into Plan 01, 02, or 03's commit. Plan 04's atomicity is the record that Phase 05.1 shipped as a coherent unit; the changelog entry is the artefact of closure.
  - Do NOT reference nonexistent commits (e.g. do not cite SHAs — SHAs change with rebases; cite Plan numbers instead: "Plan 05.1-01", "Plan 05.1-02", "Plan 05.1-03"). This matches the existing Phase 5 entry pattern which says "(Plan 05-01)", "(Plan 05-11)", etc.
  </action>

  <verify>
    <automated>
    # New Phase 05.1 subsection present under ### Added
    grep -q "^#### Phase 05.1 — CfD Scheme Page Retrofit$" CHANGES.md || { echo "Phase 05.1 subsection heading missing"; exit 1; }

    # Positioned above Phase 5 (newest-first)
    python3 - <<'PY'
    content = open("CHANGES.md").read()
    idx_051 = content.index("#### Phase 05.1 — CfD Scheme Page Retrofit")
    idx_5 = content.index("#### Phase 5 — Renewables Obligation Module")
    assert idx_051 < idx_5, f"Phase 05.1 must appear above Phase 5; got 051={idx_051} vs 5={idx_5}"
    # Ensure Phase 05.1 is inside ### Added (before any ### Changed)
    idx_changed = content.find("### Changed")
    assert idx_051 < idx_changed or idx_changed == -1, "Phase 05.1 must be under ### Added"
    print("CHANGES.md ordering verified.")
    PY

    # All 5 Plan-cited bullets present
    for plan in "05.1-01" "05.1-02" "05.1-03" "05.1-04"; do
      grep -q "Plan $plan" CHANGES.md || { echo "CHANGES.md missing citation for Plan $plan"; exit 1; }
    done

    # 4 deleted theme pages mentioned by path
    grep -q "docs/themes/cost/cfd-dynamics.md" CHANGES.md || { echo "Missing mention of cfd-dynamics.md deletion"; exit 1; }
    grep -q "docs/themes/cost/remaining-obligations.md" CHANGES.md || { echo "Missing mention of remaining-obligations.md deletion"; exit 1; }
    grep -q "docs/themes/recipients/cfd-payments-by-category.md" CHANGES.md || { echo "Missing mention of cfd-payments-by-category.md deletion"; exit 1; }
    grep -q "docs/themes/recipients/lorenz.md" CHANGES.md || { echo "Missing mention of lorenz.md deletion"; exit 1; }

    # No new Methodology versions H3 added
    METH_COUNT_BEFORE=$(git show HEAD:CHANGES.md | grep -c "^### " || echo 0)
    METH_COUNT_AFTER=$(grep -c "^### " CHANGES.md || echo 0)
    # Method version H3s are under ## Methodology versions; count should not have changed
    # (new subsection is ####, not ###, so ### count is invariant)
    [ "$METH_COUNT_BEFORE" = "$METH_COUNT_AFTER" ] || { echo "### count changed: $METH_COUNT_BEFORE → $METH_COUNT_AFTER (should be stable)"; exit 1; }

    # SCHEMEPAGE-01 mentioned
    grep -q "SCHEMEPAGE-01" CHANGES.md || { echo "CHANGES.md missing SCHEMEPAGE-01 reference"; exit 1; }

    echo "CHANGES.md edit verified."
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "^#### Phase 05.1 — CfD Scheme Page Retrofit$" CHANGES.md` returns exactly 1.
    - Phase 05.1 subsection positioned above Phase 5 subsection within the `### Added` block (newest-first ordering).
    - Phase 05.1 subsection contains at least 4 bullet items citing Plans 05.1-01 through 05.1-04.
    - All 4 deleted theme-page paths mentioned by name in the entry.
    - No new `## Methodology versions` H3 entry introduced (Phase 05.1 is docs-only; `counterfactual.METHODOLOGY_VERSION` is unchanged).
    - `grep "SCHEMEPAGE-01" CHANGES.md` returns at least 1 hit.
    - All pre-existing CHANGES.md content (Phase 4 entries, Phase 5 entries, `### Changed`, `### Fixed`, `## Methodology versions`) is byte-identical to pre-edit state — verified via `git diff HEAD CHANGES.md` showing only insertions in the [Unreleased] Added section, no deletions outside the insertion zone.
  </acceptance_criteria>

  <done>
  CHANGES.md `[Unreleased]` has a new `#### Phase 05.1 — CfD Scheme Page Retrofit` subsection with 5 Plan-cited bullets documenting what shipped (cfd.md + homepage grid), what was deleted (4 overlap theme pages), what was retargeted (inbound links + nav), and the SCHEMEPAGE-01 REQ-ID closure. All other CHANGES.md content unchanged.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add SCHEMEPAGE-01 to REQUIREMENTS.md + update Traceability + bump coverage counters</name>
  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - `.planning/REQUIREMENTS.md` lines 1-232 (the full file — read before editing; the structure is: "v1 Requirements" H2 with per-area H3 sections, "v2 Requirements" H2, "Out of Scope" H2, "Traceability" H2 with a big table, and a "Coverage" block at lines 225-228 giving `v1 requirements: 61 / Mapped to phases: 61 / Unmapped: 0`)
    - `.planning/phases/05.1-cfd-scheme-page/05.1-CONTEXT.md` lines 41-42 (the "Out of scope" bullet about REQ-ID bookkeeping — confirms planner's discretion to introduce SCHEMEPAGE-01)
  </read_first>

  <action>
  Three surgical edits to `.planning/REQUIREMENTS.md`. Preserve all other content byte-identically.

  **Edit 1 — Add a new `### Scheme Page Template` H3 section for SCHEMEPAGE-01.**

  Find a sensible section location. The Requirements "v1 Requirements" H2 has these H3 sections in order: Foundation / Test & Benchmark Scaffolding / Chart Triage & Theme Pages / Publishing Layer / Renewables Obligation (RO) Module / Feed-in Tariff (FiT) Module / Constraint Payments Module / Capacity Market (CM) Module / Balancing Services Module / Grid Socialisation Module / SEG + REGOs Completion / Cross-Scheme / Portal Integration / Governance & Adversarial-Proofing.

  **Insertion point:** Between `### Renewables Obligation (RO) Module` (ends at line 49) and `### Feed-in Tariff (FiT) Module` (starts at line 52). The new `### Scheme Page Template` section sits between the last scheme-module section that uses the template (RO) and the first scheme-module section that will inherit it (FiT onwards).

  Content to insert:

  ```markdown
  ### Scheme Page Template

  - [x] **SCHEMEPAGE-01**: Every scheme page mirrors `docs/schemes/ro.md` 8-section structure (headline + adversarial lead / what is the scheme / cost dynamics S2 / by technology S3 / concentration S4 / forward commitment S5 / methodology / data & code GOV-01 four-way coverage) + Headline FAQ section. Closed by Phase 05.1: both `docs/schemes/cfd.md` and `docs/schemes/ro.md` match the template. Phases 7-12 inherit as the acceptance criterion for each new scheme detail page.
  ```

  **Edit 2 — Add Traceability row + update Status.**

  Find the Traceability table (starts around line 157 with header `| Requirement | Phase | Status |`). Insert a new row for SCHEMEPAGE-01. Place it near the other Phase 5-era RO entries — immediately below the `| RO-06 | Phase 5 | Complete |` row is a sensible location, or alternatively at the end of the table. Prefer grouping by phase: insert BETWEEN `| RO-06 | Phase 5 | Complete |` and `| X-01 | Phase 6 | Pending |` rows.

  New row:

  ```markdown
  | SCHEMEPAGE-01 | Phase 05.1 | Complete |
  ```

  **Edit 3 — Update Coverage block.**

  At the bottom of the file, lines 225-228 read:

  ```markdown
  **Coverage:**
  - v1 requirements: 61
  - Mapped to phases: 61
  - Unmapped: 0
  ```

  Bump to:

  ```markdown
  **Coverage:**
  - v1 requirements: 62
  - Mapped to phases: 62
  - Unmapped: 0
  ```

  **What NOT to do:**
  - Do NOT add SCHEMEPAGE-01 to an existing `### <section>` and thereby break that section's cohesion. It gets its own H3 section because it is cross-cutting (applies to every scheme module).
  - Do NOT mark it `[ ]` unchecked. It is closed by Phase 05.1; both cfd.md and ro.md shipped the template. Future scheme pages will be audited against this REQ-ID but the REQ-ID itself is CLOSED.
  - Do NOT increment the counter by more than 1. Exactly one new REQ-ID.
  - Do NOT reorder any existing table rows. The table row for SCHEMEPAGE-01 can go at the end of the table or near Phase 5 rows — executor's call, document in SUMMARY.
  - Do NOT touch the "v2 Requirements" or "Out of Scope" sections — those are unrelated.
  </action>

  <verify>
    <automated>
    # New H3 section present with SCHEMEPAGE-01
    grep -q "^### Scheme Page Template$" .planning/REQUIREMENTS.md || { echo "Scheme Page Template H3 missing"; exit 1; }
    grep -q "^\- \[x\] \*\*SCHEMEPAGE-01\*\*" .planning/REQUIREMENTS.md || { echo "SCHEMEPAGE-01 bullet missing or unchecked"; exit 1; }

    # References both template files
    grep -q "docs/schemes/cfd.md" .planning/REQUIREMENTS.md || { echo "SCHEMEPAGE-01 prose missing cfd.md reference"; exit 1; }
    grep -q "docs/schemes/ro.md" .planning/REQUIREMENTS.md || { echo "SCHEMEPAGE-01 prose missing ro.md reference"; exit 1; }

    # Traceability row
    grep -qE "^\| SCHEMEPAGE-01 \| Phase 05\.1 \| Complete \|" .planning/REQUIREMENTS.md || { echo "Traceability row missing or malformed"; exit 1; }

    # Coverage counter bumped
    grep -q "^- v1 requirements: 62$" .planning/REQUIREMENTS.md || { echo "v1 requirements counter not 62"; exit 1; }
    grep -q "^- Mapped to phases: 62$" .planning/REQUIREMENTS.md || { echo "Mapped-to-phases counter not 62"; exit 1; }
    grep -q "^- Unmapped: 0$" .planning/REQUIREMENTS.md || { echo "Unmapped counter not 0"; exit 1; }

    # Algebraic invariant: count of REQ-IDs defined vs mapped in Traceability table
    # Each checklist entry of the form "- [ ] **XXX-NN**:" or "- [x] **XXX-NN**:" is one REQ-ID
    DEFINED=$(grep -cE "^- \[[ x]\] \*\*[A-Z]+-[0-9]+\*\*:" .planning/REQUIREMENTS.md)
    TRACED=$(awk '/^\| Requirement \| Phase \| Status \|$/,/^$/' .planning/REQUIREMENTS.md | grep -cE "^\| [A-Z]+-[0-9]+ ")
    [ "$DEFINED" = "$TRACED" ] || { echo "Defined REQ-IDs ($DEFINED) != Traceability rows ($TRACED); invariant broken"; exit 1; }
    [ "$DEFINED" = "62" ] || { echo "Expected 62 REQ-IDs, got $DEFINED"; exit 1; }

    echo "REQUIREMENTS.md SCHEMEPAGE-01 + Traceability + Coverage bump verified."
    </automated>
  </verify>

  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` gains a new `### Scheme Page Template` section with a single `[x]` checked `SCHEMEPAGE-01` bullet.
    - SCHEMEPAGE-01 bullet prose references both `docs/schemes/cfd.md` and `docs/schemes/ro.md`.
    - Traceability table gains a new row `| SCHEMEPAGE-01 | Phase 05.1 | Complete |`.
    - Coverage counters updated: `v1 requirements: 62`, `Mapped to phases: 62`, `Unmapped: 0`.
    - Algebraic invariant holds: count of `- [ ]` / `- [x]` REQ-ID checklist bullets equals count of Traceability-table rows equals 62.
    - No other sections of REQUIREMENTS.md modified (v2 Requirements, Out of Scope, all other sections byte-identical).
  </acceptance_criteria>

  <done>
  `.planning/REQUIREMENTS.md` has a new `SCHEMEPAGE-01` REQ-ID in its own `### Scheme Page Template` section, the Traceability table has a matching row, and the Coverage block counters are bumped to 62. Algebraic coverage invariant (REQ-ID count = Traceability row count) holds.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update ROADMAP.md Phase 05.1 — Requirements TBD → SCHEMEPAGE-01 + mark complete</name>
  <files>.planning/ROADMAP.md</files>

  <read_first>
    - `.planning/ROADMAP.md` lines 124-132 (the Phase 05.1 section — currently shows `Requirements: TBD (new SCHEMEPAGE-* IDs likely added during planning)` and `Plans: 0 plans` with a placeholder bullet)
    - `.planning/ROADMAP.md` lines 213-228 (the Progress Table — shows Phase 05.1 status)
  </read_first>

  <action>
  Three surgical edits to `.planning/ROADMAP.md`.

  **Edit 1 — Phase 05.1 Requirements line (currently line ~128):**

  Find:
  ```
  **Requirements**: TBD (new SCHEMEPAGE-* IDs likely added during planning)
  ```

  Replace with:
  ```
  **Requirements**: SCHEMEPAGE-01
  ```

  **Edit 2 — Phase 05.1 Plans line (currently line ~129):**

  Find:
  ```
  **Plans:** 0 plans
  ```

  Replace with:
  ```
  **Plans:** 4 plans
  ```

  **Edit 3 — Phase 05.1 Plans list (currently lines ~131-132):**

  Find:
  ```
  Plans:
  - [ ] TBD (run /gsd-plan-phase 05.1 to break down)
  ```

  Replace with:
  ```
  Plans:
  - [x] 05.1-01-PLAN.md — Create docs/schemes/cfd.md with migrated prose + 4 chart embeds + GOV-01 + Headline FAQ (SCHEMEPAGE-01)
  - [x] 05.1-02-PLAN.md — Delete 4 overlap theme pages + migrate inbound links + update mkdocs.yml nav + update schemes/index.md + retarget ro.md and rolling-minimum.md cross-links (SCHEMEPAGE-01)
  - [x] 05.1-03-PLAN.md — Pivot docs/index.md to hero + 2×4 scheme grid (SCHEMEPAGE-01)
  - [x] 05.1-04-PLAN.md — CHANGES.md [Unreleased] entry + REQUIREMENTS.md SCHEMEPAGE-01 + ROADMAP.md bookkeeping (SCHEMEPAGE-01)
  ```

  The `[x]` checkboxes are correct post-hoc: by the time Plan 04 commits, Plans 01-03 have already landed atomic commits on main. Plan 04 is the one still running; document it as `[x]` because its commit IS this one.

  **Edit 4 — Phase line in the top checklist (currently line 17 shows `- [ ] **Phase 5: RO Module**`; locate Phase 05.1 or insert if absent):**

  Phase 05.1 is an inserted phase — it may not appear in the top-level checklist at ROADMAP.md lines 10-21. If absent, add it between Phase 5 and Phase 6:

  ```
  - [x] **Phase 05.1: CfD Scheme Page Retrofit** — cfd.md scheme page mirroring ro.md; homepage pivoted to 2×4 scheme grid; 4 overlap theme pages removed
  ```

  If already present (unlikely), just flip `[ ]` → `[x]`.

  **Edit 5 — Progress Table (currently lines 213-228):**

  The Progress Table at ROADMAP.md lines 213-228 has 12 phase rows but may not list Phase 05.1 as its own row. If absent, add a row between `| 5. RO Module |` and `| 6. Flagship Cross-Scheme Charts |`:

  ```markdown
  | 05.1. CfD Scheme Page Retrofit | 4/4 | Complete | <today's date YYYY-MM-DD> |
  ```

  Use the actual commit date (the date Plan 04 commits). Read `date +%Y-%m-%d` for the current date.

  **What NOT to do:**
  - Do NOT modify any other Phase's entry (Phase 5's status, Phase 6's goal, etc.).
  - Do NOT remove the Phase 5 entry — Phase 5 is still in progress (only Plan 05-13 PEHR remains, per STATE.md). Phase 05.1's completion does not alter Phase 5's status.
  - Do NOT reorder phases. Phase 05.1 stays inserted between Phase 5 and Phase 6 as originally placed.
  </action>

  <verify>
    <automated>
    # Edit 1: Requirements line updated
    ! grep -q "Requirements\*\*: TBD" .planning/ROADMAP.md || { echo "Phase 05.1 still shows TBD requirements"; exit 1; }
    awk '/^### Phase 05\.1:/,/^### Phase 6:/' .planning/ROADMAP.md | grep -q "\*\*Requirements\*\*: SCHEMEPAGE-01" || { echo "Phase 05.1 Requirements line not SCHEMEPAGE-01"; exit 1; }

    # Edit 2: Plans count updated
    awk '/^### Phase 05\.1:/,/^### Phase 6:/' .planning/ROADMAP.md | grep -q "\*\*Plans:\*\* 4 plans" || { echo "Phase 05.1 plans count not 4"; exit 1; }

    # Edit 3: 4 plan entries present
    PLAN_COUNT=$(awk '/^### Phase 05\.1:/,/^### Phase 6:/' .planning/ROADMAP.md | grep -cE "05\.1-0[1-4]-PLAN\.md")
    [ "$PLAN_COUNT" -eq 4 ] || { echo "Expected 4 plan entries for Phase 05.1, got $PLAN_COUNT"; exit 1; }

    # All 4 plans are [x] checked
    CHECKED=$(awk '/^### Phase 05\.1:/,/^### Phase 6:/' .planning/ROADMAP.md | grep -cE "- \[x\] 05\.1-0[1-4]-PLAN\.md")
    [ "$CHECKED" -eq 4 ] || { echo "Expected 4 [x]-checked plans, got $CHECKED"; exit 1; }

    # Edit 4: top-level phase checklist (allow absence since it may not have existed pre-insert)
    # If Phase 05.1 is in the top-level list, it should be checked
    if grep -q "^- \[.\] \*\*Phase 05\.1:" .planning/ROADMAP.md; then
      grep -q "^- \[x\] \*\*Phase 05\.1:" .planning/ROADMAP.md || { echo "Phase 05.1 top-level checklist entry not checked"; exit 1; }
    fi

    # Edit 5: Progress table (allow absence; if present should show Complete)
    if awk '/^## Progress Table/,0' .planning/ROADMAP.md | grep -qE "^\| 05\.1\."; then
      awk '/^## Progress Table/,0' .planning/ROADMAP.md | grep -qE "^\| 05\.1\.[^|]+\| 4/4 \| Complete" || { echo "Phase 05.1 Progress Table row not Complete 4/4"; exit 1; }
    fi

    echo "ROADMAP.md Phase 05.1 updates verified."
    </automated>
  </verify>

  <acceptance_criteria>
    - Phase 05.1 section in ROADMAP.md shows `**Requirements**: SCHEMEPAGE-01` (not `TBD`).
    - Phase 05.1 section shows `**Plans:** 4 plans` (not `0 plans`).
    - Phase 05.1 plans list has exactly 4 entries: `05.1-01-PLAN.md`, `05.1-02-PLAN.md`, `05.1-03-PLAN.md`, `05.1-04-PLAN.md`, all `[x]` checked.
    - If the top-level phase checklist at ROADMAP.md lines 10-21 includes a Phase 05.1 entry, it is `[x]` checked.
    - If the Progress Table at ROADMAP.md lines 213-228 includes a Phase 05.1 row, it shows `| 4/4 | Complete | <YYYY-MM-DD> |`.
    - No other phase entries (Phase 5 status, Phase 6 goal, etc.) modified.
  </acceptance_criteria>

  <done>
  ROADMAP.md Phase 05.1 section shows `Requirements: SCHEMEPAGE-01`, `Plans: 4 plans`, and a 4-item `[x]`-checked plans list. Top-level checklist + Progress Table entries (if present) reflect Phase 05.1 as Complete. All other phase entries untouched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Atomic commit — docs(05.1-04): CHANGES.md + REQUIREMENTS.md SCHEMEPAGE-01 + ROADMAP.md bookkeeping</name>
  <files>
    CHANGES.md,
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md
  </files>

  <read_first>
    - `git status --porcelain` to confirm the 3 modified files (CHANGES.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md) are the only changes staged.
    - `git log --oneline -5` — commit-message cadence.
  </read_first>

  <action>
  Single atomic commit for the Phase 05.1 bookkeeping trio.

  ```bash
  git add CHANGES.md .planning/REQUIREMENTS.md .planning/ROADMAP.md
  # Verify nothing else is staged
  STAGED=$(git diff --cached --name-only | sort)
  EXPECTED=$'CHANGES.md\n.planning/REQUIREMENTS.md\n.planning/ROADMAP.md'
  [ "$STAGED" = "$EXPECTED" ] || { echo "Unexpected staged files:"; echo "$STAGED"; exit 1; }

  git commit -m "$(cat <<'EOF'
  docs(05.1-04): CHANGES + REQUIREMENTS SCHEMEPAGE-01 + ROADMAP close

  Ships the Phase 05.1 bookkeeping trio as a single atomic commit:

  - CHANGES.md [Unreleased] ### Added gains a new #### Phase 05.1 —
    CfD Scheme Page Retrofit subsection (above #### Phase 5, newest-
    first) with 5 Plan-cited bullets: cfd.md shipped / 4 overlap theme
    pages deleted + inbound links migrated / mkdocs.yml nav updated /
    homepage pivoted to 2×4 scheme grid / SCHEMEPAGE-01 REQ-ID closed.

  - .planning/REQUIREMENTS.md gains a new ### Scheme Page Template
    section formalising SCHEMEPAGE-01: "Every scheme page mirrors
    docs/schemes/ro.md 8-section structure + 4-chart embeds + GOV-01
    four-way coverage manifest + Headline FAQ". Closed by Phase 05.1
    via both cfd.md and ro.md matching the template. Phases 7-12
    inherit as the acceptance criterion for each new scheme page.
    Traceability table gains a | SCHEMEPAGE-01 | Phase 05.1 | Complete |
    row; Coverage counters bumped 61 → 62 with zero unmapped.

  - .planning/ROADMAP.md Phase 05.1 section: `Requirements: TBD` →
    `Requirements: SCHEMEPAGE-01`; `Plans: 0 plans` → `Plans: 4 plans`
    with all 4 plan entries [x]-checked; top-level phase checklist and
    Progress Table updated where Phase 05.1 rows exist.

  uv run mkdocs build --strict still exits 0 (regression guard —
  CHANGES.md and .planning/ are not served by mkdocs, but the gate runs
  anyway for paranoia).

  Plan 04 of 4 in Phase 05.1. This commit closes the phase: cfd.md
  shipped, homepage pivoted, 4 overlap pages removed, SCHEMEPAGE-01
  formalised, changelog + requirements + roadmap consistent.
  EOF
  )"
  ```

  **What NOT to do:**
  - Do NOT combine with a revert, rebase, or amend operation.
  - Do NOT stage files not in the expected 3-file set. If git status shows other modifications, investigate before committing (likely residue from an earlier partial Plan 01/02/03 run).
  - Do NOT skip hooks.
  </action>

  <verify>
    <automated>
    # Commit landed
    git log -1 --format='%s' | grep -q "docs(05.1-04):" || { echo "Plan 04 commit message missing"; exit 1; }

    # Exactly 3 files in the commit
    CHANGED=$(git show --stat HEAD --format='' | grep -c '|')
    [ "$CHANGED" -eq 3 ] || { echo "Plan 04 commit touches $CHANGED files, expected 3"; exit 1; }

    # Clean tree
    git diff --quiet HEAD || { echo "Uncommitted changes remain"; exit 1; }

    # Post-commit mkdocs --strict still green (regression guard)
    uv run mkdocs build --strict > /tmp/mkdocs_plan04_postcommit.log 2>&1 || { cat /tmp/mkdocs_plan04_postcommit.log; echo "post-commit strict build failed"; exit 1; }
    ! grep -E "^WARNING" /tmp/mkdocs_plan04_postcommit.log || { cat /tmp/mkdocs_plan04_postcommit.log; echo "post-commit strict build warnings"; exit 1; }

    # Phase 3 structural tests still pass
    uv run pytest tests/test_docs_structure.py -q > /tmp/pytest_plan04.log 2>&1 || { cat /tmp/pytest_plan04.log; echo "test_docs_structure.py failed"; exit 1; }

    # Phase 05.1 closure invariants
    git log --oneline | head -4 | grep -c "docs(05.1-0" | grep -q "^4$" || { echo "Expected 4 commits with docs(05.1-0*) prefix in recent history"; exit 1; }

    echo "Plan 04 commit + full regression gate verified. Phase 05.1 complete."
    </automated>
  </verify>

  <acceptance_criteria>
    - `git log -1 --format='%s'` starts with `docs(05.1-04):` and mentions CHANGES + REQUIREMENTS + ROADMAP.
    - `git show --stat HEAD` lists exactly 3 files: `CHANGES.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`.
    - `git status --porcelain` returns empty.
    - `uv run mkdocs build --strict` exits 0 with zero `WARNING` lines on post-commit HEAD.
    - `uv run pytest tests/test_docs_structure.py -q` passes.
    - `git log --oneline | head -4` shows 4 Phase 05.1 commits in sequence: `docs(05.1-04): ...` (HEAD), `docs(05.1-03): ...`, `docs(05.1-02): ...`, `docs(05.1-01): ...`.
  </acceptance_criteria>

  <done>
  Fourth and final atomic commit of Phase 05.1 lands. The 4-commit sequence on main is complete: Plan 01 added `cfd.md`; Plan 02 deleted the 4 overlap theme pages and migrated inbound links; Plan 03 pivoted the homepage; Plan 04 shipped the bookkeeping. `mkdocs build --strict` green at every intermediate HEAD. SCHEMEPAGE-01 formalised. Phase 05.1 closed.
  </done>
</task>

</tasks>

<verification>
Phase-level exit gates:

```bash
# 1. Permanent CI gate
uv run mkdocs build --strict

# 2. Phase 3 structural invariants
uv run pytest tests/test_docs_structure.py -q

# 3. 4-commit sequence on main
git log --oneline | head -4
# Expect:
#   <hash> docs(05.1-04): ...
#   <hash> docs(05.1-03): ...
#   <hash> docs(05.1-02): ...
#   <hash> docs(05.1-01): ...

# 4. Phase 05.1 artefacts present
test -f docs/schemes/cfd.md && echo "cfd.md ✓"
test ! -f docs/themes/cost/cfd-dynamics.md && echo "cfd-dynamics.md gone ✓"
test ! -f docs/themes/cost/remaining-obligations.md && echo "remaining-obligations.md gone ✓"
test ! -f docs/themes/recipients/cfd-payments-by-category.md && echo "cfd-payments-by-category.md gone ✓"
test ! -f docs/themes/recipients/lorenz.md && echo "lorenz.md gone ✓"

# 5. Requirements + Roadmap + CHANGES consistency
grep -q "SCHEMEPAGE-01" .planning/REQUIREMENTS.md && echo "REQUIREMENTS.md ✓"
grep -q "SCHEMEPAGE-01" .planning/ROADMAP.md && echo "ROADMAP.md ✓"
grep -q "Phase 05.1 — CfD Scheme Page Retrofit" CHANGES.md && echo "CHANGES.md ✓"

# 6. No stale inbound links in entire docs tree + mkdocs.yml
! grep -rE "themes/cost/cfd-dynamics\.md|themes/cost/remaining-obligations\.md|themes/recipients/cfd-payments-by-category\.md|themes/recipients/lorenz\.md" docs/ mkdocs.yml && echo "No stale inbound links ✓"
```

If all six checks pass, Phase 05.1 is verifiably complete.
</verification>

<success_criteria>
- CHANGES.md `[Unreleased] ### Added` has a new `#### Phase 05.1 — CfD Scheme Page Retrofit` subsection with 5 Plan-cited bullets (one per Plan 01-04 plus SCHEMEPAGE-01 closure bullet).
- `.planning/REQUIREMENTS.md` has a new `### Scheme Page Template` section with SCHEMEPAGE-01 closed `[x]`; Traceability row present; Coverage counters 61 → 62.
- `.planning/ROADMAP.md` Phase 05.1 section: Requirements line shows SCHEMEPAGE-01; Plans: 4 plans; all 4 plan entries `[x]`-checked.
- Single atomic commit: `docs(05.1-04): CHANGES + REQUIREMENTS SCHEMEPAGE-01 + ROADMAP close`.
- Post-commit `mkdocs build --strict` green.
- `test_docs_structure.py` passes.
- Phase 05.1 is verifiably complete: cfd.md shipped, homepage pivoted, 4 overlap pages deleted, SCHEMEPAGE-01 formalised, all bookkeeping consistent.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-cfd-scheme-page/05.1-04-SUMMARY.md` capturing:
- Confirmation that the 4-commit atomic sequence is linear on main (git log --oneline | head -4)
- Exact Traceability-table insertion point for SCHEMEPAGE-01 (which line number, which neighbour rows)
- Coverage-counter before/after (61 → 62)
- Algebraic-invariant check result (count of REQ-ID bullets = count of Traceability rows = 62)
- CHANGES.md entry placement confirmation (above Phase 5 subsection inside ### Added)
- ROADMAP.md Phase 05.1 section final state
- Phase 05.1 closure statement: "cfd.md shipped, homepage pivoted, 4 overlap pages deleted, SCHEMEPAGE-01 closed — ready for Phase 6 PORTAL-01"
</output>
