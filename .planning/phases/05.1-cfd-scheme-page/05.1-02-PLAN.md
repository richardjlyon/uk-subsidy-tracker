---
phase: 05.1-cfd-scheme-page
plan: 02
type: execute
wave: 2
depends_on:
  - "05.1-01"
files_modified:
  - docs/themes/cost/cfd-dynamics.md
  - docs/themes/cost/remaining-obligations.md
  - docs/themes/recipients/cfd-payments-by-category.md
  - docs/themes/recipients/lorenz.md
  - docs/themes/cost/index.md
  - docs/themes/recipients/index.md
  - docs/themes/reliability/rolling-minimum.md
  - docs/schemes/ro.md
  - docs/schemes/index.md
  - mkdocs.yml
autonomous: true
requirements:
  - SCHEMEPAGE-01
tags:
  - docs
  - link-migration
  - deletions
  - mkdocs-nav

must_haves:
  truths:
    - "The four overlap CfD per-chart theme pages no longer exist on disk (D-06: prose already migrated to cfd.md by Plan 01; this phase deletes the now-redundant sources)."
    - "Every inbound internal link that previously pointed at the four deleted pages now resolves to a `schemes/cfd.md#<anchor>` target or has been retargeted elsewhere (D-07)."
    - "The mkdocs.yml nav reflects the new state: schemes/cfd.md added under Schemes; four deleted theme-page entries removed from Cost + Recipients tabs."
    - "docs/schemes/index.md lists CfD as published alongside RO (D-Claude's-Discretion: alphabetical CfD-then-RO ordering)."
    - "docs/schemes/ro.md line 83 cross-link to the (now-deleted) CfD Lorenz theme page is retargeted to schemes/cfd.md#concentration-chart-s4 for cross-scheme reciprocity."
    - "docs/themes/reliability/rolling-minimum.md line 153 cross-link to the (now-deleted) remaining-obligations.md is retargeted to schemes/cfd.md#forward-commitment-chart-s5."
    - "The pre-existing RO concentration card at docs/themes/recipients/index.md lines 27-29 (which already points at schemes/ro.md#concentration-chart-s4) survives this edit — it must NOT be accidentally retargeted or removed during the CfD-card-retargeting sweep."
    - "`uv run mkdocs build --strict` exits 0 (the permanent Phase 3 CI gate — this is the load-bearing invariant for Plan 02)."
    - "The six non-overlap CfD theme pages (themes/cost/cfd-vs-gas-cost.md, themes/efficiency/subsidy-per-avoided-co2-tonne.md, themes/cannibalisation/capture-ratio.md, themes/reliability/{capacity-factor-seasonal,generation-heatmap,rolling-minimum}.md) remain unmodified in their Phase 3 D-01 six-section form per D-08 (only rolling-minimum.md gets a surgical line-153 link edit, not a structural rewrite)."
  artifacts:
    - path: "mkdocs.yml"
      provides: "Updated nav — Schemes tab lists Overview + CfD + RO (alphabetical); Cost and Recipients tabs no longer list the 4 deleted pages."
      contains: "schemes/cfd.md"
    - path: "docs/themes/cost/index.md"
      provides: "CfD-card links retargeted from ./cfd-dynamics.md etc. to ../../schemes/cfd.md#<anchor>"
      contains: "../../schemes/cfd.md#cost-dynamics-chart-s2"
    - path: "docs/themes/recipients/index.md"
      provides: "CfD-card links retargeted from ./lorenz.md + ./cfd-payments-by-category.md to scheme-page anchors; pre-existing RO concentration card (lines 27-29 pointing at schemes/ro.md#concentration-chart-s4) survives the edit."
      contains: "../../schemes/cfd.md#concentration-chart-s4"
    - path: "docs/schemes/index.md"
      provides: "CfD promoted from Coming-soon to Currently-published list alongside RO"
      contains: "[**Contracts for Difference (CfD)**](cfd.md)"
  key_links:
    - from: "docs/themes/cost/index.md"
      to: "docs/schemes/cfd.md#cost-dynamics-chart-s2"
      via: "grid-card link target"
      pattern: "schemes/cfd\\.md#cost-dynamics-chart-s2"
    - from: "docs/themes/cost/index.md"
      to: "docs/schemes/cfd.md#forward-commitment-chart-s5"
      via: "grid-card link target"
      pattern: "schemes/cfd\\.md#forward-commitment-chart-s5"
    - from: "docs/themes/cost/index.md"
      to: "docs/schemes/cfd.md#by-technology-chart-s3"
      via: "grid-card link target (cross-theme CfD-payments-by-category card)"
      pattern: "schemes/cfd\\.md#by-technology-chart-s3"
    - from: "docs/themes/recipients/index.md"
      to: "docs/schemes/cfd.md#concentration-chart-s4"
      via: "Lorenz-card link target"
      pattern: "schemes/cfd\\.md#concentration-chart-s4"
    - from: "docs/themes/recipients/index.md"
      to: "docs/schemes/cfd.md#by-technology-chart-s3"
      via: "Payments-by-technology-card link target"
      pattern: "schemes/cfd\\.md#by-technology-chart-s3"
    - from: "docs/themes/recipients/index.md"
      to: "docs/schemes/ro.md#concentration-chart-s4"
      via: "pre-existing RO concentration card — must survive the edit"
      pattern: "schemes/ro\\.md#concentration-chart-s4"
    - from: "docs/themes/reliability/rolling-minimum.md"
      to: "docs/schemes/cfd.md#forward-commitment-chart-s5"
      via: "See-also bullet (line 153)"
      pattern: "schemes/cfd\\.md#forward-commitment-chart-s5"
    - from: "docs/schemes/ro.md"
      to: "docs/schemes/cfd.md#concentration-chart-s4"
      via: "§5 Concentration prose cross-scheme reciprocal (line 83)"
      pattern: "cfd\\.md#concentration-chart-s4"
---

<objective>
Migrate every inbound internal link that points at the four doomed CfD per-chart theme pages — then delete the pages. Update `mkdocs.yml` nav (add Schemes → CfD, remove the 4 Cost + Recipients tab entries), `docs/schemes/index.md` (CfD moves from Coming-soon to Currently-published), `docs/schemes/ro.md:83` (reciprocal cross-link updated), and `docs/themes/reliability/rolling-minimum.md:153` (See-also cross-link updated). The single atomic commit that lands this plan must leave `mkdocs build --strict` green — because Phase 3's permanent CI gate is THE inbound-link validator per D-09.

Purpose: Honour D-06 (single GOV-01 unit per chart — overlap pages deleted now that cfd.md absorbs their content) and D-07 (theme-gallery cards link into scheme-page anchors). Restore the 1:1 mapping between regulator-source chart and the scheme-detail page that documents it.

Output: 4 deletions + 6 file modifications, landed in one atomic commit. `mkdocs build --strict` exits 0 on the post-commit tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-cfd-scheme-page/05.1-CONTEXT.md
@.planning/phases/05.1-cfd-scheme-page/05.1-PATTERNS.md
@.planning/phases/05.1-cfd-scheme-page/05.1-01-PLAN.md
@CLAUDE.md
@docs/schemes/cfd.md
@docs/schemes/ro.md
@docs/schemes/index.md
@docs/themes/cost/index.md
@docs/themes/recipients/index.md
@docs/themes/reliability/rolling-minimum.md
@mkdocs.yml
</context>

<threat_model>
## Trust Boundaries

Docs-only phase. No new executable code, no data ingress, no user-input surfaces. Only trust boundary: the mkdocs content pipeline (Markdown → strict build → GitHub Pages).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-02-01 | Denial of Service | site navigation | mitigate | Broken internal links would surface as 404s on the published site. `uv run mkdocs build --strict` (promoted-to-error validation at `mkdocs.yml:43-53`) is the gate; verify block runs it on the post-deletion tree. If any `WARNING` appears, the commit is rejected. |
| T-05.1-02-02 | Information Disclosure | external-bookmark drift | accept | Readers who bookmarked `themes/cost/cfd-dynamics.md` externally will 404 after deletion. Project is pre-public; 05.1 accepts per D-09. Cloudflare Pages redirect rules remain a future option if external referrer data warrants. |

No new attack surface introduced.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Retarget inbound links + update schemes/index.md + update ro.md reciprocal + update rolling-minimum.md See-also</name>
  <files>
    docs/themes/cost/index.md,
    docs/themes/recipients/index.md,
    docs/themes/reliability/rolling-minimum.md,
    docs/schemes/ro.md,
    docs/schemes/index.md
  </files>

  <read_first>
    - `docs/themes/cost/index.md` (the file being edited — lines 11-41 contain the 3 CfD cards to retarget, lines 43-57 are the existing RO card pattern that is the exemplar)
    - `docs/themes/recipients/index.md` (lines 11-25 contain the 2 CfD cards to retarget; lines 27-29 contain the PRE-EXISTING RO concentration card pointing at `schemes/ro.md#concentration-chart-s4` — this card MUST survive the edit untouched)
    - `docs/themes/reliability/rolling-minimum.md` (line 153 has the single See-also link to retarget)
    - `docs/schemes/ro.md` (line 83 has the reciprocal cross-link to retarget from `../themes/recipients/lorenz.md` to `cfd.md#concentration-chart-s4`)
    - `docs/schemes/index.md` (lines 7-15 — "Currently published" / "Coming soon" lists; CfD moves from Coming-soon to Currently-published)
    - `docs/schemes/cfd.md` (Plan 01 output — verify the anchor slugs resolve before retargeting links into them)
    - `.planning/phases/05.1-cfd-scheme-page/05.1-PATTERNS.md` §§ "docs/themes/cost/index.md (theme-gallery)" and "docs/themes/recipients/index.md (theme-gallery)" and "docs/themes/reliability/rolling-minimum.md" and "docs/schemes/ro.md (cross-link update)" and "docs/schemes/index.md (section landing page)" — each has a before/after table with line-exact edits
  </read_first>

  <action>
  Execute six surgical edits, preserving all other prose in each file. Do NOT reflow or restructure — this task is URL retargeting and list-item juggling only.

  **Edit 1 — `docs/themes/cost/index.md` lines 11-17 (CfD dynamics card):**

  Find (both occurrences — image link AND bold-title link):
  ```
  [![CfD dynamics preview](../../charts/html/subsidy_cfd_dynamics_twitter.png)](./cfd-dynamics.md)
  ```
  and
  ```
  __[CfD dynamics — the full mechanism in four panels](./cfd-dynamics.md)__
  ```

  Replace `./cfd-dynamics.md` with `../../schemes/cfd.md#cost-dynamics-chart-s2` in both occurrences. Card-body paragraph (line 17) unchanged.

  **Edit 2 — `docs/themes/cost/index.md` lines 27-33 (Remaining obligations card):**

  Find both occurrences of `./remaining-obligations.md` and replace with `../../schemes/cfd.md#forward-commitment-chart-s5`. Card-body paragraph (line 33) unchanged.

  **Edit 3 — `docs/themes/cost/index.md` lines 35-41 (CfD Payments by technology cross-theme card):**

  Find both occurrences of `../recipients/cfd-payments-by-category.md` and replace with `../../schemes/cfd.md#by-technology-chart-s3`. Card-body paragraph (line 41) unchanged.

  **Edit 4 — `docs/themes/recipients/index.md` lines 11-17 (Lorenz curve card):**

  Find both occurrences of `./lorenz.md` and replace with `../../schemes/cfd.md#concentration-chart-s4`. Card-body paragraph (line 17) unchanged.

  **DO NOT TOUCH lines 27-29** — the pre-existing RO concentration card already points at `../../schemes/ro.md#concentration-chart-s4` (RO scheme page, not CfD). A scatter-shot `sed` that matches `concentration-chart-s4` without anchoring on `cfd.md` could silently clobber this card. The verify block asserts the RO card's URL survives the edit.

  **Edit 5 — `docs/themes/recipients/index.md` lines 19-25 (Payments by technology card):**

  Find both occurrences of `./cfd-payments-by-category.md` and replace with `../../schemes/cfd.md#by-technology-chart-s3`. Card-body paragraph (line 25) unchanged.

  **Edit 6 — `docs/themes/reliability/rolling-minimum.md` line 153 (See-also bullet):**

  Find exactly:
  ```
  - [Cost / remaining obligations](../cost/remaining-obligations.md) — the forward commitment these reliability caveats apply to.
  ```

  Replace with:
  ```
  - [CfD forward commitment](../../schemes/cfd.md#forward-commitment-chart-s5) — the forward commitment these reliability caveats apply to.
  ```

  **Edit 7 — `docs/schemes/ro.md` line 83 (reciprocal cross-link):**

  Find exactly (a single-line paragraph — the `[CfD Recipients page](../themes/recipients/lorenz.md)` substring is what we target):
  ```
  The Lorenz curve plotted here is the same inequality diagnostic used on the [CfD Recipients page](../themes/recipients/lorenz.md), applied to lifetime per-station RO cost.
  ```

  Replace the substring `[CfD Recipients page](../themes/recipients/lorenz.md)` with `[CfD Concentration chart](cfd.md#concentration-chart-s4)`. Full paragraph now reads:
  ```
  The Lorenz curve plotted here is the same inequality diagnostic used on the [CfD Concentration chart](cfd.md#concentration-chart-s4), applied to lifetime per-station RO cost.
  ```

  All other prose on ro.md:83 preserved verbatim (the full paragraph is 4+ sentences long; only the link-substring changes).

  **Edit 8 — `docs/schemes/index.md` lines 7-15 (Currently-published / Coming-soon lists):**

  Find the "Currently published:" block at line 7-9:
  ```
  **Currently published:**

  - [**Renewables Obligation (RO)**](ro.md) — £67 bn all-in GB total since 2002; 2002-2037 span; the largest legacy renewables subsidy in the UK.
  ```

  Replace with (alphabetical: CfD before RO per D-Claude's-Discretion):
  ```
  **Currently published:**

  - [**Contracts for Difference (CfD)**](cfd.md) — £29bn since 2015; £14bn premium over gas counterfactual; 15-year strike-price commitments running to the late 2030s (plus Hinkley Point C at 35 years).
  - [**Renewables Obligation (RO)**](ro.md) — £67 bn all-in GB total since 2002; 2002-2037 span; the largest legacy renewables subsidy in the UK.
  ```

  Find the "Coming soon:" block at lines 11-15:
  ```
  **Coming soon:**

  - Contracts for Difference (CfD) — currently documented via the [Cost theme](../themes/cost/index.md) chart gallery; a dedicated scheme page will land in a future release.
  - Feed-in Tariff (FiT) — Phase 7.
  - Constraint Payments, Capacity Market, Balancing Services, Grid Socialisation, Smart Export Guarantee — Phases 8-12.
  ```

  Replace with (drop the CfD bullet; keep the rest verbatim):
  ```
  **Coming soon:**

  - Feed-in Tariff (FiT) — Phase 7.
  - Constraint Payments, Capacity Market, Balancing Services, Grid Socialisation, Smart Export Guarantee — Phases 8-12.
  ```

  The "How to read a scheme page" block at lines 17-23 is unchanged; the explainer is already template-agnostic.

  **What NOT to do:**
  - Do NOT restructure or re-word any card-body paragraphs. This is a URL-retargeting + list-item editing task, NOT a copy-editing task.
  - Do NOT touch the pre-existing RO concentration card at `docs/themes/recipients/index.md:27-29`. It already points at `schemes/ro.md#concentration-chart-s4` and must stay that way (verified in the verify block).
  - Do NOT touch the 6 non-overlap CfD theme pages (`themes/cost/cfd-vs-gas-cost.md`, `themes/efficiency/subsidy-per-avoided-co2-tonne.md`, `themes/cannibalisation/capture-ratio.md`, `themes/reliability/{capacity-factor-seasonal,generation-heatmap}.md`) per D-08 — they stay in Phase 3 D-01 six-section form. Only `rolling-minimum.md` gets a single-line edit.
  - Do NOT update the "Module in focus" section of `docs/index.md` here — that migration is Plan 03's atom.
  - Do NOT run `mkdocs build --strict` yet — it will fail because the 4 doomed files still exist and still have See-also links pointing into the deletion set internally; the strict-build gate lives on Task 3 after deletions + nav edits.
  - Do NOT use `sed -i` without backup — the MkDocs slug patterns contain special characters (`#`, `(`, `)`). Prefer `python -c` or the `Edit` tool for surgical replacements; if using sed, use explicit `-i ''` (macOS BSD sed) with escape discipline.
  </action>

  <verify>
    <automated>
    # Every expected retargeted URL is now present in the 5 modified files
    grep -q "schemes/cfd.md#cost-dynamics-chart-s2" docs/themes/cost/index.md || { echo "cost/index.md missing S2 retarget"; exit 1; }
    grep -q "schemes/cfd.md#forward-commitment-chart-s5" docs/themes/cost/index.md || { echo "cost/index.md missing S5 retarget"; exit 1; }
    grep -q "schemes/cfd.md#by-technology-chart-s3" docs/themes/cost/index.md || { echo "cost/index.md missing S3 cross-theme retarget"; exit 1; }
    grep -q "schemes/cfd.md#concentration-chart-s4" docs/themes/recipients/index.md || { echo "recipients/index.md missing S4 retarget"; exit 1; }
    grep -q "schemes/cfd.md#by-technology-chart-s3" docs/themes/recipients/index.md || { echo "recipients/index.md missing S3 retarget"; exit 1; }
    grep -q "schemes/cfd.md#forward-commitment-chart-s5" docs/themes/reliability/rolling-minimum.md || { echo "rolling-minimum.md:153 not retargeted"; exit 1; }
    grep -q "\[CfD Concentration chart\](cfd.md#concentration-chart-s4)" docs/schemes/ro.md || { echo "ro.md:83 reciprocal link not retargeted"; exit 1; }
    grep -q "\[\*\*Contracts for Difference (CfD)\*\*\](cfd.md)" docs/schemes/index.md || { echo "schemes/index.md Currently-published CfD entry missing"; exit 1; }

    # Warning 8 defence — PRE-EXISTING RO concentration card survives the edit.
    # This card lives at docs/themes/recipients/index.md lines 27-29 and points at schemes/ro.md (not cfd.md).
    # A scatter-shot sed on "concentration-chart-s4" without anchoring could clobber it.
    grep -q "schemes/ro.md#concentration-chart-s4" docs/themes/recipients/index.md || {
      echo "REGRESSION: pre-existing RO concentration card at recipients/index.md:27-29 lost its schemes/ro.md#concentration-chart-s4 target"
      exit 1
    }

    # No remaining inbound links to the doomed files in the 5 modified files
    ! grep -E "(themes/cost/cfd-dynamics\.md|themes/cost/remaining-obligations\.md|themes/recipients/cfd-payments-by-category\.md|themes/recipients/lorenz\.md)" docs/themes/cost/index.md docs/themes/recipients/index.md docs/themes/reliability/rolling-minimum.md docs/schemes/ro.md docs/schemes/index.md || { echo "Stale link to a doomed file still present in one of the edited files"; exit 1; }

    # Coming-soon list no longer has CfD
    ! grep -E "Contracts for Difference \(CfD\) — currently documented" docs/schemes/index.md || { echo "CfD still in Coming-soon"; exit 1; }

    echo "Task 1 edits verified."
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "schemes/cfd.md#" docs/themes/cost/index.md` returns at least 6 (3 cards × 2 link-occurrences each: image link + bold-title link).
    - `grep -c "schemes/cfd.md#" docs/themes/recipients/index.md` returns at least 4 (2 cards × 2 occurrences each).
    - `grep -q "schemes/ro.md#concentration-chart-s4" docs/themes/recipients/index.md` returns 0 (pre-existing RO card survives; Warning 8 defence).
    - `grep -c "schemes/cfd.md#forward-commitment-chart-s5" docs/themes/reliability/rolling-minimum.md` returns exactly 1.
    - `grep -c "cfd.md#concentration-chart-s4" docs/schemes/ro.md` returns exactly 1 (the reciprocal cross-link at line 83).
    - `grep -c "\[\*\*Contracts for Difference (CfD)\*\*\](cfd.md)" docs/schemes/index.md` returns exactly 1.
    - `grep -E "(themes/cost/cfd-dynamics\.md|themes/cost/remaining-obligations\.md|themes/recipients/cfd-payments-by-category\.md|themes/recipients/lorenz\.md)" docs/themes/cost/index.md docs/themes/recipients/index.md docs/themes/reliability/rolling-minimum.md docs/schemes/ro.md docs/schemes/index.md` returns empty (zero stale inbound links in the edited files).
    - No unrelated prose is modified. `git diff` shows only URL substitutions and 2 list-item changes in `docs/schemes/index.md`.
  </acceptance_criteria>

  <done>
  Six surgical edits landed across 5 files. Every inbound internal link in the repo that previously pointed at one of the 4 doomed files — including the two outside-the-gallery cases (`ro.md:83` reciprocal + `rolling-minimum.md:153` See-also) — now resolves to a `schemes/cfd.md#<anchor>` target. The pre-existing RO concentration card at `recipients/index.md:27-29` survives the edit untouched. `docs/schemes/index.md` lists CfD as published.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete 4 overlap theme pages + update mkdocs.yml nav</name>
  <files>
    docs/themes/cost/cfd-dynamics.md,
    docs/themes/cost/remaining-obligations.md,
    docs/themes/recipients/cfd-payments-by-category.md,
    docs/themes/recipients/lorenz.md,
    mkdocs.yml
  </files>

  <read_first>
    - `mkdocs.yml` lines 55-92 (the `nav:` block — read in full to understand the existing Schemes / Cost / Recipients / etc. structure before editing)
    - `.planning/phases/05.1-cfd-scheme-page/05.1-PATTERNS.md` §§ "mkdocs.yml (site configuration)" — contains the before/after diff for the nav block edits
    - Verify Task 1 has landed: `grep -q "schemes/cfd.md#cost-dynamics-chart-s2" docs/themes/cost/index.md` before running any `git rm`.
  </read_first>

  <action>
  Phase-1 D-16 atomicity: Task 1 retargeted all inbound links; Task 2 now deletes the (link-free) pages and updates the nav — then Task 3 commits the full set as one atomic commit so `mkdocs --strict` never fails mid-sequence.

  **Step 1 — Delete the 4 overlap theme pages via `git rm`:**

  ```bash
  git rm docs/themes/cost/cfd-dynamics.md
  git rm docs/themes/cost/remaining-obligations.md
  git rm docs/themes/recipients/cfd-payments-by-category.md
  git rm docs/themes/recipients/lorenz.md
  ```

  Prose from each of these files already migrated into `docs/schemes/cfd.md` §§3/6/4/5 respectively in Plan 01. No content is lost — the chart-page context is preserved in the scheme-page sections.

  **Step 2 — Edit `mkdocs.yml` nav (add CfD under Schemes; remove 4 entries under Cost + Recipients):**

  Read the current nav block at lines 55-92. Find the Cost block (lines 58-63):

  ```yaml
    - Cost:
        - themes/cost/index.md
        - CfD dynamics: themes/cost/cfd-dynamics.md
        - CfD vs gas cost: themes/cost/cfd-vs-gas-cost.md
        - Remaining obligations: themes/cost/remaining-obligations.md
        - Methodology: themes/cost/methodology.md
  ```

  Replace with (remove the two doomed entries):

  ```yaml
    - Cost:
        - themes/cost/index.md
        - CfD vs gas cost: themes/cost/cfd-vs-gas-cost.md
        - Methodology: themes/cost/methodology.md
  ```

  Find the Recipients block (lines 64-68):

  ```yaml
    - Recipients:
        - themes/recipients/index.md
        - Lorenz curve: themes/recipients/lorenz.md
        - Payments by technology: themes/recipients/cfd-payments-by-category.md
        - Methodology: themes/recipients/methodology.md
  ```

  Replace with:

  ```yaml
    - Recipients:
        - themes/recipients/index.md
        - Methodology: themes/recipients/methodology.md
  ```

  Find the Schemes block (lines 83-85):

  ```yaml
    - Schemes:
        - Overview: schemes/index.md
        - Renewables Obligation (RO): schemes/ro.md
  ```

  Replace with (insert CfD between Overview and RO — alphabetical per D-Claude's-Discretion default):

  ```yaml
    - Schemes:
        - Overview: schemes/index.md
        - Contracts for Difference (CfD): schemes/cfd.md
        - Renewables Obligation (RO): schemes/ro.md
  ```

  All other `nav:` blocks (Efficiency, Cannibalisation, Reliability, Data, Methodology, About) are unchanged.

  **Step 3 — Run mkdocs strict build to validate the post-edit tree:**

  ```bash
  uv run mkdocs build --strict 2>&1 | tee /tmp/mkdocs_plan02_task2.log
  ```

  Any `WARNING` → fix before proceeding. The Phase 3 CI gate (`mkdocs.yml:43-53`) escalates every warn to error under `--strict`. Expected warnings to watch for (all should be absent after the edits):
  - `Doc file 'schemes/cfd.md' contains a link to '...' which is not found` → means cfd.md §2 or §5 still references one of the deleted pages (check `grep -E "themes/cost/cfd-dynamics|..." docs/schemes/cfd.md` — must return empty after Plan 01).
  - `Doc file 'themes/cost/index.md' contains a link to '...' which is not found` → means Task 1 retargeting missed a card.
  - `The following pages exist in the docs directory, but are not included in the "nav" configuration` → means `omitted_files: warn` fired. Should not happen because the 4 deleted files are gone.

  **What NOT to do:**
  - Do NOT `rm` the files via shell `rm` — use `git rm` so the deletions are staged for commit. Shell `rm` leaves the git index out of sync and requires a separate `git add -u` that's easy to forget.
  - Do NOT delete or rename any of the 6 non-overlap CfD theme pages. Run `ls docs/themes/cost/cfd-vs-gas-cost.md docs/themes/efficiency/subsidy-per-avoided-co2-tonne.md docs/themes/cannibalisation/capture-ratio.md docs/themes/reliability/capacity-factor-seasonal.md docs/themes/reliability/generation-heatmap.md docs/themes/reliability/rolling-minimum.md` — all six must still be present after Task 2.
  - Do NOT reorder the Schemes nav entries to put RO before CfD — alphabetical CfD-then-RO is the default per D-Claude's-Discretion. If the executor prefers chronological (RO shipped first in Phase 5), document the preference choice in the SUMMARY but the baseline is alphabetical.
  - Do NOT touch any `nav:` block other than Cost, Recipients, and Schemes. Efficiency / Cannibalisation / Reliability / Data / Methodology / About must be byte-identical.
  </action>

  <verify>
    <automated>
    # All 4 doomed files are gone and git knows they are staged for deletion
    for f in docs/themes/cost/cfd-dynamics.md docs/themes/cost/remaining-obligations.md docs/themes/recipients/cfd-payments-by-category.md docs/themes/recipients/lorenz.md; do
      test ! -f "$f" || { echo "$f still exists"; exit 1; }
      git status --porcelain -- "$f" | grep -qE "^D " || { echo "$f not staged for deletion"; exit 1; }
    done

    # 6 non-overlap CfD theme pages still exist
    for f in docs/themes/cost/cfd-vs-gas-cost.md docs/themes/efficiency/subsidy-per-avoided-co2-tonne.md docs/themes/cannibalisation/capture-ratio.md docs/themes/reliability/capacity-factor-seasonal.md docs/themes/reliability/generation-heatmap.md docs/themes/reliability/rolling-minimum.md; do
      test -f "$f" || { echo "non-overlap page $f was accidentally deleted"; exit 1; }
    done

    # mkdocs.yml nav reflects the new state
    grep -q "Contracts for Difference (CfD): schemes/cfd.md" mkdocs.yml || { echo "mkdocs.yml missing CfD Schemes nav entry"; exit 1; }
    grep -q "Renewables Obligation (RO): schemes/ro.md" mkdocs.yml || { echo "mkdocs.yml missing RO Schemes nav entry (regression)"; exit 1; }
    ! grep -E "CfD dynamics: themes/cost/cfd-dynamics\.md|Remaining obligations: themes/cost/remaining-obligations\.md|Lorenz curve: themes/recipients/lorenz\.md|Payments by technology: themes/recipients/cfd-payments-by-category\.md" mkdocs.yml || { echo "mkdocs.yml still lists a deleted page in nav"; exit 1; }
    grep -q "CfD vs gas cost: themes/cost/cfd-vs-gas-cost.md" mkdocs.yml || { echo "Non-overlap CfD vs gas cost entry accidentally removed"; exit 1; }

    # Alphabetical Schemes order: CfD before RO.
    # Write the ordering-check Python to a temp file (column-0 content) to avoid heredoc indentation traps.
    cat > /tmp/check_scheme_order.py <<'PY'
content = open("mkdocs.yml").read()
cfd_idx = content.index("Contracts for Difference (CfD): schemes/cfd.md")
ro_idx = content.index("Renewables Obligation (RO): schemes/ro.md")
assert cfd_idx < ro_idx, "CfD must come before RO in Schemes nav (alphabetical per D-Claude's-Discretion)"
print("Alphabetical Schemes order verified.")
PY
    python3 /tmp/check_scheme_order.py || { echo "Schemes ordering check failed"; exit 1; }

    # THE CI GATE — must exit 0
    uv run mkdocs build --strict 2>&1 | tee /tmp/mkdocs_plan02_task2.log
    [ "${PIPESTATUS[0]}" -eq 0 ] || { echo "mkdocs build --strict FAILED — see /tmp/mkdocs_plan02_task2.log"; exit 1; }

    # Extra belt-and-braces: scan the full strict-build log for any WARNING lines
    ! grep -E "^WARNING" /tmp/mkdocs_plan02_task2.log || { echo "mkdocs build emitted WARNING lines (all should be absent under --strict)"; exit 1; }

    echo "Task 2 deletions + nav edits + strict build verified."
    </automated>
  </verify>

  <acceptance_criteria>
    - `ls docs/themes/cost/cfd-dynamics.md docs/themes/cost/remaining-obligations.md docs/themes/recipients/cfd-payments-by-category.md docs/themes/recipients/lorenz.md 2>&1` reports "No such file" for all 4.
    - `git status --porcelain` shows `D` status for the 4 deleted files (staged for commit).
    - `ls docs/themes/cost/cfd-vs-gas-cost.md docs/themes/efficiency/subsidy-per-avoided-co2-tonne.md docs/themes/cannibalisation/capture-ratio.md docs/themes/reliability/capacity-factor-seasonal.md docs/themes/reliability/generation-heatmap.md docs/themes/reliability/rolling-minimum.md` lists all 6 files (no accidental deletion of non-overlap pages).
    - `grep -q "Contracts for Difference (CfD): schemes/cfd.md" mkdocs.yml` — CfD Schemes nav entry present.
    - `grep -q "Renewables Obligation (RO): schemes/ro.md" mkdocs.yml` — RO Schemes nav entry preserved.
    - `grep -cE "CfD dynamics: themes/cost/cfd-dynamics\.md|Remaining obligations: themes/cost/remaining-obligations\.md|Lorenz curve: themes/recipients/lorenz\.md|Payments by technology: themes/recipients/cfd-payments-by-category\.md" mkdocs.yml` returns 0 — no doomed-file nav entries remain.
    - Schemes nav has CfD listed before RO (alphabetical per D-Claude's-Discretion default) — verified by `python3 /tmp/check_scheme_order.py`.
    - `uv run mkdocs build --strict` exits 0 with no `WARNING` lines in output.
  </acceptance_criteria>

  <done>
  4 overlap theme pages deleted via `git rm` and staged. `mkdocs.yml` nav updated: Schemes tab lists Overview / CfD / RO; Cost and Recipients tabs no longer list the deleted pages; 6 non-overlap CfD theme pages + all other nav blocks preserved. `mkdocs build --strict` exits 0 with zero warnings — the Phase 3 permanent CI gate confirms every internal link resolves against the new tree.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Atomic commit — docs(05.1-02): delete 4 overlap pages + migrate inbound links + update nav</name>
  <files>
    docs/themes/cost/cfd-dynamics.md,
    docs/themes/cost/remaining-obligations.md,
    docs/themes/recipients/cfd-payments-by-category.md,
    docs/themes/recipients/lorenz.md,
    docs/themes/cost/index.md,
    docs/themes/recipients/index.md,
    docs/themes/reliability/rolling-minimum.md,
    docs/schemes/ro.md,
    docs/schemes/index.md,
    mkdocs.yml
  </files>

  <read_first>
    - `git status --porcelain` to confirm the working tree contains: 4 D entries (deletions) + 6 M entries (modifications from Task 1 + Task 2).
    - Recent commit log: `git log --oneline -5` — confirm the commit-message cadence.
  </read_first>

  <action>
  One atomic commit bundling Task 1's inbound-link migration + Task 2's deletions + nav update. This is the Phase 1 D-16 pattern: the intermediate state between Task 1 and Task 3 is never committed — the tree moves from "Plan 01 HEAD" directly to "all inbound links retargeted + 4 pages deleted + nav updated + --strict green" in a single atomic step. This avoids the hypothetical broken-build state a split commit would leave on `main`.

  Command:

  ```bash
  git add docs/themes/cost/index.md
  git add docs/themes/recipients/index.md
  git add docs/themes/reliability/rolling-minimum.md
  git add docs/schemes/ro.md
  git add docs/schemes/index.md
  git add mkdocs.yml
  # Deletions from Task 2's `git rm` are already staged; confirm:
  git status --porcelain | grep -E "^D " | wc -l | grep -q "^4$" || { echo "Expected 4 staged deletions"; exit 1; }

  git commit -m "$(cat <<'EOF'
  docs(05.1-02): delete 4 overlap theme pages + migrate inbound links + update nav

  Completes the scheme-page↔theme-page consolidation started in Plan 01.
  The 4 overlap CfD per-chart theme pages (themes/cost/cfd-dynamics.md,
  themes/cost/remaining-obligations.md, themes/recipients/cfd-payments-
  by-category.md, themes/recipients/lorenz.md) are deleted now that
  docs/schemes/cfd.md absorbs their content into §§3/6/4/5 (Plan 01).
  Every inbound internal link is retargeted to schemes/cfd.md#<anchor>:

  - themes/cost/index.md: 3 CfD cards (S2 dynamics, S5 forward, cross-
    theme S3 payments-by-tech) retargeted to scheme-page anchors
  - themes/recipients/index.md: 2 CfD cards (Lorenz → S4 concentration,
    payments → S3 by-technology) retargeted; pre-existing RO
    concentration card at lines 27-29 preserved untouched
  - themes/reliability/rolling-minimum.md: See-also bullet retargeted
    from themes/cost/remaining-obligations.md to schemes/cfd.md#forward-
    commitment-chart-s5 (line 153)
  - schemes/ro.md: §5 reciprocal cross-link retargeted from themes/
    recipients/lorenz.md to schemes/cfd.md#concentration-chart-s4 (line
    83) — cross-scheme symmetry restored
  - schemes/index.md: CfD promoted from Coming-soon to Currently-
    published (alphabetical: CfD before RO per D-Claude's-Discretion)

  mkdocs.yml nav updated: Schemes tab gains a CfD entry between Overview
  and RO; Cost and Recipients tabs lose the 4 deleted-page entries.
  6 non-overlap CfD theme pages (cfd-vs-gas-cost, subsidy-per-avoided-
  co2-tonne, capture-ratio, capacity-factor-seasonal, generation-heatmap,
  rolling-minimum) preserved in Phase 3 D-01 six-section form per D-08.

  uv run mkdocs build --strict exits 0 with zero warnings — the Phase 3
  permanent CI gate confirms every inbound link resolves against the new
  tree (D-09: --strict is THE inbound-link gate for this phase).

  Plan 02 of 4 in Phase 05.1. Plans 03-04 handle homepage pivot +
  CHANGES.md + REQUIREMENTS.md SCHEMEPAGE-01 bookkeeping.
  EOF
  )"
  ```

  **What NOT to do:**
  - Do NOT split this into multiple commits. The intermediate states (deletions without retargeting, or nav update without deletions) break `mkdocs --strict`. Atomicity is load-bearing.
  - Do NOT use `git add .` — stage files by name to avoid accidentally picking up Plan 03 or Plan 04 work in progress.
  - Do NOT skip hooks.
  - Do NOT run `git reset --soft HEAD~1` if the commit message needs editing — create a new commit and document the amend rationale in SUMMARY. We preserve atomic-commit linearity on `main`.
  </action>

  <verify>
    <automated>
    # Commit landed
    git log -1 --format='%s' | grep -q "docs(05.1-02): delete 4 overlap theme pages" || { echo "Commit message missing/wrong"; exit 1; }

    # Commit touches exactly 10 paths: 4 deletions + 6 modifications.
    # Use git diff-tree / git show --name-status with --format= (suppresses commit-message body entirely)
    # to avoid the Warning-4 `tail -n +7` fragility.
    CHANGED=$(git show --name-status --format= HEAD | grep -cE '^[AMD]')
    [ "$CHANGED" -eq 10 ] || {
      echo "Commit touches $CHANGED files, expected 10 (4 D + 6 M). Name-status output:"
      git show --name-status --format= HEAD
      exit 1
    }

    # 4 deletions in commit (same --format= discipline)
    DEL=$(git show --name-status --format= HEAD | grep -cE '^D[[:space:]]')
    [ "$DEL" -eq 4 ] || { echo "Commit has $DEL deletions, expected 4"; exit 1; }

    # 6 modifications in commit
    MOD=$(git show --name-status --format= HEAD | grep -cE '^M[[:space:]]')
    [ "$MOD" -eq 6 ] || { echo "Commit has $MOD modifications, expected 6"; exit 1; }

    # Tree is clean post-commit
    git diff --quiet HEAD || { echo "Uncommitted changes remain"; exit 1; }
    git diff --cached --quiet HEAD || { echo "Staged but uncommitted changes"; exit 1; }

    # mkdocs --strict still green on post-commit HEAD
    uv run mkdocs build --strict > /tmp/mkdocs_plan02_postcommit.log 2>&1 || { cat /tmp/mkdocs_plan02_postcommit.log; echo "post-commit strict build failed"; exit 1; }
    ! grep -E "^WARNING" /tmp/mkdocs_plan02_postcommit.log || { cat /tmp/mkdocs_plan02_postcommit.log; echo "post-commit strict build emitted warnings"; exit 1; }

    # Repo-wide inbound-link sweep — no remaining references to the 4 doomed files anywhere in docs/ or mkdocs.yml
    # Use .md anchor to avoid false positives on cfd_dynamics.py chart-source link in cfd.md §8
    ! grep -rE "themes/cost/cfd-dynamics\.md|themes/cost/remaining-obligations\.md|themes/recipients/cfd-payments-by-category\.md|themes/recipients/lorenz\.md" docs/ mkdocs.yml || { echo "Repo-wide sweep found stale inbound link to a doomed file"; exit 1; }

    echo "Plan 02 commit + post-commit --strict gate + repo-wide sweep all green."
    </automated>
  </verify>

  <acceptance_criteria>
    - `git log -1 --format='%s'` contains `docs(05.1-02): delete 4 overlap theme pages + migrate inbound links + update nav`.
    - `git show --name-status --format= HEAD | grep -cE '^[AMD]'` returns exactly 10: 4 `D` (deletions) + 6 `M` (modifications to `docs/themes/cost/index.md`, `docs/themes/recipients/index.md`, `docs/themes/reliability/rolling-minimum.md`, `docs/schemes/ro.md`, `docs/schemes/index.md`, `mkdocs.yml`).
    - `git show --name-status --format= HEAD | grep -cE '^D[[:space:]]'` returns exactly 4.
    - `git show --name-status --format= HEAD | grep -cE '^M[[:space:]]'` returns exactly 6.
    - `git status --porcelain` returns empty (clean tree post-commit).
    - `uv run mkdocs build --strict` exits 0 on post-commit HEAD with zero `WARNING` lines.
    - Repo-wide `grep -rE "themes/cost/cfd-dynamics\.md|themes/cost/remaining-obligations\.md|themes/recipients/cfd-payments-by-category\.md|themes/recipients/lorenz\.md" docs/ mkdocs.yml` returns empty (no lingering inbound references anywhere in the docs tree or build config).
  </acceptance_criteria>

  <done>
  Single atomic commit with message `docs(05.1-02): delete 4 overlap theme pages + migrate inbound links + update nav` containing 4 deletions + 6 modifications (verified via `git show --name-status --format=` without the Warning-4 `tail -n +7` fragility). `mkdocs build --strict` green on post-commit HEAD. Repo-wide sweep confirms zero stale inbound links. The scheme-page↔theme-page consolidation is complete; Plans 03-04 can proceed in parallel.
  </done>
</task>

</tasks>

<verification>
Final gates after all 3 tasks:

```bash
# 1. The Phase 3 permanent CI gate
uv run mkdocs build --strict

# 2. Repo-wide inbound-link sweep — zero stale references
grep -rE "themes/cost/cfd-dynamics\.md|themes/cost/remaining-obligations\.md|themes/recipients/cfd-payments-by-category\.md|themes/recipients/lorenz\.md" docs/ mkdocs.yml
# (expected: empty)

# 3. Phase 3 TRIAGE structural tests still pass (non-overlap pages intact)
uv run pytest tests/test_docs_structure.py -q

# 4. Schemes nav reads CfD before RO
awk '/- Schemes:/,/- Data:/' mkdocs.yml

# 5. Pre-existing RO concentration card at recipients/index.md survives (Warning 8 defence)
grep -q 'schemes/ro.md#concentration-chart-s4' docs/themes/recipients/index.md && echo "RO concentration card preserved"
```

No code changes in Plan 02 — the full pytest suite is not required here. `test_docs_structure.py` is sufficient because that test enforces theme-page presence invariants (TRIAGE-01 absence guards + TRIAGE-02 PROMOTE-page presence) — Plan 02 touches 4 PROMOTE pages and the test may need a one-line update if it enumerated the deleted files specifically. Run it; if it fails, the executor fixes the enumeration surgically as a Rule-2 auto-fix (documented in SUMMARY).
</verification>

<success_criteria>
- 4 theme pages deleted; 6 non-overlap CfD theme pages preserved.
- Every inbound internal link in the repo that previously resolved to one of the 4 deleted files now resolves to `schemes/cfd.md#<anchor>` (or has been rewritten for reciprocity on ro.md).
- Pre-existing RO concentration card at `docs/themes/recipients/index.md:27-29` survives the edit untouched (Warning 8 defence).
- `mkdocs.yml` nav reflects the new state: Schemes → Overview / CfD / RO (alphabetical); 4 deleted-page entries gone from Cost + Recipients tabs.
- `docs/schemes/index.md` lists CfD as published alongside RO; Coming-soon list drops the CfD bullet.
- `uv run mkdocs build --strict` exits 0 with zero `WARNING` lines on post-commit HEAD.
- Single atomic commit: `docs(05.1-02): delete 4 overlap theme pages + migrate inbound links + update nav` with 10-file touch (4 D + 6 M) verified via `git show --name-status --format=` (Warning 4 fix: no `tail -n +7` dependency).
- `test_docs_structure.py` passes (if it needs a one-line update to drop enumeration of the deleted files, that update is a Rule-2 auto-fix included in this commit or a follow-up commit; SUMMARY documents the decision).
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-cfd-scheme-page/05.1-02-SUMMARY.md` capturing:
- repo-wide sweep output confirming zero stale inbound links
- the exact before/after of the mkdocs.yml nav diff
- any Rule-2 auto-fix executed (e.g. `test_docs_structure.py` enumeration update if needed)
- strict-build log (timestamp + build duration + 0-warning confirmation)
- confirmation that the 6 non-overlap CfD theme pages are byte-identical to their pre-Plan-02 state (run `git diff HEAD~1 HEAD -- docs/themes/cost/cfd-vs-gas-cost.md docs/themes/efficiency/subsidy-per-avoided-co2-tonne.md docs/themes/cannibalisation/capture-ratio.md docs/themes/reliability/capacity-factor-seasonal.md docs/themes/reliability/generation-heatmap.md` — expected: empty; rolling-minimum.md has a single-line diff on line 153)
- confirmation that the pre-existing RO concentration card at `docs/themes/recipients/index.md:27-29` points at `schemes/ro.md#concentration-chart-s4` after the edit (Warning 8 defence passed)
- commit file-count verification output: `git show --name-status --format= HEAD` (expect 4 D-lines + 6 M-lines; no `tail -n +7` dependency)
</output>
</content>
